<div id=toc></div>

# Table of Contents

- [cs.CV](#cs.CV) [Total: 82]
- [cs.AI](#cs.AI) [Total: 28]
- [eess.SP](#eess.SP) [Total: 15]
- [eess.IV](#eess.IV) [Total: 5]
- [cs.IT](#cs.IT) [Total: 3]
- [cs.LG](#cs.LG) [Total: 109]
- [cs.RO](#cs.RO) [Total: 44]


<div id='cs.CV'></div>

# cs.CV [[Back]](#toc)

### [1] [Pixel-Precise Explainable Stress Indexing: A Semantic Segmentation Framework for Disease Severity Quantification in Field Crops](https://arxiv.org/abs/2607.06585)
*Raunak Kumar,Soumyashree Kar*

Main category: cs.CV

TL;DR: Proposes a unified deep learning pipeline combining semantic segmentation, severity estimation, and disease classification for plant stress quantification. U-Net with MobileNetV2 achieves best performance (98.20% pixel accuracy, 0.70 mIoU, 14.7ms/image) on Apple Tree Leaf Disease Segmentation dataset, with severity index strongly correlating with expert annotations (r=0.968).


<details>
  <summary>Details</summary>
Motivation: Plant diseases cause 20-40% global yield loss ($220B+), and traditional manual stress assessments are labour-intensive, subjective, and not scalable for precision agriculture.

Method: Develops a unified deep learning pipeline integrating three tasks: semantic segmentation, regression-based severity estimation (4 levels: Low to Very High), and disease classification. Evaluated U-Net (MobileNetV2), SegFormer, FCN, and PSPNet on Apple Tree Leaf Disease Segmentation dataset (1,641 samples, 6 classes).

Result: U-Net with MobileNetV2 excels: 98.20% pixel accuracy, 0.70 mIoU, 99.41% detection accuracy, 14.7ms/image (real-time capable). SegFormer competitive (0.66 mIoU); FCN/PSPNet lower (~0.49 mIoU). Severity index correlates strongly with experts (r=0.968, R²=0.937).

Conclusion: The proposed system is reliable, accurate, and real-time capable for automated crop monitoring and decision support in precision agriculture.

Abstract: Plant diseases, resulting from both biotic and abiotic stresses, cause an estimated 20-40% loss in global agricultural yield annually, resulting in economic damages exceeding USD 220 billion. Accurate and scalable stress quantification is essential for precision agriculture, yet traditional manual assessments are labour-intensive and subjective. This paper proposes a unified deep learning pipeline integrating semantic segmentation, regression-based severity estimation, and disease classification. Stress severity is categorised into four levels (Low to Very High) based on the proportion of infected leaf area. Experiments on the Apple Tree Leaf Disease Segmentation dataset (1,641 samples, six classes) evaluate four models: U-Net (MobileNetV2), SegFormer, FCN, and PSPNet. U-Net with MobileNetV2 achieves the best performance with 98.20% pixel accuracy, 0.70 mIoU, and 99.41% detection accuracy at 14.7 ms per image, making it suitable for real-time use. SegFormer performs competitively (mIoU 0.66), while FCN and PSPNet show lower spatial accuracy (approximately 0.49 mIoU). The computed severity index strongly correlates with expert annotations (r = 0.968, R^2 = 0.937), demonstrating the system's reliability for automated crop monitoring and decision support.

</details>


### [2] [CoFINN: Conservation Flux Informed Neural Networks for Physics Problems Governed by Conservation Laws](https://arxiv.org/abs/2607.06587)
*Adnan Harun Doğan,Mert Deniz,Hande Alemdar,Özgür Uğraş Baran*

Main category: cs.CV

TL;DR: CoFINN embeds finite-volume conservation laws into CNN training for compressible flow prediction, improving drag prediction accuracy by up to 34% while maintaining computational efficiency.


<details>
  <summary>Details</summary>
Motivation: Conventional data-driven CNNs optimize only pixel-wise similarity metrics, and classical PINNs enforce differential equations via automatic differentiation rather than finite-volume conservation, which is inconsistent with modern CFD methodology. This gap motivates a physics-informed framework that aligns with actual conservation law discretization.

Method: CoFINN interprets CNN output fields as structured computational grids (each pixel = finite-volume cell) and enforces conservation consistency through numerical flux calculations during training, embedding finite-volume conservation physics directly into the loss function.

Result: Drag prediction error reduced by up to 34% at extreme angles of attack and ~15% on average; improvements are especially significant in limited-data regimes; maintains CNN computational efficiency.

Conclusion: CoFINN demonstrates that conservation-based loss acts as an effective physical regularizer, is architecture-agnostic, and can be extended to broader classes of conservation-law-governed physical systems.

Abstract: We present CoFINN (Conservation Flux Informed Neural Networks), a physics-informed deep learning framework for predicting compressible flow fields governed by conservation laws. Unlike conventional data-driven convolutional neural networks (CNNs), which optimize only pixel-wise similarity metrics, CoFINN embeds finite-volume conservation physics directly into the training process. Unlike classical physics-informed methods which enforce differential-equation residuals at collocation points through automatic differentiation, CoFINN adopts a finite-volume perspective consistent with modern CFD methodology. CoFINN interprets CNN output fields as structured computational grids, where each pixel represents a finite-volume cell, and enforces conservation consistency through sophisticated numerical flux calculations. The framework is evaluated on transonic flow prediction around airfoils at (M=0.7, Re=6 * 10^6), including challenging conditions involving shock waves and high angles of attack. Results show that CoFINN improves aerodynamic force prediction accuracy, reducing drag prediction error by up to 34% at extreme angles of attack and by approximately 15% on average across the test set. Improvements are particularly significant in limited-data regimes, demonstrating that the conservation-based loss acts as an effective physical regularizer. The proposed approach maintains the computational efficiency advantages of CNN surrogates while significantly improving physical consistency and conservation behavior. The framework is architecture-agnostic and extensible to broader classes of conservation-law-governed physical systems.

</details>


### [3] [AI for Cultural Heritage Textiles: Fine-Tuned Latent Diffusion for Novel Ulos Motif Synthesis](https://arxiv.org/abs/2607.06590)
*Humasak Tommy Argo Simanjuntak,Jesika Purba,Sitogab Girsang,Widya Manurung,Samuel Situmeang,Arlinta Barus,Daniel Oranova Siahaan*

Main category: cs.CV

TL;DR: The study fine-tunes two latent diffusion models (Protogen v3.4 and Stable Diffusion v1.4) on traditional Ulos motifs to generate culturally consistent yet novel textile designs, finding Protogen v3.4 superior in fidelity and diversity, with optimal guidance scale of 5-9 balancing quality and creativity.


<details>
  <summary>Details</summary>
Motivation: Traditional Ulos weaving faces limited motif variety and time-intensive design processes, necessitating innovative AI-assisted approaches to preserve and revitalise this Batak cultural heritage while meeting contemporary design demands.

Method: A generative AI framework fine-tuning Protogen v3.4 and Stable Diffusion v1.4 on curated, annotated high-resolution Ulos motif datasets, evaluated via FID, IS, KID, and qualitative assessments by weavers and the public, examining strength and guidance scale effects.

Result: Protogen v3.4 outperformed Stable Diffusion v1.4 with ~10.5x lower FID and 2.0x higher IS. Lower strength values improved fidelity; higher strength increased diversity. Guidance scale 5-9 optimally balanced fidelity-diversity tradeoff.

Conclusion: Carefully fine-tuned generative AI can effectively support creative renewal of intangible cultural heritage while preserving stylistic and symbolic integrity of traditional textiles.

Abstract: Preserving and revitalising traditional textiles such as Ulos, a cultural heritage of the Batak ethnic group in North Sumatra, Indonesia, requires balancing fidelity to tradition with innovative approaches that meet contemporary design demands. Traditional Ulos weaving faces two key limitations: a narrow range of motifs and a time-intensive design process. This study presents a generative AI framework that fine-tunes two pretrained latent diffusion models: Protogen v3.4 and Stable Diffusion v1.4, on a curated, annotated dataset of high-resolution Ulos motifs to generate culturally consistent yet novel designs. Model performance is evaluated quantitatively using Frechet Inception Distance (FID), Inception Score (IS), and qualitatively through assessments by traditional weavers and members of the public. Protogen v3.4 consistently outperforms Stable Diffusion v1.4, achieving substantially lower FID (~10.5x) and higher IS (2.0x), indicating superior visual fidelity, diversity, and closer alignment with the real Ulos motif distribution. We further examine the effects of strength and guidance scale on generation quality across both models. Lower strength values consistently yield higher fidelity (lower FID), while higher strength values increase generative diversity at the cost of realism, revealing a clear fidelity-diversity tradeoff for both models. Across all tested configurations, a guidance scale of 5-9 provides the most effective balance between fidelity and diversity, stabilising FID, KID, and IS, and is recommended as the operating range for high-quality, diverse Ulos motif generation. These findings demonstrate that carefully fine-tuned generative AI can support the creative renewal of intangible cultural heritage while preserving its stylistic and symbolic integrity.

</details>


### [4] [LipSSD: Lipschitz-Constrained Single-Shot Detection for Adversarially Robust Object Detection](https://arxiv.org/abs/2607.06592)
*Vincent Lébé,Yannick Prudent,Corentin Friedrich,Thomas Massena,Ronan Sicre,Franck Mamalet*

Main category: cs.CV

TL;DR: This paper introduces Lipschitz-constrained object detectors (LipSSD) as robust-by-design alternatives to standard detectors, showing they improve adversarial robustness, complement adversarial training, and allow controlled accuracy-robustness trade-offs across multiple datasets and attacks.


<details>
  <summary>Details</summary>
Motivation: Object detectors are vulnerable to adversarial attacks, yet robust detection has received less attention than classification. Existing methods like adversarial training often fail to transfer across attacks, budgets, or architectures, motivating a more fundamental architectural approach to robustness.

Method: The authors propose Lipschitz-constrained variants of object detection architectures (LipSSD), which enforce Lipschitz continuity constraints on the network. They study the accuracy-robustness trade-off controlled by a single hyperparameter and combine LipSSD with adversarial training.

Result: On Pascal VOC, adversarially trained LipSSD improves mAP@50 on unseen attacks by up to 15 points over adversarially trained SSD. On LARD and KITTI datasets, Lipschitz-constrained detectors preserve clean performance while improving robustness.

Conclusion: Architectural Lipschitz control is a practical, attack-agnostic direction for improving robustness of object detectors, complementary to adversarial training.

Abstract: Object detectors have many applications in safety-critical systems, but they are known to be sensitive to worst-case perturbations such as adversarial attacks, which limits their applicability in real-world scenarios. Compared with classification, adversarial robustness for object detection has received less attention, and existing methods are often tied to adversarial training, whose performance may not transfer across attacks, perturbation budgets, or architectures. In this work, we introduce Lipschitz-constrained variants of object detection architectures as robust-by-design alternatives to standard detectors. We validate this approach with LipSSD, a Lipschitz-constrained Single Shot MultiBox Detector (SSD), and provide a comprehensive study of its adversarial robustness using multiple white-box adversarial attacks and datasets. We first analyze the accuracyrobustness trade-off induced by Lipschitz constraints and show that it can be controlled through a single training hyperparameter. We then demonstrate that Lipschitzconstrained detectors are complementary to adversarial training: under the same training setup on the Pascal VOC dataset, adversarially trained LipSSD improves mAP@50 on unseen attacks by up to 15 points over classical adversarially trained SSD. Finally, we use more specific safety-critical datasets such as LARD and KITTI, and show that Lipschitz-constrained detectors can improve robustness while largely preserving clean performance. These results suggest that architectural Lipschitz control is a practical and attack-agnostic direction for improving the robustness of object detectors.

</details>


### [5] [MiLSD: A Micro Line-Segment Detector for Resource-Constrained Devices](https://arxiv.org/abs/2607.06600)
*Parsa Hassani Shariat Panahi,Amir Hossein Jalilvand,M. Hassan Najafi*

Main category: cs.CV

TL;DR: MiLSD is a sub-megabyte line segment detection model for MCU-level devices, achieving 24.1 sAP10 (vs 10.6 baseline) via F-Clip representation, 8-bit quantization, and inference enhancements under 1 MB budget.


<details>
  <summary>Details</summary>
Motivation: Existing deep learning line segment detectors achieve high accuracy but require several MB of memory, exceeding the capacity of low-cost MCUs used in embedded vision. The authors aim to maximize accuracy under a strict sub-megabyte budget.

Method: Proposes MiLSD with a compact fully-convolutional backbone; systematically compares three output representations (F-Clip center-with-length-and-angle formulation performs best). Applies 8-bit/4-bit quantization, sub-pixel decoding, test-time augmentation, and a lightweight verifier.

Result: 8-bit quantization preserves full-precision accuracy but 4-bit causes significant degradation (angle regression most affected). MiLSD improves sAP10 from 10.6 (25k params, 0.25 MB) to 24.1 within 1 MB on ShanghaiTech Wireframe.

Conclusion: Maps accuracy-memory trade-offs across representations, bit-widths, capacities, and post-processing, demonstrating viable sub-megabyte line segment detection for embedded systems, though GPU-scale performance remains out of reach.

Abstract: Line segment detection is a key building block in visual SLAM, 3D reconstruction, and industrial inspection. Recent deep learning methods have greatly improved accuracy, yet even the smallest models require several megabytes of memory, exceeding low-cost MCU capacity. This work investigates the maximum achievable accuracy under a sub-megabyte budget. We propose MiLSD, a detector tailored for MCU-level constraints, and systematically compare three output representations within a compact fully-convolutional backbone. Our study shows that the proposed F-Clip center-with-length-and-angle formulation learns most effectively at small model sizes. We find that 8-bit quantization preserves full-precision performance, while 4-bit quantization causes significant degradation, particularly in angle regression, with quantization-aware training recovering only part of the loss. With a one-megabyte activation budget and inference enhancements including sub-pixel decoding, test-time augmentation, and a lightweight verifier, MiLSD improves sAP10 on ShanghaiTech Wireframe from 10.6 (25k parameters, 0.25 MB) to 24.1 within 1 MB. Rather than competing with GPU-scale parsers, we map the accuracy memory trade-off across representations, bit-widths, capacities, and post-processing strategies for embedded vision systems.

</details>


### [6] [Do Counterfactually Fair Image Classifiers Satisfy Group Fairness? -- A Theoretical and Empirical Study](https://arxiv.org/abs/2607.06603)
*Sangwon Jung,Sumin Yu,Sanghyuk Chun,Taesup Moon*

Main category: cs.CV

TL;DR: This paper constructs new image datasets to evaluate counterfactual fairness (CF) and group fairness (GF) simultaneously, finding that CF does not imply GF in image classification (unlike tabular data). The authors attribute this to a latent attribute G correlated with the sensitive attribute, and propose Counterfactual Knowledge Distillation (CKD) to mitigate this correlation, showing that CF-achieving models can satisfy GF when G reliance is reduced.


<details>
  <summary>Details</summary>
Motivation: To resolve the unclear relationship between counterfactual fairness (CF) and group fairness (GF) in image classification tasks, since counterfactual samples for evaluating CF are difficult to obtain from existing images.

Method: Created new image datasets (Celeb and LFW) using high-quality image editing and human annotation for CF evaluation. Proposed Counterfactual Knowledge Distillation (CKD) to mitigate correlation between latent attribute G and the sensitive attribute.

Result: Empirically found that CF does not imply GF in image classification (opposite to prior tabular studies). Theoretically showed this is due to latent attribute G correlated with the sensitive attribute. CKD successfully reduces reliance on G, enabling CF-achieving models to satisfy GF.

Conclusion: CF does not automatically guarantee GF in vision tasks due to latent spurious correlations. However, with proper mitigation methods like CKD, CF and GF can be simultaneously achieved in image classification.

Abstract: The notion of algorithmic fairness has been actively explored from various aspects of fairness, such as counterfactual fairness (CF) and group fairness (GF). However, the exact relationship between CF and GF remains to be unclear, especially in image classification tasks; the reason is because we often cannot collect counterfactual samples regarding a sensitive attribute, essential for evaluating CF, from the existing images (\eg, a photo of the same person but with different secondary sex characteristics). In this paper, we construct new image datasets for evaluating CF by using a high-quality image editing method and carefully labeling with human annotators. Our datasets, \oursceleb and \ourslfw, build upon the popular image GF benchmarks; hence, we can evaluate CF and GF simultaneously. We empirically observe that CF does not imply GF in image classification, whereas previous studies on tabular datasets observed the opposite. We theoretically show that it could be due to the existence of a latent attribute $G$ that is correlated with, but not caused by, the sensitive attribute (\eg, secondary sex characteristics are highly correlated with hair length). From this observation, we propose a simple baseline, Counterfactual Knowledge Distillation (CKD), to mitigate such correlation with the sensitive attributes. Extensive experimental results on \oursceleb and \ourslfw demonstrate that CF-achieving models satisfy GF if we successfully reduce the reliance on $G$ (\eg, using CKD).

</details>


### [7] [Context-Aware Slum Mapping in Sub-Saharan Africa Using Sentinel-1 Texture and Local Climate Zones](https://arxiv.org/abs/2607.07532)
*Peterson Chepkilot,Babak Memar,Paolo Gamba*

Main category: cs.CV

TL;DR: This study presents an Optical-SAR framework combining Sentinel-1 (SAR) and Sentinel-2 (optical) imagery to improve informal settlement (LCZ 7) mapping in Sub-Saharan African cities, achieving ~0.81 accuracy and reducing confusion with formal low-rise areas (LCZ 3) to 7%, with SAR textures being the key performance driver across seasons.


<details>
  <summary>Details</summary>
Motivation: Accurate mapping of informal settlements in SSA cities is challenging because optical imagery alone struggles to distinguish informal settlements (LCZ 7) from spectrally similar formal compact low-rise areas (LCZ 3), and existing methods like WUDAPT perform poorly.

Method: A three-tier SAR integration strategy (calibrated backscatter, GLCM textures, physics-guided feature) combined with Sentinel-2 optical features within an adapted LCZ taxonomy, evaluated via stratified hold-out and season-aware ablation study across Nairobi and Eldoret, Kenya.

Result: Optical-SAR model achieved OA of 0.816 (dry) and 0.807 (wet), outperforming WUDAPT baseline (0.704). LCZ 3-LCZ 7 confusion reduced to 7%. SAR textures provided dominant performance gain and stabilized mapping across seasons.

Conclusion: Incorporating SAR-derived features yields consistent improvements for urban morphology mapping in data-scarce environments across seasons, though cross-city transfer remains limited without local adaptation.

Abstract: Accurate mapping of informal settlements remains a major challenge in Sub-Saharan African (SSA) cities because optical imagery often fails to distinguish Informal Settlements (defined here as LCZ 7) from spectrally similar formal Compact Low-Rise areas (LCZ 3). This study presents a context-aware, reproducible Optical-SAR framework that improves informal settlement delineation using Sentinel-2 spectral features and Sentinel-1 structural information within an adapted Local Climate Zone (LCZ) taxonomy. We implement a three-tier SAR integration strategy: calibrated backscatter, GLCM textures, and a physics-guided feature engineered to capture the high structural disorder and weak radar return characteristic of SSA informal settlements. Using reference data across Nairobi and Eldoret (Kenya), we evaluate performance via a stratified hold-out protocol and a season-aware ablation study. Results show that SAR textures provide the dominant performance gain for LCZ 7 detection. The Optical-SAR model achieves overall accuracy of 0.816 (dry) and 0.807 (wet), significantly outperforming the WUDAPT baseline (OA 0.704) and reducing the critical LCZ 3 - LCZ 7 confusion to 7%. Seasonal analysis indicates that while optical-only separability varies with phenology, SAR-derived textures stabilize informal settlement mapping across seasons. These findings demonstrate that the incorporation of SAR-derived features yields consistent improvements for urban morphology mapping in data-scarce environments across seasons and across the evaluated source cities, while cross-city transfer remains limited without local adaptation strategies.

</details>


### [8] [Overview of the NLPCC 2026 Shared Task 1: Difficulty-Aware Multilingual and Multimodal Medical Instructional Video Understanding Evaluation](https://arxiv.org/abs/2607.06618)
*Shenxi Liu,Kan Li,Mingyang Zhao,Yuhang Tian,Bin Li*

Main category: cs.CV

TL;DR: DA-MIVQA (NLPCC 2026) introduces a difficulty-aware medical instructional video QA benchmark with three tracks, distinguishing simple (text-based) from complex (visual/procedural/cross-modal) questions across diverse medical scenarios.


<details>
  <summary>Details</summary>
Motivation: Prior benchmarks (CMIVQA, MMI-VQA, M4IVQA) lacked explicit distinction of question difficulty based on evidence complexity. The authors aim to evaluate systems under varying textual, visual, temporal, and procedural reasoning demands.

Method: Three tracks: (1) Difficulty-Aware Temporal Answer Grounding in Single Video (DA-TAGSV), (2) Difficulty-Aware Video Corpus Retrieval (DA-VCR), (3) Difficulty-Aware Temporal Answer Grounding in Video Corpus (DA-TAGVC). Dataset from public medical instructional channels, manually annotated with difficulty levels.

Result: The paper presents competition results, participation overview, and representative systems; specific quantitative results are not detailed in the abstract.

Conclusion: DA-MIVQA offers a practical benchmark for assessing medical instructional video QA under varying reasoning complexities, advancing beyond past shared tasks.

Abstract: Following the CMIVQA, MMI-VQA, and M4IVQA challenges in NLPCC 2023--2025, we introduce the Difficulty-Aware Medical Instructional Video Question Answering (DA-MIVQA) shared task for NLPCC 2026. DA-MIVQA extends previous multilingual and multimodal medical video benchmarks by explicitly distinguishing questions according to the type and complexity of evidence required for answering. Specifically, simple questions can often be answered from subtitle-based textual cues, whereas complex questions require visual grounding, procedural understanding, and cross-modal evidence integration. The challenge contains three tracks: Difficulty-Aware Temporal Answer Grounding in Single Video (DA-TAGSV), Difficulty-Aware Video Corpus Retrieval (DA-VCR), and Difficulty-Aware Temporal Answer Grounding in Video Corpus (DA-TAGVC). The dataset is collected from public medical instructional channels, covers diverse scenarios such as first aid, emergency response, rehabilitation, nursing, and general medical education, and is manually verified with difficulty annotations. This paper presents the task motivation, dataset construction, evaluation protocol, participation overview, competition results, and representative systems of DA-MIVQA. DA-MIVQA provides a practical benchmark for evaluating medical instructional video question answering systems under varying textual, visual, temporal, and procedural reasoning requirements.

</details>


### [9] [SpaR3D-MoE: Adaptive 3D Spatial Reasoning from Sparse Views Meets Geometry-Inductive Mixture-of-Experts](https://arxiv.org/abs/2607.06620)
*Haida Feng,Hao Wei,Haolin Wang,Shiwei Li,Chade Li,Yihong Wu*

Main category: cs.CV

TL;DR: SpaR3D-MoE is an end-to-end MLLM framework that uses sparse RGB inputs, adaptive spatiotemporal sampling, and instruction-pose aware MoE to achieve SOTA spatial reasoning, outperforming baselines by 7.8 points on VSI-Bench with 35.4% and 51.4% gains in Route Plan and Relative Direction tasks.


<details>
  <summary>Details</summary>
Motivation: Existing MLLMs struggle with the gap between 2D semantic understanding and 3D spatial geometry. Current 3D-aware models either require costly 3D-specific data or use RGB-only inputs with heuristic sampling and monolithic fusion, which disrupt spatiotemporal connectivity and cause modality contention across spatial tasks.

Method: 1) Adaptive spatiotemporal manifold sampling mechanism that constructs a geometry-aware spatiotemporal graph to extract informative keyframes, reducing redundancy while preserving scene topological connectivity. 2) Heterogeneous geometry-inductive Mixture-of-Experts with an instruction-pose aware router that adaptively routes multimodal tokens to specialized experts, resolving cross-modal contention in monolithic fusion.

Result: SOTA on VSI-Bench, ScanQA, and SQA3D. On VSI-Bench: average 63.5 (7.8 points above strongest baseline), with relative improvements of 35.4% in Route Plan and 51.4% in Relative Direction.

Conclusion: SpaR3D-MoE effectively equips MLLMs with geometry-aware spatial reasoning from only sparse RGB inputs, overcoming key bottlenecks in spatiotemporal connectivity and modality contention.

Abstract: Recent Multimodal Large Language Models (MLLMs) struggle to bridge the representational gap between 2D semantic understanding and 3D spatial geometry. Existing 3D-aware models either rely on costly 3D-specific data or utilize RGB-only inputs with heuristic sampling and monolithic, shallow fusion, which respectively disrupt essential spatiotemporal connectivity and induce modality contention across diverse spatial tasks. To overcome these bottlenecks, we introduce SpaR3D-MoE, an end-to-end framework that enables adaptive spatial reasoning by equipping MLLMs with geometry-aware capabilities from only sparse RGB inputs. First, we propose an adaptive spatiotemporal manifold sampling mechanism that constructs a geometry-aware spatiotemporal graph to extract informative keyframes, effectively mitigating sequence redundancy while preserving the scene's topological connectivity. Second, we introduce the heterogeneous geometry-inductive Mixture-of-Experts driven by an instruction-pose aware router, which adaptively routes multimodal tokens to specialized experts, resolving the cross-modal contention inherent in monolithic fusion. Extensive experiments on VSI-Bench, ScanQA, and SQA3D demonstrate that our method achieves state-of-the-art performance. Notably, SpaR3D-MoE achieves the highest average score of 63.5 on VSI-Bench, outperforming the strongest baseline by 7.8 absolute points, alongside relative improvements of 35.4% and 51.4% in Route Plan and Relative Direction tasks, respectively.

</details>


### [10] [Dynamic-in-Few-Step: Unifying Dynamic Computation and Few-Step Distillation for Efficient Video Generation](https://arxiv.org/abs/2607.06631)
*Yu Cheng,Siyue Yao,Zhongang Qi,Shanyan Guan,Wei Li,Fajie Yuan*

Main category: cs.CV

TL;DR: This paper integrates dynamic structural sparsification into few-step distillation for Video Diffusion Models, creating a step-specific Mixture-of-Models that reduces 24% FLOPs per step and achieves 30x speedup over the teacher model with competitive quality.


<details>
  <summary>Details</summary>
Motivation: VDMs suffer from prohibitive computational costs, and existing few-step distillation methods ignore varying computational demands across different noise levels, using a static architecture for all denoising stages.

Method: Jointly optimizes denoising steps and structured model sparsity via a Progressive Training Strategy and Output Rollout Mechanism, transforming a pre-trained VDM into a step-specific Mixture-of-Models (MoM). A specialized inference engine is built for efficient deployment.

Result: On Wan-14B, removes 24% per-step FLOPs on top of 4-step distillation, adds 1.2x wall-clock gain, achieves 30x speedup over 50-step teacher while preserving competitive generation quality.

Conclusion: The proposed post-training acceleration framework effectively exploits noise-level redundancy via dynamic sparsification, is orthogonal to existing acceleration techniques, and significantly speeds up VDMs without quality degradation.

Abstract: Video Diffusion Models (VDMs) have demonstrated superior generation quality but suffer from prohibitive computational costs. While recent few-step distillation techniques significantly accelerate inference, they typically enforce a static model architecture across all denoising stages, ignoring the varying computational demands inherent to different noise levels. In this work, we propose a novel post-training acceleration framework that exploits this redundancy by integrating dynamic structural sparsification directly into the distillation process. Unlike conventional post-hoc compression applied to a fixed diffusion pipeline, our approach jointly optimizes the denoising steps and structured model sparsity, transforming a pre-trained VDM into a compact, step-specific Mixture-of-Models (MoM). To address the training instability arising from this joint optimization, we introduce a Progressive Training Strategy coupled with an Output Rollout Mechanism, which ensures the coherent learning of structural decisions across timesteps. Furthermore, we develop a specialized inference engine to deploy the resulting MoM efficiently. Our method is orthogonal to existing acceleration techniques and highly effective: On Wan-14B, it removes 24% of the per-step FLOPs on top of 4-step distillation, adding a 1.2x wall-clock gain and reaching a 30x speedup over the 50-step teacher while preserving competitive generation quality.

</details>


### [11] [ProMoE-FL: Prototype-conditioned Mixture of Experts for Multimodal Federated Learning with Missing Modalities](https://arxiv.org/abs/2607.06633)
*Aavash Chhetri,Bibek Niroula,Eduard Vazquez,Yash Raj Shrestha,Prashnna Gyawali,Loris Bazzani,Binod Bhattarai*

Main category: cs.CV

TL;DR: ProMoE-FL introduces a Prototype-conditioned Mixture-of-Experts framework for synthesizing missing modality features in multimodal federated learning, outperforming SOTA methods on four chest X-ray datasets.


<details>
  <summary>Details</summary>
Motivation: Existing multimodal federated learning methods struggle with missing modalities, relying on external public datasets or naive feature synthesis based only on available modalities, which limits robustness and generalization.

Method: ProMoE-FL uses a global client-aware prototype bank to capture modality priors across institutions, then employs a Mixture-of-Experts conditioned on these prototypes and modality indices for direction-aware expert routing to dynamically synthesize missing features.

Result: Outperforms state-of-the-art methods on four public chest X-ray datasets (MIMIC-CXR, NIH Open-I, PadChest, CheXpert) in both homogeneous and heterogeneous federated learning settings.

Conclusion: Prototype-conditioned MoE with direction-aware routing is an effective approach for robust missing-modality feature synthesis in multimodal federated learning without needing external public datasets.

Abstract: In this paper, we address the problem of multimodal federated learning with missing modality. Existing methods utilize an additional public dataset or perform naive feature synthesis that is based solely on the available modality. To address these limitations, we propose ProMoE-FL, a Prototype-conditioned Mixture-of-Experts framework for robust missing-modality feature synthesis in multimodal federated learning. ProMoE-FL builds a global client-aware prototype bank that captures clinically meaningful modality priors across institutions. Our Mixture of Experts is conditioned on these prototypes and modality indices to enable direction-aware expert routing for dynamically synthesizing missing features. We perform extensive quantitative and qualitative evaluations on four public chest X-ray datasets (MIMIC-CXR, NIH Open-I, PadChest, and CheXpert) and demonstrate that ProMoE-FL consistently outperforms state-of-the-art methods in both homogeneous as well as the more challenging heterogeneous settings.

</details>


### [12] [CoMind: Understanding Collaborative Human Activity from Multiple Minds and Views](https://arxiv.org/abs/2607.06691)
*Alexey Gavryushin,Dingxi Zhang,Zhao Huang,Alexandros Delitzas,Jiaqi Chen,Ben Ellis,Cedric Zöllner,Manthan Patel,Manuel Kaufmann,Marc Pollefeys,Xi Wang*

Main category: cs.CV

TL;DR: CoMind is a novel egocentric+exocentric video dataset of real-world cooking collaborations, featuring multimodal data (gaze, audio, 3D scans) and annotations for joint attention, object interaction anticipation, and handover prediction, enabling AI research on Theory of Mind in natural settings.


<details>
  <summary>Details</summary>
Motivation: To study Theory of Mind (mental state inference) in natural collaborative settings, as existing research lacks ecologically valid, multimodal datasets for modeling real-world human-human coordination.

Method: CoMind dataset: multi-perspective (egocentric/exocentric) video + audio + gaze tracking + 3D scene/object scans, with annotations for shared attention, social cues, agent-object interactions, and benchmarks for Joint Attention Estimation, Socially Conditioned Object Interaction Anticipation, and Collaborative Handover Prediction.

Result: Established three benchmarks (Joint Attention Estimation, Socially Conditioned Object Interaction Anticipation, Collaborative Handover Prediction) enabling research on multimodal perception and proactive assistance in collaboration.

Conclusion: CoMind facilitates development of AI systems capable of modeling complex social interactions and reasoning about human behavior in collaborative environments through its richly annotated multimodal data.

Abstract: Human-human collaboration is a fundamental aspect of everyday life, essential to success in a wide range of goal-directed activities from household tasks to professional teamwork. While much research has focused on modeling coordination and task execution, the cognitive processes that support such collaboration, particularly Theory of Mind (the ability to infer the mental states of others), remain difficult to study in natural settings. To address this gap, we introduce a novel egocentric and exocentric video dataset capturing real-world collaboration in cooking scenarios. The dataset integrates multi-perspective video, high-quality audio, gaze tracking, and 3D scene and object scans, with annotations for shared attention to objects, social cues and interactions between agents, as well as agent-object interactions. We establish benchmarks for Joint Attention Estimation, Socially Conditioned Object Interaction Anticipation, and Collaborative Handover Prediction, enabling research on multimodal perception, proactive assistance, and collaborative planning. By providing temporally aligned, richly annotated multimodal data, CoMind facilitates the development and evaluation of AI systems capable of modeling complex social interactions and reasoning about human behaviors in collaborative environments. Our dataset and benchmarks are made available at https://comind.ethz.ch/.

</details>


### [13] [SPEAR: A Simulator for Photorealistic Embodied AI Research](https://arxiv.org/abs/2607.06701)
*Mike Roberts,Renhan Wang,Rushikesh Zawar,Rachith Dey-Prakash,Quentin Leboutet,Stephan R. Richter,Matthias Müller,German Ros,Rui Tang,Stefan Leutenegger,Yannick Hold-Geoffroy,Kalyan Sunkavalli,Vladlen Koltun*

Main category: cs.CV

TL;DR: SPEAR is a Python library that connects to Unreal Engine, offering 14K+ programmable functions, 73 FPS rendering at 1920x1080, novel ground truth modalities, and a high-level programming model for complex UE workflows.


<details>
  <summary>Details</summary>
Motivation: Existing photorealistic simulators suffer from limited generality, programmability, and rendering speed, hindering embodied AI research.

Method: SPEAR uses a modular plugin architecture to connect Python to any Unreal Engine application, exposing 14K+ UE functions, enabling fast direct-to-NumPy rendering, and introducing a high-level graph-based programming model for deterministic UE frame execution.

Result: Renders 1920x1080 at 73 FPS (10x faster than existing UE plugins); provides novel ground truth modalities (non-diffuse intrinsic decomposition, material IDs, physically based shading parameters); supports diverse applications across multiple domains.

Conclusion: SPEAR significantly advances photorealistic simulation for embodied AI by combining speed, programmability, generality, and novel data modalities in a unified framework.

Abstract: Interactive simulators have become powerful tools for training embodied agents and generating synthetic visual data, but existing photorealistic simulators suffer from limited generality, programmability, and rendering speed. We address these limitations by introducing SPEAR: A Simulator for Photorealistic Embodied AI Research. At its core, SPEAR is a Python library that can connect to, and programmatically control, any Unreal Engine (UE) application via a modular plugin architecture. SPEAR exposes over 14K unique UE functions to Python, representing an order-of-magnitude increase in programmable functionality over existing UE-based simulators. Additionally, a single SPEAR instance can render 1920x1080 photorealistic beauty images directly into a user's NumPy array at 73 frames per second - an order of magnitude faster than existing UE plugins - while also providing ground truth image modalities that are not available in any existing UE-based simulator (e.g., a non-diffuse intrinsic image decomposition, material IDs, and physically based shading parameters). Finally, SPEAR introduces an expressive high-level programming model that enables users to specify complex graphs of UE work with arbitrary data dependencies among work items, and to execute these graphs deterministically within a single UE frame. We demonstrate the utility of SPEAR through a diverse collection of example applications: controlling multiple embodied agents with distinct action spaces (e.g., humans, cars, and robots) across several in-the-wild UE projects; rendering photorealistic city-scale environments; manipulating UE's procedural content generation systems; rendering synchronized multi-view images of detailed human faces; coordinating an interactive co-simulation with the MuJoCo physics simulator; and editing scenes with natural language via an AI coding assistant.

</details>


### [14] [A Good Initialization is All You Need for Faithful Visual Attribution](https://arxiv.org/abs/2607.06726)
*Zihan Gu,Jiayu Wang,Hua Zhang,Yue Hu*

Main category: cs.CV

TL;DR: This paper introduces two forward-only methods (CoPAIR and TRACE) for computing compact top-k evidence masks for faithful visual attribution, achieving state-of-the-art results on ImageNet classification and MLLM attribution/repair tasks, and showing that compact masks can serve as actionable outputs themselves.


<details>
  <summary>Details</summary>
Motivation: Existing search-based perturbation methods output complete orderings of all image regions, but many applications (especially MLLM attribution and repair) only need a compact top-k evidence mask. Directly searching for an exactly k-region mask is combinatorial and challenging due to region interactions.

Method: Two forward-only methods: (1) CoPAIR uses PhaseWin-Greedy gap diagnosis to construct coarse singleton/pair candidates that warm-start full-ordering search. (2) TRACE directly searches fixed-cardinality fine-region masks using cross-entropy sampling, elite retention, and distribution updates, with finite-budget recovery analysis.

Result: Achieves state-of-the-art frontier for faithful full-ordering attribution on ImageNet with CLIP ViT-L/14, CLIP RN101, and ResNet-101. TRACE+Greedy gives strongest search-based MLLM attribution on POPE and RePOPE with Qwen2.5-VL-3B-Instruct and LLaVA-v1.5-7B. Direct TRACE masks achieve 94.44% and 96.00% RePOPE repair rates.

Conclusion: Compact evidence masks can be actionable attribution outputs (not just prefixes of full rankings), and the proposed methods establish new state-of-the-art results in both full-ordering and mask-first attribution settings.

Abstract: Faithful visual attribution identifies which image regions support a model prediction. Search-based perturbation methods lead the insertion--deletion faithfulness frontier by masking regions and measuring score changes, but they usually output a complete ordering of all regions. Many applications, especially MLLM attribution and repair, only need a compact top-\(k\) evidence mask. We study this mask-first attribution problem. An exactly \(k\)-region mask is combinatorial: useful evidence can depend on interactions among fine regions. Coarse grouping can stabilize early search but aggregates redundant content, whereas one-step scoring can miss high-value combinations. We introduce two forward-only methods. \textsc{CoPAIR} uses a PhaseWin--Greedy gap diagnosis to construct coarse singleton/pair candidates that warm-start full-ordering search. \textsc{TRACE} directly searches fixed-cardinality fine-region masks with cross-entropy sampling, elite retention, and distribution updates, with a finite-budget recovery analysis. The resulting evidence set can be returned as a compact attribution mask or used to initialize Greedy or PhaseWin when a complete ranking is required. Across ImageNet classification with CLIP ViT-L/14, CLIP RN101, and ResNet-101, our initialized search methods establish a new state-of-the-art frontier for faithful full-ordering attribution under inclusive forward-call accounting. On POPE and RePOPE with Qwen2.5-VL-3B-Instruct and LLaVA-v1.5-7B, \textsc{TRACE}+Greedy gives the strongest search-based MLLM attribution results. Direct \textsc{TRACE} masks further achieve single-point RePOPE repair rates of \(94.44\%\) and \(96.00\%\), showing that compact evidence masks can be actionable attribution outputs, not merely prefixes of full rankings.

</details>


### [15] [Hardware-aware Graph Neural Networks prunning for embedded event-based vision](https://arxiv.org/abs/2607.06739)
*Piotr Wzorek,Kamil Jeziorek,Tomasz Kryjak*

Main category: cs.CV

TL;DR: This paper proposes a hardware-aware optimization strategy for Graph Convolutional Neural Networks on FPGA platforms, using pruning and quantization with design space exploration methods (Fine Grid Search and Greedy layer-wise Iterative Deepening Search) to balance memory savings and accuracy, achieving 26.5-31.4% BRAM reduction with minor accuracy drops across three datasets.


<details>
  <summary>Details</summary>
Motivation: Event-based cameras require efficient real-time data processing with low latency and power consumption, but running GCNNs on embedded FPGA platforms is constrained by limited resources. The authors aim to adapt GCNN architectures to meet these stringent hardware limitations.

Method: The method incorporates hardware-aware pruning and quantization to optimize GCNN models for FPGA deployment, using Fine Grid Search and Greedy layer-wise Iterative Deepening Search for systematic design space exploration, balancing on-chip memory savings against inference accuracy.

Result: BRAM memory reductions of 28.8% (CIFAR-10, 1.65% accuracy drop), 31.4% (MNIST-DVS, 3.55% drop), and 26.5% (N-Caltech101, 5.18% drop) were achieved across multiple datasets and network configurations.

Conclusion: The proposed optimization strategy effectively adapts GCNN architectures to limited FPGA resources while preserving the temporal sparsity of event data, enabling efficient hardware acceleration for event-based vision applications on embedded platforms.

Abstract: Event-based cameras are gaining popularity as the sensor of choice for mobile robotics, due to their high performance in dynamic environments. However, these applications require efficient real-time data processing with low latency and power consumption. One strategy to meet these stringent requirements is hardware acceleration of efficient algorithms that preserve the temporal sparsity of event data. In this work, we propose an optimization strategy for Graph Convolutional Neural Networks models aimed at adapting their architecture to the limited resources of embedded heterogeneous FPGA platforms. Our method incorporates hardware-aware pruning and quantization, taking into account the trade-off between on-chip memory savings and inference accuracy. Strategic exploration of the design space with Fine Grid Search and Greedy layer-wise Iterative Deepening Search methods enables flexible adaptation of the model architecture to the target platform. Our approach was evaluated across various network configurations and multiple datasets, resulting in BRAM memory reductions of 28.8% for CIFAR-10 (with a 1.65% decrease in accuracy), 31.4% for MNIST-DVS (accuracy drop of 3.55%), and 26.5% for N-Caltech101 (with a 5.18% accuracy reduction).

</details>


### [16] [URS-Stereo: Uncertainty-Guided Residual Search for Real-Time Stereo Matching](https://arxiv.org/abs/2607.06779)
*Pouya Sohrabipour,Chaitanya kumar reddy Pallerla,Dongyi Wang*

Main category: cs.CV

TL;DR: URS-Stereo introduces an Uncertainty-Guided Residual Search Module (UGRSM) that adaptively adjusts local cost volume centers based on propagated disparity confidence, improving robustness in coarse-to-fine stereo matching while maintaining real-time performance.


<details>
  <summary>Details</summary>
Motivation: Coarse-to-fine stereo matching methods suffer from unrecoverable failures when propagated disparities from previous stages are inaccurate, causing ground-truth correspondences to fall outside the predefined local search range.

Method: Proposes URS-Stereo with an Uncertainty-Guided Residual Search Module (UGRSM) that predicts reliability of propagated disparities and computes residual search offsets to adaptively relocate local cost volume centers before disparity refinement.

Result: Consistent improvements in disparity estimation accuracy on SceneFlow, KITTI 2012/2015, Middlebury, and ETH3D benchmarks while maintaining real-time inference speed.

Conclusion: The uncertainty-guided search adaptation effectively enhances robustness of local correspondence estimation without sacrificing computational efficiency, validating the approach for real-time applications.

Abstract: Real-time stereo matching is crucial for robotics, autonomous systems, and embedded vision applications, where both computational efficiency and disparity accuracy are required. Recent coarse-to-fine stereo matching methods improve efficiency by progressively refining disparity estimates using local cost volumes at higher resolutions. However, these methods rely heavily on the accuracy of propagated disparity estimates from previous stages. When the propagated disparity is inaccurate, the ground-truth correspondence may fall outside the predefined local search range, leading to unrecoverable matching failures during subsequent refinement. In this paper, we propose URS-Stereo, a real-time coarse-to-fine stereo matching framework that addresses this limitation through uncertainty-guided search adaptation. Specifically, we introduce an Uncertainty-Guided Residual Search Module (UGRSM), which predicts the reliability of propagated disparities together with residual search offsets to adaptively relocate the centers of local cost volumes before disparity refinement. By dynamically adjusting the search region according to the confidence of the propagated disparity, the proposed method significantly improves the robustness of local correspondence estimation while preserving the computational efficiency of coarse-to-fine stereo matching. Extensive experiments on SceneFlow, KITTI 2012, KITTI 2015, Middlebury, and ETH3D demonstrate that URS-Stereo consistently improves disparity estimation while maintaining real-time inference speed, validating the effectiveness of the proposed uncertainty-guided search strategy

</details>


### [17] [Rail Track Extraction from Rasterized Classified Point Clouds Using a Full-Resolution, Fully Convolutional Recurrent Neural Network](https://arxiv.org/abs/2607.06829)
*Alexander Gribov,Jie Chang*

Main category: cs.CV

TL;DR: This paper presents a novel rail track extraction method from 3D point clouds using a fully convolutional recurrent neural network trained on synthetic data, followed by morphological operations, smoothing, and Dynamic Time Warping to extract rail top and track centerlines with minimal manual intervention.


<details>
  <summary>Details</summary>
Motivation: To automate rail track extraction from 3D point clouds for improved railway asset management and maintenance, reducing manual effort in inspection and mapping workflows.

Method: 1) Rasterize points of railroad tracks; 2) Apply fully convolutional recurrent neural network (trained on synthetic data) to reduce noise; 3) Use morphological operations to refine data and extract centerlines; 4) Smooth centerlines to remove irregularities; 5) Transfer 3D lidar information onto 2D polylines with vertical smoothing; 6) Apply Dynamic Time Warping (DTW) to find a single centerline for both tracks.

Result: Experimental validation confirms the method yields high-quality rail track extraction with minimal manual intervention.

Conclusion: The proposed approach effectively extracts rail top centerlines and track centerlines from classified 3D point clouds, demonstrating strong potential for automated railway infrastructure mapping.

Abstract: Rail track extraction is essential for effective railway asset management and maintenance, especially in automated inspection and mapping workflows. This paper introduces a novel method for extracting rail tracks from classified 3D point clouds using a fully convolutional recurrent neural network that preserves full spatial resolution and is trained exclusively on synthetically generated data. This approach enhances per-pixel quality and is particularly suited for rail track extraction. The proposed method begins by rasterizing points corresponding to railroad tracks, then applies the neural network to reduce noise and yield a cleaner track representation suitable for vectorization [1]. Subsequent morphological operations further refine the resultant data, enabling accurate track centerline extraction. Next, the extracted centerlines undergo smoothing to eliminate residual irregularities [2, 3]. Finally, the algorithm transfers 3D information from lidar points onto 2D polylines and applies additional vertical smoothing. A single centerline for both tracks is found using the Dynamic Time Warping (DTW) algorithm [4]. The final outcome consists of rail top centerlines and track centerlines derived for rail pairs, with minimal manual intervention. Experimental validation confirms the effectiveness of this method in yielding high-quality rail track extraction.

</details>


### [18] [WildCity: A Real-World City-Scale Testbed for Rendering, Simulation, and Spatial Intelligence](https://arxiv.org/abs/2607.06838)
*Xiangyu Han,Mengyu Yang,Jiaqi Li,Bowen Chang,Ziyu Chen,Hexu Zhao,Rahul Kumar Agrawal,Anthony Rodriguez,Fiona Hua,Marco Pavone,Chen Feng,Yiming Li*

Main category: cs.CV

TL;DR: WildCity is a large-scale multimodal dataset (~83.7km per trajectory) for city-scale 3D reconstruction, enabling simulation-ready urban digital twins and benchmarking scalability, extrapolation, and uncertainty.


<details>
  <summary>Details</summary>
Motivation: Humans can form spatial mental maps of entire cities, but AI lacks city-scale data to build comparable representations; existing datasets are too small or too clean for real-world urban reconstruction.

Method: Collected 18 long-range trajectories (avg. 83.7km) from autonomous fleets in complex urban environments; built an urban-tailored reconstruction baseline; converted reconstructed scenes into a closed-loop simulator.

Result: Established a benchmark dataset and baseline for city-scale neural reconstruction, and identified three key challenges: scalability, extrapolation, and uncertainty in simulation-ready urban digital twins.

Conclusion: WildCity aims to push AI toward human-like spatial perception, memory, and reasoning at the scale of entire cities, catalyzing progress in city-scale rendering and embodied intelligence.

Abstract: Humans can navigate an unfamiliar city and gradually form a coherent spatial mental map spanning tens of square kilometers. Can AI build spatial representations at a comparable scale? Although recent foundation models have advanced scene reconstruction and embodied intelligence, scaling to entire cities remains an open challenge, primarily due to the lack of city-scale data. To bridge the gap, we introduce WildCity, a real-world multimodal dataset collected by autonomous fleets traversing complex urban environments. Our dataset includes 18 trajectories, each averaging 83.7 kilometers in length, and preserves the core challenges of in-the-wild perception, e.g., dynamic objects, lighting variations, and imperfect camera poses. We further establish an urban-tailored reconstruction baseline and convert the reconstructed environments into a closed-loop simulator. Beyond the dataset and baseline, we systematically analyze the key challenges on the path to simulation-ready urban digital twins: scalability, extrapolation, and uncertainty. Ultimately, WildCity aims to catalyze progress not only in city-scale rendering, but more broadly in the pursuit of AI that can perceive, remember, and reason across space at a scale comparable to human cognition. Project page: https://han-xiangyu.github.io/Wild-City/

</details>


### [19] [Retrieving and Refining Winning Noise Tickets for Diffusion-Based Motion Generation](https://arxiv.org/abs/2607.06843)
*Sakuya Ota,Qing Yu,Kent Fujiwara,Satoshi Ikehata,Ikuro Sato*

Main category: cs.CV

TL;DR: WINRO is a training-free framework that improves text-to-motion alignment by retrieving and optimizing "winning noise tickets" (noise instances with latent structure) before diffusion sampling, reducing semantic drift without retraining.


<details>
  <summary>Details</summary>
Motivation: Diffusion-based text-to-motion models suffer from semantic drift—where generated motions deviate from input text semantics—especially in compositional and long-duration sequences requiring temporal consistency.

Method: WINRO maps random noises to motion features under null prompts, retrieves the best-aligned noise for a given text, then refines it via KL-regularized optimization to reduce semantic gap while preserving Gaussian prior. An optional LoRA adapter speeds this into a single forward pass.

Result: WINRO improves text-motion fidelity on MDM and MotionLCM over HumanML3D without retraining, enhances temporal robustness on MTT benchmark, and generalizes to motion stylization and spatial constraint satisfaction.

Conclusion: Initial noise selection is key to semantic consistency in text-to-motion diffusion; WINRO offers an effective, training-free solution that generalizes across models and tasks.

Abstract: Diffusion-based text-to-motion models synthesize realistic human motions but often exhibit semantic drift from the input text. Motion is inherently temporal, especially in compositional and long-duration sequences that require semantic consistency across multiple action segments and smooth kinematic transitions throughout the trajectory. We posit that the initial noise is central to this consistency: within the Gaussian noise space, certain instances, i.e. winning noise tickets, carry latent structure that biases denoising toward particular motion semantics, even under null prompts. We propose WInning Noise Retrieval and Optimization (WINRO), a training-free, model-agnostic framework that improves text-motion alignment by selecting and refining such tickets before diffusion sampling. WINRO maps random noises to motion features generated under null prompts, retrieves the best-aligned noise for a given text, and refines it via a KL-regularized objective that reduces the residual semantic gap while preserving the Gaussian prior. An optional LoRA-based adapter amortizes this refinement into a single forward pass. WINRO consistently improves text-motion fidelity across different base models, MDM and MotionLCM, on HumanML3D without retraining, improves temporal robustness on the MTT benchmark, and generalizes to applications such as motion stylization and spatial constraint satisfaction.

</details>


### [20] [Gen4U: Unifying Video Generation and Understanding via Diffusion](https://arxiv.org/abs/2607.06856)
*Michael King,Aravindh Mahendran,Matthew Koichi Grimes,Fedor Kitashov,Adham Elarabawy,Pedro Velez,Maks Ovsjanikov,Viorica Pătrăucean*

Main category: cs.CV

TL;DR: Video diffusion models encode rich, structured visual representations that capture both low-level geometry and high-level semantics. Gen4U repurposes these frozen representations as powerful video encoders across diverse tasks without fine-tuning, unifying generation and understanding.


<details>
  <summary>Details</summary>
Motivation: Prior work shows diffusion representations capture low-level geometry but struggle with high-level semantics. The authors aim to investigate whether video diffusion models overcome this limitation and can serve as general-purpose video encoders.

Method: Probe intermediate activations of video diffusion models using mutual-kNN alignment metrics. Analyze how visual representations evolve across network depth and noise levels. Propose Gen4U, a framework that uses frozen video diffusion model representations with a single forward pass for downstream tasks.

Result: Video diffusion models exhibit highly structured latent spaces. Moderate noise levels yield linearly separable global semantics, while fine-grained details persist at lower noise levels but become spatially scattered. Gen4U achieves strong performance on video classification, depth estimation, camera pose estimation, and image/video captioning without fine-tuning.

Conclusion: Large-scale video diffusion models function as highly competitive video encoders. Gen4U unifies generation and understanding paradigms, preserving generative ability while achieving strong perception performance across diverse tasks.

Abstract: Prior work suggests that diffusion representations capture low-level geometry but struggle with high-level semantics. We demonstrate that state-of-the-art video diffusion models overcome this limitation. By systematically probing their intermediate activations using recent mutual-kNN alignment metrics, we reveal a highly structured latent space where visual representations evolve across both network depth and noise levels. We show that while moderate noise levels yield linearly separable global semantics, fine-grained details persist at lower noise levels but become spatially scattered, requiring attention mechanisms to decode. Building on these insights, we introduce Gen4U (Generation for Understanding), a framework that repurposes these generative representations with a single forward pass. Our experiments establish that frozen, large-scale video diffusion models function as highly competitive video encoders across a wide spectrum of tasks, spanning semantic and non-semantic objectives (video classification, depth estimation, camera pose estimation, image and video captioning). Bypassing fine-tuning, Gen4U unifies the generation and understanding paradigms, achieving strong perception performance while fully preserving the model's ability to generate high-quality video.

</details>


### [21] [Geometric Collapse: When Vision Models Fail to Verify Physical Causality](https://arxiv.org/abs/2607.06871)
*Wentao Zhang,Jinhu Qi,Weiqiang Jin,Yifei Zhang,Chan-Tong Lam,Irwin King*

Main category: cs.CV

TL;DR: The paper introduces Scrambled Edges, a controlled counterfactual that injects unnatural edge cues, revealing that current dense geometric predictors (CNNs, ViTs, SSL, diffusion models) lack mechanisms to detect or quarantine physically implausible edge information, causing global geometric collapse.


<details>
  <summary>Details</summary>
Motivation: To investigate whether large-scale self-supervised models for dense geometric prediction can detect physically implausible inputs at inference time, specifically whether they can distinguish natural edges from edge-like cues that violate physical scene constraints (surface continuity, illumination, occlusion).

Method: Proposes "Scrambled Edges" — a controlled counterfactual that injects edge-like cues violating physical plausibility, with energy-matched and structure-matched controls. Tests across CNN, ViT, and SSL depth predictors on NYU Depth v2 and KITTI datasets, plus diffusion/flow-matching models.

Result: Scrambled Edges cause up to 3.2x larger prediction deviation than energy-matched noise. The geometric collapse propagates globally; even with oracle knowledge of corrupted regions, output-level repair recovers only 47%, with substantial errors outside the mask.

Conclusion: Current dense predictors lack reliable mechanisms for quarantining physically unsupported edge cues, motivating the need for explicit plausibility scoring and selective cue integration in future architectures.

Abstract: Recent progress in large-scale self-supervised learning has improved dense geometric prediction, but it remains unclear whether such scaling yields inference-time physical plausibility checks. We propose Scrambled Edges, a controlled counterfactual that injects salient edge-like cues while violating surface continuity, illumination coherence, and occlusion ordering. With energy-matched and structure-matched controls, we isolate the effect of unsupported edge evidence from high-frequency energy and edge sparsity. Across CNN/ViT/SSL depth predictors on NYU Depth v2 and KITTI, Scrambled Edges induce up to 3.2x larger deviation from clean predictions than energy-matched noise; additional diffusion and flow-matching depth estimators show attenuated but still significant collapse. The resulting Geometric Collapse propagates globally: even with oracle knowledge of the corrupted region, output-level repair recovers only 47%, with substantial error outside the mask. These findings provide controlled behavioral evidence that current dense predictors lack reliable mechanisms to quarantine physically unsupported edge cues, motivating explicit plausibility scoring and selective cue integration.

</details>


### [22] [Ensemble Deep Learning Approaches for AI-Altered Video Detection](https://arxiv.org/abs/2607.06872)
*Laiba Khan,Hung-Mao Wu,Wei Lin,Frank Bi,Yousef Abdelhadi,Joshua Jung*

Main category: cs.CV

TL;DR: This paper proposes a multimodal deepfake detection system combining audio (AASIST) and visual (EfficientNet, XceptionNet, MesoNet) models via ensemble strategies (mean averaging and stacking), achieving ~70% average accuracy and highlighting generalization as the main challenge.


<details>
  <summary>Details</summary>
Motivation: The rapid rise of AI-generated videos makes it harder to distinguish real from manipulated content. Existing single-model detection methods struggle to generalize across different types of deepfakes.

Method: A multimodal pipeline: input video → audio separated (AASIST for detection) + face frames extracted via MTCNN → visual analysis by EfficientNet, XceptionNet, MesoNet → scores combined via ensemble strategies (mean averaging and stacking with a trained meta-model).

Result: Individual models perform well on trained datasets but drop on diverse datasets. The ensemble approach improves robustness, but average accuracy remains around 70%, highlighting generalization as a central challenge.

Conclusion: Using both audio and visual information together is more reliable for deepfake detection. However, generalization to unseen manipulations remains the key open problem.

Abstract: The increasing accessibility of artificial intelligence has led to a rapid rise in AI-generated videos, making it more difficult to distinguish between real and manipulated content. Many existing detection methods rely on a single model and often struggle to generalize across different types of deepfakes. In this work, we developed a multimodal deepfake detection system that combines both audio and visual analysis using an ensemble of models. The system includes AASIST for audio-based detection, and EfficientNet, XceptionNet, and MesoNet for analyzing visual features in video frames. The pipeline takes a video as input, separates the audio, and extracts face frames using MTCNN. Each model produces a score indicating the likelihood of the input being fake. These scores are then combined using ensemble strategies, including mean averaging and stacking. Mean fusion provides a simple and stable baseline, while stacking uses a trained meta-model to learn how to combine predictions more effectively. Results show that while individual models perform well on the datasets they were trained on, their performance drops when tested on more diverse datasets. The ensemble approach helps improve overall robustness by combining predictions from multiple models, leading to more consistent performance across different types of deepfakes. This suggests that using both audio and visual information together is a more reliable approach for deepfake detection. Our results highlight generalization to unseen manipulations as the central open challenge, with average accuracy around 70%.

</details>


### [23] [Video2Reaction: Mapping Video to Audience Reaction Distribution in the Wild](https://arxiv.org/abs/2607.06875)
*Trang Nguyen,Sidong Zhang,Shiv Shankar,Gauri Jagatap,Deepak Chandran,Andrea Fanelli,Madalina Fiterau*

Main category: cs.CV

TL;DR: The paper introduces Video2Reaction, a large-scale multimodal dataset of over 10,000 videos mapping movie segments to audience emotional reactions via social media. A two-stage multi-agent LLM pipeline achieves 86% annotation correctness. Benchmarks show foundation video models fail in zero-shot but improve with finetuning (77% Top-3 F1), indicating significant remaining challenges.


<details>
  <summary>Details</summary>
Motivation: To enable audience reaction prediction for video content, improving content creation, recommendation systems, and media analysis by providing a reliable dataset and benchmark for modeling induced emotions of viewers in the wild.

Method: Created Video2Reaction dataset with 10,000+ videos; developed a two-stage multi-agent annotation pipeline using open-source LLMs achieving 86% correctness; benchmarked foundation video models in zero-shot and finetuned settings for reaction distribution prediction.

Result: Pretrained foundation models fail in zero-shot; finetuning yields state-of-the-art predictors but only 77% Top-3 F1 (LLaVA-Next) for dominant reaction prediction. The task remains challenging.

Conclusion: Video2Reaction enables audience reaction prediction research, but the substantial performance gap highlights the difficulty of modeling collective audience emotions from video alone.

Abstract: Understanding and forecasting audience reactions to video content are crucial for improving content creation, recommendation systems, and media analysis. To enable audience reaction prediction and other content engagement applications, we introduce $\textbf{Video2Reaction}$, a multimodal dataset that maps short movie segments to a distribution of $\textit{induced emotions}$ of viewers in the wild, as expressed through social media. $\textbf{Video2Reaction}$ spans more than 10,000 videos and serves as a reliable benchmark as well as a training resource for audience reaction prediction. To enable cost-effective continuous annotations as reactions may change over time, we develop a two-stage multi-agent pipeline using only open-source LLMs, achieving 86% correctness under blind human verification despite the inherently noisy and subjective nature of the task. We establish the first benchmark for video-to-reaction-distribution prediction in the wild and show that pretrained foundation video models fail in zero-shot settings, while finetuning transforms them into state-of-the-art predictors capable of modeling both full reaction distributions and dominant responses from video alone. However, the task remains challenging: even the strongest methods achieve only 77% Top-3 F1 in dominant reaction prediction (LLaVA-Next), highlighting a substantial gap in modeling collective audience reaction. \modification{Dataset and code are available at our project page: https://information-fusion-lab-umass.github.io/video2reaction-bench.github.io

</details>


### [24] [ReMoDEx: A Local-to-Global Relevance-Based Model Decision Explainability Framework for large-Scale Image Datasets](https://arxiv.org/abs/2607.06889)
*Abhay Kumar Pathak,Mrityunjay Chaubey,Manjari Gupta*

Main category: cs.CV

TL;DR: ReMoDEx is a scalable framework for systematically analyzing classifier decision patterns across entire datasets, applied to a COVID-19 chest X-ray classifier. It combines local explainability methods with a global clustering module, revealing that the VGG16 model relied on both central thoracic regions and border/corner artifacts—indicating shortcut learning that accuracy metrics alone could not detect.


<details>
  <summary>Details</summary>
Motivation: Deep learning image classifiers achieve high accuracy but remain opaque; inspecting individual heatmaps does not scale to large datasets, and models may rely on irrelevant cues (shortcuts, artifacts) that standard metrics fail to reveal.

Method: ReMoDEx pipeline: model inference → target class selection → relevance map generation (GradCAM++, Integrated Gradients, Occlusion Sensitivity, LRP) → heatmap standardization → similarity-based clustering of patterns → cluster-level interpretation → spatial relevance assessment. Local explainers are each combined with a single global clustering module.

Result: Applied to a VGG16 COVID-19 classifier (86.27% test accuracy, 0.9624 AUC). All explainers consistently revealed two decision strategies: central thoracic region focus and border/corner sensitivity, indicating shortcut learning. Masked image validation confirmed that occluding either region altered model confidence and predictions.

Conclusion: ReMoDEx provides a scalable, automatic alternative to sample-by-sample inspection, serving as an essential complement to accuracy-based evaluation by revealing problematic decision patterns (e.g., shortcut learning) that conventional metrics cannot detect.

Abstract: Deep learning image classifiers achieve strong predictive performance yet remain opaque in how decisions are formed. A model may predict correctly while relying on irrelevant cues, shortcut associations, peripheral structures, or device level artifacts instead of task relevant regions. On large scale datasets this opacity is especially problematic, since inspecting heatmaps one sample at a time cannot scale to thousands of predictions. We propose Relevance Based Model Decision Explainability (ReMoDEx), a framework for systematic, dataset scale assessment of model decision behaviour in image classification. ReMoDEx defines a stepwise pipeline: model inference, target class selection, relevance map generation, heatmap standardisation, similarity based grouping of patterns, cluster level interpretation, and spatial relevance assessment. Local methods GradCAM++, Integrated Gradients, Occlusion Sensitivity, and Layerwise Relevance Propagation are each combined independently with a single global module that summarises an entire set of relevance maps into a few decision strategy clusters, replacing sample by sample inspection with an automatic, scalable summary. To demonstrate ReMoDEx, we applied it to a VGG16 based classifier distinguishing COVID-19, Normal, Lung Opacity, and Viral Pneumonia. The classifier showed stable performance (86.27% test accuracy, 0.9624 test AUC). However, each explainer combined with the global module consistently produced two recurring strategies: central thoracic region decisions and border/corner sensitive decisions, indicating possible shortcut learning that conventional metrics could not reveal. Masked image validation confirmed that model confidence and predicted class changed when central or peripheral regions were occluded. ReMoDEx thus provides a scalable relevance based decision assessment framework and an essential complement to accuracy based evaluation.

</details>


### [25] [Seeing What Matters: Lesion-Aware High-Resolution Patch Discovery and Fusion for Chest X-ray Report Generation](https://arxiv.org/abs/2607.06909)
*Yingshu Li,Yunyi Liu,Zhenghao Chen,Tong Chen,Zailong Chen,Lingqiao Liu,Lei Wang,Luping Zhou*

Main category: cs.CV

TL;DR: LePaX enables efficient high-resolution chest X-ray report generation (up to 1920x1920) without increasing visual token count, by adaptively allocating high-res capacity to diagnostically relevant regions and fusing local and global features.


<details>
  <summary>Details</summary>
Motivation: Existing RRG systems use heavily downsampled inputs (e.g., 256x256), losing subtle clinical cues. Naive high-resolution tiling causes prohibitive token inflation, and global compression degrades diagnostic fidelity.

Method: LePaX proposes (1) Learnable Spatial Resolution Allocation (LSRA) to learn a spatial utility map that allocates limited high-res tokens to diagnostically relevant regions, and (2) Global-Regional Fusion (GRF) for token-preserving region-to-global refinement via spatially aligned resolution write-back.

Result: LePaX consistently improves clinical and linguistic metrics on multiple CXR benchmarks, enabling native-resolution perception with over 10x fewer visual tokens than naive high-res tiling.

Conclusion: LePaX is the first RRG framework achieving efficient high-resolution CXR perception without increasing token count, demonstrating that adaptive spatial allocation can bridge the resolution gap in medical vision-language tasks.

Abstract: Despite rapid advances in chest X-ray (CXR) foundation models, most radiology report generation (RRG) systems still rely on heavily downsampled inputs (e.g., 256x256) due to the fixed visual token budgets of pretrained vision encoders, suppressing subtle yet clinically important cues present in native-resolution images. However, enabling high-resolution (high-res) perception remains challenging: naive tiling causes prohibitive token inflation, while global compression suppresses subtle lesions and degrades diagnostic fidelity. Inspired by radiologists' workflow, localizing suspicious regions before detailed high-res assessment. We propose Lesion-Aware High-Resolution Patch Discovery and Fusion for Chest X-ray Reporting (LePaX), the first RRG framework that enables efficient high-res CXR perception (up to 1920x1920) without increasing the vision-token count. LePaX formulates high-res perception as a constrained spatial resolution allocation problem under a fixed token budget and introduces two key components: Learnable Spatial Resolution Allocation (LSRA), which learns a spatial utility map that adaptively allocates limited high-res capacity to diagnostically relevant regions, enabling targeted extraction of high-res patches from native CXRs; and Global-Regional Fusion (GRF), which performs token-preserving region-to-global refinement by projecting high-resolution regional evidence back onto the global feature grid through spatially aligned resolution write-back, avoiding token inflation. Experiments on multiple CXR benchmarks demonstrate that LePaX consistently improves both clinical and linguistic metrics while enabling native-resolution CXR perception with over 10x fewer visual tokens than naive high-res tiling.

</details>


### [26] [Smart Scissor: Coupling Spatial Redundancy Reduction and CNN Compression for Embedded Hardware](https://arxiv.org/abs/2607.06915)
*Hao Kong,Di Liu,Shuo Huai,Xiangzhong Luo,Weichen Liu,Ravi Subramaniam,Christian Makaya,Qian Lin*

Main category: cs.CV

TL;DR: Smart Scissor is a unified CNN compression framework that combines dynamic image cropping (foreground localization at small resolution) with compound shrinking (coordinated compression of depth, width, resolution) to reduce computational overhead while maintaining accuracy. It reduces ResNet50's cost by 41.5% with 0.3% accuracy improvement on ImageNet-1K.


<details>
  <summary>Details</summary>
Motivation: Scaling down input image resolution reduces CNN computation but loses important foreground features, causing accuracy degradation due to spatial redundancy (e.g., background pixels). Existing methods shrink whole images uniformly, failing to preserve critical object details.

Method: 1) Dynamic image cropping: a lightweight foreground predictor localizes and crops the foreground object, enabling accurate recognition at small resolutions. 2) Compound shrinking: coordinately compresses three CNN dimensions (depth, width, resolution) for embedded efficiency. Both are combined into a unified framework called Smart Scissor.

Result: On ImageNet-1K: Reduces ResNet50 computational cost by 41.5% while improving top-1 accuracy by 0.3%. Compared to HRank (state-of-the-art), achieves 4.1% higher top-1 accuracy at the same computational cost.

Conclusion: Smart Scissor effectively reduces spatial and architectural redundancy in CNNs, achieving significant computational savings without accuracy loss—even slight improvement—making it highly suitable for edge AI deployment.

Abstract: Scaling down the resolution of input images can greatly reduce the computational overhead of convolutional neural networks (CNNs), which is promising for edge AI. However, as an image usually contains much spatial redundancy, e.g., background pixels, directly shrinking the whole image will lose important features of the foreground object and lead to severe accuracy degradation. In this paper, we propose a dynamic image cropping framework to reduce the spatial redundancy by accurately cropping the foreground object from images. To achieve the instance-aware fine cropping, we introduce a lightweight foreground predictor to efficiently localize and crop the foreground of an image. The finely cropped images can be correctly recognized even at a small resolution. Meanwhile, computational redundancy also exists in CNN architectures. To pursue higher execution efficiency on resource-constrained embedded devices, we also propose a compound shrinking strategy to coordinately compress the three dimensions (depth, width, resolution) of CNNs. Eventually, we seamlessly combine the proposed dynamic image cropping and compound shrinking into a unified compression framework, Smart Scissor, which is expected to significantly reduce the computational overhead of CNNs while still maintaining high accuracy. Experiments on ImageNet-1K demonstrate that our method reduces the computational cost of ResNet50 by 41.5% while improving the top-1 accuracy by 0.3%. Moreover, compared to HRank, the state-of-the-art CNN compression framework, our method achieves 4.1% higher top-1 accuracy at the same computational cost. The codes and data are available at https://github.com/ntuliuteam/smart-scissor

</details>


### [27] [LoCA: Spatially-Aware Low-Rank Convolutional Adaptation of Vision Foundation Models](https://arxiv.org/abs/2607.06918)
*Sojung An,Junha Lee,Sujeong You,Nam Ik Cho,Donghyun Kim*

Main category: cs.CV

TL;DR: LoCA proposes a convolution-aware PEFT method that decouples channel and spatial adaptation via low-rank channel mixing and SVD-based spatial refinement, outperforming standard LoRA on convolutional layers.


<details>
  <summary>Details</summary>
Motivation: Standard LoRA is designed for 2D matrices in transformer attention layers, but convolutional kernels are 4D tensors with coupled spatial-channel information. Forcing them into 2D matrices disrupts spatial topology.

Method: LoCA decouples channel and spatial adaptation: (1) low-rank channel adaptation for cross-channel mixing, (2) spatial bases extracted from pre-trained kernels via SVD for spatial refinement.

Result: Achieves competitive or state-of-the-art performance on fine-grained classification, domain-generalized semantic segmentation, and generative benchmarks.

Conclusion: LoCA preserves pre-trained spatial priors better than standard LoRA and serves as an effective convolution-aware PEFT framework.

Abstract: Pre-trained Vision Foundation Models (VFMs) provide strong visual representations for diverse downstream tasks. The key challenge of VFM adaptation stems from the prohibitive costs of full fine-tuning and catastrophic forgetting. To address this, Low-Rank Adaptation (LoRA) has emerged as the prevailing paradigm for Parameter-Efficient Fine-Tuning (PEFT). However, LoRA is typically designed for transformer self-attention layers parameterized by 2D matrices. Since convolutional kernels inherently couple spatial and channel information within a 4D tensor, forcing them into a monolithic 2D matrix disrupts the inherent spatial topology. In this paper, we propose Low-Rank Convolutional Adaptation (LoCA), a convolution-aware PEFT framework that addresses spatial-channel entanglement by decoupling channel and spatial adaptation. LoCA introduces a low-rank channel adaptation for dense cross-channel mixing and refines spatial bases extracted from pre-trained kernels via Singular Value Decomposition (SVD). Experimental results show that LoCA preserves pre-trained spatial priors and achieves competitive or state-of-the-art performance across fine-grained classification, domain-generalized semantic segmentation, and generative benchmarks.

</details>


### [28] [Compass: Prostate Cancer Detection Needs Multi-View Context](https://arxiv.org/abs/2607.06919)
*Paul F. R. Wilson,Mohamed Harmanani,Zhuoxin Guo,Obed K. Dzikunu,Hannes Cash,Adam Kinnaird,Brian Wodlinger,Purang Abolmaesumi,Parvin Mousavi*

Main category: cs.CV

TL;DR: Compass is a novel AI framework that analyzes full micro-ultrasound video studies (rather than single images) for prostate cancer detection, using a transformer conditioned on probe rotational angle to aggregate evidence, achieving strong performance on multi-center clinical trial data.


<details>
  <summary>Details</summary>
Motivation: Existing AI methods analyze single μUS images in isolation, whereas expert readers assess full video studies for better prostate cancer detection. Compass aims to bridge this gap by modeling the complete video context.

Method: Compass models a μUS study as a stream of 2D images, jointly integrating rotational sweep videos with biopsy-acquired frames. It uses a transformer conditioned on the probe's rotational angle to aggregate evidence across the study, with a decoder head predicting frame-level and study-level risk scores.

Result: Evaluated on a multi-center clinical trial dataset, Compass outperforms baseline AI methods and competes with clinical expert risk scores, highlighting the value of multi-view context.

Conclusion: Compass demonstrates strong performance for prostate cancer detection using micro-ultrasound, outperforming baseline AI methods and complementing clinical expert risk scores by integrating full video studies rather than single frames.

Abstract: Artificial intelligence (AI) analysis of micro-ultrasound ($μ$US) has shown promise for prostate cancer (PCa) detection. However, most existing AI methods focus on the analysis of single $μ$US images in isolation. By contrast, expert $μ$US readers typically assess a full recorded video study, which provides three-dimensional context, to improve PCa detection compared to single-frame analysis. Inspired by this clinical workflow, we propose Compass, a novel AI methodology which models a $μ$US study as a stream of 2D images. Compass jointly integrates rotational sweep videos of the prostate with $μ$US frames acquired at the moment of biopsy, and performs evidence aggregation across the study using a transformer conditioned on the probe's rotational angle. Finally, a decoder head predicts frame-level and study-level risk scores for the patient. The model is trained and evaluated using a multi-center clinical trial dataset of $μ$US studies, including continuous rotational scans of the prostate and videos captured during biopsy acquisition. We compare the proposed method to baseline AI methods from the literature and to risk scores provided by clinical experts. Our framework shows strong performance, highlighting the value of multi-view context for $μ$US PCa detection, and providing a potentially powerful tool to complement human expertise in $μ$US-based PCa diagnosis. Our code is available at: https://github.com/mharmanani/Compass.

</details>


### [29] [Bi-PT: Bidirectional Cross-Attention Point Transformers for Four-Chamber Heart Reconstruction from Sparse Cardiac MRI Data](https://arxiv.org/abs/2607.06923)
*Chenchuhui Hu,Shaoming Pan,Leon Axel,Meng Ye*

Main category: cs.CV

TL;DR: Bi-PT reconstructs 3D four-chamber heart meshes from sparse clinical CMR data using bidirectional point cross-attention and Neural ODE-based diffeomorphic deformation, achieving robust performance.


<details>
  <summary>Details</summary>
Motivation: To address the error-prone generation of 3D cardiac shapes from sparse point clouds extracted from routine clinical CMR (long-axis and short-axis views), enabling accurate 3D heart mesh reconstruction from limited clinical data.

Method: Bi-PT uses bidirectional point cross-attention between an atlas and the sparse point cloud (SPC) with semantic labels for robust feature learning. The deformation field is modeled as a Neural ODE (NODE) with per-point affine transformations, ensuring locally affine diffeomorphic deformation. A semantic label loss and smoothness regularization are added to improve correspondence and stability.

Result: Extensive experiments demonstrate that Bi-PT achieves accurate and robust performance compared to baselines for reconstructing four-chamber heart meshes from sparse clinical CMR data.

Conclusion: Bi-PT provides an effective pipeline for 3D cardiac mesh reconstruction from sparse clinical CMR data, leveraging bidirectional attention and NODE-based diffeomorphic deformation for improved accuracy and robustness.

Abstract: We propose Bi-PT, a pipeline for reconstructing 3D four-chamber human heart meshes from clinical sparsely sampled cardiac magnetic resonance imaging (CMR) data. This work addresses the error-prone generation of 3D cardiac shape from a sparse point cloud (SPC) extracted from 2D long-axis and short-axis views used in routine clinical CMR protocols. Bi-PT enables accurate inference of the four-chamber heart mesh from the SPC by learning robust point features via bidirectional point cross-attention between an atlas and the SPC, together with per-point semantic labels that improve correspondence estimation. We formulate the deformation field as a Neural Ordinary Differential Equation (NODE) parameterized by a per-point affine transformation and translation to deform the atlas toward the target heart shape. By learning such a NODE, we can guarantee the deformation field to be a locally affine diffeomorphic deformation. We also integrate a semantic label loss into the Chamfer distance to encourage label-consistent correspondences and add a smoothness regularization to stabilize and improve the learning of the deformation field. Extensive experiments demonstrate that Bi-PT achieves accurate and robust performance compared to baselines.

</details>


### [30] [General Incomplete Multimodal Learning via Dynamic Quality Perception](https://arxiv.org/abs/2607.06943)
*Xiangyu Meng,Shicai Wei*

Main category: cs.CV

TL;DR: GIML is a unified framework for multimodal learning that handles both missing entire modalities (inter-modality) and severely corrupted modalities (intra-modality degradation) through dynamic quality perception, noise-aware quality estimation, and noise-semantic decoupling.


<details>
  <summary>Details</summary>
Motivation: Existing multimodal methods focus on entire modality absence (inter-modality missing) but fail when modalities are present yet severely corrupted (intra-modality degradation). These two missing types often coexist in practice, but no prior work addresses both simultaneously.

Method: (1) Models heterogeneous missing patterns as continuous modality information degradation for degradation-aware adaptive fusion. (2) Noise-aware Quality Estimator learns corrupted-feature-to-noise-intensity mapping via controlled noise injection. (3) Noise-Semantic Decoupled module separates semantic information from noise for better robustness and generalization.

Result: Extensive experiments across datasets with diverse modality types demonstrate effectiveness and generality of GIML.

Conclusion: GIML provides a general solution for real-world multimodal learning where both inter-modality and intra-modality missing coexist, with code publicly available.

Abstract: Multimodal learning robust to missing modalities is essential for real-world applications. Existing methods mainly focus on inter-modality missing, where entire modalities are absent, while overlooking intra-modality degradation, where modalities are present but severely corrupted. In practice, these two types of missing often coexist, making existing approaches ineffective. To address this limitation, we propose General Incomplete Multimodal Learning (GIML), a unified framework that simultaneously handles both inter-modality missing and intra-modality degradation through dynamic quality perception. Specifically, GIML models heterogeneous missing patterns as continuous modality information degradation, enabling degradation-aware adaptive fusion. To achieve reliable quality perception, we introduce a Noise-aware Quality Estimator that learns the mapping from corrupted features to noise intensity through controlled noise injection. Furthermore, we propose a Noise-Semantic Decoupled module that separates semantic information from noise interference. This improves robustness and generalization to unseen corruption patterns. Extensive experiments across datasets with diverse modality types demonstrate the effectiveness and generality of GIML. Code is available at: https://github.com/Yu-Five/GIML.

</details>


### [31] [Self-Supervised Pretraining Improves Cross-Site and Cross-Scale Robustness of Point Cloud Leaf-Wood Segmentation](https://arxiv.org/abs/2607.06948)
*Heeju Mun,Tackang Yang,Yunsoo Nam,Changhyun Choi*

Main category: cs.CV

TL;DR: Self-supervised pretraining (Point-M2AE on ShapeNet-55 + 2,400 tree point clouds) significantly improves leaf-wood segmentation, achieving higher accuracy, cross-site generalization, and scale adaptability, with downstream wood volume estimation error (MAE=2.40 m³) less than half that of algorithmic baselines.


<details>
  <summary>Details</summary>
Motivation: Existing leaf-wood segmentation methods lack generalization across forest types and sites; SSL has been beneficial for other forestry tasks but its potential for leaf-wood segmentation remains untested.

Method: Pretrained Point-M2AE on ShapeNet-55 + 2,400 individual tree point clouds; used recursive voxel subdivision to handle point density variation across scales; fine-tuned and evaluated on multi-site, multi-climate benchmark.

Result: Pretrained model improved wood IoU from 60.5%→70.0% (needleleaf) and 69.7%→76.3% (broadleaf); best cross-site generalization; maintained plot-level accuracy (mIoU 84.7% broadleaf, 77.7% needleleaf); achieved lowest wood volume estimation error (MAE=2.40 m³) in tropical forests.

Conclusion: SSL pretraining with appropriate scale-handling techniques enables robust, generalizable leaf-wood segmentation across forest types, scales, and geographic regions, significantly improving downstream ecological applications.

Abstract: The accuracy of existing leaf-wood segmentation methods for tree point clouds varies across forest types and sites. Self-supervised learning (SSL) on point clouds has improved the generalization of deep learning models for forestry point cloud tasks, including biomass regression and individual tree segmentation, but its applicability to leaf-wood segmentation remains untested. In this study, we pretrained Point-M2AE, a widely used SSL architecture for point clouds, on ShapeNet-55 augmented with 2,400 individual tree point clouds. For fine-tuning and inference, we used recursive voxel subdivision to handle the wide variation in point density across inputs, allowing the same model to operate at both individual-tree and plot scales without architecture change. Compared to the model without pretraining, the pretrained model improved wood IoU from 60.5% to 70.0% for needleleaf and from 69.7% to 76.3% for broadleaf trees. On a benchmark spanning four countries across three climatic zones, the pretrained model achieved the smallest cross-site variation and highest overall performance among compared methods (LeWos, CWLS, and PointTransformer). Plot-level segmentation maintained accuracy comparable to individual-tree performance, with mIoU of 84.7% for broadleaf and 77.7% for needleleaf plots, showing that the model generalizes across scales without additional finetuning. As a downstream test in tropical forests, where dense canopies make segmentation challenging, we applied our model and a quantitative structure model to estimate wood volume for 28 trees from Guyana, Indonesia, and Peru to assess whether the segmentation improvements from SSL pretraining translate into improved downstream performance. The resulting volume estimates achieved the lowest error among all methods tested (MAE = 2.40 m$^3$), less than half that of algorithmic baselines (LeWos: 5.94 m$^3$; CWLS: 5.27 m$^3$).

</details>


### [32] [SpiS-GAN: Spiral-Modulated Handwriting Synthesis with Star Operation](https://arxiv.org/abs/2607.06949)
*Nguyen Duy Hieu,Dang Hoai Nam,Pham Hoang Giap,Quang Huu Hieu,Vo Nguyen Le Duy*

Main category: cs.CV

TL;DR: This paper proposes SpiS-GAN, a handwriting synthesis framework using Spiral-Modulated blocks and edge-regularized loss to generate realistic handwritten text, outperforming existing methods on English and Vietnamese datasets.


<details>
  <summary>Details</summary>
Motivation: Existing handwriting synthesis models struggle with tracing cursive strokes, detecting structural flaws, handling high-resolution synthesis efficiently, and producing sharp stroke boundaries, limiting their usefulness for augmenting handwriting recognition training data.

Method: The framework uses a generator with Star-Spiral Blocks (combining Modulated Elliptical SpiralFC with star operation) to capture spatial stroke trajectories, a Spiral-Modulated discriminator for multi-domain flaw detection, and a Sobel-Regularized Edge Reconstruction Loss for explicit edge guidance.

Result: SpiS-GAN significantly outperforms current state-of-the-art models on English and Vietnamese datasets, producing highly authentic images that preserve original writer's style and reduce error rates in downstream HTR systems.

Conclusion: The proposed spiral-modulated approach with edge constraints effectively addresses key limitations in handwriting synthesis, enabling better data augmentation for handwriting recognition across different languages.

Abstract: Training robust handwriting recognition (HTR) systems requires massive amounts of annotated data, which is often difficult to acquire. While synthetic handwriting generation offers a practical solution to expand training sets, existing models struggle with several core issues. First, previous approaches, even MLP-based models fail to effectively trace cursive handwriting due to fixed-grid spatial receptive field. Second, their CNN-relied discriminators usually lose structural details through aggressive downsampling, making broken connections difficult to detect. Third, existing architectures are either limited to linear feature interactions or too expensive for high-resolution synthesis. Finally, existing approaches lack explicit edge constraints, often resulting in blurred stroke boundaries. To address these challenges, this study proposes a Spiral-Modulated Handwriting Synthesis framework based on Generative Adversarial Networks (SpiS-GAN). Our generator employs Star-Spiral Blocks combining proposed Modulated Elliptical SpiralFC with the star operation to capture spatial relationships and efficiently follow complex handwriting stroke trajectories, while a Spiral-Modulated discriminator is introduced for multi-domain flaws detection. Additionally, we introduce a Sobel-Regularized Edge Reconstruction Loss that provides edge guidance, ensuring every character remains clear and legible. Evaluations on the English and Vietnamese datasets demonstrate that SpiS-GAN significantly outperforms current state-of-the-art models. The generated images are highly authentic, accurately preserve the original writer's style across languages, and successfully lower error rates when training downstream HTR systems.

</details>


### [33] [HPR-SAM: Hierarchical Probabilistic Representation Learning for Prompt-free SAM-based Medical Image Segmentation](https://arxiv.org/abs/2607.06972)
*Yingzhen Hu,Yiheng Zhong,Keying Zhu,Zimu Zhang,Zihan Ye,Sifan Song,Jionglong Su,Xiaofeng Liu*

Main category: cs.CV

TL;DR: HPR-SAM introduces a Hierarchical Probabilistic Representation framework that learns multi-level anatomical representations to improve prompt-free medical image segmentation with SAM, achieving SOTA on Synapse and strong few-shot performance on LA and PROMISE12.


<details>
  <summary>Details</summary>
Motivation: Existing prompt-free SAM methods overlook that prompt quality is constrained by the expressiveness of anatomical representations. Deterministic prototypes or semantic tokens fail to jointly capture global anatomical priors, intra-structure diversity, and local structural reliability.

Method: Proposes Hierarchical Probabilistic Representation (HPR) with three complementary components: Distributional Anatomical Representation (DAR), Multi-component Anatomical Representation (MAR), and Local Reliability Representation (LRR), integrated via Hierarchical Prediction Fusion (HPF) while staying compatible with the original SAM decoder.

Result: HPR-SAM achieves state-of-the-art on Synapse, and best performance under few-shot settings on LA and PROMISE12 datasets.

Conclusion: The hierarchical probabilistic representation learning framework effectively addresses the limitation of deterministic representations, validating its effectiveness for prompt-free medical image segmentation.

Abstract: Prompt-free adaptation of the Segment Anything Model (SAM) has emerged as a promising paradigm for automatic medical image segmentation. Existing methods mainly focus on prompt generation, while overlooking that prompt quality is fundamentally constrained by the expressiveness of anatomical representations. However, deterministic prototypes or semantic tokens are insufficient to jointly capture global anatomical priors, intra-structure diversity, and local structural reliability. To address this limitation, we propose the Hierarchical Probabilistic Representation (HPR) framework, which learns complementary anatomical representations through Distributional Anatomical Representation (DAR), Multi-component Anatomical Representation (MAR), and Local Reliability Representation (LRR), and integrates their predictions via Hierarchical Prediction Fusion (HPF) while remaining compatible with the original SAM decoder. Experiments on the Synapse, LA, and PROMISE12 datasets demonstrate that HPR-SAM achieves state-of-the-art performance on Synapse and the best performance under few-shot settings on LA and PROMISE12, validating the effectiveness of the proposed hierarchical probabilistic representation learning framework for prompt-free medical image segmentation. Code is available at https://anonymous.4open.science/r/HPR-SAM-E4AF.

</details>


### [34] [EdgeCompress: Coupling Multidimensional Model Compression and Dynamic Inference for EdgeAI](https://arxiv.org/abs/2607.06982)
*Hao Kong,Di Liu,Shuo Huai,Xiangzhong Luo,Ravi Subramaniam,Christian Makaya,Qian Lin,Weichen Liu*

Main category: cs.CV

TL;DR: EdgeCompress is a CNN compression framework combining Dynamic Image Cropping (DIC) to focus on foreground objects and Compound Shrinking (CS) to collaboratively compress depth, width, and resolution, plus a dynamic inference framework for processing images with varying difficulty. It reduces ResNet-50 computation by 48.8% while improving accuracy by 0.8% on ImageNet-1K.


<details>
  <summary>Details</summary>
Motivation: CNNs achieve strong image classification performance but have prohibitive computational costs that hinder deployment on resource-constrained embedded devices.

Method: (1) Dynamic Image Cropping (DIC): lightweight foreground predictor to crop informative foreground objects, reducing background computation. (2) Compound Shrinking (CS): collaboratively compress depth, width, and resolution dimensions based on their contribution. (3) Dynamic inference framework: cascade multiple compressed models of varying complexity and dynamically select models based on input recognition difficulty.

Result: Reduces ResNet-50 computation by 48.8% with 0.8% top-1 accuracy improvement on ImageNet-1K. Improves accuracy by 4.1% at similar computation compared to HRank (state-of-the-art compression framework).

Conclusion: EdgeCompress comprehensively reduces computational redundancy in both input images and network architectures, enabling efficient CNN deployment on embedded hardware.

Abstract: Convolutional neural networks (CNNs) have demonstrated encouraging results in image classification tasks. However, the prohibitive computational cost of CNNs hinders the deployment of CNNs onto resource-constrained embedded devices. To address this issue, we propose EdgeCompress, a comprehensive compression framework to reduce the computational overhead of CNNs. In EdgeCompress, we first introduce dynamic image cropping (DIC), where we design a lightweight foreground predictor to accurately crop the most informative foreground object of input images for inference, which avoids redundant computation on background regions. Subsequently, we present compound shrinking (CS) to collaboratively compress the three dimensions (depth, width, and resolution) of CNNs according to their contribution to accuracy and model computation. DIC and CS together constitute a multidimensional CNN compression framework, which is able to comprehensively reduce the computational redundancy in both input images and neural network architectures, thereby improving the inference efficiency of CNNs. Further, we present a dynamic inference framework to efficiently process input images with different recognition difficulties, where we cascade multiple models with different complexities from our compression framework and dynamically adopt different models for different input images, which further compresses the computational redundancy and improves the inference efficiency of CNNs, facilitating the deployment of advanced CNNs onto embedded hardware. Experiments on ImageNet-1K demonstrate that EdgeCompress reduces the computation of ResNet-50 by 48.8% while improving the top-1 accuracy by 0.8%. Meanwhile, we improve the accuracy by 4.1% with similar computation compared to HRank, the state-of-the-art compression framework. The source code and models are available at https://github.com/ntuliuteam/edge-compress

</details>


### [35] [Ego-Human Motion Prediction with 3D-Aware LLM](https://arxiv.org/abs/2607.07001)
*Yujin Bae,Jaewoo Jeong,Hyeonseong Kim,Kuk-Jin Yoon*

Main category: cs.CV

TL;DR: Ego3DLM is a unified model for egocentric human motion forecasting that simultaneously predicts past/future poses and narrations in a single autoregressive pass, leveraging 3D scene understanding and cross-modal grounding to achieve state-of-the-art results on the Nymeria benchmark.


<details>
  <summary>Details</summary>
Motivation: Existing egocentric motion forecasting works neglect 3D spatial-semantic context and treat pose prediction and language generation as separate streams, leading to physically implausible or semantically incoherent forecasts.

Method: Ego3DLM uses a three-stage training scheme: (1) spatial-semantic scene awareness pretraining; (2) holistic instruction tuning over all four outputs (past pose, future pose, past narration, future narration) in a single autoregressive pass; (3) GRPO-based reinforcement finetuning with intra- and inter-modal rewards to optimize pose-language fidelity.

Result: Achieves state-of-the-art performance on Nymeria benchmark across future motion prediction, past motion tracking, and motion description tasks.

Conclusion: 3D scene grounding and holistic cross-modal prediction produce physically plausible and semantically coherent motion forecasts, validating that pose and language should be predicted together rather than as separate streams.

Abstract: Anticipating human motion from an egocentric perspective is fundamental for proactive assistance in AR/VR, human-robot collaboration, and embodied AI. While recent works incorporate language as a semantic prior to reduce the ill-posed nature of egocentric forecasting, they largely neglect the 3D spatial and semantic context that governs how motion unfolds, and treat pose and language prediction as separate inference streams. We introduce Ego3DLM, built on two core principles: accurate motion forecasting requires explicit spatial and semantic understanding of the 3D environment, and pose and language must be predicted holistically in a single pass, since motion is inherently tied to the semantic interpretation of actions being performed. Given three-point tracking, 3D scene features, and egocentric video, Ego3DLM simultaneously decodes past pose, future pose, past narration, and future narration in a single autoregressive pass, grounding predicted poses and descriptions in one another to enforce cross-modal and temporal consistency. We adopt a three-stage training scheme: (1) spatial-semantic scene awareness pretraining; (2) holistic instruction tuning over all four outputs in a single pass; and (3) GRPO-based reinforcement finetuning with intra- and inter-modal rewards that directly optimize pose-language fidelity. Experiments on the Nymeria benchmark demonstrate that Ego3DLM achieves state-of-the-art performance across future motion prediction, past motion tracking, and motion description, showing that 3D scene grounding and holistic cross-modal prediction yield physically plausible and semantically coherent motion forecasts. The project page is available at https://jaewoo97.github.io/Ego3DLM/.

</details>


### [36] [SHTA: Semantic Hard Token Correction and Center Alignment for Semi-Supervised Medical Image Segmentation](https://arxiv.org/abs/2607.07019)
*Zhuoru Zhang,Yiheng Zhong,Zimu Zhang,Xiaofeng Liu*

Main category: cs.CV

TL;DR: SHTA is a lightweight training-time module that improves semi-supervised medical image segmentation by enforcing semantic consistency in hard regions through semantic assignment, hard token refinement, and center alignment, without adding inference cost.


<details>
  <summary>Details</summary>
Motivation: Existing semi-supervised methods improve prediction-level supervision but fail to enforce semantic consistency in hard regions (unstable semantic assignments), limiting further segmentation improvement.

Method: SHTA introduces a training-time semantic representation branch with three components: Semantic Assignment, Hard Token Refinement, and Semantic Center Alignment, which refines intermediate representations of hard regions without altering the original prediction pathway.

Result: Integrated into GA-CPS, CPS, URPC, and MagicNet, SHTA achieves consistent improvements on Synapse and AMOS datasets, with gains in segmentation accuracy, weak-organ recovery, and reduced semantic ambiguity.

Conclusion: SHTA effectively addresses semantic inconsistency in hard regions for semi-supervised medical image segmentation, delivering consistent improvements across frameworks with only training-time overhead.

Abstract: Recent advances in semi-supervised medical image segmentation have achieved remarkable performance through prediction consistency, pseudo-label supervision, and hard-region supervision. However, these methods primarily improve supervision quality rather than explicitly enforcing semantic consistency in the learned representations of hard regions. Consequently, even under increasingly stronger prediction-level supervision, difficult regions exhibiting unstable semantic assignment often fail to establish semantically consistent representations during training, thereby limiting further segmentation improvement. To address this issue, we propose SHTA (Semantic Hard Token Correction and Center Alignment), a lightweight training-time semantic representation branch. Instead of introducing additional prediction supervision, SHTA refines intermediate semantic representations through Semantic Assignment, Hard Token Refinement, and Semantic Center Alignment, thereby improving semantic consistency in hard regions while preserving the original prediction pathway and introducing no additional inference cost. We integrate SHTA into representative semi-supervised segmentation frameworks, including GA-CPS, CPS, URPC, and MagicNet, and conduct evaluations on the Synapse and AMOS datasets. Experimental results demonstrate that SHTA delivers consistent paired improvements across frameworks, with especially clear gains in segmentation accuracy, weak-organ recovery, and semantic ambiguity reduction, while incurring only training-time overhead. The code is available at https://anonymous.4open.science/r/release_SHTA-42D5/.

</details>


### [37] [AnchorPrune: Relevance-Anchored Contextual Expansion for Visual Token Pruning](https://arxiv.org/abs/2607.07033)
*Kyuan Oh,Bumsoo Kim*

Main category: cs.CV

TL;DR: AnchorPrune is a training-free framework that reduces visual token redundancy in large vision-language models by constructing a protected relevance anchor and expanding it with complementary context, achieving 97.6% of full-token performance using only 160 out of 2,880 visual tokens on LLaVA-NeXT-7B.


<details>
  <summary>Details</summary>
Motivation: High-resolution inputs in large vision-language models generate thousands of visual tokens, many redundant for a given query. Existing pruning methods suffer from conflicting objectives between query relevance and token diversity under aggressive compression.

Method: AnchorPrune first constructs a protected relevance anchor (query-critical tokens) adaptively sized via novelty profiling, then allocates remaining budget through importance-weighted novelty to recover informative, non-redundant context relative to the anchor.

Result: On LLaVA-NeXT-7B, AnchorPrune preserves 97.6% of full-token performance using only 160 of 2,880 visual tokens. It consistently improves accuracy-efficiency trade-off over training-free baselines, especially under severe compression.

Conclusion: Relevance-anchored contextual expansion is an effective principle for efficient multimodal inference, preventing contextual expansion from displacing indispensable query cues while improving visual coverage.

Abstract: Large vision-language models incur substantial inference costs because high-resolution inputs introduce thousands of visual tokens, many of which are redundant for a given query. Existing pruning methods often combine query relevance and token diversity, yet these objectives can conflict under aggressive compression: relevance-driven selection may overconcentrate the budget on correlated local evidence, while diversity-driven selection may suppress indispensable tokens or retain distinct but uninformative regions. We introduce AnchorPrune, a training-free framework that first constructs a protected relevance anchor and then expands it with complementary visual context. AnchorPrune adaptively determines the anchor size from the novelty profile of relevance-ranked tokens, preserving a compact set of query-critical evidence, and allocates the remaining budget through importance-weighted novelty to recover informative, non-redundant context relative to the anchor. This ordered design prevents contextual expansion from displacing indispensable query cues while improving overall visual coverage. AnchorPrune is lightweight, architecture-aware, and requires neither retraining nor model modification. Across image and video vision-language models and benchmarks, it consistently improves the accuracy-efficiency trade-off over training-free baselines, particularly under severe compression. On LLaVA-NeXT-7B, AnchorPrune preserves 97.6% of full-token performance using only 160 of 2,880 visual tokens. These results establish relevance-anchored contextual expansion as an effective principle for efficient multimodal inference. Code is available at https://github.com/MULTI-cau/AnchorPrune.

</details>


### [38] [TRACE-Seg3D: Counterfactual Context Auditing For Robust 3D Glioma Segmentation Under Institutional Shift](https://arxiv.org/abs/2607.07038)
*Nguyen Linh Dan Le,Nguyen Pham Hoang Le,Tran Dang Khoi*

Main category: cs.CV

TL;DR: TRACE-Seg3D is a counterfactual context auditing framework that systematically varies imaging context to quantify prediction stability in 3D medical image segmentation, revealing failure modes missed by standard metrics like Dice and HD95.


<details>
  <summary>Details</summary>
Motivation: Medical image segmentation models are sensitive to scanner, protocol, and institutional variations (context shifts) that change image appearance without altering underlying lesions. Standard metrics (Dice, HD95) fail to expose models exploiting these nuisance cues, motivating a need for reliability assessment beyond overlap-based evaluation.

Method: TRACE-Seg3D is a counterfactual context auditing framework that preserves lesion-relevant evidence while systematically varying imaging context to quantify prediction stability under controlled context shifts. It pairs each segmentation with audit evidence for context sensitivity and anatomical plausibility.

Result: Achieves competitive in-distribution and cross-domain performance on BraTS and UTSW glioma segmentation benchmarks. Exposes context-sensitive failure modes missed by conventional metrics like Dice and HD95.

Conclusion: Counterfactual context auditing is a practical route toward transparent and reliable 3D medical image segmentation under distribution shift.

Abstract: Medical image segmentation models can achieve strong benchmark performance while remaining sensitive to scanner, protocol, and institutional variation. These context shifts alter image appearance without changing the underlying lesion, allowing models to exploit nuisance cues that Dice and HD95 fail to expose. We present TRACE-Seg3D, a counterfactual context auditing framework for robust 3D medical image segmentation. TRACE-Seg3D preserves lesion-relevant evidence and systematically varies imaging context to quantify prediction stability under controlled context shifts. The framework pairs each segmentation with audit evidence for context sensitivity and anatomical plausibility, enabling case-level reliability assessment beyond overlap-based evaluation. Experiments on BraTS and UTSW glioma segmentation benchmarks demonstrate competitive in-distribution and cross-domain performance. TRACE-Seg3D also exposes context-sensitive failure modes missed by conventional metrics. These results establish counterfactual context auditing as a practical route toward transparent and reliable 3D medical image segmentation under distribution shift. Our code is available at https://github.com/danleneurocom/Counterfactual-Representation-Network.

</details>


### [39] [Making Implicit Preservation Intent Explicit in Conversational Image Editing](https://arxiv.org/abs/2607.07051)
*Soomin Han,Jihyung Ahn,Bumsoo Kim,Buru Chang*

Main category: cs.CV

TL;DR: OCCUR-Bench is a benchmark for evaluating temporal preservation in conversational image editing, and ReSpec is a training-free framework that improves restoration fidelity by pairing restoration-aware instructions with historical visual references.


<details>
  <summary>Details</summary>
Motivation: Existing conversational image editing systems fail to recover occluded-but-unchanged content when newly added objects temporarily cover previously visible regions, producing inconsistent or hallucinated results.

Method: ReSpec identifies what should persist from editing history, selects the historical image state providing missing visual evidence, and conditions an in-context editor on the resulting instruction and reference image.

Result: ReSpec improves restoration fidelity and temporal consistency on OCCUR-Bench compared to existing systems.

Conclusion: Grounding preservation in editing history rather than only the current image is crucial for faithful temporal consistency in conversational image editing.

Abstract: Conversational image editing requires preserving not only visible content, but also content that temporarily disappears across turns. When newly added or modified content occludes a previously visible region, that region should reappear if it was never semantically changed. However, existing systems often fail to recover such occluded-but-unchanged content, producing inconsistent or hallucinated results. We introduce OCCUR-Bench, a diagnostic benchmark for temporal preservation in conversational image editing. OCCUR-Bench provides diverse occlusion-and-revelation scenarios with historical restoration references, enabling evaluation of faithful restoration rather than plausible regeneration. We also propose ReSpec, a training-free framework that makes implicit preservation explicit by pairing restoration-aware instructions with historical visual references. Given an editing history, ReSpec identifies what should persist, selects the historical image state that provides missing visual evidence, and conditions an in-context editor on the resulting instruction and reference image. Experiments show that ReSpec improves restoration fidelity and temporal consistency on OCCUR-Bench, highlighting the need to ground preservation in editing history rather than only the current image.

</details>


### [40] [Navigating Hierarchy: Hyperbolic Learning on Brain Graphs for Disorder Diagnosis](https://arxiv.org/abs/2607.07077)
*Yapeng Li,Bo Jiang,Ziyan Zhang,Dongdong Chen,Zhengzheng Tu*

Main category: cs.CV

TL;DR: HLBG is a hyperbolic learning framework that models hierarchical brain networks (ROI→community→whole-brain) using Lorentzian hyperbolic space, geometric entailment constraints, and a Graph-aware Mamba module, outperforming SOTA on ABIDE-I and REST-MDD datasets.


<details>
  <summary>Details</summary>
Motivation: Existing brain graph models fail to capture ROI-community interactions and cannot fully exploit the hierarchical organization (ROI, community, whole-brain) of functional brain networks.

Method: Projects ROI, community, and whole-brain representations into Lorentzian hyperbolic space; imposes multi-level hierarchy via geometric entailment constraints; introduces Graph-aware Mamba (GaMamba) integrating topology-derived structural prompts into Mamba for long-range dependencies with graph topology preservation.

Result: Outperforms state-of-the-art methods on ABIDE-I (autism) and REST-MDD (depression) datasets; identifies disorder-relevant functional biomarkers.

Conclusion: Leveraging hyperbolic geometry to model brain network hierarchy yields more discriminative representations and improves diagnosis/biomarker identification.

Abstract: Functional brain networks exhibit a hierarchical organization across ROI, community, and whole-brain levels, supporting local processing, inter-community coordination, and global integration. Recent studies have demonstrated that brain community-aware modeling is beneficial for both diagnosis and biomarker identification of brain networks. However, existing brain graph modeling methods often struggle to model ROI-community interactions, thereby failing to fully exploit the hierarchy across ROI, community, and whole-brain network levels. To address this issue, inspired by deep hyperbolic learning in modeling hierarchical structures, we propose a novel framework, termed Hyperbolic Learning on Brain Graphs (HLBG), for brain network analysis. The core idea of HLBG is to exploit the inherent hierarchical geometry of hyperbolic space to model the hierarchical relationships among ROIs, functional communities, and the whole-brain network, thereby learning hierarchy-aware and highly discriminative representations for brain network data. Specifically, HLBG first projects representations from ROIs, communities, and the whole-brain network into Lorentzian hyperbolic space. Then, the multi-level hierarchy is imposed via two geometric entailment constraints. In addition, we introduce a new Graph-aware Mamba (GaMamba) model, which incorporates topology-derived structural prompts into Mamba to capture long-range dependencies while preserving graph topological information. Experiments on ABIDE-I and REST-MDD demonstrate that HLBG outperforms state-of-the-art methods and identifies disorder-relevant functional biomarkers.

</details>


### [41] [AT-Attn: Temporal-Aware Cross-Attention for Longitudinal Multimodal Alzheimer's Disease Diagnosis](https://arxiv.org/abs/2607.07091)
*Xinyue Du,Yibo Liu,Zhenglei Zhou,Xuancheng Yao,Weimin Zhong,Qiuhui Chen*

Main category: cs.CV

TL;DR: AT-Attn is a temporal-aware multimodal framework using Change-and-Time encoding, asymmetric cross-attention, and gated fusion to integrate MRI with longitudinal clinical data for Alzheimer's diagnosis, achieving 0.719 accuracy and outperforming unimodal/naive fusion baselines.


<details>
  <summary>Details</summary>
Motivation: Clinical and imaging data for AD diagnosis are collected at irregular visits; naive multimodal fusion degrades performance when MRI is noisy or intermittently unavailable, motivating a more robust temporal-aware integration approach.

Method: AT-Attn combines Change-and-Time encoding, time-biased asymmetric cross-attention, and gated fusion to integrate structural MRI with longitudinal clinical information (cognitive-scale trajectories and static variables).

Result: On ADNI (1,520 patients, 5-fold CV): accuracy 0.719±0.024, macro F1 0.721±0.023, ROC-AUC 0.873±0.013, PR-AUC 0.783±0.018, outperforming unimodal and naive multimodal fusion baselines.

Conclusion: A temporal-aware and constrained fusion strategy can help structural MRI contribute clinically relevant complementary information for patient-level AD diagnosis support.

Abstract: In longitudinal Alzheimer's disease (AD) diagnosis support, clinical and imaging information is often collected at irregular visits. Integrating these multimodal observations may improve diagnostic assessment, but naive fusion can degrade performance when MRI is noisy or intermittently unavailable. We propose AT-Attn, a temporal-aware multimodal framework that combines Change-and-Time encoding, time-biased asymmetric cross-attention, and gated fusion to integrate MRI with longitudinal clinical information. We evaluate AT-Attn on an MRI-retained ADNI cohort of 1,520 patients using structural MRI, six cognitive-scale trajectories, and seven static clinical variables under patient-level five-fold cross-validation. The main asymmetric AT-Attn model achieves accuracy 0.719+/-0.024, macro F1 0.721+/-0.023, ROC-AUC 0.873+/-0.013, and PR-AUC 0.783+/-0.018, outperforming unimodal and naive multimodal fusion baselines while remaining competitive with strong tabular baselines. These results suggest that a temporal-aware and constrained fusion strategy can help structural MRI contribute clinically relevant complementary information for patient-level AD diagnosis support.

</details>


### [42] [Video-Based Detection of squint and cataract for accessibility-aware adaptive web interface rendering](https://arxiv.org/abs/2607.07099)
*Amar Ranjan Dash,Manas Ranjan Patra*

Main category: cs.CV

TL;DR: This paper presents a real-time video-based system using MediaPipe face-mesh and grayscale intensity analysis to automatically detect squint (98.39% accuracy) and cataract (96.90% accuracy) via standard laptop/mobile cameras, with future integration planned for adaptive user interfaces for visually impaired users.


<details>
  <summary>Details</summary>
Motivation: Squint and cataract are major ocular disorders affecting visual perception and interaction, motivating the need for a low-cost, large-scale automated detection system that can be deployed using standard cameras.

Method: The system uses MediaPipe face-mesh (478-point facial landmark model) to extract geometric ocular features for multi-class squint classification, and grayscale intensity/histogram-based lens opacity analysis to estimate cataract presence and severity from short video sequences.

Result: Squint detection accuracy: 98.39%; cataract classification accuracy: 96.90%.

Conclusion: The proposed framework enables low-cost, real-time automated ocular disorder detection and is designed to be integrated into adaptive user interfaces and Web accessibility systems for visually impaired users.

Abstract: Squint and cataract are major ocular disorders that majorly affect visual perception and interaction capability. This paper proposes a real-time video-based automated detection system for squint and cataract detection based on computer vision and image processing methods. The proposed system uses a media-pipe face-mesh (a 478-point facial landmark detection model) to extract geometric ocular features for multi-class squint classification. Simultaneously, The presence and severity cataract is estimated through grayscale intensity and histogram-based lens opacity analysis. The system records short video sequences with standard laptop or mobile cameras, which can be deployed at low costs and on a large scale. The experimental performance has shown great accuracy in the detection of squint (98.39%) and classification of cataract (96.90%). Besides automatic ocular analysis, the proposed framework is also made accessible for visual impairment inference which will be integrated with future adaptive user interface and Web accessibility systems for people with visual impairment.

</details>


### [43] [Tree-of-Thoughts Reasoning for Text-to-Image In-Context Learning](https://arxiv.org/abs/2607.07117)
*Stepanida Alekseeva,Jenifer Kalafatovich,Seong-Whan Lee*

Main category: cs.CV

TL;DR: Proposes a Tree-of-Thoughts (ToT) reasoning framework for text-to-image in-context learning that uses multi-branch hypothesis generation, evaluation, and selection before prompt construction, improving compositional reasoning without additional training.


<details>
  <summary>Details</summary>
Motivation: State-of-the-art multimodal LLMs struggle with T2I-ICL due to limited compositional reasoning and sensitivity to prompt construction, leading to ambiguous and erroneous image generations.

Method: Introduces a ToT reasoning framework with multi-stage reasoning: generates multiple candidate hypotheses, evaluates them, and selects the best interpretation before constructing the final prompt for image synthesis.

Result: On the CoBSAT benchmark, the ToT-T2IICL pipeline outperforms baseline and Chain-of-Thought prompting strategies, producing more consistent and semantically aligned image generations.

Conclusion: Structured multi-branch reasoning effectively mitigates prompt ambiguity and compositional errors in T2I-ICL without requiring additional training or fine-tuning.

Abstract: In text-to-image in-context learning (T2I-ICL), a model has to infer a latent compositional pattern from fewshot demonstrations for generating a query image. Recent studies show that state-of-the-art multimodal large language models struggle with this setting, particularly due to limited compositional reasoning and sensitivity to prompt construction. In this work, we propose a Tree-of-Thoughts (ToT) reasoning framework for T2I-ICL that introduces a multi-stage reasoning and selection layer that generates, evaluates, and selects among multiple candidate hypotheses before constructing the final prompt for image synthesis. By exploring alternative reasoning branches and selecting a coherent interpretation, the proposed approach mitigates prompt ambiguity and compositional errors. We implement the proposed approach in a complete ToT-T2IICL inference pipeline and evaluate it on the CoBSAT benchmark. Both qualitative and quantitative results show that structured multi-branch reasoning leads to more consistent and semantically aligned image generation compared to baseline and Chain-of-Thought prompting strategies, without any additional training or fine-tuning.

</details>


### [44] [ColorFM: An Optimization-to-Learning Framework for Color Transfer via Flow Matching](https://arxiv.org/abs/2607.07119)
*Yuhang He,Kai Zhang,Xiaoming Li,Du Chen,Jian Yang*

Main category: cs.CV

TL;DR: ColorFM is an optimization-to-learning framework for color transfer that uses Flow Matching to model pixel distribution transport, consisting of ColorFM-O (instance-specific optimization with semantic priors) and ColorFM-L (efficient feed-forward model trained on generated pseudo-supervision), achieving state-of-the-art results in visual quality, structural fidelity, and semantic consistency.


<details>
  <summary>Details</summary>
Motivation: Existing color transfer methods suffer from inaccurate global mapping, semantic misalignment, and visual artifacts, motivating the need for a more precise and semantically consistent approach.

Method: ColorFM reformulates color transfer as transport of pixel distributions along velocity fields via Flow Matching. It has two components: (1) ColorFM-O, which fits velocity fields through hierarchical color coupling with semantic priors and numerical integration for precise results and pseudo-supervision data; (2) ColorFM-L, a feed-forward model trained on generated pairs that uses implicit state modeling to extract deep semantic features and predict flow parameters for bidirectional linearized transport.

Result: Extensive experiments show ColorFM-L outperforms state-of-the-art methods in visual quality, structural fidelity, and semantic consistency, combining the accuracy of optimization with the speed of feed-forward inference.

Conclusion: ColorFM successfully bridges optimization-based accuracy and feed-forward inference speed for color transfer, with both components (ColorFM-O for precise optimization and ColorFM-L for efficient inference) demonstrating strong performance.

Abstract: Color transfer aims to align the color distribution of a source image with that of a reference image while preserving structural and semantic consistency. However, existing methods often suffer from inaccurate global mapping, semantic misalignment, and visual artifacts. To address these issues, we propose ColorFM, an optimization-to-learning framework. ColorFM connects online optimization to offline inference by reformulating color transfer as the transport of pixel distributions along velocity fields via Flow Matching. Specifically, we introduce ColorFM-O, an instance-specific optimization scheme that fits the velocity field through hierarchical color coupling guided by semantic priors. By numerically integrating the induced flow trajectories, ColorFM-O produces precise and semantically consistent color transfer results, while generating high-quality paired data as pseudo-supervision. Building upon this, we design ColorFM-L, an efficient feed-forward model trained on the generated pairs. Through implicit state modeling, ColorFM-L extracts deep semantic features to predict flow parameters for bidirectional linearized transport, ensuring accurate color transfer. Extensive experiments demonstrate that ColorFM-L outperforms state-of-the-art methods in visual quality, structural fidelity, and semantic consistency, successfully combining the accuracy of optimization with the speed of feed-forward inference.

</details>


### [45] [Widest-Path Reachability Fields for Connectivity-Preserving Slender Structure Segmentation](https://arxiv.org/abs/2607.07123)
*Youcheng Zong,Runda Jia,Minxuan Hu,Weilan Su,Dakuo He*

Main category: cs.CV

TL;DR: This paper identifies topological gradient starvation (TGS) in curvilinear structure segmentation, where pixel-wise losses neglect bottleneck pixels critical for connectivity. The authors propose Widest-Path Reachability Fields (WPRF), a differentiable Max-Min reachability objective that redirects gradient flow to connectivity bottlenecks, achieving consistent improvements across architectures and datasets.


<details>
  <summary>Details</summary>
Motivation: Standard binary-mask supervision with pixel-wise losses produces topologically broken predictions for slender curvilinear structures because gradient contributions from connectivity-critical bottleneck pixels are negligible compared to thick structures and background — a phenomenon termed topological gradient starvation (TGS).

Method: WPRF implements a differentiable Max-Min reachability objective using dynamic programming on a domain-restricted graph, coupled with a bottleneck-aware observation term that balances gradients across varying structures, directly optimizing end-to-end reachability without post-hoc skeletonization.

Result: Across nine architectures and six datasets, WPRF improves 87% of experiments with fixed hyperparameters and achieves clDice gains of 7.2 percentage points on structurally fragile datasets.

Conclusion: WPRF effectively addresses topological gradient starvation by routing gradients to connectivity bottlenecks, is plug-and-play and backbone-agnostic, and incurs no inference overhead, making it a practical solution for topology-aware segmentation.

Abstract: Segmenting slender curvilinear structures such as retinal vessels, cracks, and roads demands topological correctness, as even a single-pixel discontinuity can fragment a continuous network and invalidate downstream analysis. Under standard binary-mask supervision, models optimized for pixel-level overlap frequently produce topologically broken predictions. We trace this to a fundamental mismatch: pixel-wise losses distribute gradients uniformly, yet connectivity hinges on a sparse set of bottleneck pixels. These pixels are vastly outnumbered by thick structures and background, rendering their aggregate gradient contribution negligible. We term this phenomenon topological gradient starvation (TGS). To address it, we propose Widest-Path Reachability Fields (WPRF), a differentiable Max-Min reachability objective that redirects gradient flow to connectivity bottlenecks. The module is plug-and-play, backbone-agnostic, and incurs no inference overhead. WPRF implements a differentiable Max-Min objective via dynamic programming on a domain-restricted graph, coupled with a bottleneck-aware observation term that balances gradient contributions across varying structures. Compared to prior topology-aware losses that rely on post-hoc skeletonization or homology computation, WPRF directly optimizes end-to-end reachability via differentiable Max-Min algebra, enabling gradient flow to concentrate on connectivity bottlenecks without auxiliary structures. We introduce OMVIS, a new oral microvessel segmentation dataset. Experiments across nine architectures and six datasets validate the bottleneck-focused gradient routing mechanism. WPRF improves 87\% of experiments with fixed hyperparameters and achieves clDice gains of 7.2 percentage points on structurally fragile datasets.

</details>


### [46] [Sparse Attention for Dense Open-Vocabulary Prediction in CLIP](https://arxiv.org/abs/2607.07135)
*Fatimah Zohra,Chen Zhao,Shuming Liu,Bernard Ghanem*

Main category: cs.CV

TL;DR: This paper proposes replacing softmax with α-entmax in the final visual self-attention layers of CLIP to sparse attention weights, which eliminates noise from irrelevant tokens and improves performance on dense open-vocabulary prediction tasks like semantic segmentation and fine-grained retrieval.


<details>
  <summary>Details</summary>
Motivation: CLIP's softmax-based self-attention assigns probability mass to all token pairs, including semantically irrelevant ones, creating noise that obscures fine-grained spatial cues needed for dense open-vocabulary prediction tasks.

Method: Replace row-wise softmax in final visual self-attention layers with α-entmax transform, applied to both standard query-key attention and self-correlation variants. Entmax applies a data-dependent threshold that zeroes out low-scoring tokens and redistributes attention to the most relevant ones.

Result: Evaluation on dense semantic segmentation (Pascal VOC, Pascal Context, ADE20K) and fine-grained retrieval (FG-OVD) shows performance improves proportionally to how much the baseline attention spreads off the target class.

Conclusion: Attention sparsification via entmax acts as an implicit denoiser for CLIP, improving open-vocabulary dense prediction by eliminating noise from irrelevant tokens without requiring additional training.

Abstract: Contrastive Language-Image Pre-training (CLIP) relies on softmax-based self-attention, a strictly positive distribution that assigns probability mass to every pair of tokens-even semantically irrelevant ones. While these dense softmax weights are effective for gathering broad context during pre-training, they spread attention across many low-salience tokens, producing noise that obscures the fine-grained, spatially localized cues required for dense, open-vocabulary prediction. We study an inference-time substitution of the row-wise softmax in the final visual self-attention layers with the $α$-entmax transform, applied across both the standard query-key attention and self-correlation variants. Because entmax applies a data-dependent threshold that maps low scores exactly to zero, it acts as an implicit denoiser, zeroing contextually irrelevant dependencies while redistributing mass onto the most relevant tokens. We evaluate on open-vocabulary tasks-dense semantic segmentation (Pascal VOC, Pascal Context, ADE20K) and fine-grained retrieval (FG-OVD)-and find the gain from attention sparsification is proportional to how much the baseline attention spreads off the target class.

</details>


### [47] [ASFR-Net: Adversarial Alignment and Spatio-Frequency Refinement Network for Heterogeneous Remote Sensing Image Change Detection](https://arxiv.org/abs/2607.07161)
*Xin-Jie Wu,Zhi-Hui You,Si-Bao Chen,Qing-Ling Shu,Xiao Wang,Jin Tang,Bin Luo*

Main category: cs.CV

TL;DR: ASFR-Net is an end-to-end network for heterogeneous change detection that uses modality-invariant representation learning, spatio-frequency refinement, and hierarchical guided fusion to suppress pseudo-changes caused by cross-modal differences. A new benchmark dataset (VisNIR-HCD) is also released.


<details>
  <summary>Details</summary>
Motivation: Heterogeneous change detection struggles with pseudo-changes caused by modal disparities between images, hindering detection accuracy.

Method: (1) MIR-Learner extracts modality-invariant features; (2) SFEM synergistically refines spatial and frequency domains to suppress residual noise; (3) HGFM cascades multi-level fused features for precise decoding.

Result: State-of-the-art performance on VisNIR-HCD and public benchmarks.

Conclusion: ASFR-Net effectively decouples real changes from modal differences; the VisNIR-HCD dataset provides a challenging benchmark for future research.

Abstract: The core challenge of heterogeneous change detection in remote sensing imagery lies in effectively decoupling genuine land-cover changes from significant modal disparities caused by distinct imaging mechanisms. These intrinsic inconsistencies are prone to introducing pseudo-changes, thereby constraining detection accuracy. To address this, we propose a novel, end-to-end adversarial spatio-frequency refinement network (ASFR-Net). Initially, a modality-invariant representation learner (MIR-Learner) guides the backbone to extract modality-invariant features, effectively bridging the primary domain gap. Subsequently, to address persistent residual modal differences, we design an innovative spatio-frequency synergistic enhancement module (SFEM), which identifies and suppresses sensor-specific noise and artifacts that are difficult to discern in the spatial domain by leveraging frequency-domain processing. Multi-level difference features are then computed from these refined representations and fed into a decoder equipped with cascaded hierarchical guided fusion module (HGFM) blocks to generate precise change maps. To alleviate the data scarcity in heterogeneous tasks, we construct and release a new high-resolution benchmark specifically focused on building changes: the visible-near-infrared heterogeneous change detection (VisNIR-HCD) dataset. It presents unique scientific challenges arising from deceptive visual similarity and non-linear spectral inversions, providing a robust platform for evaluating model generalization. Extensive experiments on VisNIR-HCD and public datasets demonstrate that ASFR-Net achieves state-of-the-art (SOTA) performance, significantly outperforming existing methods. The source code and the VisNIR-HCD dataset are publicly available at https://github.com/LuoYang2024/ASFR-Net.

</details>


### [48] [NoDrift3R: Raymap-Guided Coupling for Drift-Robust Unposed Feed-Forward 3D Reconstruction](https://arxiv.org/abs/2607.07168)
*Xiangyu Sun,Liu Liu,Seungkwon Yang,Jingbing Han,Seungtae Nam,Zhizhong Su,Eunbyung Park*

Main category: cs.CV

TL;DR: This paper proposes a pose-free feed-forward 3D Gaussian Splatting framework that addresses camera pose drift in long sequences by introducing a Raymap-Guided Coupling Module (RGC) for geometry-appearance synergy and a Dual-Frequency Viewpoint Scheduling strategy for stable training, achieving robust reconstruction and rendering across long image sequences.


<details>
  <summary>Details</summary>
Motivation: Existing pose-free feed-forward 3DGS suffers from significant performance degradation in long image sequences due to cumulative camera pose estimation drift, which propagates errors into geometric modeling and limits rendering fidelity. Purely rendering-based supervision leads to optimization instability, while SfM-based pseudo ground-truth poses introduce sensor noise.

Method: (1) Raymap-Guided Coupling Module (RGC): anchors Gaussian centers to raymap-induced geometry and jointly optimizes RGB reconstruction, raymap consistency, and camera regularization under a unified objective, creating a bidirectional feedback loop between geometry and appearance. (2) Dual-Frequency Viewpoint Scheduling: combines easy-to-hard interval expansion with replay of short-interval pairs to stabilize learning across wide temporal ranges.

Result: Consistent gains in both rendering and pose estimation across in-domain and cross-domain datasets, with notably improved robustness on long sequences. Ablation studies confirm that explicitly designed geometry-appearance synergy is key to scalable and drift-robust reconstruction.

Conclusion: Explicitly coupling geometry and appearance via RGC, along with Dual-Frequency Viewpoint Scheduling, effectively mitigates pose drift in long sequences for pose-free feed-forward 3DGS, achieving drift-robust and scalable 3D reconstruction.

Abstract: Pose-Free Feed-forward 3D Gaussian Splatting (3DGS) has recently emerged as a powerful paradigm for fast scene reconstruction. However, its performance degrades significantly in long image sequences due to cumulative camera pose estimation drift, which propagates errors into geometric modeling and severely limits rendering fidelity. In this work, we revisit the long-sequence bottleneck and identify pose drift as the primary factor restricting reconstruction quality. Furthermore, while SfM-based pseudo ground-truth poses introduce sensor noise, purely rendering-based supervision often leads to optimization instability and local minima due to the entangled optimization of geometry and pose. To address the challenges, we propose a synergistic pose-free framework that explicitly couples geometry and appearance via a Raymap-Guided Coupling Module (RGC). Concretely, we anchor Gaussian centers to raymap-induced geometry and jointly optimize RGB reconstruction, raymap consistency, and camera regularization under a unified objective, yielding a bidirectional feedback loop: stronger geometry improves rendering, and appearance supervision in turn refines geometry and pose. To further stabilize learning across wide temporal ranges, we introduce a Dual-Frequency Viewpoint Scheduling strategy that combines easy-to-hard interval expansion with replay of short-interval pairs. Extensive experiments across in-domain and cross-domain datasets show consistent gains in both rendering and pose estimation, with notably improved robustness on long sequences. Ablation studies validate our central insight: explicitly designed geometry-appearance synergy is the key to scalable and drift-robust pose-free feed-forward 3D reconstruction.

</details>


### [49] [TACoS: Weakly Supervised Learning of Two-Dimensional Materials from Scribble Annotations to Precise Segmentation](https://arxiv.org/abs/2607.07169)
*Jiabei Chen,Liping Zhang,Jiang-Bin Wu,Zhongming Wei,Enhao Ning,Su Yan,Weijun Li,Ping-Heng Tan,Xin Ning*

Main category: cs.CV

TL;DR: TACoS is a scribble-based semi-supervised segmentation framework for 2D material flakes that achieves >96% of fully supervised performance using <0.6% annotated data, via combining consistency learning, tree energy constraints, and asymmetric regional contrast learning.


<details>
  <summary>Details</summary>
Motivation: Traditional fully supervised segmentation methods for 2D material flakes require dense pixel-level annotations which are costly and time-consuming, limiting practical deployment for high-throughput screening.

Method: Proposes TACoS with three key components: (1) a unified framework integrating semi-supervised consistency learning with structured tree energy constraints (including an unlabeled weak-strong distribution alignment module and a tree energy regularization module), (2) minimum spanning tree-based pixel affinity to generate structure-aware soft pseudo labels, and (3) asymmetric regional contrast learning that fuses high-confidence predictions with scribbles to construct category prototypes and prioritize contrast on boundary-unlabeled regions.

Result: On graphene and MoS2 datasets, TACoS achieves over 96% of fully supervised performance using less than 0.6% annotated data, with superior structural coherence and boundary stability in low-contrast edge and complex background scenarios.

Conclusion: TACoS provides an efficient and scalable solution for automated high-throughput screening of 2D material flakes, significantly reducing annotation cost while maintaining high segmentation quality.

Abstract: The precise pixel-level localization of 2D material flakes is crucial for high-throughput screening. However, traditional fully supervised methods rely on dense annotations, which are costly and time-consuming, severely limiting the practical deployment of segmentation models. This paper proposes TACoS, a specialized scribble segmentation framework tailored for 2D materials. First, we design a unified framework that integrates semi-supervised consistency learning with structured tree energy constraints. This framework comprises two core components: an unlabeled weak-strong distribution alignment module and a tree energy regularization module. The former employs cosine consistency constraints to enhance prediction alignment across views. Meanwhile, the latter utilizes minimum spanning trees to establish pixel affinity relationships and generate structure-aware soft pseudo labels for online semantic guidance. Next, we introduce asymmetric regional contrast learning. This approach fuses high-confidence predictions from the weak augmentation branch with scribbles to form augmented labels, and construct category prototypes in the representation space. Simultaneously, we prioritize contrastive constraints on challenging pixels in boundary-unlabeled regions. This strategy enhances intra-class cohesion and inter-class separation at the representation level, effectively reducing category confusion in low-contrast edges and complex backgrounds. Experiments conducted on the constructed graphene and MoS2 datasets demonstrate that our method TACoS achieves over 96% of fully supervised performance using less than 0.6% annotated data. Furthermore, it exhibits superior structural coherence and boundary stability in scenarios with weakly contrasting edges and complex backgrounds, providing an efficient and scalable solution for automated high-throughput screening of 2D material flakes.

</details>


### [50] [PUF: Plug-and-Play Uncertainty-Aware Fusion for Online 3D Scene Graph Generation](https://arxiv.org/abs/2607.07170)
*Yi Yang,Myrna Castillo,Bodo Rosenhahn,Michael Ying Yang*

Main category: cs.CV

TL;DR: PUF is a plug-and-play, uncertainty-aware fusion framework for online 3D scene graph generation that reformulates node association as probabilistic likelihood over semantic/spatial factors and uses Dirichlet evidence accumulation to improve accuracy while maintaining real-time performance.


<details>
  <summary>Details</summary>
Motivation: Existing online 3D scene graph generation methods treat fusion as a fully deterministic pipeline, overlooking three sources of uncertainty: observation uncertainty, 2D model uncertainty, and 3D representation uncertainty.

Method: PUF reformulates scene graph node association as probabilistic likelihood over semantic and spatial factors (instead of binary gates), uses Dirichlet evidence accumulation to distribute evidence proportionally, and applies an optional class-conditional prior for completing edges of sparsely co-observed object pairs.

Result: Outperforms existing approaches on 3DSSG and ReplicaSSG benchmarks while maintaining real-time latency; consistent improvements shown with both 3D Gaussian and 3D voxel backends.

Conclusion: Uncertainty-aware fusion is a principled and effective paradigm for online 3D scene understanding, and the proposed method generalizes across different 3D representations.

Abstract: Online 3D scene graph generation builds a persistent, structured representation of a scene by incrementally fusing 2D observations into a global 3D graph. Existing online methods treat this fusion as a fully deterministic pipeline, where we identify three sources of uncertainty that are overlooked: observation, 2D model, and 3D representation. We propose PUF: a Plug-and-play, Uncertainty-aware, and training-free Fusion framework. Scene graph node association is reformulated as a probabilistic likelihood over semantic and spatial factors, replacing binary accept/reject gates. Dirichlet evidence accumulation distributes class and relationship evidence across plausible candidates proportional to association likelihood. An optional class-conditional prior completes edges for sparsely or never co-observed object pairs. We instantiate PUF with both a 3D Gaussian and a 3D voxel backend and observe consistent improvements, demonstrating its ability to generalize across different representations. Experiments on the 3DSSG and ReplicaSSG benchmarks show that our method substantially outperforms existing approaches while maintaining real-time latency. These results establish uncertainty-aware fusion as a principled and effective paradigm for online 3D scene understanding. The source code is publicly available at https://github.com/yyyyangyi/PUF.

</details>


### [51] [Stage-Aware Adaptation and Distribution Calibration for Subject-Driven Personalized Text-to-Image Generation](https://arxiv.org/abs/2607.07173)
*Wenyan Xu,Alizer Wong*

Main category: cs.CV

TL;DR: SPaRa-DCAL improves personalized text-to-image generation by using stage-aware low-rank adaptation (SPaRa) and distribution-calibrated candidate selection (DCAL), showing that identity-biased selection trades off diversity for consistency.


<details>
  <summary>Details</summary>
Motivation: Existing subject-driven personalization methods (e.g., PaRa) apply uniform low-rank constraints across all denoising stages, failing to distinguish capacity needs at different timesteps, and inference selection driven solely by identity similarity compresses sample diversity.

Method: Decomposes into two components: (1) SPaRa — stage-aware low-rank adaptation with timestep-dependent scaling, and (2) DCAL — distribution-calibrated candidate selection that avoids identity bias. Combined as SPaRa-DCAL framework.

Result: Under SDXL and DreamBooth 30-subject protocol, DCAL improves 1-LPIPS, CLIP-I, DINO-I, and CLIP-T on fixed LoRA pool, but reveals a clear trade-off with CLIP/DINO pairwise diversity and pairwise LPIPS.

Conclusion: Personalized generation should be evaluated through identity consistency, text alignment, AND representation diversity, not identity metrics alone.

Abstract: Subject-driven personalized text-to-image generation requires a pretrained diffusion model to acquire a specific subject from a few reference images while preserving subject identity, following novel text prompts, and maintaining sample diversity. Existing optimization-based methods instantiate subject adaptation through full fine-tuning, textual embedding optimization, or low-rank parameter updates; PaRa further constrains personalization from the perspective of parameter rank reduction. However, a uniform low-rank constraint or a uniform adapter strength cannot explicitly distinguish the capacity requirements of different denoising stages. Moreover, inference-time candidate selection driven mainly by identity similarity may compress the selected samples in the visual representation space. We decompose the problem into two complementary components: SPaRa denotes training-side stage-aware low-rank adaptation, DCAL denotes inference-side distribution-calibrated candidate selection, and SPaRa-DCAL denotes the combined framework. Theoretical analysis shows that timestep-dependent scaling controls the effective perturbation magnitude of a low-rank adapter, while identity-biased candidate selection restricts the radius of selected features around the reference center under explicit conditions. Auditable experiments under the SDXL and DreamBooth 30-subject protocol show that DCAL improves 1-LPIPS, CLIP-I, DINO-I, and CLIP-T on a fixed LoRA candidate pool, while revealing a clear trade-off with CLIP/DINO pairwise diversity and pairwise LPIPS. These results indicate that personalized generation should be evaluated through identity consistency, text alignment, and representation diversity rather than identity metrics alone.

</details>


### [52] [Comparative Study of Domain-adapted VLMs for General Document Visual Question Answering](https://arxiv.org/abs/2607.07179)
*Miguel Lopez-Duran,Elena Marrero,Julian Fierrez,Marta Robledo-Moreno,Ruben Vera-Rodriguez,Daniel DeAlcala,Aythami Morales,Ruben Tolosana,Oscar Delgado,Alvaro Ortigosa,Javier Ortega-Garcia*

Main category: cs.CV

TL;DR: This paper evaluates 8 open-source VLMs on DocVQA across three document domains, finding that while large models excel on structured layouts, they struggle with visually complex documents. Visual understanding—not knowledge—is the key bottleneck, and few-shot finetuning (50 samples) can match or surpass fully supervised performance.


<details>
  <summary>Details</summary>
Motivation: Although VLMs perform well on general text-vision tasks, their robustness and transferability across different document domains (industrial docs, infographics, slides) in DocVQA remain underexplored.

Method: Evaluated 8 open-source pretrained VLMs on DocVQA across three domains using zero-shot, fully supervised (inter/intra-dataset), and few-shot learning settings to assess knowledge transfer.

Result: Large VLMs have strong zero-shot performance on structured layouts but degrade on visually complex layouts; parameter scaling dominates performance, but smaller models gain more from finetuning; visual understanding is the main bottleneck, not knowledge.

Conclusion: With only 50 target domain samples, models finetuned on DocVQA can rapidly adapt to new domains, sometimes surpassing fully supervised counterparts, highlighting that visual comprehension is the core challenge.

Abstract: Document Visual Question Answering (DocVQA) presents a complex multimodal challenge, requiring models to exploit visual, textual, and layout information from documents. Although Vision-Language Models (VLMs) have shown remarkable performance in text-vision tasks, their robustness and transferability to different document domains remains underexplored. In this study, we present a comprehensive evaluation of 8 open-source pretrained VLMs on DocVQA in three different document domains: industrial documents of varying type, infographics, and presentation slides. We systematically assess model performance under zero-shot evaluations, fully supervised finetuning with inter- and intra-dataset evaluations, and few-shot learning evaluations of knowledge transfer between domains. Our findings demonstrate that while large pretrained VLMs possess strong zero-shot baselines for structured layouts, their performance strongly decreases on visually complex layouts of infographics and slides. Although parameter scaling is a dominant factor on performance, supervised finetuning yields higher relative gains in smaller architectures. Furthermore, our cross-domain and few-shot experiments show that visual understanding is the main bottleneck for DocVQA, not a lack of knowledge from the VLMs. Using 50 target domain samples, the models finetuned in DocVQA with datasets of different domains rapidly adapt to the target domain documents, even surpassing their fully supervised counterparts in some cases.

</details>


### [53] [EditVerse3D: High-Quality 3D Object Editing with Region-Aware Learning](https://arxiv.org/abs/2607.07187)
*Youtan Yin,Yanning Zhou,Jiacheng Wei,Xiaofeng Yang,Jun Zhang,Jiayang Bai,Jingwen Ye,Weidong Zhang,Guosheng Lin*

Main category: cs.CV

TL;DR: EditVerse3D is a novel 3D editing framework that enables high-quality local editing of 3D objects using only coarse guidance (a 3D bounding box and a reference 2D image), introducing a region-aware adaptive loss and data augmentations to achieve superior performance over existing methods.


<details>
  <summary>Details</summary>
Motivation: Existing 3D editing methods rely on fully edited 2D images, precise 3D masks, or complex pipelines, which creates a gap with how humans naturally interact—by specifying only coarse regions of interest for modification.

Method: The framework takes a 3D object, a coarse 3D bounding box, and a reference 2D image as input. It introduces (1) a region-aware adaptive loss that emphasizes hard-to-learn regions and balances target/preserved areas, (2) targeted data augmentations like scaled 3D masks and filtering unrealistic editing pairs, and (3) a large-scale 3D editing dataset derived from parts information.

Result: Extensive experiments show EditVerse3D achieves superior visual quality and quantitative performance compared to existing 3D editing approaches.

Conclusion: EditVerse3D successfully bridges the gap between coarse human guidance and high-quality 3D object editing, offering a more natural and effective editing pipeline.

Abstract: Local editing of 3D objects remains a long-standing challenge. When interacting with 3D content, humans naturally tend to specify a coarse region of interest for modification rather than defining precise editing boundaries. However, previous methods rely on fully edited 2D images, precise 3D masks, or redundant pipelines, which present a gap. To bridge this gap, we propose EditVerse3D, a novel 3D editing framework that enables high-quality object editing under such coarse guidance. Our approach takes as input a 3D object to be edited, a coarse 3D bounding box indicating the target region, and a reference 2D image describing the desired modification. It produces a coherent, high-fidelity edited 3D object. To facilitate this editing, we introduce a novel region-aware adaptive loss that emphasizes hard-to-learn regions and balances the objective between target and preserved areas. Complementing our loss function, we enhance model robustness and generalization through targeted data augmentations, such as training with scaled 3D masks and filtering out unrealistic editing pairs. We construct a large-scale 3D editing dataset derived from parts information. Extensive experiments demonstrate that EditVerse3D achieves superior visual quality and quantitative performance compared to existing 3D editing approaches. Please visit our project page at https://editverse3d.github.io.

</details>


### [54] [Prototype-Anchored Generalized Manifold Regression for Unknown-Domain Object Detection](https://arxiv.org/abs/2607.07192)
*Zihao Zhang,Aming Wu,Yang Li,Yahong Han*

Main category: cs.CV

TL;DR: MR-DCoT proposes a manifold regression approach for Single-Domain Generalized Object Detection, using Visual-Text Dual Chain-of-Thought to generate off-manifold examples and Class-Specific Prototype Anchoring to rectify them back to the semantic manifold, achieving robust generalization across unseen domains.


<details>
  <summary>Details</summary>
Motivation: Existing single-domain generalization methods rely on simulation-driven strategies (data augmentation or textual prompts) that cannot cover real-world dynamic variations, leading to overfitting to synthetic styles and limited robustness to complex structural degradations.

Method: MR-DCoT: (1) Visual-Text Dual Chain-of-Thought module combines VLM-guided semantic evolution with diffusion-based structural perturbation to generate structured off-manifold hard examples; (2) Class-Specific Prototype Anchoring learns a rectification operator that projects deviant features back to the source semantic manifold; (3) The outlier generation and semantic correction form a closed loop to narrow the distribution gap.

Result: Extensive experiments on three benchmarks (adverse-weather detection, real-to-art generalization, zero-shot semantic segmentation) demonstrate effectiveness and versatility.

Conclusion: Robust generalization should rectify deviant samples back to the semantic manifold rather than exhaustively simulating external perturbations.

Abstract: In this paper, we study Single-Domain Generalized Object Detection (Single-DGOD), which aims to transfer a detector trained on a single source domain to multiple unseen domains. Existing methods mainly rely on simulation-driven strategies, such as data augmentation or textual prompts, to enlarge the training distribution. However, finite simulations can hardly cover the dynamic variations of real-world scenarios, often causing overfitting to synthetic styles and limited robustness to complex structural degradations. Inspired by the manifold hypothesis, we argue that semantic features, despite diverse visual changes, should lie on a compact and stable low-dimensional manifold. Therefore, robust generalization requires rectifying deviant samples back to this semantic manifold, rather than exhaustively simulating external perturbations. To this end, we propose Manifold Regression with Visual-Text Dual Chain-of-Thought (MR-DCoT), which formulates unknown-domain generalization as a manifold regression problem. MR-DCoT first uses a Visual-Text Dual Chain-of-Thought module to combine VLM-guided semantic evolution with diffusion-based structural perturbation, generating structured off-manifold hard examples. It then introduces Class-Specific Prototype Anchoring to learn a rectification operator that projects deviant features toward the source semantic manifold. By integrating outlier generation and semantic correction into a closed loop, MR-DCoT effectively narrows the distribution gap and improves robustness under unseen shifts. Extensive experiments on three complementary benchmarks, including adverse-weather detection, real-to-art generalization, and zero-shot semantic segmentation, demonstrate the effectiveness and versatility of our method.

</details>


### [55] [DiffCVE: Diffusion-based Compressed Video Enhancement](https://arxiv.org/abs/2607.07195)
*Wenqiang Xiao,Wenzhuo Ma,Junxi Zhang,Zhenzhong Chen*

Main category: cs.CV

TL;DR: This paper proposes DiffCVE, a diffusion-based method for enhancing severely compressed videos by incorporating coding priors (residuals, motion vectors) and compression degradation semantic prompting (QP-based textual prompts with LoRA) to improve perceptual quality without introducing structural hallucinations.


<details>
  <summary>Details</summary>
Motivation: Severely compressed videos suffer from complex artifact patterns and substantial information loss. Existing diffusion models applied to compressed video ignore compression degradation characteristics and may produce structure-inconsistent hallucinations.

Method: DiffCVE introduces three key components: (1) Coding Prior-enhanced Dual Conditioning (CPDC) branches that jointly model compressed video and coding priors (residuals, motion vectors); (2) Compression Degradation Semantic Prompting (CDSP) using QP-conditioned textual prompts with LoRA fine-tuning; (3) Coding Prior-guided Weighted Fusion (CPWF) in the VAE decoder to fuse features with QP-predicted weights.

Result: Extensive experiments demonstrate effectiveness in improving perceptual quality, especially under severe compression settings.

Conclusion: The proposed diffusion-based approach effectively enhances compressed video quality by leveraging coding priors and compression-aware conditioning, outperforming direct application of diffusion models.

Abstract: Perceptual quality enhancement of severely compressed videos remains challenging due to complex artifact patterns and substantial information loss. Recent diffusion models have demonstrated strong generative capability for visual restoration, but directly applying them to compressed video often ignores compression degradation characteristics and may introduce structure-inconsistent hallucinations. To address this issue, this paper presents a diffusion-based compressed video enhancement method, named DiffCVE. Coding Prior-enhanced Dual Conditioning (CPDC) branches are designed to jointly model compressed video and coding prior conditions, where coding priors including residuals and motion vectors provide complementary structural and motion guidance during the diffusion denoising process. To make the diffusion process aware of compression severity, a Compression Degradation Semantic Prompting (CDSP) mechanism is introduced to leverage QP-conditioned textual prompts together with LoRA fine-tuning. In addition, a Coding Prior-guided Weighted Fusion (CPWF) module is incorporated into the VAE decoder to fuse VAE encoder and coding prior encoder features with QP-predicted weights. Extensive experiments demonstrate the effectiveness of the proposed method in improving perceptual quality, especially under severe compression settings. The project page with enhanced video demonstrations is available at https://wqmaker.github.io/projects/DiffCVE/.

</details>


### [56] [Why Fake ? Unveiling the Semantic Vocabulary of Deepfake Detectors](https://arxiv.org/abs/2607.07216)
*Vazgken Vanian,Alexandros Doumanoglou,Dimitris Zarpalas*

Main category: cs.CV

TL;DR: This paper uses Encoding-Decoding Direction Pairs (EDDP), a post-hoc XAI technique, to uncover the learned concept space of black-box deepfake detectors, revealing implicit real/fake features and enabling global understanding, spatial localization, and counterfactual analysis for explainable DF detection.


<details>
  <summary>Details</summary>
Motivation: Deepfake detection lacks explainability—most detectors only output binary labels (real/fake) without justification, while existing explainable methods either rely on costly human annotations for artifact localization or produce superficial, ungrounded textual explanations.

Method: The authors employ Encoding-Decoding Direction Pairs (EDDP), a post-hoc XAI technique, to analyze black-box deepfake detectors by uncovering their internal concept space (semantic vocabulary) and examining how concept information is written to and read from internal representations.

Result: EDDP analysis reveals previously hidden real and fake features learned implicitly during detector training, enabling global model understanding, spatially aware concept localization, and counterfactual what-if analysis beyond conventional explanation methods.

Conclusion: Post-hoc XAI with EDDP provides nuanced, grounded explanations for deepfake detector decisions, offering deeper comprehension of detection strategies without requiring human annotations.

Abstract: Deepfake (DF) technology poses a significant threat to information integrity, driving the need for robust detection methods. Most DF detectors only consider predicting a binary label for whether the input is real or fake, lacking the justification required for real-world applications like legal proceedings. Explainable DF Detection has emerged to address this limitation, but existing techniques frequently fall short by either relying on human annotations for precise artifact localization or generating superficially plausible textual explanations without grounding. This work investigates the use of post-hoc explainable AI (XAI) to analyze the decision-making process of state-of-the-art black-box DF detectors. Specifically, we employ Encoding-Decoding Direction Pairs (EDDP), a technique suitable for uncovering the concept space of DF detectors (their semantic vocabulary) as well as the mechanism for writing and reading concept information to and from internal representations. Our analysis reveals previously hidden real and fake features learned implicitly during detector training, offering nuanced explanations unattainable through conventional methods. This enables global model understanding, spatially aware concept localization, and counterfactual what-if analysis, all contributing to a deeper comprehension of DF detection strategies.

</details>


### [57] [Vision Foundation Models in Radiology: A Scoping Review of Data, Methodology, Evaluation and Clinical Translation](https://arxiv.org/abs/2607.07219)
*Alejandro Vergara-Richart,Xavier Rafael-Palou,Almudena Fuster-Matanzo,Ignacio Iborra Roncales,Ángel Alberich-Bayarri,Ana Jiménez-Pastor*

Main category: cs.CV

TL;DR: This PRISMA-ScR scoping review of 67 studies (2017–2026) on radiology-specific vision foundation models reveals predominant use of brain MRI/chest X-ray/thoracoabdominal CT, Transformer architectures, self-supervised learning (masked image modeling, contrastive learning), and evaluation focused on segmentation/classification, but with inconsistent cross-domain validation and uneven alignment with FUTURE-AI principles, limiting clinical translation.


<details>
  <summary>Details</summary>
Motivation: To systematically map the heterogeneous landscape of vision foundation models developed specifically for radiological imaging, given the lack of standardized definition, development, and evaluation practices in this rapidly growing field.

Method: PRISMA-ScR scoping review of peer-reviewed studies (Jan 2017–Mar 2026) describing foundation models trained exclusively on radiological imaging data; 67 studies were analyzed across three pillars: data scale/heterogeneity, architectural/pretraining scalability, and downstream transferability/generalization.

Result: Datasets mainly covered brain MRI, thoracoabdominal CT, and chest X-ray (from <100K to multi-million images). Transformers and self-supervised pretraining (masked image modeling, contrastive learning, multi-stage) dominated. Evaluation focused on segmentation/classification; cross-center, cross-scanner, anatomical, and modality-shift validation was inconsistent. FUTURE-AI alignment was uneven.

Conclusion: Radiology-specific VFMs show promising transferability, but clinical translation is limited by insufficient data representativeness, heterogeneous benchmarks, incomplete reporting, and inadequate deployment-oriented evaluation.

Abstract: Vision foundation models (VFMs) are increasingly being developed for radiological imaging, yet their definition, development and evaluation remain heterogeneous. We conducted a PRISMAScR scoping review of peer-reviewed studies published between January 2017 and March 2026 describing foundation models trained exclusively on radiological imaging data. Sixty-seven studies were included and mapped across three pillars: data scale and heterogeneity, architectural and pretraining scalability, and downstream transferability and generalization. Datasets primarily covered brain MRI, thoracoabdominal CT, and chest X-ray, ranging from fewer than 100,000 samples to multi-million-image cohorts. Transformer-based architectures and self-supervised pretraining predominated, particularly masked image modeling, contrastive learning and multi-stage approaches. Evaluation focused mainly on segmentation and classification, whereas cross-center, cross-scanner, anatomical and modality-shift validation was inconsistently reported. Alignment with FUTURE-AI principles was uneven. Overall, radiology-specific VFMs show promising transferability, but clinical translation remains constrained by limited data representativeness, heterogeneous benchmarks, incomplete reporting and insufficient deployment-oriented evaluation.

</details>


### [58] [`Attention-Guided Cross-Temporal Clustering for Self-Supervised Video Object Segmentation](https://arxiv.org/abs/2607.07230)
*Waqas Arshid,Mohammad Awrangjeb,Alan Wee-Chung Liew,Yongsheng Gao*

Main category: cs.CV

TL;DR: The paper proposes a self-supervised video object segmentation framework (Cross-Temporal Consistency and Clustering) that learns part-aware representations via attention-guided token selection and temporal clustering, achieving spatial accuracy and temporal coherence without labels, optical flow, or task-specific pretraining.


<details>
  <summary>Details</summary>
Motivation: Supervised VOS methods rely on costly dense annotations with limited domain coverage. Existing self-supervised approaches struggle with spatial accuracy and temporal coherence in multi-object scenarios and often depend on optical flow or synthetic cues, limiting scalability.

Method: A self-supervised framework combining attention-guided token selection with lightweight temporal clustering. It aligns soft part assignments across time via a saliency-weighted symmetric consistency objective, using a frozen transformer backbone with lightweight modules for adaptive token selection and multi-offset temporal alignment.

Result: The method achieves efficient scaling across resolutions and motion patterns, addressing both spatial accuracy and temporal coherence in unconstrained multi-object scenarios without reliance on dense labels, optical flow, or task-specific pretraining.

Conclusion: Self-supervised learning of mid-level, part-aware representations via cross-temporal consistency is a scalable and generalizable alternative for VOS, eliminating the need for costly annotations and task-specific priors.

Abstract: Video object segmentation (VOS) is a fundamental task in video understanding, requiring accurate delineation and consistent tracking of objects across frames. While supervised methods achieve strong performance, they rely on densely annotated datasets that are costly to obtain and have limited domain coverage. Self-supervised learning offers a promising alternative by removing the need for manual labels; however, existing approaches often struggle to jointly maintain spatial accuracy and temporal coherence, particularly in unconstrained multi-object scenarios. Many rely on optical flow, synthetic motion cues, or task-specific pretraining, limiting scalability and generalisation. We propose a self-supervised framework, Cross-Temporal Consistency and Clustering, that learns mid-level, part-aware representations by combining attention-guided token selection with lightweight temporal clustering. Instead of operating at the pixel or whole-object level, the method aligns soft part assignments across time using a saliency-weighted symmetric consistency objective. The framework leverages a frozen transformer backbone with lightweight modules for adaptive token selection and multi-offset temporal alignment, enabling efficient scaling across resolutions and motion patterns.

</details>


### [59] [Unraveling Machine Behavior by Multi-Level Bias Analysis and Detection: Methodology and Application to Computer Vision](https://arxiv.org/abs/2607.07236)
*Ignacio Serna,Aythami Morales,Julian Fierrez*

Main category: cs.CV

TL;DR: This paper proposes three bias detection methods (SpaceBias, ActivationBias, WeightBias) that analyze bias at different levels within neural networks—latent space, layer activations, and parameters—rather than treating the model as a black box. Experiments on over 127,000 models across gender classification (DiveFace) and digit classification (colored-MNIST) show detection performance decreases smoothly as training distribution approaches balance.


<details>
  <summary>Details</summary>
Motivation: Existing bias detection methods treat neural networks as black boxes, assessing only final outcomes without understanding how bias manifests internally within the network architecture at different levels (latent space, activations, parameters).

Method: Three bias detection approaches: (1) SpaceBias—characterizes latent space via neighbor-probability distributions and uses KS test for bias quantification; (2) ActivationBias—extends InsideBias by analyzing filter activations using Mann-Whitney U test; (3) WeightBias—extends IFBiD by training a secondary network to identify biased patterns in model parameters.

Result: Experiments on 72,000 face images (DiveFace) and colored-MNIST with over 127,000 models show internal disparity and detection performance decrease smoothly as training distribution approaches balance. The multi-level analysis provides nuanced understanding of bias manifestation.

Conclusion: Methods providing deeper insight into internal model behavior are important for understanding bias. The proposed techniques reveal how biases manifest at different architectural levels, offering more detailed understanding than black-box approaches.

Abstract: This study investigates the presence and propagation of bias within Neural Networks through a comprehensive multi-level analysis spanning the learned latent space, layer activations, and the network's parameters. Based on this taxonomy, we propose three bias detection approaches: 1) SpaceBias (new method), which characterizes the latent space prior to the final classification layer using neighbor-probability distributions and quantifies bias with the two-sample Kolmogorov-Smirnov test on the per-group distributions. 2) ActivationBias (extension of the existing method InsideBias), which analyzes the activations of neural network filters and quantifies bias via a Mann-Whitney U test, based on the observed fact that underrepresented groups exhibit lower activation levels in the final convolutional layers. 3) WeightBias (extension of the existing method IFBiD), which uses a secondary neural network trained to identify biased patterns directly in the parameters of task-specific models. Unlike conventional methods, which assess neural network outcomes and treat the model as a black box, our proposed techniques provide insight into how biases manifest within the network architecture itself at different levels, offering a more nuanced and detailed understanding. Experiments are conducted on two complementary applications: gender classification in the DiveFace dataset (72,000 face images) and digit classification on a colored-MNIST benchmark with controlled bias severity. In total, more than 127,000 models with varying degrees and types of bias were trained and evaluated. The severity sweep shows that the internal disparity, and with it the detection performance, decreases smoothly as the training distribution approaches balance. The results highlight the importance of methods that provide deeper insight into the behavior of AI models.

</details>


### [60] [An Edge-aware Prompt-enhanced SAM for Ultrasound Image Segmentation](https://arxiv.org/abs/2607.07240)
*Wenhao Li,Fangyi Liu,Bo Du*

Main category: cs.CV

TL;DR: EP-SAM enhances SAM for ultrasound segmentation by integrating multi-block feature extraction and edge-aware supervision, achieving superior performance over existing SAM-based methods.


<details>
  <summary>Details</summary>
Motivation: SAM performs poorly on ultrasound images due to poor boundary delineation caused by speckle noise and contour ambiguity.

Method: (1) Multi-block feature extraction from the image encoder for coarse-to-fine semantics; (2) Edge-aware supervision to handle contour noise; (3) Integration of these cues to generate high-quality prompts for the target region.

Result: EP-SAM consistently outperforms existing SAM-based methods on multiple ultrasound benchmarks.

Conclusion: Edge-aware and prompt-enhanced adaptation effectively improves SAM's ultrasound segmentation, especially boundary delineation.

Abstract: Ultrasound image segmentation is essential for delineating anatomical structures and lesions, providing the foundation for accurate diagnosis. While the Segment Anything Model (SAM) has demonstrated remarkable success on natural images, its performance on ultrasound data is often hindered by poor boundary delineation. To address this limitation, we propose EP-SAM, an edge-aware and prompt-enhanced adaptation of SAM. Specifically, we leverage multi-block feature extraction from the image encoder to enrich coarse-to-fine semantic representations, while edge-aware supervision of the image encoder improves robustness to contour ambiguity and speckle noise. By integrating these complementary cues, EP-SAM generates high-quality prompts that effectively guide the model toward target regions of interest. Experimental results on multiple benchmarks demonstrate that EP-SAM consistently outperforms existing SAM-based methods.

</details>


### [61] [Naming the Concepts Classifiers Rely On: Language-Anchored Decomposition for Faithful Explanation](https://arxiv.org/abs/2607.07264)
*Ahsan Habib Akash,Dipkamal Bhusal,Stacey Jones,Donald A. Adjeroh,Binod Bhattarai,Prashnna Kumar Gyawali*

Main category: cs.CV

TL;DR: LAD is a post-hoc interpretability framework that generates named, faithful concept explanations without modifying the original model by using LLM-proposed vocabularies and CLIP-based similarity maps anchored via an inverted NMF structure.


<details>
  <summary>Details</summary>
Motivation: Existing concept-based explanations suffer from a trade-off: post-hoc methods are faithful but produce unnamed concepts, while naming methods require retraining or model alteration. The goal is to achieve both naming and faithfulness post-hoc without modifying the model.

Method: LAD uses a large language model to propose concept vocabularies per class, localizes them via CLIP similarity maps, then applies an inverted NMF where these language-grounded maps serve as a fixed coefficient matrix, learning only a concept basis that reconstructs the frozen encoder's activations—making naming a structural constraint.

Result: Across natural-image, scene, and medical-imaging benchmarks, LAD produces spatially precise explanations that outperform alternatives under concept insertion/deletion tests, and uniquely provides stable, human-interpretable concept names.

Conclusion: LAD resolves the faithfulness-naming trade-off in concept-based interpretability without model modification, and removing the language anchor preserves accuracy but collapses attribution faithfulness, confirming the anchor's critical role.

Abstract: Deep neural networks are widely deployed in high-stakes visual applications where interpretability is critical, yet existing explanations face a trade-off: post-hoc concept methods recover factors that are faithful to a model's behavior but unnamed, while naming and by-design methods attach human-readable concepts only by retraining or altering the classifier. We propose Language-Anchored Decomposition (LAD), a post-hoc framework that delivers concepts which are simultaneously named, faithful, and obtained without modifying the model. For each class, a large language model proposes a concept vocabulary that CLIP-based similarity maps localize across image regions. Inverting standard non-negative matrix factorization, LAD fixes these language-grounded maps as the coefficient matrix and learns only a concept basis that reconstructs the frozen encoder's activations, so naming becomes a structural constraint and the model's own feature geometry determines which concepts are retained. Removing this anchor preserves accuracy but collapses attribution faithfulness. Across natural-image, scene, and medical-imaging benchmarks, LAD produces spatially precise explanations that are decision-relevant under both concept insertion and deletion, while uniquely providing stable, human-interpretable concept names.

</details>


### [62] [InfraQR: Edge-Placed QR-Inspired Structured Patch Attacks on Infrared Vision-Language Models](https://arxiv.org/abs/2607.07288)
*Xin Li,Jiaju Han,Ma Yaqi,Chengyin Hu,Yingying Zhao,Jiahuan Long,Fengyu Zhang,Yahui Chai*

Main category: cs.CV

TL;DR: InfraQR is a QR-inspired structured patch attack that, when placed along image boundaries, severely degrades the performance of infrared vision-language models across classification, captioning, and VQA tasks, reducing OpenAI CLIP accuracy from 98.67% to 0.70%.


<details>
  <summary>Details</summary>
Motivation: Infrared vision-language models are increasingly used for perception under low-light conditions, but their robustness to localized structured perturbations remains underexplored, with existing adversarial studies mainly focused on object detectors rather than vision-language models.

Method: A QR-inspired structured patch attack (InfraQR) that places a compact patch along image boundaries, optimizing learnable grid cells through surrogate CLIP-style encoders to produce a near-binary structured appearance without requiring valid QR code functionality.

Result: On a 300-image infrared benchmark, InfraQR reduces OpenAI CLIP accuracy from 98.67% to 0.70%, and the adversarial images transfer to black-box captioning and VQA models, causing semantic degradation and more error-prone answers under GPT-5.4-based evaluation.

Conclusion: Infrared vision-language models remain vulnerable to structured edge-placed perturbations, motivating further study of cross-task robustness beyond direct object occlusion.

Abstract: Infrared vision-language models are increasingly used for perception under low-light and adverse visual conditions, yet their robustness to localized structured perturbations remains underexplored. Existing infrared adversarial studies mainly focus on object detectors, leaving the security of infrared vision-language models less systematically examined. We present InfraQR, a QR-inspired structured patch attack for infrared vision-language models. Unlike localized attacks that attach perturbations to the target object, InfraQR places a compact structured patch along image boundaries and optimizes learnable grid cells through surrogate CLIP-style encoders. The resulting patch has a near-binary structured appearance, but is not required to be a valid or machine-readable QR code. We evaluate InfraQR on infrared classification, caption transfer, and question-answer-aware visual question answering (VQA) tasks. On a 300-image infrared benchmark, InfraQR sharply reduces the accuracy of multiple CLIP-style classifiers, including reducing OpenAI CLIP accuracy from 98.67% to 0.70%. The generated adversarial images also transfer to black-box captioning and VQA models, causing semantic degradation in captions and more error-prone answers under GPT-5.4-based evaluation. These results show that infrared vision-language models remain vulnerable to structured edge-placed perturbations, motivating further study of cross-task robustness beyond direct object occlusion.

</details>


### [63] [CarbonCLIP: Enhance Carbon Prediction from Satellite Imagery via Integrated Street-View Semantics and Temporal Context Training](https://arxiv.org/abs/2607.07292)
*Zeru Yang,Fang-Ying Gong,Steve H. L. Yim,Chau Yuen*

Main category: cs.CV

TL;DR: CarbonCLIP is a multimodal distillation framework that improves satellite-based urban carbon emission prediction by transferring contextual knowledge from street-view images and temporal data into unified satellite representations via contrastive learning, achieving superior results on Beijing and Singapore datasets.


<details>
  <summary>Details</summary>
Motivation: Existing urban carbon emission estimation methods are difficult to apply consistently across cities due to heterogeneous data sources and the lack of fine-grained semantic-temporal context in remote sensing data.

Method: CarbonCLIP uses a dual-branch contrastive learning framework: (1) a spatial branch that leverages LMM-generated textual descriptions from street-view images for semantic priors, and (2) a temporal branch with a month encoder for temporal priors. Multimodal data is only needed during pretraining; inference uses satellite imagery alone.

Result: Experiments on Beijing and Singapore show CarbonCLIP outperforms baselines in both cities, validating that multimodal knowledge can be effectively transferred into satellite representations for carbon modeling.

Conclusion: CarbonCLIP offers a robust, scalable solution for satellite-based urban carbon modeling by bridging the gap between top-down satellite views and ground-level human activities.

Abstract: Accurately estimating urban carbon emissions is critical for sustainable urban planning, yet many existing approaches remain difficult to apply consistently across cities due to data-source heterogeneity and the lack of fine-grained semantic-temporal context in remote sensing data. We propose CarbonCLIP, a task-oriented multimodal distillation framework that improves satellite-based carbon emission prediction by transferring contextual knowledge into a unified satellite representation through dual-branch contrastive learning. Unlike conventional methods that rely on static visual features, CarbonCLIP explicitly bridges the gap between top-down satellite views and ground-level human activities. Specifically, the spatial branch uses fine-grained textual descriptions automatically generated from street-view images by Large Multimodal Models (LMMs) to provide semantic priors reflecting building functions, infrastructure, and urban activities, while the temporal branch employs a month encoder to encode temporal priors associated with monthly emission variation. CarbonCLIP requires multimodal data only during the pretraining phase; during inference, it relies solely on satellite imagery, thereby supporting scalable deployment when ground-level data are unavailable at inference. Experiments on Beijing and Singapore demonstrate that CarbonCLIP outperforms baselines in both study cities. The results validate that our method effectively transfers multimodal knowledge into satellite representations, offering a robust solution for satellite-based urban carbon modeling.

</details>


### [64] [SoccerNet 2026 Challenges Results](https://arxiv.org/abs/2607.07320)
*Anthony Cioppa,Silvio Giancola,Håkan Ardö,Mohamad Dalal,Jan Held,Jérémie Ochin,Jiayuan Rao,Karen Sanchez,Renaud Vandeghen,Artur Xarles,Olivier Barnich,Albert Clapés,Mathieu Delvaux,Sergio Escalera,Bernard Ghanem,Cédric Hons,Antoine Houet,Sotiris Manitsaris,Tom Michel,Pierre Miralles,Thomas B. Moeslund,Mikael Nilsson,Bogdan Stanciulescu,Marc Van Droogenbroeck,Yanfeng Wang,Weidi Xie,Faisal Altawijri,Mohamed Atef,Semen Budennyy,Vasiliy Chelpanov,Puhua Chen,Yixin Chen,Lechao Cheng,Jianling Chu,Ju-Seong Do,Oleg Durygin,Omar Fetouh,Mirco Fuchs,Youssef Ghallab,Falguni Ghosh,Wonjun Heo,Yufeng Hu,Weixuan Huang,Phuong-Linh Huynh-Ha,Matvey Isupov,Yangguang Ji,Siyuan Jiang,Zhenxiang Jiang,Wonyong Jo,Ho-Young Jung,SeongHeon Kang,MinJae Kim,Youngseon Kim,Jakub Komosa,Artem Konshin,Trung-Hoang Le,Jongmin Lee,Lingling Li,Litao Li,Vadim Linkov,Fang Liu,Haoxuan Ma,Shun Makino,Ismail Mathkour,Konstantin Mitin,Mikhail Moiseev,Takumi Nagaya,Yuki Nakamura,Thanh-Khoi Nguyen,Hoang-Phuc Nguyen,Trong-Thuan Nguyen,Christian Orduz,Kwanyong Park,Fabian Perez,Parthsarthi Rawat,SuHyun Rim,Hoover Rueda-Chacón,Atom Scott,Minori Sugimura,Yuyang Sun,Shengeng Tang,Minh-Triet Tran,Ikuma Uchida,Juan Vanegas,Thanh-Nhan Vo,Jiangtao Wang,Yaxiong Wang,Xiaogang Wang,Ruifeng Wang,Rio Watanabe,Jiali Wen,Yongliang Wu,Di Yang,Xu Yang,Zhuo Yang,Xinyu Ye,Yibo Yu,Zihan Zhai,Yu Zhang,Zhenyu Zhao,Zhun Zhong,Yixi Zhou,Xingyu Zhu,Wenbo Zhu,Julian Ziegler*

Main category: cs.CV

TL;DR: The SoccerNet 2026 Challenges present five vision-based tasks (Ball Action Anticipation, Player-Centric Ball Action Spotting, Novel View Synthesis, Spiideo SoccerNet Synloc, and Visual Question Answering) for sports video understanding, with 427 teams submitting 1,129 entries across tasks.


<details>
  <summary>Details</summary>
Motivation: To advance computer vision research in sports video understanding through a unified benchmarking effort spanning diverse tasks including action anticipation, spotting, view synthesis, localization, and question answering.

Method: Five tasks are designed with annotated data, unified evaluation protocols, and public baselines. Tasks cover ball action anticipation, player-centric spotting, novel view synthesis, athlete localization, and visual question answering.

Result: Broad participation with 427 teams, 1,129 entries, and 28 technical reports. Leaderboards for each task are presented with leading submissions documented.

Conclusion: The paper documents the current state of each computer vision task on held-out challenge data, serving as a benchmark for sports video understanding research.

Abstract: The SoccerNet 2026 Challenges constitute the sixth annual edition of the SoccerNet open benchmarking effort, dedicated to advancing computer vision research in sports video understanding. This year's challenges span five vision-based tasks: (1) Ball Action Anticipation, predicting the timing and class of ball-related actions within a short future window from a preceding observation window; (2) Player-Centric Ball Action Spotting, temporally localizing and classifying ball-related actions while assigning each action to the acting player through team affiliation and jersey number; (3) Novel View Synthesis, rendering images from unobserved camera poses in multi-view football scenes; (4) Spiideo SoccerNet Synloc, localizing athletes in real-world pitch coordinates from a single calibrated static-camera image; and (5) Visual Question Answering, answering multiple-choice questions about football broadcasts across text, image, and video inputs. For each task, participants were provided with annotated data, a unified evaluation protocol, and a public baseline. This edition saw broad participation, with 427 teams submitting 1,129 entries across the five tasks and 28 teams contributing reviewed technical reports. This paper describes each task and its evaluation protocol, presents the challenge leaderboards, and summarizes the leading submissions, with the aim of documenting the current state of each task as measured on held-out challenge data.

</details>


### [65] [HAJJv2-CrowdCount: Zero-Shot Benchmark for Dense Crowd Counting](https://arxiv.org/abs/2607.07322)
*Reem AlYabis,Fares AlTuwaim,AlJawharh AlOtaibi,Mohamed Eltahir*

Main category: cs.CV

TL;DR: This paper introduces HAJJv2-CrowdCount, a per-second annotated dataset for Hajj crowd counting, and benchmarks three zero-shot counting methods. While SAM3Count has the best overall MAE (70.4), the point-based APGCC performs better on the densest frames (MAE 114.9 vs 300+), revealing a critical performance inversion for deployment.


<details>
  <summary>Details</summary>
Motivation: Automated crowd counting in Hajj videos is difficult due to steep camera angles, extreme occlusion, and high-density crowds (1000+ people per frame). Existing benchmarks are private or lack per-second detail, motivating the need for a publicly available, finely annotated dataset to enable reproducible evaluation.

Method: The authors release HAJJv2-CrowdCount with per-second human-annotated crowd counts, and benchmark three zero-shot paradigms: YOLO-World (open-vocabulary detector), APGCC (point-based counter), and SAM3Count (promptable segmentation-based counter).

Result: SAM3Count achieves the lowest overall MAE (70.4, CI: 56.0-86.1), followed by YOLO-World (92.0) and APGCC (152.9). However, on the densest frames, SAM3Count and YOLO-World degrade sharply (MAE >300), while APGCC degrades gracefully (MAE 114.9).

Conclusion: The performance inversion is decision-relevant for Hajj crowd management: reliable counts are most needed in the densest scenes, where point-based counters outperform detection/segmentation-based methods. The annotations are released publicly for reproduction and extension.

Abstract: Automated crowd counting in Hajj video is difficult not because current models lack capacity, but because the footage violates the assumptions those models were built on: cameras observe the crowd from steep, near-vertical angles, individuals occlude one another extensively, and a single frame can contain well over a thousand people. Benchmarks that test crowd counting in such an environment are either private or not detailed per second. We revisit the HAJJv2 dataset and contribute HAJJv2-CrowdCount: per-second human-annotated crowd counts for its testing videos. Using these annotations, we benchmark three recent zero-shot counting paradigms: an open-vocabulary detector (YOLO-World), a point-based counter (APGCC), and a promptable segmentation-based counter (SAM3Count). SAM3Count attains the lowest overall mean absolute error (MAE 70.4, 95% CI 56.0-86.1), ahead of YOLO-World (92.0) and APGCC (152.9). This ordering reverses, however, in the regime most relevant to deployment: on the densest frames, the detection- and segmentation-based counters both degrade sharply (MAE exceeding 300), while the point-based counter degrades far more gracefully (MAE 114.9). This inversion is decision-relevant for Hajj crowd management, where reliable counts are needed most precisely in the densest and most occluded scenes. The annotations are released to support reproduction and extension of these results.

</details>


### [66] [BUS: Brain-Inspired Unsupervised Self-Reflection for Advanced Multimodal Reasoning](https://arxiv.org/abs/2607.07361)
*Jiacheng Yang,Tongying Xiao,Yunkai Dang,Cong Wang,Yuekun Yang,Qi Fan,Wenbin Li,Feng Miao,Yang Gao*

Main category: cs.CV

TL;DR: This paper introduces BUS, a label-free training framework inspired by the brain's backward prediction mechanism, which enhances Vision-Language Models' reflective reasoning on complex visual tasks without requiring annotated data.


<details>
  <summary>Details</summary>
Motivation: Current VLMs struggle with complex visual tasks requiring consistent and fine-grained reasoning. Existing self-reflection methods need large annotated datasets and lack explicit test-time reflective behavior. The authors seek a label-free solution inspired by neuroscience.

Method: The authors first verify that VLMs can perform backward prediction (predicting which current states precede a future state), similar to the human brain. Then they propose BUS, an unsupervised framework that leverages backward prediction to provide explicit learning signals on unlabeled data, compatible with SFT and RL fine-tuning methods.

Result: Experiments on 8 benchmarks show BUS achieves notable improvements over base models across complex visual tasks, using only unlabeled training data.

Conclusion: Backward prediction capability is critical for VLM reasoning, and BUS effectively enhances reflective reasoning without labeled data.

Abstract: Current Vision-Language Models (VLMs) often struggle to handle complex visual tasks that require consistent and fine-grained reasoning. Recent methods aim to train models to facilitate self-reflective reasoning, i.e., reviewing and improving the generated reasoning. However, they require large volumes of annotated data and lack explicit reflective behavior during test time. This work aims to bridge this gap through inspiration from neuroscience. The human brain exhibits efficient backward prediction, i.e., predicting which current states are likely to precede a given future state. In this work, we first verify that mainstream VLMs can perform backward prediction, similar to the human brain. Then, we propose Brain-inspired Unsupervised Self-reflection (BUS), a label-free training framework to enhance reflective reasoning capability in challenging image analysis. BUS enables VLMs to perform backward prediction and provide explicit learning signals on data without ground-truth labels. In this way, BUS eliminates reliance on annotated data while improving reasoning performance. Notably, BUS is compatible with popular fine-tuning methods, such as Supervised Fine-Tuning (SFT) and Reinforcement Learning (RL). Finally, extensive experiments on 8 benchmarks demonstrate the effectiveness of BUS across a wide range of complex visual tasks. It achieves notable improvements over the base model while using only unlabeled training data. Our experimental findings validate that backward prediction capability is critical for VLM reasoning.

</details>


### [67] [MMAgent-R$^2$: Learning to Rerank and Reject for Agentic mRAG](https://arxiv.org/abs/2607.07383)
*Tao Zhang,Ziqi Zhang,Zongyang Ma,Yuxin Yang,Bing Li,Chunfeng Yuan,Kang Rong,Fengyun Rao,Jing Lyu,Weiming Hu*

Main category: cs.CV

TL;DR: MMAgent-R^2 proposes an agentic mRAG framework with visual reranking and active rejection to improve KB-VQA by distinguishing visually similar entities and reducing error propagation from failed retrieval.


<details>
  <summary>Details</summary>
Motivation: Existing mRAG methods rely on global visual features that struggle to distinguish visually similar but factually mismatched entities, causing retrieval errors that propagate to final answers.

Method: Proposes an agentic framework (MMAgent-R^2) with internal verification: (1) visual reranking directly compares query and candidate images; (2) active rejection discards unreliable results and retrieves more candidates. Uses a composite reward function with step-level verification rewards and GRPO training for joint optimization.

Result: Achieves state-of-the-art performance on InfoSeek, E-VQA, and MMhops, with notable advantages in challenging retrieval scenarios and multi-image multi-hop reasoning.

Conclusion: The proposed internal verification mechanism effectively addresses visual entity disambiguation and retrieval error propagation, leading to superior KB-VQA performance.

Abstract: Knowledge-based Visual Question Answering (KB-VQA) requires models to retrieve visual entities matching the query image from large-scale encyclopedic knowledge bases and answer related questions. Existing multimodal Retrieval Augmented Generation (mRAG) methods rely on global visual features to match candidate entities, yet when the knowledge base contains numerous visually similar entities, the retriever struggles to distinguish them, populating the candidate set with visually similar but factually mismatched distractors. Since subsequent processing steps such as noise filtering are also confined to this fixed candidate set, errors from failed retrieval inevitably propagate to the final answer. To address these challenges, we propose MMAgent-R$^2$, an agentic mRAG framework that integrates visual reranking and active rejection as its internal verification mechanism. Visual reranking directly compares query and candidate images, capturing discriminative details beyond textual descriptions to precisely identify the target entity among similar candidates; active rejection discards unreliable results and retrieves additional candidates when no confident match is found, moving beyond the fixed candidate pool. We design a composite reward function with step-level verification rewards and achieve joint optimization of external retrieval, internal verification, and answer generation via GRPO training. Experiments on InfoSeek, E-VQA, and MMhops demonstrate that \ours{} achieves state-of-the-art performance, with particularly notable advantages in challenging retrieval scenarios and complex multi-image multi-hop reasoning tasks.

</details>


### [68] [When Prompts Ignore Structure: Graph-Based Attribute Reasoning for Calibrated VLMs](https://arxiv.org/abs/2607.07395)
*Tanay Sodha,Aditya Sharma,Ramya Hebbalaguppe,Vinti Agarwal,Pranav Murthy Yeluripaty*

Main category: cs.CV

TL;DR: ARGTCA improves test-time adaptation in VLMs by modeling (class, attribute) pairs as nodes in a Symbolic Attribute Graph with a GAT, reducing ECE by ~37% (ARGTCA-DIV) and ~17% (ARGTCA-DISC) over baselines.


<details>
  <summary>Details</summary>
Motivation: Test-time adaptation in VLMs suffers from poor calibration due to entropy-driven overconfidence from prompt tuning. Existing methods treat class attributes independently, ignoring their relational structure.

Method: ARGTCA constructs a Symbolic Attribute Graph where (class, attribute) pairs are nodes, trained with a GAT using contrastive objectives. Two strategies: ARGTCA-DIV (intra-class diversity) and ARGTCA-DISC (inter-class discrimination).

Result: ARGTCA-DIV reduces average ECE by ~37% over baselines; ARGTCA-DISC reduces average ECE by ~17%, consistently ranking as second-best across nine benchmarks.

Conclusion: Modeling symbolic attribute interactions offers a principled approach for reliable test-time adaptation in VLMs.

Abstract: Reliable confidence estimation remains a key limitation of test-time adaptation in vision-language models (VLMs), where prompt tuning improves zero-shot accuracy but often degrades calibration due to entropy-driven overconfidence. Prior approaches mitigate this using LLM-derived class attributes and contrastive regularization, yet treat attributes independently, ignoring their relational structure. We propose ARGTCA, which represents (class, attribute) pairs as nodes in a Symbolic Attribute Graph and trains a Graph Attention Network (GAT) using contrastive objectives to produce structurally informed embeddings that capture inter-attribute dependencies. We introduce two attribute selection strategies: ARGTCA-DIV for intra-class diversity and ARGTCA-DISC for inter-class discrimination. Experiments across nine benchmarks show that ARGTCA-DIV reduces average Expected Calibration Error (ECE) by approximately ~37% over baselines, while ARGTCA-DISC consistently performs as the second-best variant, reducing average ECE by approximately ~17% over baselines. These results suggest that modeling symbolic attribute interactions provides a principled approach for reliable test-time adaptation in VLMs.

</details>


### [69] [Heterogeneity-Adaptive Diffusion Schrodinger Bridge for PET-Guided Whole-Body MRI Translation](https://arxiv.org/abs/2607.07401)
*Chengbo Wang,Jiacheng Yu,Linjie Bian,Ming Qi,Xiaosheng Liu,Tongtong Che,Jichang Zhang,Shuyu Li,Shaoli Song,Xiuying Wang*

Main category: cs.CV

TL;DR: HA-DSB framework uses diffusion Schrödinger bridges with VLM region embeddings and PET-guided dual-stage mechanisms to translate whole-body MRI, addressing heterogeneity across anatomical regions and pathological tissues.


<details>
  <summary>Details</summary>
Motivation: PET-MR scanning has excessive long acquisition time for whole-body scans; existing deep learning MRI translation models focus on specific regions and fail on whole-body with heterogeneous feature distributions across different anatomical regions and pathological tissues.

Method: HA-DSB framework: (1) diffusion Schrödinger bridge for stochastic transport between source and target distributions; (2) VLM-derived region context embeddings for region-specific modeling; (3) dual-stage PET-guided guidance mechanism with a forward noise modulation and reverse lesion attention amplification.

Result: Outperforms existing methods across different body regions in whole-body MRI translation; shows improved translation quality in lesion areas with PET guidance.

Conclusion: The HA-DSB framework effectively handles whole-body heterogeneity and improves pathological region fidelity, offering a promising solution to accelerate PET-MR scanning.

Abstract: While whole-body multimodal medical imaging scanners have been increasingly recognized for more effective medical applications, the excessive long acquisition time in PET-MR scanning is a major obstacle in more efficient clinical practice. Deep learning-based MRI translation provides a potential solution to reduce scan duration. However, current models often focus on specific anatomical regions and face challenges for whole-body scans that consists of highly heterogeneous feature distributions mainly due to (1) different anatomical regions across whole-body, and (2) lesions or pathological tissues. This paper tackles the challenges through a novel Heterogeneity-Adaptive Diffusion Schrodinger Bridge (HA-DSB) framework. By explicitly modeling translation as stochastic transport between source and target distributions, HA-DSB incorporates region context embeddings derived from a vision-language model (VLM) to enable region-specific modeling. To enhance fidelity of the pathological tissue, lesion-aware metabolic prior from PET is integrated directly into the bridge dynamics through a dual-stage guidance mechanism. Specifically, a PET-guided noise modulation module adaptively scales spatial diffusion perturbations during the forward process, while PET features are leveraged during the reverse process to selectively amplify lesion-relevant structures via an attention mechanism. Experiments demonstrate the superiority of our method across different body regions in whole-body MRI translation and show improved translation quality in lesion areas under PET guidance. Our code is available at Github.

</details>


### [70] [VCDP: Variation-Conditioned Distributional Proxy Learning for Semi-Supervised Medical Image Segmentation](https://arxiv.org/abs/2607.07416)
*Zimu Zhang,Yiheng Zhong,Zhuoru Zhang,Yingzhen Hu,Yanan He,Fanliang Meng,Xiaofeng Liu*

Main category: cs.CV

TL;DR: VCDP is a plug-and-play training module for semi-supervised 3D medical image segmentation that uses variation-conditioned distributional proxy learning to improve feature-space organization, especially for small and ambiguous organs, without adding inference cost.


<details>
  <summary>Details</summary>
Motivation: Existing semi-supervised 3D medical segmentation methods (consistency regularization, pseudo-labeling, co-training) improve prediction-level robustness but fail to provide sufficient feature-space organization for anatomically complex structures, particularly small organs and ambiguous boundaries with large intra-class variations.

Method: VCDP represents each class with a learnable Gaussian distribution for shared class semantics and multiple variation prototypes for fine-grained intra-class patterns. A variation-conditioned compatibility score fuses distributional similarity and soft variation aggregation to guide voxel embeddings toward both global organ identity and local anatomical variations. It is attached to decoder features during training only.

Result: Experiments on multi-organ segmentation benchmarks show VCDP improves most evaluated baselines, especially for small, ambiguous, and highly variable organs.

Conclusion: VCDP is an effective plug-and-play regularization module that enhances semi-supervised 3D medical segmentation by better organizing feature space for complex anatomical structures, with zero additional cost at inference.

Abstract: Semi-supervised 3D medical image segmentation reduces the need for dense voxel-level annotations by exploiting unlabeled volumes. Although existing methods such as consistency regularization, pseudo-labeling, and co-training improve prediction-level robustness, they often provide insufficient feature-space organization for anatomically complex structures, especially small organs and ambiguous boundary regions with large intra-class variations. To address this issue, we propose Variation-Conditioned Distributional Proxy Learning (VCDP), a plug-and-play training-only regularization module for semi-supervised 3D medical image segmentation. VCDP represents each class with a learnable Gaussian distribution for shared class semantics and multiple variation prototypes for fine-grained intra-class patterns. A unified variation-conditioned compatibility score is further formulated to fuse distributional similarity and soft variation aggregation, guiding voxel embeddings to align with both global organ identity and local anatomical variations. VCDP is attached to decoder features during training and removed during inference, introducing no additional inference cost. Experiments on multi-organ segmentation benchmarks show that VCDP improves most evaluated baselines, particularly for small, ambiguous, and highly variable organs. Our anonymous code is released at https://anonymous.4open.science/r/VCDP_code-41ED.

</details>


### [71] [Two-Stage Multi-Modal Fusion with Adaptive Alignment for Action Quality Assessment](https://arxiv.org/abs/2607.07438)
*Kanglei Zhou,Ruizhi Cai,Xinning Wang,Yijian Zheng,Liyuan Wang,Jianguo Li,Xiaohui Liang*

Main category: cs.CV

TL;DR: DualAlign is a two-stage multi-modal fusion framework for Action Quality Assessment that uses adaptive alignment to integrate RGB, optical flow, skeleton, and text modalities. It introduces the MM-JDM benchmark and achieves SOTA results on multiple datasets, with robustness under missing-modality and low-label conditions.


<details>
  <summary>Details</summary>
Motivation: Unimodal AQA methods struggle with subtle movement quality cues. Multi-modal approaches face cross-modal misalignment, unstable fusion, and scarce/expensive multi-modal annotations, limiting dataset diversity.

Method: DualAlign uses a two-stage framework: Stage 1 constructs a coherent visual representation by maximizing shared structural information across RGB, optical flow, and skeleton. Stage 2 incorporates textual semantics after visual stabilization to complement without distorting the visual manifold.

Result: Improves average correlation on MM-JDM by 21.16% over SOTA; gains of 3.53% on RG and 5.95% on Fis-V benchmarks; robust under missing-modality and label-scarce conditions.

Conclusion: The proposed two-stage adaptive alignment framework effectively addresses cross-modal misalignment and annotation scarcity in multi-modal AQA, as shown by strong performance and robustness across multiple datasets.

Abstract: Action Quality Assessment (AQA) aims to evaluate how well a person performs a movement, which is essential in applications such as sports scoring, skill assessment, and healthcare. However, unimodal approaches often struggle to capture subtle cues of movement quality in real-world settings. Although multi-modal inputs provide complementary information, existing methods still face two major challenges: heterogeneous modalities often lead to cross-modal misalignment and unstable fusion, and reliable multi-modal annotation is costly, resulting in limited dataset diversity. To address these challenges, we propose DualAlign, a two-stage multi-modal fusion framework with adaptive alignment. The framework first constructs a coherent visual representation by maximizing shared structural information across RGB video, optical flow, and skeleton modalities. Textual semantics are then incorporated after visual stabilization, allowing high-level descriptions to complement rather than distort the underlying visual manifold. To evaluate the framework under realistic multi-modal conditions, we introduce MM--JDM, a movement-quality assessment dataset integrating RGB videos, optical flow, skeleton sequences, and structured text. MM--JDM naturally exhibits modality noise, class imbalance, and label scarcity, making it a challenging benchmark for studying multi-modal fusion and alignment. Extensive experiments show that DualAlign improves average correlation on MM--JDM by 21.16% over the state-of-the-art methods and achieves gains of 3.53% and 5.95% on the RG and Fis-V benchmarks, respectively. DualAlign also remains robust under missing-modality and label-scarce conditions.

</details>


### [72] [A Theory of Contrastive Learning with Natural Images](https://arxiv.org/abs/2607.07470)
*Antonio Torralba,Yair Weiss*

Main category: cs.CV

TL;DR: This paper theoretically analyzes why contrastive learning works, showing that optimal representations for certain augmentations involve CNN first-layer sinusoids, global pooling, and partial whitening, with weights computed via a waterfilling algorithm on the dataset's power spectrum.


<details>
  <summary>Details</summary>
Motivation: To understand theoretically why contrastive learning with simple augmentations produces useful representations for downstream tasks, by analytically deriving optimal representations.

Method: Analytically computing optimal representations under contrastive loss for basic augmentations on image datasets with stationary statistics, deriving that optimal first-layer filters are sinusoids with weights from a waterfilling algorithm, and validating empirically with SGD-trained CNNs.

Result: Optimal representations consist of sinusoidal first-layer filters, pointwise nonlinearity, global average pooling, and a final linear layer performing partial whitening. Frequencies/weights are determined by the dataset's power spectrum via a waterfilling algorithm. Empirical SGD training matches these theoretical predictions.

Conclusion: Contrastive learning's effectiveness can be theoretically explained through optimal representation analysis, revealing that learned features resemble Fourier-based filtering with partial whitening, directly tied to the dataset's power spectrum.

Abstract: Why does contrastive learning with simple images and augmentations yield useful representations for downstream tasks? We address this question by analytically computing the optimal representation in terms of a contrastive loss for a range of basic augmentations and any image dataset with stationary statistics. We show that for certain augmentations the optimum can be attained by a CNN whose first layer filters are sinusoids, followed by a pointwise nonlinearity, global average pooling, and a final linear layer that performs partial whitening. We also show that the optimal weights in such CNNs for more complicated augmentations are still sinusoids. The frequencies of the sinusoids and their weights can be computed using a simple waterfilling algorithm given the dataset's expected power spectrum. Experiments with different image datasets and augmentations show that such CNNs trained with SGD empirically learn sinusoids in their first layer and to perform partial whitening

</details>


### [73] [Discovering Geometric Biases in 3D Face Reconstruction: A Curvature-Aware Spectral Framework for Fairness Evaluation](https://arxiv.org/abs/2607.07486)
*Veronika Shilova,Emmanuel Malherbe,Giovanni Palma,Panagiotis-Alexandros Bokaris,Laurent Risser,Jean-Michel Loubes*

Main category: cs.CV

TL;DR: The paper proposes a curvature-based framework using Laplace-Beltrami Operator to analyze biases in 3D Morphable Model reconstructions, revealing systematic demographic biases (age, gender, ethnicity) that standard Euclidean metrics fail to capture.


<details>
  <summary>Details</summary>
Motivation: Existing 3DMM-based face reconstruction models inherit morphological biases from training data (limited diversity), and standard Euclidean metrics fail to capture subtle geometric discrepancies, potentially leading to unfair performance across global populations.

Method: The authors leverage the Laplace-Beltrami Operator (LBO) to generate high-resolution curvature error maps, producing localized, geometrically meaningful visualizations of discrepancies between ground truth and reconstructed meshes, and derive a curvature-aware error metric validated via user study.

Result: The proposed metric shows significantly higher correlation with human perception than traditional methods. Experiments across multiple 3DMM bases and fitting algorithms reveal systematic age-related biases and preliminary evidence of gender and ethnicity biases.

Conclusion: Curvature-aware evaluation protocols are necessary to ensure demographic fairness and geometric precision in 3D face reconstruction, as standard Euclidean distance-based metrics are insufficient for detecting biases.

Abstract: 3D Morphable Models (3DMMs) remain the standard parametric shape priors for many state-of-the-art 3D face reconstruction algorithms. However, as these models are derived from a finite number of 3D face samples, they inherit the morphological biases of their training data, potentially limiting their generalizability across diverse global populations. In this paper, we propose a novel framework to analyze 3DMM reconstructions through the lens of surface curvature, with the objective to discover, quantify and visualize biases. While standard evaluation metrics often rely on Euclidean distances, our reconstruction error captures subtle surface nuances such as local topology or undulations. To do so, we leverage the Laplace-Beltrami Operator (LBO) to generate high-resolution curvature error maps, providing a localized and geometrically meaningful visualization of discrepancies between ground truth faces and reconstructed meshes. We derive from it an error metric that we validated through a user study, observing a significantly higher correlation to human perception compared to traditional methods. Furthermore, we conduct extensive experiments across several 3DMM bases and fitting algorithms, uncovering systematic age-related biases and providing preliminary evidence of biases associated with gender and ethnicity. Our findings highlight the necessity of adopting curvature-aware evaluation protocols to ensure demographic fairness and geometric precision in future 3D face reconstruction research.

</details>


### [74] [HIVE: Understanding Post-Hallucination Reasoning in Vision Language Models](https://arxiv.org/abs/2607.07507)
*Feng He,Zhenting Wang,Qifan Wang,Qiang Guan,Dongfang Liu,Ruixiang Tang,Qiankun Li*

Main category: cs.CV

TL;DR: This paper introduces HIVE, an evaluation framework for studying Post Hallucination Reasoning (PHR) in VLMs, revealing that hallucinated captions can surprisingly improve vision-language task accuracy while having limited effect on text-only tasks, suggesting hallucinated semantics enter and influence downstream reasoning in structured, modality-dependent ways.


<details>
  <summary>Details</summary>
Motivation: Prior work focuses on detecting or suppressing hallucinations at generation time, leaving the subsequent reasoning stage (Post Hallucination Reasoning) largely unexplored. The authors aim to understand how hallucinated semantics, once they enter the inference context, influence downstream predictions.

Method: The authors introduce HIVE (Hallucination Inference and Verification Engine), an evaluation infrastructure enabling controlled comparisons between faithful and hallucinated captions. They conduct experiments across 9 tasks and 9 models to systematically investigate PHR.

Result: Hallucinated captions often improve accuracy on vision-language tasks, while text-only tasks show limited or unstable effects. Hallucinated cues broaden semantic coverage, reshape reasoning dynamics, yet preserve stable inference patterns.

Conclusion: Hallucinated semantics can influence downstream reasoning once they enter the model's inference context. Understanding the post-hallucination stage is important for improving reliability and interpretability of multimodal reasoning systems.

Abstract: Hallucinations in vision language models (VLMs) are commonly treated as semantic errors, yet they often arise from partial or ambiguous visual evidence. Prior work mainly focuses on detecting or suppressing hallucinations at generation time, leaving the subsequent reasoning stage largely unexplored. In this work, we study Post Hallucination Reasoning (PHR), the stage in which hallucinated semantics enter the model's inference context and influence downstream predictions. To systematically investigate PHR, we introduce HIVE, Hallucination Inference and Verification Engine, an evaluation infrastructure that enables controlled comparisons between faithful and hallucinated captions. Across nine tasks and nine models, we observe structured modality dependent patterns: hallucinated captions often improve accuracy on vision language tasks, while text only tasks exhibit limited or unstable effects. Further analyses show that hallucinated cues broaden semantic coverage and reshape reasoning dynamics while preserving stable inference. These findings highlight that hallucinated semantics may influence downstream reasoning once they enter the model's inference context. Understanding this post hallucination stage is important for improving the reliability and interpretability of multimodal reasoning systems. Code is publicly available at https://github.com/hefengcs/HIVE.

</details>


### [75] [Learning to Unify Deformable Shape and Texture Representations for Cardiac Video Classification](https://arxiv.org/abs/2607.07518)
*Tonmoy Hossain,Miaomiao Zhang*

Main category: cs.CV

TL;DR: A novel cardiac video classification model uses bi-directional cross-attention to fuse deformable shape and image texture features in an integrated latent space, learning dynamic temporal weightings across cardiac phases for improved interpretability and state-of-the-art performance on CMR video data.


<details>
  <summary>Details</summary>
Motivation: Existing deep networks simply concatenate deformable shape and texture features, failing to exploit their complementary nature and learn cross-modal dependencies. They also apply uniform attention across all cardiac phases, ignoring varying diagnostic importance over time.

Method: Proposes a model with bi-directional cross-attention in latent space to fuse deformable shape and image features, allowing each modality to adaptively weight the other based on spatio-temporal correspondence. Dynamic adjustment of shape and texture contributions over cardiac phases is learned.

Result: Achieves state-of-the-art classification performance on a cine cardiac magnetic resonance (CMR) video dataset.

Conclusion: The approach demonstrates improved interpretability through attention mechanisms that identify diagnostically critical cardiac phases and modality contributions, outperforming existing methods that use simple concatenation and uniform temporal weighting.

Abstract: Deformable shape representations have proven to be robust complements to texture features in cardiac image classification, offering geometric priors that are invariant to imaging artifacts and intensity variations. However, existing deep networks perform simple concatenation to combine these distinct feature representations, which neither fully exploits their complementary nature nor learns cross-modal feature dependencies. Furthermore, this results in uniform attention across all timepoints; hence ignoring the varying diagnostic importance across the cardiac phases. In this paper, we propose a novel cardiac video classification model that, for the first time, learns temporal features in an integrated space of deformable shape and image texture representations. In particular, we design a bi-directional cross-attention in the latent space to fuse latent deformable shape and image features, allowing each modality to adaptively weight the other based on spatio-temporal correspondence. In contrast to current methods that apply uniform weighting across all the cardiac phases, our approach learns to dynamically adjust the contributions of shape and texture representations, derived from images, over time. We demonstrate state-of-the-art classification performance on a cine cardiac magnetic resonance (CMR) video dataset, achieving improved interpretability from attention mechanisms that identify diagnostically critical cardiac phases and modality contributions.

</details>


### [76] [Infinite Worlds with Versatile Interactions](https://arxiv.org/abs/2607.07534)
*Zelin Gao,Qiuyu Wang,Jiapeng Zhu,Jingye Chen,Zichen Liu,Qingyan Bai,Jiahao Wang,Yufeng Yuan,Hanlin Wang,Yichong Lu,Ka Leong Cheng,Haojie Zhang,Jian Gao,Tianrui Feng,Yuzheng Liu,Yao Yao,Yinghao Xu,Xing Zhu,Yujun Shen,Hao Ouyang*

Main category: cs.CV

TL;DR: LingBot-World 2.0 is an upgraded world simulator featuring unbounded interaction, real-time 720p/60fps rendering, diverse interactive elements (attacking, archery, spell-casting, shooting), a dual-agent framework (pilot + director), multi-player support, and a 14B model paired with a lightweight 1.3B model for single-GPU deployment.


<details>
  <summary>Details</summary>
Motivation: To advance world modeling beyond previous limitations by achieving infinite interaction horizons, real-time performance, richer interactivity, and multi-agent orchestration for dynamic scene generation.

Method: Four upgrades: (1) causal pretraining for unbounded interaction, (2) distillation of a real-time variant for 60fps 720p video, (3) expanded action/event diversity, (4) dual-agent harness (pilot for behavior planning, director for novel environment synthesis) plus multi-player interface.

Result: A 14B primary model capable of unbounded, real-time, highly interactive world simulation, paired with a distilled 1.3B model deployable on a single GPU, supporting multi-player experiences.

Conclusion: LingBot-World 2.0 demonstrates significant progress in scalable, interactive, and deployable world modeling with a novel agentic harness design.

Abstract: We present LingBot-World 2.0 (also known as LingBot-World-Infinity), an advanced iteration of LingBot-World featuring four distinct upgrades. (1) Our model achieves an unbounded interaction horizon while maintaining consistent output quality, benefiting from a carefully crafted causal pretraining paradigm. (2) Through distilling a real-time variant from the base model, our system guarantees rapid response time, sufficient to drive 720p video streams at 60 fps. (3) Compared to the previous version, this update introduces highly diverse interactive elements, comprising a broader spectrum of actions (e.g., attacking, archery, spell-casting, and shooting) alongside a richer variety of text-driven events. (4) We pioneer the integration of an agentic harness within the domain of world modeling, wherein a pilot agent is tasked with planning and executing character behaviors, while a director agent is responsible for synthesizing novel environmental elements as the scene progresses. Additionally, to facilitate a shared experience, we develop an interface that permits multiple players to simultaneously immerse themselves in this vivid world simulator. We pair our primary 14B model with a lightweight 1.3B counterpart, which supports effortless deployment on a single GPU.

</details>


### [77] [Face-trace: Open-Set Attribution and Progressive Discovery of Synthetic Face Generators](https://arxiv.org/abs/2607.07545)
*Alessia Infantino,Claudio Schiavella,Irene Amerini*

Main category: cs.CV

TL;DR: The paper proposes an open-set synthetic face source attribution pipeline that classifies known generators, rejects unknown ones via energy-based OOD detection, and discovers unknown generator groups through clustering of I-JEPA embeddings combined with forensic descriptors, achieving strong results on the WILD dataset in both static and incremental settings.


<details>
  <summary>Details</summary>
Motivation: Existing synthetic face attribution methods operate in closed-set settings, assuming all possible generators are known during training. In reality, new generative models continuously emerge, and rejected (unknown) samples should be organized rather than discarded.

Method: A three-stage pipeline: (1) Known generator classification using frozen I-JEPA embeddings, (2) Energy-based OOD rejection to detect unknown generators, (3) Unknown generator discovery by combining projected I-JEPA features with Forensic Self-Descriptors and clustering. The method also extends to incremental scenarios where rejected samples arrive over time.

Result: 96.73% closed-set accuracy; 71.25% balanced accuracy for OOD rejection; clustering achieves ARI=0.81, NMI=0.90, purity=87.74%; incremental setting reaches 99.23% final purity. Cross-dataset experiments show potential generalization but post-processing remains challenging.

Conclusion: The open-set pipeline effectively handles known and unknown synthetic face generators, enables meaningful discovery of unknown generator groups, and supports incremental learning. However, cross-dataset generalization still faces post-processing difficulties.

Abstract: Recent advances in generative Artificial Intelligence have made synthetic face images increasingly realistic, creating new challenges for multimedia forensics. Source attribution methods should not only identify the generator of an image when the source is known, but also handle samples produced by previously unseen models. However, most existing approaches address synthetic face attribution in a closed-set setting, where all possible generators are available during training. This assumption does not hold in real-world scenarios, where new generators continuously appear and rejected samples should be organized rather than simply discarded. In this work we propose a pipeline for open-set synthetic face source attribution that combines known generator classification, energy-based OOD rejection, and unknown generator discovery. A classifier is trained on known generators using frozen I-JEPA embeddings, while rejected samples are represented by combining projected I-JEPA features with Forensic Self-Descriptors and then clustered to discover groups of unknown generators. We also extend the discovery stage to an incremental scenario, where rejected samples arrive over time. Experiments on the WILD dataset show that the proposed method achieves 96.73% closed-set attribution accuracy. In the open-set setting, energy-based rejection reaches 71.25% balanced accuracy, while rejected samples are clustered into meaningful unknown-generator groups, obtaining an ARI of 0.81, an NMI of 0.90, and an overall clustering purity of 87.74%. In the incremental setting, the discovered generator space is progressively extended while maintaining a final purity of 99.23%. Cross-dataset experiments suggest that the pipeline can operate beyond the original dataset distribution, although post-processing remains challenging.

</details>


### [78] [AA-ViT: Anatomically Aware Vision Transformer with Structural and Frequency Guidance for Contrast Enhanced Brain MRI Synthesis](https://arxiv.org/abs/2607.07553)
*Talha Meraj,Tom Flannery,Charlie Cummins,Matt Townend,Thomas C Booth,Peter Crossley,Michael McCann,Ian Overton,Saritha Unnikrishnan*

Main category: cs.CV

TL;DR: The paper proposes AA-ViT, an anatomically aware frequency-and-structure-guided vision transformer for synthesizing contrast-enhanced MRI from pre-contrast scans, achieving state-of-the-art performance on BraTS 2021 and obtaining clinical validation (mean score 3.94/5) from neuroradiologists and a neurosurgeon.


<details>
  <summary>Details</summary>
Motivation: Standard MRI has limited contrast and artifacts, contrast agents are contraindicated in some patients (e.g., renal impairment), and existing generative AI methods for CEMRI synthesis fail to preserve anatomical boundaries and tumour-specific fine structures due to implicit feature learning.

Method: Proposes AA-ViT, an anatomically aware frequency-and-structure-guided vision transformer that leverages frequency-domain and structural guidance to better preserve anatomical boundaries and lesion details when synthesizing CEMRI from pre-contrast T1, T2, and FLAIR modalities.

Result: On BraTS 2021, AA-ViT achieves higher PSNR and SSIM than state-of-the-art methods. Clinical evaluation by three neuroradiologists and one neurosurgeon on 19 glioma cases yields a mean score of 3.94/5, demonstrating rare preliminary clinical validation.

Conclusion: The synthetic post-contrast scans can lower scanning costs, shorten imaging time, and avoid risks of gadolinium-based contrast agents, offering a clinically viable non-invasive alternative for CEMRI.

Abstract: Accurate tumour localization and diagnosis is a critical component of clinical care for brain cancers. Magnetic Resonance Imaging (MRI) is the most commonly used imaging modality due to its superior soft-tissue contrast. However, standard MRI often exhibits limited contrast and imaging artifacts, which necessitates the use of contrast agents to enhance lesion visibility. The administration of chemical contrast agents is not always feasible and may be contraindicated in patients with renal impairment or other health conditions. As a result, developing accurate and non-invasive contrast enhanced MRI (CEMRI) synthesis methods has clinical importance. In recent years, numerous approaches for CEMRI synthesis have been proposed, predominantly relying on generative artificial intelligence models. While these methods demonstrate promising performance, their dependence on implicit feature learning often limits their ability to preserve anatomical boundaries and tumour-specific fine structures. To address these challenges, we propose an anatomically aware frequency-and-structure-guided vision transformer (AA-ViT), for CEMRI synthesis using pre-contrast MRI modalities (T1, T2, and FLAIR). Experiments on the BraTS 2021 dataset demonstrate that the proposed method preserves anatomical and lesion boundaries, achieving higher PSNR and SSIM than state-of-the-art approaches. Clinical evaluation by three neuroradiologists and a neurosurgeon on 19 randomly selected cases across diverse gliomas yielded a mean score of 3.94/5, providing preliminary clinical validation rarely seen in prior studies. Synthetic post-contrast scans from our model could lower scanning costs, shorten imaging time, and avoid the potential risks of using gadolinium-based contrast agents.

</details>


### [79] [Automatic Echocardiography Segmentation via Transition Probability Correlation for Stable Semantic Extraction](https://arxiv.org/abs/2607.07580)
*Xinran Chen,Xiyuan Wang,Guangquan Zhou,Chuan Chen*

Main category: cs.CV

TL;DR: A convolution-based network with a STLSF module (semantic correction + texture enhancement) and frequency-aware denoising pre-training achieves state-of-the-art echocardiography segmentation (93.87% Dice on CAMUS, 92.62% on EchoNet-Dynamic).


<details>
  <summary>Details</summary>
Motivation: Echocardiography suffers from speckle noise and low SNR, causing ambiguous semantic features and fragmented boundaries that hinder deep learning segmentation accuracy in complex clinical cases.

Method: (1) STLSF module with window-matching-based semantic correction and semantics-guided texture enhancement; (2) Frequency-aware denoising pre-training for encoder adaptation to ultrasound priors; (3) Convolution-based network with locality inductive bias and long-range dependencies.

Result: 93.87% Dice (3.29mm HD95) on CAMUS; 92.62% Dice (2.73mm HD95) on EchoNet-Dynamic; SOTA performance.

Conclusion: The proposed approach effectively mitigates texture instability and ambiguous semantics in echocardiography segmentation, achieving superior results on two benchmarks.

Abstract: While echocardiography is essential for cardiovascular diagnosis, inherent speckle noise and low signal-to-noise ratio often lead to ambiguous semantic features and fragmented boundaries. These limitations significantly hinder the segmentation accuracy of deep learning models in complex clinical cases. Moreover, temporal motion of the heart plays a critical role in recognizing anatomical structures. To address these challenges, we designed a STLSF module which comprises a window-matching-based semantic correction component and a semantics-guided texture enhancement component. By leveraging local transition probability correlations to correct semantics and employing semantics-guided texture enhancement, the STLSF module effectively mitigates texture instability and ambiguous semantic interpretations caused by disadvantaged echocardiography quality. Additionally, to facilitate the encoder's adaptation to the intrinsic priors of ultrasound-specific imaging patterns, we propose a frequency-aware denoising pre-training method. The entire work builds a convolution-based network with locality inductive bias and long-range dependencies. Extensive experiments confirm our SOTA performance, achieving 93.87\% Dice on CAMUS and 92.62\% on EchoNet-Dynamic, with respective HD95 values of 3.29mm and 2.73mm.

</details>


### [80] [Cardiac MRI Through-Plane Super-Resolution Guided by Reference and Memory](https://arxiv.org/abs/2607.07581)
*Shaoming Pan,Chenchuhui Hu,Leon Axel,Meng Ye*

Main category: cs.CV

TL;DR: STRMSR is a reference- and memory-guided through-plane super-resolution framework for cardiac MRI that uses HR reference views and intermediate SR results to reconstruct high-resolution 3D volumes, achieving consistent improvements at 4x and 8x upsampling on the WHS dataset.


<details>
  <summary>Details</summary>
Motivation: Clinical cardiac MRI suffers from coarse through-plane resolution due to scan time and motion constraints, limiting 3D analysis and diagnostic accuracy.

Method: STRMSR leverages HR reference views and intermediate SR results as memory, uses coarse-to-fine contextual matching for robust correspondence, and employs a learnable patch-wise dynamic feature aggregation module for content-adaptive fusion.

Result: Consistent improvements over baselines at 4x and 8x upsampling on the WHS cardiac MRI dataset under two reference protocols (orthogonal-plane and long-axis chamber views).

Conclusion: The proposed method effectively enhances through-plane resolution for cardiac MRI, improving 3D analysis capability while maintaining slice-to-slice consistency.

Abstract: Clinical cardiac MRI is commonly acquired with high in-plane resolution but coarse through-plane resolution to reduce scan time and accommodate breath-hold and cardiac-motion constraints, which limits 3D analysis and diagnostic accuracy. We propose STRMSR, a reference- and memory-guided through-plane super-resolution (SR) framework that reconstructs high-resolution (HR) cardiac volumes by leveraging HR reference views acquired from the same subject and intermediate SR results as the memory. Our method uses coarse-to-fine contextual matching to establish robust correspondence between low-resolution target and reference/memory images under spatial misalignment. A learnable patch-wise dynamic feature aggregation module predicts content-adaptive mixture weights for each local patch, effectively fusing dynamic information while suppressing unreliable feature transfers. The intermediate SR results stored in the memory bank ensure slice-to-slice consistency for the super-resolved 3D volume. Experiments on the WHS cardiac MRI dataset under two reference protocols, orthogonal-plane views and long-axis chamber views, demonstrate consistent improvements over baselines at 4x and 8x upsampling factors.

</details>


### [81] [MedPMC: A Systematic Framework for Scaling High-Fidelity Medical Multimodal Data for Foundation Models](https://arxiv.org/abs/2607.07673)
*Hyunjae Kim,Dain Kim,Pan Xiao,Serina S. Applebaum,Younjoon Chung,Xuguang Ai,Yu Yin,Roy Jiang,Yuexi Du,Yawen Wei,Yiming Kong,Tuo Guo,Zhiyuan Cao,Mengmeng Du,Yuelei Fu,Yan Hu,Rui Shi,Gui Yang,Kevin W. Jin,Yuntian Liu,Yuxuan Tian,Jonathan Marquez,Zhen Chen,Sheng Zhang,Hoifung Poon,Hua Xu,Jaewoo Kang,Qingyu Chen*

Main category: cs.CV

TL;DR: MedPMC is an automated framework that extracts 11 million high-fidelity medical image-text pairs from 6.1 million PMC articles, significantly outperforming prior datasets and improving multimodal medical AI models across benchmarks and clinical settings.


<details>
  <summary>Details</summary>
Motivation: Medicine is inherently multimodal but limited by scarce, low-quality clinical data. Existing PMC-derived datasets suffer from poor fidelity, reproducibility, and clinical relevance, motivating the need for a higher-quality, continuously updatable infrastructure.

Method: MedPMC is an automated pipeline that processes 6.1M PMC articles through: (1) initial screening (F1=93.2), (2) multi-panel figure detection (F1=96.5), (3) figure separation (mAP=89.8), (4) caption separation & alignment (F1=81.4; ROUGE-L=85.3), and (5) medical figure classification (F1=96.5), yielding 11M image-text pairs.

Result: Manual review showed 95.3% medical relevance vs. 19.7% in prior datasets. MedPMC-trained CLIP improved zero-shot AUC by 7.1pp over baselines, boosted medical VQA by 1.9-16.9pp, and improved dermatology image retrieval Recall@5 by 11.7pp across 26 benchmarks.

Conclusion: High-fidelity literature curation significantly strengthens medical multimodal foundation models in both benchmark and clinical settings. The framework, corpus, benchmarks, and models are publicly released.

Abstract: Medicine is inherently multimodal, requiring clinicians to synthesize information across diverse data streams. Yet the development of multimodal foundation models is constrained by limited access to large-scale, high-quality clinical data. Although PubMed Central (PMC) offers a complementary source of expert-authored image-text data, existing PMC-derived resources remain limited in fidelity, reproducibility, and clinical validation. We introduce MedPMC, an automated, continuously updatable framework that transforms permissively licensed literature into high-fidelity infrastructure for medical multimodal models. Applied to 6.1 million PMC articles, MedPMC curated 11 million medical image-text pairs. Component evaluations showed strong performance for initial screening (F1 = 93.2), multi-panel figure detection (F1 = 96.5), figure separation (mAP = 89.8), caption separation and alignment (F1 = 81.4; ROUGE-L = 85.3), and medical figure classification (F1 = 96.5). Manual review by five annotators, three with medical training, found 95.3% of MedPMC images medically relevant, versus 19.7% in a prior PMC-derived dataset. Across 26 benchmarks spanning 11 specialties, a MedPMC-trained CLIP-style model improved average zero-shot AUC by 7.1 percentage points over the strongest architecture-matched biomedical CLIP baseline despite using fewer than half as many image-text pairs. As the vision encoder in a multimodal large language model, it improved medical visual question-answering by 1.9 and 16.9 percentage points across two benchmarks. In 10,524 Yale New Haven Health System dermatology photographs, it improved morphology-to-image retrieval Recall@5 by 11.7 percentage points. These findings show that high-fidelity literature curation strengthens medical multimodal foundation models across benchmark and clinical settings. We publicly release the framework, corpus, benchmarks, and pretrained models.

</details>


### [82] [Scaling Mixture-of-Experts Video Pretraining for Embodied Intelligence](https://arxiv.org/abs/2607.07675)
*Shuailei Ma,Jiaqi Liao,Xinyang Wang,Jingjing Wang,Chaoran Feng,Zijing Hu,Chong Bao,Zichen Xi,Yuqi Gan,Weisen Wang,Yanhong Zeng,Qin Zhao,Zifan Shi,Wei Wu,Hao Ouyang,Qiuyu Wang,Shangzhan Zhang,Jiahao Shao,Yipengjing Sun,Liangxiao Hu,Lunke Pan,Nan Xue,Kecheng Zheng,Yinghao Xu,Xing Zhu,Yujun Shen,Ka Leong Cheng*

Main category: cs.CV

TL;DR: LingBot-Video is a Mixture-of-Experts (MoE) video generative model designed for embodied intelligence, with a data profiling engine for robot-oriented footage and a multi-dimensional reward system enforcing physical rationality and task completion.


<details>
  <summary>Details</summary>
Motivation: Existing video generative models suffer from domain mismatch for robot control because they prioritize visual fidelity and creativity over computational efficiency and physical realism.

Method: (1) Adopts MoE (instead of dense) architecture for better capacity-efficiency trade-off; (2) Constructs a data profiling engine augmenting internet videos with robot-oriented footage (manipulation, navigation, egocentric); (3) Develops a multi-dimensional reward system enforcing physical rationality and task completion beyond standard criteria.

Result: Comprehensive evaluations validate its performance and efficiency as a video foundation model for embodied intelligence.

Conclusion: Contributes LingBot-Video as the first large-scale, open-source MoE video foundation model to bridge digital creativity and physical actuation.

Abstract: Despite the recent promise in robot control, video generative models suffer from a domain mismatch due to their primary focus on content creation. For example, their design inherently prioritizes visual fidelity and creativity over computational efficiency and physical realism. In this work, we present LingBot-Video, a DiT-based video pretraining paradigm specifically tailored for embodied intelligence. From the architecture perspective, we adopt the Mixture-of-Experts (MoE), instead of dense, framework to achieve a better trade-off between modeling capacity and inference efficiency, and manage to scale it up from scratch. From the data perspective, we construct a data profiling engine that augments standard internet videos with extensive robot-oriented footage, encompassing manipulation, navigation, and egocentric perspectives, to equip the base model with an intrinsic understanding of actions and world dynamics. From the training perspective, we develop a multi-dimensional reward system to enforce the alignment regarding physical rationality and task completion, going beyond standard criteria such as aesthetics, prompt-following, and motion consistency. Comprehensive evaluations validate its performance and efficiency as a video foundation model. We contribute LingBot-Video as the inaugural large-scale, open-source MoE video foundation model to the community, in a pioneering effort to bridge digital creativity and physical actuation.

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [83] [AgentLens: Production-Assessed Trajectory Reviews for Coding Agent Evaluation](https://arxiv.org/abs/2607.06624)
*Andrey Podivilov,Vadim Lomshakov,Sergey Savin,Matvei Startsev,Roman Pozharskiy,Maksim Parshin,Sergey Nikolenko*

Main category: cs.AI

TL;DR: AgentLens is a benchmark for interactive code agents that evaluates entire trajectories (not just pass/fail), using formal verification plus LLM-based reviews to provide explainable scores, useful for diagnosing model behavior and catching regressions.


<details>
  <summary>Details</summary>
Motivation: Existing code-agent benchmarks reduce performance to a single pass/fail metric, ignoring the full trajectory of agent behavior (instruction following, tool use, error recovery, user interaction) that actually matters to users.

Method: Pairs formal verification (objective checks) with LLM-written trajectory reviews and side-by-side comparisons, producing readable explanations for each score.

Result: Enables diagnosis of model behavior, comparison of agent versions, and catching product regressions in nightly evaluation pipelines.

Conclusion: AgentLens is released as open source and serves as a more informative benchmark than binary pass/fail evaluations for interactive code agents.

Abstract: We present AgentLens, a production-assessed benchmark for interactive code agents. Most code-agent benchmarks reduce a run to a single bit -- did the task pass? -- but the people who actually use these agents experience the entire trajectory: how the agent follows instructions, uses its tools, verifies its own work, recovers from mistakes, and talks to them along the way. AgentLens evaluates that whole trajectory. It pairs formal verification, where an objective check exists, with LLM-written trajectory reviews and side-by-side comparisons, so that each run yields a readable explanation of why the score is what it is. This makes AgentLens useful for more than ranking models: we use it to diagnose model behavior, compare successive versions of our own agent, and catch product regressions in a nightly evaluation pipeline. We release the benchmark as open source at https://github.com/agent-lens/agent-lens-bench.

</details>


### [84] [When Does In-Context Search Help? A Sampling-Complexity Theory of Reflection-Driven Reasoning](https://arxiv.org/abs/2607.06720)
*Yotam Wolf,Noam Wies,Amnon Shashua*

Main category: cs.AI

TL;DR: This paper theoretically analyzes in-context search in LLMs, showing that when self-reflection reliably identifies early mistakes, it can achieve exponential improvements over base models using polynomial sequential attempts; these gains are robust to approximate posterior updates and learnable via cross-entropy training.


<details>
  <summary>Details</summary>
Motivation: To provide a rigorous theoretical understanding of why and when in-context search (iterative generation, critique, and revision) works for LLM reasoning, and to characterize the sampling complexity and conditions for exponential improvements.

Method: The authors model in-context search as approximate Bayesian inference over reasoning traces, where the base model defines a prior and self-reflection provides posterior updates. They analyze inference-time sampling complexity, study conditions for exponential improvement, prove robustness to approximate posteriors, show learnability via cross-entropy training, and connect to reinforcement learning with verifiable rewards.

Result: When reflection can reliably localize early mistakes, in-context search yields exponential improvements (polynomial attempts solve problems with exponentially small zero-shot pass rates). Without this property, conditioning on past attempts offers no asymptotic benefit over parallel sampling. These gains are robust and learnable, and the optimal RL policy implements the same posterior reweighting rule.

Conclusion: In-context search can be fundamentally more efficient than parallel sampling under the right conditions, and these improvements are theoretically grounded and practically achievable.

Abstract: Training large language models (LLMs) with extended reasoning has enabled in-context search, in which models iteratively generate, critique, and revise solution attempts. We provide a theoretical analysis of in-context search by modeling it as approximate inference over reasoning traces, where the base model defines a prior and self-reflection provides feedback for posterior updates, and study the resulting inference-time sampling complexity - the number of sequential attempts needed to achieve high success probability. We show that when reflections reliably localize early mistakes, in-context search can yield exponential improvements over the base model, solving problems with exponentially small zero-shot pass rates using only a polynomial number of sequential attempts, whereas when this property fails, conditioning on past attempts offers no asymptotic benefit over parallel sampling. We further show that these gains are robust and learnable: approximate posterior updates suffice, and cross-entropy training on search rollouts recovers the required behavior with polynomial sample complexity. Finally, we show that under a stagewise abstraction of reinforcement learning with verifiable rewards, the optimal policy extension implements the same posterior reweighting rule. We validate key qualitative predictions of the theory on real large reasoning models.

</details>


### [85] [LLM-powered reasoning in agent-based modeling](https://arxiv.org/abs/2607.06757)
*Sifat Afroj Moon,Dakotah Maguire,Adam Spannaus,Joe Tuccillo,Maksudul Alam,Sudip K. Seal,John Gounley,Heidi Hanson*

Main category: cs.AI

TL;DR: HALE integrates LLMs with agent-based modeling to enable adaptive, real-time human decision-making predictions in epidemic simulations, demonstrated with COVID-19 in Salt Lake County.


<details>
  <summary>Details</summary>
Motivation: Traditional ABMs rely on static priors and cannot adapt to real-time behavioral changes, limiting their usefulness for policy making.

Method: Introduces HALE (Hybrid Agent-based and Language-driven Epidemic), a scalable framework that uses LLMs to predict human decision-making within ABM simulations.

Result: Proof-of-concept simulation of COVID-19 dynamics in Salt Lake County, UT shows the framework's feasibility.

Conclusion: LLM-driven ABM can bridge the information gap in traditional modeling, enabling more adaptive and realistic policy simulations.

Abstract: Agent-based modeling (ABM) has the capability to model millions of individuals and their interactions, which is useful for policy making. However, ABMs have traditionally relied on static prior, which prevents the models from adapting to real-time changes. Our research provides a novel approach to addressing this information gap. Large language models (LLMs) offer new opportunities to predict human decision-making. Here, we introduce a scalable Hybrid Agent-based and Language-driven Epidemic (HALE) modeling framework that leverages LLMs to predict human decision-making in an ABM simulation. As a proof-of-concept, we use HALE to simulate COVID-19 and its effects in Salt Lake County, UT.

</details>


### [86] [QANTIS: Hardware-Calibrated Sequential POMDP Belief Updates on IBM Heron](https://arxiv.org/abs/2607.06760)
*Bayram Yuksel Eker,Suayb S. Arslan,Ozgur Nazli,Mustafa Serhat Demirgil,Furkan Deligoz*

Main category: cs.AI

TL;DR: QANTIS uses quantum processors as a belief-update service for autonomous systems under partial observability. This paper tests whether the quantum posterior can be reused across sequential Tiger POMDP steps on IBM Heron hardware without corrupting planner decisions. Using all-step FPAA, the hardware posterior matches exact Bayes posterior and selects the same action across 8, 12, 20, and 32-step horizons.


<details>
  <summary>Details</summary>
Motivation: To determine if a quantum belief-update service (QANTIS) can consistently return accurate posteriors across sequential decision steps in a partially observable environment, without causing the classical planner to select suboptimal actions.

Method: Controlled hardware case study on IBM Heron comparing no amplification, guarded Grover amplification, and all-step fixed-point amplification (FPAA) on sequential Tiger POMDP trajectories. Boundary-aware BIQAE stabilizes amplitude estimation near zero and one.

Result: All-step FPAA preserves the Tiger posterior across 8-step and 12-step primary runs; 20-step and 32-step controls remain in the same operating band. Hardware posterior and exact Bayes posterior select the same immediate action in every reported decision check.

Conclusion: Establishes an operating envelope for a hardware-calibrated belief-update primitive, but does not claim standalone hardware advantage.

Abstract: Autonomous systems under partial observability act on beliefs, not raw sensor events. QANTIS treats the quantum processor as a calibrated belief-update service in that loop: it receives a prior and an observation model, estimates the rare-event evidence term, and returns an ordinary posterior to a classical planner. This paper asks whether that service can be reused across a sequential Tiger POMDP horizon on present IBM Heron hardware without corrupting the planner-facing posterior. We answer with a controlled hardware case study rather than an end-to-end autonomy or wall-clock speedup claim. The study compares no amplification, guarded Grover amplification, and all-step fixed-point amplification on the same trajectory, then checks whether the returned posterior would change the downstream action. All-step FPAA preserves the Tiger posterior across the reported 8-step and 12-step primary runs, and the 20-step and 32-step controls remain inside the same operating band. In every reported decision check, the hardware posterior and the exact Bayes posterior select the same immediate action. Boundary-aware BIQAE stabilizes amplitude estimation near zero and near one, while a rare-event sweep maps the logical sample-complexity envelope for one-in-a-million evidence. The result is an operating envelope for a hardware-calibrated belief-update primitive, not a standalone hardware-advantage claim.

</details>


### [87] [Cost-Effective Agent Harnesses for Abstract Reasoning and Generalization on ARC-AGI-1](https://arxiv.org/abs/2607.06764)
*Kabir Moghe,Peter Chin*

Main category: cs.AI

TL;DR: Using an open-weight model (DeepSeek V3.2) with no ARC-specific fine-tuning and minimal compute, the authors build agentic pipelines (Explorer-Definer, Reflective Orchestrator) that decompose pattern discovery and program synthesis, achieving 57.50% and 67.25% pass@2 on ARC-AGI-1 at low cost, lifting a 15.50% baseline by ~52 points.


<details>
  <summary>Details</summary>
Motivation: To explore a third regime beyond heavy test-time compute or benchmark-specific training: whether open-weight, general-purpose models can solve ARC tasks through architecture alone, without ARC-specific fine-tuning or massive compute.

Method: Two agentic architectures: (1) Explorer-Definer Pipeline, a two-stage pipeline separating pattern discovery from transformation synthesis; (2) Reflective Orchestrator, which adds autonomous re-exploration when hypotheses fail. Both use DeepSeek V3.2 in non-thinking mode under strict budget.

Result: Explorer-Definer Pipeline: 57.50% pass@2 at $0.25/task; Reflective Orchestrator: 67.25% pass@2 at $0.62/task; 52-point lift over 15.50% baseline; pass@k analysis shows the pipeline is generation-bound (not selection-bound), with the think tool contributing 5.75 pp.

Conclusion: Significant ARC-AGI-1 gains are achievable via architecture alone (no ARC fine-tuning, no heavy compute). The system is generation-bound, meaning future gains require broader candidate generation rather than better ranking. The Reflective Orchestrator confirms this prediction through adaptive re-exploration.

Abstract: Recent progress on ARC-AGI-1 from disclosed architectures has come broadly from two regimes: heavy test-time compute over frontier models (evolutionary search, exhaustive sampling, extended chain-of-thought), or benchmark-specific training in which small models are fine-tuned on ARC data, often with task-specialized architectures. We study a third regime: an open-weight model in non-thinking mode (DeepSeek V3.2) under a strict budget, with no ARC-specific fine-tuning. We study what is recoverable through architecture alone, building agentic harnesses that decompose pattern-discovery and program-synthesis stages explicitly. First, we introduce an Explorer-Definer Pipeline that separates pattern discovery from executable transformation synthesis, implemented as a two-stage agent pipeline. Next, we present the Reflective Orchestrator, which augments the pipeline with autonomous exploration of new transformations when previous hypotheses fail on training pairs. On the ARC-AGI-1 public 400-task evaluation set, the pipeline reaches 57.50% pass@2 at \$0.25 per task, and the orchestrator reaches 67.25% pass@2 at \$0.62 per task. Together these architectures lift a 15.50% one-shot baseline by ~52 points without benchmark-specific training or heavy test-time compute. Furthermore, the orchestrator-driven lift tests a falsifiable diagnostic the pipeline produces; unbiased pass@k analysis suggests the pipeline is generation-bound, not selection-bound (selection via training-pair accuracy captures ~95% of the candidate ceiling) and predicts that significant improvement requires broader generation, not better ranking. The orchestrator implements this prediction via adaptive re-exploration and confirms it (unbiased pass@1 lift +9.81 pp, matching selection-mediated pass@2 lift). An additional pipeline ablation identifies its think tool as a significant component, with removal reducing pass@2 by 5.75 pp.

</details>


### [88] [Evaluating SageMath-Augmented LLM Agents for Computational and Experimental Mathematics](https://arxiv.org/abs/2607.06820)
*Pavel Snopov,German Magai*

Main category: cs.AI

TL;DR: This paper proposes a ReAct-style agentic setup combining LLM reasoning with SageMath (CAS) feedback and Context7 documentation, evaluating it on research-level math problems. Results show significant accuracy gains (avg +9.7pp, up to +27.8pp) across models, narrowing the gap between open-weight and closed models, with GPT-5.5 achieving 75.2% solve rate.


<details>
  <summary>Details</summary>
Motivation: The role of Computer Algebra Systems in agentic LLM workflows is underexplored. This work aims to fill that gap by combining LLM reasoning with verifiable CAS feedback to assist mathematicians in computational exploration and automated conjecture discovery.

Method: A ReAct-style agentic setup is proposed that integrates LLM reasoning with SageMath (CAS) for verifiable feedback and Context7 for up-to-date documentation. The benchmark (RealMath) is refined with multi-step post-processing and multi-stage validation pipelines to improve dataset quality.

Result: SageMath access yields substantial performance gains across all models (+9.7pp on average, ranging 1.5pp–27.8pp). Qwen 3.7-Max benefits most from SageMath. GPT-5.5 achieves the highest solve rate (75.2%) with lowest token usage among tool-enabled setups.

Conclusion: CAS-augmented agents represent a promising direction for assisting mathematicians in computational exploration and represent a step toward automated conjecture discovery.

Abstract: Recent advances in AI for Mathematics have focused largely on autoformalization and theorem proving, leaving the role of Computer Algebra Systems (CAS) in agentic LLM workflows underexplored. We propose a ReAct-style agentic setup that combines LLM reasoning with verifiable feedback from SageMath, together with Context7 for the up-to-date documentation. We evaluate this agentic setup across frontier models for solving research-level mathematical problems from the RealMath benchmark in a setting that emulates a computational-mathematics research loop. We also propose a refinement to the RealMath benchmark by introducing a multi-step post-processing procedure and a multi-stage validation pipeline, both of which improve the quality and reliability of the extracted problem set. Our experiments reveal substantial performance gains from SageMath access across all evaluated models on +9.7~pp on average, the gains range from 1.5~pp to 27.8~pp and narrow the gap between open-weight and closed models. Qwen~3.7-Max benefits from SageMath the most, while GPT-5.5 achieves the highest solve rate of $75.2\%$ and the lowest token usage among tool-enabled configurations. Our findings suggest that CAS-augmented agents represent a promising direction for assisting mathematicians in computational exploration, and we believe that this work is a step towards automated conjecture discovery. The project repository is available online.

</details>


### [89] [The Harness Effect: How Orchestration Design Sets the Token Economics of Enterprise Agentic AI](https://arxiv.org/abs/2607.06906)
*Muayad Sayed Ali,Aliaksandra Novik,Anji Boddupally,Artem Yavorskyi,Chris Nickerson,Daniel Rica,Emily DuGranrut,Felix Leung,Garrett Prince,Grace Barnett,Heath Robinson,Hosain Al Ahmad,Jesse Resnick,Juan Carlos Farah,Jyothi Swaroop Meruga,Leonid Kuznetsov,Luke Gorham,Marie Schmoll,Michael Paciullo,Saumya Das,Sharath Sheripally,Tommy Griscom,Mykyta Osadchyi,Neha Mantri,Nick Westrum,Olivia Benowitz,Parikshith Kulkarni,Radik Chernyshov,Rakshith Vasudev,Rohith Nadimpally,Vikas Gangadevi,Waseem AlShikh*

Main category: cs.AI

TL;DR: Better orchestration (harness design) cuts token costs 38%, time 44%, and cost per task 41% across all models, while maintaining or improving quality — with gains correlated to model strength.


<details>
  <summary>Details</summary>
Motivation: Current AI development is on a "token maxing" trajectory where per-task token consumption grows faster than task value, masked by falling per-token prices. The authors argue that the orchestration layer (harness) is the decisive lever to counter this trend, and set out to isolate and quantify its impact.

Method: Controlled experiment: 22 locked evaluation tasks, 6 foundation models (Claude, Gemini, Qwen, GLM, Palmyra), swapping only the orchestration layer — conventional production loop vs. Writer Agent Harness — while holding all else constant.

Result: Harness cut blended cost per task 41% ($0.21→$0.12), wall-clock 44% (48s→27s), tokens per task 38% (14.2k→8.8k), with quality at parity (0.78→0.81). Cost per task shifted more by orchestration than by the full spread of model choices. Quality per dollar rose 82%; task-completions per million tokens rose from 54.9 to 92.0.

Conclusion: The orchestration harness is the most impactful efficiency multiplier — it works across all models (present and future), and its quality benefits scale with model capability ("harness leverage"). Token economics must be formalized at the orchestration layer, not just the model level.

Abstract: Agentic AI development today runs on token maxing: buying capability with tokens -- longer reasoning traces, more turns, wider tool payloads, bigger replayed contexts -- so tokens per task grow faster than task value. Falling per-token prices mask the pattern; total spend rises anyway. We argue the decisive lever against token maxing is the harness: the orchestration layer that assembles context, exposes tools, sequences turns, delegates work, and carries enterprise observability and governance. We isolate it with a controlled swap: 22 locked evaluation tasks, six foundation models (Claude Sonnet 4.6, Gemini 3.1, Gemini Flash 3.5, Qwen 3.6, GLM 5.1, Palmyra X6), changing only the orchestration layer -- a frozen conventional production loop versus the Writer Agent Harness. Holding models constant, the harness cuts blended cost per task 41% ($0.21->$0.12), median wall-clock 44% (48s->27s), and tokens per task 38% (14.2k->8.8k), with task-completion quality at parity (0.78->0.81, directional at this sample size). Efficiency is model-invariant -- every model gets cheaper (33-61%) -- while quality gains are capability-dependent: a model's gain correlates almost perfectly with its baseline strength (r=0.99, n=6), a phenomenon we term harness leverage. Quality per dollar rises 82%; task-completions per million tokens rise from 54.9 to 92.0. On this workload the orchestration layer moved cost per task more than the full spread of the model menu did. We formalize token economics at the orchestration layer (including effective input price under prompt caching), detail the six mechanism families behind the effect -- cache-shape discipline to failure-spend governance -- compare six widely used agent systems on the same axes, and argue the harness is the one component whose efficiency multiplies across every model an organization runs -- present and future.

</details>


### [90] [Grounding Spatial Relations in a Compact World Model: Instruction Leakage and a Goal-Free Dynamics Fix](https://arxiv.org/abs/2607.06925)
*Yufeng Wang,Lu Wei,Haibin Ling*

Main category: cs.AI

TL;DR: Goal-conditioned world models can achieve high relation-readout accuracy through "instruction transcription"—simply copying the answer from the goal text—rather than genuine perception of the scene. This confound is widespread and can be detected by withholding the goal or using counterfactual instructions. The fix is to exclude the goal from the dynamics model and supervise the read path separately.


<details>
  <summary>Details</summary>
Motivation: To investigate whether compact world models that condition on language goals actually ground spatial relations through genuine perception of the scene, or if they merely transcribe the answer from the instruction text.

Method: The authors test goal-conditioned world models across three settings (tabletop, BabyAI, Language-Table) by: (1) withholding the goal to collapse accuracy, (2) feeding counterfactual instructions, and (3) conducting within-task ablations. They then propose a fix that removes the goal from dynamics and supervises the read path separately.

Result: Models achieve 0.90 relation-readout accuracy but collapse to 0.27 when goal is withheld. Counterfactual instructions cause predicted anchors to follow the false instruction 94.5% of the time vs. 2.3% for the true scene. The confound (instruction leakage) occurs when the instruction names the answer. The proposed fix recovers genuine grounding (0.88 accuracy, identical with/without goal).

Conclusion: Instruction leakage is a serious confound in goal-conditioned world models that can masquerade as genuine perceptual grounding. The detection protocol (withholding goal, counterfactual instructions) and remedy (separating goal from dynamics) apply broadly to any such model where the instruction names the scored quantity.

Abstract: Compact world models that condition on a language goal promise to ground relations such as ``put the red block left of the blue block'' using a sparse set of explicit \emph{reference anchors}. We ask when such references actually ground a relation, and identify a trap: a goal-conditioned predictor reaches a striking $0.90$ relation-readout accuracy, yet this is \emph{instruction transcription}, not perception. Withholding the goal collapses it to chance ($0.90\!\to\!0.27$, three seeds) and a counterfactual instruction makes the predicted anchors follow the \emph{false} instruction $94.5\%$ of the time (true scene $2.3\%$; $N{=}256$). Tested across three settings and a within-task ablation, our central claim characterizes the confound: \textbf{instruction leakage occurs when the scored quantity is transcribable from the instruction (when the instruction names the answer) and is essentially independent of how predictive the non-instruction inputs are.} Our tabletop and the external BabyAI benchmark leak, whereas a Language-Table forward-dynamics world model whose instruction names \emph{referents} does not, until the instruction is augmented to name the direction; and degrading the action never increases leakage, the opposite of what predictor-competition predicts. The diagnosis prescribes the fix: keep the goal out of the dynamics (it belongs to the planner's cost) and supervise the \emph{read} path, recovering genuine, instruction-independent grounding ($0.88$, identical with and without the goal). The detection protocol and remedy apply to any goal-conditioned world model whose instruction names the scored quantity.

</details>


### [91] [Large Behavior Model: A Promptable Digital Twin of the Retail Customer](https://arxiv.org/abs/2607.06993)
*Wachiravit Modecrua,Krittin Pachtrachai,Touchapon Kraisingkorn*

Main category: cs.AI

TL;DR: The paper introduces the Large Behavioral Model (LBM), a framework that learns customer decision-making from large-scale retail transactions by combining a Person-Environment formulation, retrieval-augmented generation, and multi-stage training (continued pre-training, supervised fine-tuning, and reinforcement learning). LBM outperforms general-purpose LLMs on retail tasks and shows strong transfer across domains.


<details>
  <summary>Details</summary>
Motivation: Existing approaches either optimize predictive accuracy without explaining decisions or simulate users without grounding them in real behavioral data. The authors aim to bridge this gap by learning customer decision-making directly from real transaction data.

Method: LBM uses a unified Person-Environment formulation: customer state is a behavioral profile from historical purchases, product context is incorporated via retrieval-augmented generation. Training involves continued pre-training on verbalized behavioral data, supervised fine-tuning for decision generation, and reinforcement learning with verifiable rewards for evidence-based calibration.

Result: LBM consistently outperforms frontier general-purpose language models on in-domain retail tasks (purchase prediction, hard-negative discrimination, basket completion, promotion response, cross-domain voucher redemption) and demonstrates strong zero-shot and fine-tuned transfer across retailers and decision domains.

Conclusion: Behavioral knowledge encoded in transaction histories can be effectively learned by language models, providing a scalable foundation for customer digital twins and behavior simulation. Ablations show continued pre-training drives behavioral generalization, retrieval is most effective during both training and inference, and RL improves reliance on explicit behavioral evidence.

Abstract: Customer behavior modeling underpins recommendation, marketing, and decision support, yet existing approaches either optimize predictive accuracy without explaining decisions or simulate users without grounding them in real behavioral data. We present the Large Behavioral Model (LBM) that learns customer decision making directly from large-scale retail transactions through a unified Person-Environment formulation. Customer state is represented by a behavioral profile derived from historical purchases, while product context is incorporated through retrieval-augmented generation. The model is trained using continued pre-training on verbalized behavioral data, supervised fine-tuning for decision generation, and reinforcement learning with verifiable rewards for evidence-based calibration. We evaluate the proposed framework on purchase prediction, hard-negative discrimination, basket completion, promotion response, and cross-domain voucher redemption. The model consistently outperforms frontier general-purpose language models on in-domain retail tasks while demonstrating strong zero-shot and fine-tuned transfer across retailers and decision domains. Ablation studies show that continued pre-training is the primary driver of behavioral generalization, retrieval is most effective when applied during both training and inference, and reinforcement learning improves reliance on explicit behavioral evidence over generic language-model priors. These results demonstrate that behavioral knowledge encoded in transaction histories can be effectively learned by language models, providing a scalable foundation for customer digital twins and behavior simulation.

</details>


### [92] [Learning social norms enhances compatibility in dynamic human-AI coordination](https://arxiv.org/abs/2607.07021)
*Yi Yang,Siyuan Liu,Xin Gao,Huamu Sun,Chao Liu,Qing Zhou,Bingbing Nie*

Main category: cs.AI

TL;DR: This paper identifies three explicit social norms (outcome predictability, value alignment, advantage awareness) from human pedestrian-vehicle interactions and shows that integrating them into LLM agents dramatically improves human-AI coordination, achieving nearly 4x higher scores than baseline and outperforming human-human interactions by 43%.


<details>
  <summary>Details</summary>
Motivation: AI agents (including LLMs) struggle to coordinate with humans in dynamic interactions because existing alignment methods fail to explicitly quantify the underlying social norms that generate natural human behavior.

Method: The authors developed a simplified pedestrian-vehicle interaction platform, collected 3,456 dynamic human interactions, identified three social norm principles (outcome predictability, value alignment, advantage awareness), and incorporated these principles into LLM agents for closed-loop human evaluation.

Result: Social-norm-informed LLM achieved nearly fourfold higher total score than baseline strategy and outperformed human-human interactions by 43%.

Conclusion: Formalizing tacit social norms into explicit, quantifiable principles enables AI agents to achieve mutually beneficial coordination in dynamic interactions, supporting more natural integration into human society.

Abstract: Humans continuously coordinate with others in dynamic interactions, often through implicit, hard-to-quantify social norms that act as shared tacit expectations among interacting agents. As AI agents, including large language models (LLMs), become embedded in daily life, they increasingly participate in such interactions and reshape social interaction structures. Yet they often fail to coordinate with humans in an effective, considerate, and natural manner. We hypothesize that this gap arises because existing approaches align model behavior with human demonstrations without explicitly quantifying the underlying norms that generate such behavior. We selected pedestrian-vehicle interaction as a representative dynamic interaction and developed a simplified experimental platform that captures its key interactive features. From 3,456 dynamic human interactions collected via this platform, we identified three principles underlying human social norms: outcome predictability, value alignment, and advantage awareness. Incorporating these principles into AI agents significantly improves human-AI coordination. In the closed-loop interaction task with humans, the social-norm-informed LLM achieved a nearly fourfold higher total score than the baseline strategy and outperformed human-human interactions by 43%. These findings indicate that formalizing tacit social norms into explicit, quantifiable principles can enable AI agents to achieve mutually beneficial coordination in dynamic interactions, supporting their more natural integration into human society.

</details>


### [93] [Measuring Intelligence Beyond Human Scale](https://arxiv.org/abs/2607.07040)
*Jerry Han,Rafael Moschopoulos,Ella Colby,Vishrut Goyal,Andrew Tu,Kia Ghods,Mark Braverman,Elad Hazan*

Main category: cs.AI

TL;DR: This paper proposes a new paradigm for measuring intelligence beyond human capability by using relative measurement where models generate public challenges to separate other systems, creating an adversarial psychometric rating system that scales with capabilities.


<details>
  <summary>Details</summary>
Motivation: Human-authored benchmarks quickly saturate, and beyond human-level performance, it becomes hard for humans to design tasks that are both difficult and verifiable. This paper argues this is an inherent limitation of absolute-scale evaluation.

Method: The paper proposes a relative measurement paradigm using adversarial psychometric ratings: models generate public challenges that separate other systems. It includes protocols to reduce private-information attacks, support judge-free adjudication, and naturally scale with agent capabilities.

Result: The framework is instantiated across both verifiable and open-ended (non-verifiable) domains, demonstrating that model-generated evaluation can continue to measure systems beyond the human frontier.

Conclusion: Absolute-scale evaluation is inherently limited for super-human intelligence; relative adversarial benchmarking offers a scalable path forward for measuring intelligence beyond human capability.

Abstract: How can we measure intelligence beyond human capability?
  Human-authored benchmarks saturate, and above human capability, examiners may not know which tasks are both hard and verifiable. We argue that this difficulty is inherent to absolute-scale evaluation and propose a new paradigm based on relative measurement in which models generate public challenges that separate other systems. Aggregating these outcomes yields an adversarial psychometric rating system that can scale with the systems being measured. We describe practical protocols that reduce incentives for private-information attacks, support judge-free adjudication, and naturally scale with agent capabilities. We instantiate the framework across verifiable and open-ended, non-verifiable domains, illustrating how model-generated evaluation can continue to measure systems beyond the human frontier.

</details>


### [94] [Operational Reframing and Approval-Framed Delegation in Multi-Agent LLM Safety](https://arxiv.org/abs/2607.07097)
*Lifei Liu,Haoran Yu,Xiaochong Jiang,Su Wang,Pin Qian,Yihang Chen*

Main category: cs.AI

TL;DR: This paper argues that the common practice of comparing direct prompts vs. planner-executor pipelines as a single "pipeline effect" is problematic because it conflates three distinct mechanisms. The authors propose a five-condition controlled contrast design to isolate reframing, planner behavior, and delegation framing effects, showing that aggregate pipeline safety is unstable and varies by model, prompt design, and scenario.


<details>
  <summary>Details</summary>
Motivation: Existing multi-agent LLM safety evaluations use an aggregate "pipeline effect" metric that conflates multiple mechanisms (reframing, planner refusal/transformation, executor delegation), making it difficult to interpret whether failures stem from architecture or other factors.

Method: A five-condition controlled contrast design evaluated on 30 synthetic harmful scenarios plus four agent-safety benchmarks, using LLM-judged compliance across GPT, Gemini, DeepSeek, and Claude models.

Result: (1) Operational reframing is the most portable risk signal, increasing compliance across GPT, Gemini, DeepSeek. (2) Planner refusal can offset risk, but when planners produce executable steps, executors become more compliant. (3) Approval-framed delegation is sensitive to prompt design and model pairing. (4) Raw-direct rankings mispredict deployed behavior (e.g., Gemini safest raw but largest amplification with Claude planner: 8.9%→38.9%). (5) GPT's near-zero aggregate effect hides reframing increase canceled by planner refusal.

Conclusion: Multi-agent safety evaluations should report reframing, planner behavior, delegation framing, and model pairing separately before attributing failures to architecture itself.

Abstract: Safety evaluations of multi-agent LLM systems often compare a direct prompt with a planner-executor pipeline and report the difference as a single "pipeline effect." We argue that this aggregate is difficult to interpret because it conflates three mechanisms: harmful intent may be reframed as plausible operational work, the planner may refuse or transform the request, and the executor may act under delegation prompts implying prior approval. To separate these factors, we introduce a five-condition controlled contrast design, evaluated on 30 synthetic harmful scenarios and an exploratory external validation set from four agent-safety benchmarks using LLM-judged compliance.
  Our results show that aggregate pipeline safety is not a stable architectural property. Operational reframing is the most portable risk signal, increasing compliance for GPT, Gemini, and DeepSeek across both scenario sets, while Claude is comparatively resistant. Planner behavior can offset this risk mainly through refusal; however, when the planner produces executable steps, the executor may become more compliant than under the direct operational baseline. Approval-framed delegation is sensitive to prompt design, model pairing, and scenario source, and a skeptical executor prompt sharply reduces compliance.
  Raw-direct model rankings can also mispredict deployed planner-executor behavior. Gemini is safest under raw direct prompts in the primary set yet shows the largest amplification with a Claude planner, rising from 8.9 percent to 38.9 percent compliance. GPTs near-zero aggregate pipeline effect instead hides a reframing increase canceled by planner refusal. These findings suggest that multi-agent safety evaluations should report reframing, planner behavior, delegation framing, and model pairing separately before attributing failures to architecture itself.

</details>


### [95] [Does AI Understand Imaging? A Systematic Benchmark of Agentic AI for Computational Imaging Tasks](https://arxiv.org/abs/2607.07189)
*Ethan Chung,Chuanjun Zheng,Jasper Tan,Jingxi Li,Haopeng Zhang,Huaijin Chen*

Main category: cs.AI

TL;DR: ImagingBench is a benchmark of 20 computational imaging tasks showing that current VLMs and agentic AI, despite strong semantic visual performance, significantly underperform specialized methods on physically grounded imaging problems like lensless imaging, holography, and time-of-flight reconstruction.


<details>
  <summary>Details</summary>
Motivation: To assess whether vision-language models and agentic AI, which excel at semantic visual tasks, can handle the physics and inverse problems fundamental to computational imaging.

Method: 20 computational imaging tasks across 5 categories (ray/wave optics, ISP, inverse reconstruction, computational sensing, calibration) with 3 evaluation settings: Expert (fixed expert-guided), Planner (planner-guided), and Forward (simulation consistency). Benchmarks Gemini, GPT, Qwen vs. task-specific non-agentic baselines.

Result: Agentic models are consistently weaker than specialized methods, especially on computational sensing (lensless imaging, event-based reconstruction, ToF imaging, holography). Planner guidance yields only modest/inconsistent gains. Models generate visually plausible outputs but have poor reference-based fidelity.

Conclusion: There is a substantial gap between semantic visual competence and physically grounded imaging performance. ImagingBench provides a unified testbed to measure this gap and track progress in agentic AI for computational imaging.

Abstract: Vision-language models (VLMs) and agentic AI have shown strong performance on semantic visual tasks, but it remains unclear whether they can handle the physics and inverse problems that underlie computational imaging. We present ImagingBench, a benchmark of 20 computational imaging tasks spanning five categories: ray and wave optics, image signal processing, inverse reconstruction, computational sensing, and calibration. ImagingBench evaluates three complementary settings: Expert, fixed expert-guided inverse reconstruction; Planner, planner-guided inverse reconstruction; and Forward, forward-system simulation for consistency checking. We benchmark leading proprietary and open-source image-centric multimodal systems, including Gemini, GPT, and Qwen, and compare them with representative task-specific non-agentic baselines. Across tasks, agentic models remain consistently weaker than specialized methods, especially on computational sensing problems such as lensless imaging, event-based reconstruction, time-of-flight imaging, and holography. Planner guidance provides only modest and inconsistent gains over the fixed-prompt Expert baseline. Although the models often generate visually plausible outputs, their reference-based fidelity remains poor, revealing a substantial gap between semantic visual competence and physically grounded imaging performance. ImagingBench provides a unified testbed for measuring this gap and tracking progress in agentic AI for computational imaging.

</details>


### [96] [Reasoning Consistency Scanning: A Framework for Auditing Chain-of-Thought Validity in AI Safety Evaluations](https://arxiv.org/abs/2607.07229)
*Silvia Santano*

Main category: cs.AI

TL;DR: This paper introduces reasoning consistency scanning, a method to detect logical inconsistencies between a model's stated reasoning and its answer in evaluation transcripts, without requiring experimental interventions like faithfulness checks. The authors formalize inconsistency types, build a benchmark, implement a scanner, and show that inconsistency varies across models and tasks.


<details>
  <summary>Details</summary>
Motivation: Prior work shows chain-of-thought reasoning is often unfaithful, but detecting unfaithfulness requires controlled interventions that cannot be applied post-hoc. The authors aim to address a more tractable problem: assessing whether stated reasoning is logically consistent with the answer, which can be evaluated from transcripts alone.

Method: The authors formalize reasoning consistency as distinct from faithfulness, define a six-subtype taxonomy of inconsistency, build a manually adapted benchmark of 60 transcripts from InstrumentalEval outputs, and implement a working scanner for InspectScout.

Result: Results across four generator models and three evaluations from inspect_evals show that reasoning inconsistency is present, detectable, and varies systematically across both models and task types.

Conclusion: Reasoning inconsistency is a practical and detectable property in safety evaluation transcripts, offering a scalable alternative to faithfulness assessment that requires no experimental intervention.

Abstract: Prior work has shown that chain-of-thought (CoT) reasoning is often unfaithful: a model's stated reasoning does not reliably reflect the process that produced its output. Detecting unfaithfulness, though, requires controlled experimental interventions, which cannot be applied to evaluation transcripts after the fact. We turn instead to a more tractable question that has received less attention: whether the stated reasoning is logically consistent with the answer it accompanies. Unlike faithfulness, consistency can be assessed from a transcript alone, with no intervention. We introduce reasoning consistency scanning, a reusable method for detecting this property in AI safety evaluation transcripts. Our contributions are fourfold. First, we formalize reasoning consistency as distinct from faithfulness and define a six-subtype taxonomy of inconsistency. Second, we build a validated benchmark of 60 transcripts, manually adapted from InstrumentalEval outputs. Third, we implement a working scanner for InspectScout, the first to target this property in safety evaluation transcripts. Fourth, we report results across four generator models and three evaluations from inspect_evals, showing that reasoning inconsistency is present, detectable, and varies systematically across both models and task types.

</details>


### [97] [From Atomic Actions to Standard Operating Procedures: Iterative Tool Optimization for Self-Evolving LLM Agents](https://arxiv.org/abs/2607.07321)
*Haipeng Ding,Yuexiang Xie,Zhewei Wei,Yaliang Li,Bolin Ding*

Main category: cs.AI

TL;DR: EvoSOP enables LLM agents to self-evolve by synthesizing atomic actions into reusable Standard Operating Procedures (SOPs), improving task success rates and reducing interaction rounds.


<details>
  <summary>Details</summary>
Motivation: Existing agent frameworks rely on static, granular atomic actions that force agents to reinvent low-level logic for recurring workflows, causing high reasoning overhead and failure rates.

Method: EvoSOP extracts SOPs from execution trajectories and iteratively optimizes the toolset through a lifecycle of construction, merging, evaluation, and pruning.

Result: EvoSOP significantly boosts task success rates and substantially reduces the number of interaction rounds compared to baselines.

Conclusion: Iterative tool optimization fosters reliable and efficient tool-use patterns, providing a scalable pathway for self-evolving agents.

Abstract: Tool utilization enables Large Language Model (LLM) agents to interact with the real world and resolve complex tasks. However, existing agent frameworks predominantly rely on static toolsets composed of granular atomic actions (e.g., basic file I/O or single-turn search), which forces agents to reinvent low-level logic for every recurring workflow, leading to increased reasoning overhead and failure rates. In this study, we propose that agents can achieve self-evolution by synthesizing these atomic actions into reusable Standard Operating Procedures (SOPs), which function as callable higher-order tools that encapsulate multi-step logic. We further introduce EvoSOP, a framework that empowers agents to extract SOPs from execution trajectories and iteratively optimize the toolset through a systematic lifecycle of construction, merging, evaluation, and pruning. Extensive experiments demonstrate that EvoSOP significantly boosts task success rates while substantially reducing the number of interaction rounds compared to baselines. Our analysis also reveals that iterative tool optimization fosters reliable and efficient tool-use patterns, providing a scalable pathway for the development of self-evolving agents.

</details>


### [98] [Physics-Audited Agentic Discovery in Scientific Machine Learning](https://arxiv.org/abs/2607.07379)
*Diab W. Abueidda,Bilal Ahmed,Panos Pantidis,Mostafa E. Mobasher*

Main category: cs.AI

TL;DR: PA-SciML introduces a physics-audited verification-first workflow for agentic scientific machine learning, ensuring surrogate models satisfy mechanical physics requirements (boundary conditions, causality, etc.) rather than relying solely on error metrics. In solid mechanics examples, it selects models that pass physics checks while error-only baselines fail causality tests.


<details>
  <summary>Details</summary>
Motivation: Standard error metrics in agentic SciML do not guarantee that discovered surrogate models satisfy fundamental physics requirements such as boundary conditions, superposition, stiffness scaling, or causality—properties essential for mechanics.

Method: PA-SciML implements a verification-first workflow: fixes a scoring evaluator before search, derives machine-checkable physics requirements, checks each candidate's predicted fields, and searches for high-violation cases without reference solutions. It also adds advisory numerical probes and tests isolated modeling changes.

Result: In static elasticity, PA-SciML selects a surrogate with lower validation error than the error-only baseline, both passing linear-elastic checks. In transient elastodynamics, the error-only baseline fails a causality check (responding to future loading), while PA-SciML's selected surrogate passes all stated checks.

Conclusion: The key distinction is per-candidate physics evidence on predicted fields rather than a richer aggregate score. Physics auditing is necessary to ensure surrogate models are genuinely physically meaningful, not just numerically accurate.

Abstract: In agentic scientific machine learning (SciML), large language model (LLM) agents can discover surrogate models and select one by an automated score, typically an error metric. A low error, however, does not establish that the predicted fields satisfy the physics that matter for mechanics, such as boundary conditions, superposition, stiffness scaling, or causality. We introduce Physics-Audited Agentic SciML (PA-SciML), a verification-first workflow for agentic SciML discovery. The workflow fixes a scoring evaluator before search, derives reviewable machine-checkable physics requirements, checks each trained candidate on its outputs, and separately searches prescribed input ranges or measured load-history spans for high-violation cases without reference solution fields. A surrogate is reported as verified only under the stated checks. When enabled, the workflow also adds advisory numerical probes before training and tests one modeling change at a time to record which isolated edits are associated with score gains before reuse. In the reported computational-solid-mechanics numerical examples, the static elasticity run selects a surrogate with lower validation error than the error-only baseline while both selected models pass the common linear-elastic checks. In the transient elastodynamics run, an error-only baseline with similar mean error fails a stricter causality check by responding to future parts of the loading history, while the selected surrogate passes the stated checks. The main distinction is per-candidate physics evidence on predicted fields, not a richer aggregate score.

</details>


### [99] [MIRA-Math: A Benchmark for Minimal Information Requesting and Mathematical Reasoning](https://arxiv.org/abs/2607.07391)
*Charbel Al Bateh,Samer Saab*

Main category: cs.AI

TL;DR: MIRA-Math is a benchmark for evaluating mathematical reasoning where solvers must identify and request exactly one missing atomic fact under a fixed budget, then integrate it to produce an exact answer. It contains 2,310 instances across 22 mathematical families, enabling separate evaluation of information-requesting accuracy and downstream computation ability.


<details>
  <summary>Details</summary>
Motivation: Existing benchmarks conflate mathematical reasoning with other capabilities like tool use or long-horizon dialogue. The authors aim to isolate and measure a specific diagnostic capability: requesting exactly one missing atomic fact needed to solve a problem, under strict budget constraints.

Method: Each problem provides all context except one necessary atomic fact. The solver must request that missing fact in natural language from a fixed constrained LLM responder, which only provides the fact if the request matches exactly. Instance generation, hint specifications, validation, and answer verification are all deterministic.

Result: Experiments show that request success and final-answer accuracy are separable—models may correctly request the right fact but fail downstream computation, or fail to obtain the canonical hint altogether. The benchmark spans 22 typed mathematical families and 2,310 instances.

Conclusion: MIRA-Math provides a reproducible, controlled framework for evaluating minimal information requesting in mathematical reasoning, decoupling the ability to ask for missing information from the ability to compute the final answer.

Abstract: Mathematical reasoning benchmarks typically provide all facts needed to solve each problem, while interactive benchmarks often mix reasoning with tools, retrieval, and long-horizon dialogue. We introduce MIRA-Math, a benchmark for a narrower diagnostic capability: solving mathematical problems whose full latent state has a unique answer, but whose solver-facing view is missing exactly one necessary atomic fact. The solver must request the missing information in natural language under a strict budget and then integrate the returned fact into an exact final answer. A fixed constrained LLM responder sees only the dataset-provided atomic fact and must either offer the quoted fact when the request matches it, or decline otherwise. Thus, instance generation, typed hint specifications, validation, and final-answer verification are deterministic, while request metrics are measured under a fixed LLM-mediated responder channel. MIRA-Math contains 2{,}310 generated instances from 22 typed mathematical families spanning algebra, probability, linear systems, discrete structures, signal processing, Markov chains, circuits, interpolation, and numerical boundary-value problems. Experiments across frontier and small models show that request success and final-answer accuracy are separable: models may ask for the right fact yet fail the downstream computation, or fail before obtaining the canonical hint. We release generators, verifiers, prompts, run metadata, and dataset documentation to support reproducible evaluation of minimal information requesting in mathematical reasoning.

</details>


### [100] [Agentic Data Environments](https://arxiv.org/abs/2607.07397)
*Elaine Ang,Chenxi Huang,Georgios Liargkovas,Jerry Liu,Jinhui Liu,Nikos Pagonas,Charlie Summers,Haonan Wang,Jiakai Xu,Tianle Zhou,Yusen Zhang,Zhou Yu,Zhuo Zhang,Tianyi Peng,Kostis Kaffes,Eugene Wu*

Main category: cs.AI

TL;DR: This paper proposes reframing data systems as active substrates (Agentic Data Environments) to enable safe and reliable autonomous agent execution while bounding failure risks.


<details>
  <summary>Details</summary>
Motivation: Autonomous agents can impose abrupt and costly failures, so there is a need to increase automation benefits while bounding failure consequences.

Method: Proposes Agentic Data Environments as an execution substrate for agents, spanning files, APIs, applications, and system state to support safe, reliable execution.

Result: Early work on Agentic Data Environments that both amplifies agent capabilities and enforces safety guarantees.

Conclusion: Data systems should be reframed from passive stores into active substrates that both amplify agent capabilities and enforce safety guarantees.

Abstract: Autonomous agents promise substantial gains in speed, scale, and labor efficiency, but their failures can impose abrupt and often irreversible costs. The central challenge for agentic automation is therefore to increase the benefits of automation while bounding the consequences of failure.
  While databases remain central to modern computing, agents operate over a broader data environment spanning files, APIs, applications, and system state. In this talk, I will outline early work on Agentic Data Environments -- the execution substrate in which agents operate -- that both amplify agent capabilities and enforce safety guarantees. This perspective reframes data systems from passive stores of state into active substrates for safe, reliable execution.

</details>


### [101] [Reason Less, Verify More: Deterministic Gates Recover a Silent Policy-Violation Failure Mode in Tool-Using LLM Agents](https://arxiv.org/abs/2607.07405)
*Vikas Reddy,Sumanth Reddy Challaram,Abhishek Basu*

Main category: cs.AI

TL;DR: LLM agents with tool access can silently violate domain policies without triggering errors. In an airline benchmark (τ²-bench), 78% of failures are silent wrong-state failures. A lightweight intervention—deterministic read-only pre-execution gates that inspect calls before writes—improves success from 29.6% to 42.0% (+12.4pp) on gpt-4o-mini, with consistent replication. The effect is concentrated on tasks where gates fire. The failure mode persists even in frontier models like gpt-5.2.


<details>
  <summary>Details</summary>
Motivation: LLM agents with tool access can silently violate domain policies they are meant to enforce, producing incorrect states (e.g., cancelled bookings, unauthorized changes) without any tool error or agent self-reporting the failure. This hidden failure mode undermines trust in tool-using LLM agents.

Method: The authors study the failure mode in the τ²-bench airline domain. They propose a lightweight intervention: deterministic, read-only pre-execution gates that inspect the proposed tool call and current state before allowing a write operation. A suite of four gates is evaluated.

Result: On gpt-4o-mini, full-benchmark success improved from 29.6% to 42.0% (+12.4pp, P=0.0012), replicated on a disjoint 15-seed set (+12.3pp, P=0.0008). On firing tasks (26/50), success rose +19.2pp; non-firing tasks showed no significant change. On gpt-5.2, success improved from 61.2% to 71.6% (+10.4pp, P=0.020).

Conclusion: Deterministic gates do not guarantee task success, but they can deterministically prevent a known class of silent policy-violating writes at the action boundary. The mechanism is bounded: gates help when tools are policy-permissive, and add little where tools already self-enforce.

Abstract: Tool-using LLM agents can violate the very policies they are deployed to enforce while appearing to complete the task successfully. In policy-permissive environments, a tool may execute any well-formed call even when the corresponding state transition is forbidden by domain policy. The result is a silent wrong state (a booking cancelled, a passenger count changed, a claim acted on without verification) that neither the tool nor the agent's self-report exposes.
  We study this failure mode in the $τ^2$-bench airline domain. On a budget agent, 78% of observed failures are silent wrong-state failures with no tool error, and the aggregate failure rate is reproducible across disjoint seeds, not sampling noise. We then evaluate a lightweight intervention: deterministic, read-only pre-execution gates that inspect the proposed call and current state before allowing a write. A four-gate suite raises full-benchmark success from 29.6% to 42.0% on gpt-4o-mini (+12.4pp; paired task-level bootstrap P=0.0012), and the lift reproduces on a disjoint 15-seed set (+12.3pp; P=0.0008).
  The effect is concentrated where the gates fire: on the 26/50 firing tasks, success rises by +19.2pp, while movement on the 24 non-firing tasks does not exclude zero. Two negative controls (a self-enforcing retail domain and BFCL) bound the mechanism: gates help when tools are policy-permissive and add little where tools already self-enforce. As suggestive evidence, not a central claim, the same failure mode persists at the frontier: gpt-5.2 at default reasoning still attempts policy-violating writes, and the same suite improves success from 61.2% to 71.6% (+10.4pp; P=0.020; n=5, no replication). The contribution is a bounded evaluation and reliability result: deterministic gates do not guarantee task success, but they can deterministically prevent a known class of silent policy-violating writes at the action boundary.

</details>


### [102] [InductWave: Inductive Multi-Hop Logical Query Answering on Knowledge Graphs](https://arxiv.org/abs/2607.07422)
*Mayank Kharbanda,Michael Cochez,Rajiv Ratn Shah,Raghava Mutharaju*

Main category: cs.AI

TL;DR: InductWave is a wavelet-based inductive embedding method for logical multi-hop query answering over large knowledge graphs that requires fewer message-passing layers (75% fewer) while matching or outperforming state-of-the-art models, enabling reasoning on graphs with unseen entities at test time.


<details>
  <summary>Details</summary>
Motivation: Existing logical query answering methods assume transductive reasoning, failing on unseen entities, and real-world KGs suffer from resource scarcity where training on all nodes is infeasible.

Method: Proposes InductWave, a wavelet-based inductive embedding method that uses wavelet transforms instead of deep message-passing layers to enable inductive reasoning on large KGs with fewer computational resources.

Result: Performs on par with baselines using half the message-passing layers, outperforms them in most cases with 75% fewer layers, and scales to massive graphs like Wiki-KG.

Conclusion: InductWave demonstrates that wavelet-based inductive reasoning is effective and resource-efficient for logical query answering on large knowledge graphs, enabling practical deployment where full graph training is impossible.

Abstract: Logical Multi-Hop Query Answering over Knowledge Graphs (KGs) can be formulated as querying, with an implicit completeness assumption. Current works mainly focus on Existential First Order Logic (EFO) queries. These EFO queries contain conjunction, disjunction, and negation operators. Most existing works employ transductive reasoning, meaning they are not capable of reasoning over entities unseen during training. In the real world, there is a resource scarcity, and we cannot train a model with all the nodes of a large KG. Hence, we propose InductWave, a wavelet-based inductive embedding method for logical query answering on large KGs. Here, the training graph consists of fewer nodes than the test graph. Our model performs on par with the baseline models while having half the number of message-passing layers. It outperforms all of them in most cases, with 75% of the layers. These fewer resource requirements enable us to evaluate InductWave on massive graphs, such as Wiki-KG. We test our model using extensive experiments across varying train-test graph proportions of the FB15k-(237) dataset, comparing it with the state-of-the-art models. The code and datasets for the model are available at https://github.com/kracr/inductwave/.

</details>


### [103] [The Blind Curator: How a Biased Judge Silently Disables Skill Retirement in Self-Evolving Agents](https://arxiv.org/abs/2607.07436)
*Xing Zhang,Yanwei Cui,Guanghui Wang,Ziyuan Li,Wei Qiu,Bing Zhu,Peiyang He*

Main category: cs.AI

TL;DR: A biased LLM judge silently disables the skill retirement mechanism in self-evolving agents, causing a hidden failure that standard metrics don't reveal, unless the judge has near-zero false-pass rate.


<details>
  <summary>Details</summary>
Motivation: To understand what happens when self-evolving agents retire bad skills based on biased LLM judges, since reference-free tasks force reliance on such judges whose false-pass bias can silently break the curator without detection.

Method: Corrupted-reward analysis, behavioral study on reference-free report-writing testbed with code-generation cross-check, injecting corruption on top of deterministic reward.

Result: False-pass bias disables contribution-based retirement past a sharp data-independent threshold. This mechanism failure is universal across domains, but the downstream outcome is regime-dependent—eval quality degrades only where the same corruption also starves skill synthesis.

Conclusion: A cheap defect-injection audit can reveal before deployment whether a judge is on the wrong side of the threshold. The contribution is a behavioral safety result about silent curator failure, not a performance result.

Abstract: A self-evolving agent retires its bad skills by watching them fail, so what happens when the judge cannot see the failures? Skill retirement is the structural constraint that keeps a growing library from drifting below the no-skill baseline, but its guarantee assumes an unbiased reward, which is false for the LLM judges that reference-free tasks force upon us. We show that a biased judge does not merely add noise; it \emph{silently switches off the curator}. We make this precise with a corrupted-reward analysis and, isolating the causal channel by injecting corruption on top of a deterministic reward, a behavioral study on a reference-free report-writing testbed with a code-generation cross-check. Symmetric noise leaves retirement intact, but \emph{false-pass} bias (failures slipping through as passes) disables contribution-based retirement past a sharp threshold that no amount of data can cross. Separating genuine retirement from cap-eviction churn shows this \emph{mechanism} failure is universal, holding across domains and failure rates and sparing only near-zero-false-pass, verifier-like graders. The downstream \emph{outcome}, though, is regime-dependent: eval quality degrades only where the same corruption also starves skill synthesis, and otherwise holds steady, so the disabled curator is \emph{silent}, surfacing in no aggregate metric. The contribution is a behavioral safety result, not a performance one. A cheap defect-injection audit then tells an operator, before deployment, which side of the threshold their judge occupies.

</details>


### [104] [SpaCellAgent: A Self-Evolving LLM-Based Multi-Agent Framework for Trajectory Analysis](https://arxiv.org/abs/2607.07467)
*Songhan Wang,Haoang Chi,He Li,Zhiheng Zhang,Jiayan Yuan,Cheems Wang,Hao Peng,Xinwang Liu,Wenjing Yang*

Main category: cs.AI

TL;DR: SpaCellAgent is an LLM-powered multi-agent framework that fully automates end-to-end spatiotemporal trajectory inference from natural language input, achieving 40%+ efficiency gains across diverse datasets.


<details>
  <summary>Details</summary>
Motivation: Existing trajectory inference methods require extensive manual intervention and proficiency in heterogeneous tools, creating a significant barrier to efficient analysis. The goal is to democratize advanced spatiotemporal modeling.

Method: A multi-agent architecture for strategic workflow planning, a dynamic tool-orchestration engine for adaptive algorithm selection, and a self-evolution module that iteratively refines performance through feedback.

Result: Evaluated on six heterogeneous datasets; demonstrates over 40% improvement in analytical efficiency while maintaining expert-aligned performance.

Conclusion: By converting natural language specifications into optimized analytical workflows and fully automating the pipeline, SpaCellAgent democratizes advanced spatiotemporal modeling and establishes a scalable, agent-driven paradigm for computational biology.

Abstract: Spatial and Single-cell transcriptomics are transformative in deciphering cellular dynamics. As the fundamental paradigm for reconstructing cell developmental paths, trajectory inference (TI) is critical. However, existing methods require extensive manual intervention and proficiency in heterogeneous tools, posing a significant barrier to efficient TI analysis. To bridge this gap, we propose SpaCellAgent, an autonomous large language model (LLM) multi-agent framework that automates end-to-end spatiotemporal analysis and narrative generation. SpaCellAgent utilizes a multi-agent architecture for strategic workflow planning, a dynamic tool-orchestration engine for adaptive algorithm selection, and a self-evolution module that iteratively refines performance through feedback. We evaluate SpaCellAgent on six heterogeneous datasets encompassing complex temporal developmental trajectories, diverse sequencing platforms, and spatially-resolved tissue architectures. SpaCellAgent consistently demonstrates over 40\% improvement in analytical efficiency while maintaining expert-aligned performance. By converting natural language specifications into optimized analytical workflows and fully automating the pipeline, SpaCellAgent democratizes advanced spatiotemporal modeling and establishes a scalable, agent-driven paradigm for computational biology. The code and materials are available at https://github.com/LittleXH-shw/SpaCellAgent.

</details>


### [105] [Search, Fail, Recover: A Training Framework for Correction-Aware Reasoning](https://arxiv.org/abs/2607.07492)
*Dmitry Beresnev,Vladimir Makharev,Roman Khalikov,Ivan Oseledets,Petr Anokhin*

Main category: cs.AI

TL;DR: Pyligent is a framework that trains LLMs to backtrack from failed reasoning branches by using validated search trees as supervised targets, significantly improving solve rates on tasks requiring delayed-failure recovery.


<details>
  <summary>Details</summary>
Motivation: Many reasoning tasks require backtracking after pursuing a plausible branch that leads to delayed failure, but standard left-to-right supervised fine-tuning on gold solutions does not teach models how to recover from such failures.

Method: Pyligent uses task validators to label generated continuations and failures, converts search trees into supervised targets for three actions (continue, finish, backtrack), optionally with traces summarizing abandoned branches.

Result: Improves solve rate by 72.7 pp on hidden graphs, 17/18 pp on mixed/expert Sudoku, 27/14 pp on Sudoku with reasoning traces, and 13 pp on Blocksworld compared to gold-only SFT.

Conclusion: Explicit failed-branch supervision teaches useful recovery behavior that imitation of polished solution chains cannot provide.

Abstract: Many reasoning tasks are not well described by a single left-to-right chain: a solver may need to pursue a plausible branch, observe delayed failure, and return to the latest prefix that can still be completed. We introduce Pyligent, a training and inference framework inspired by the Diligent Learner formulation that represents reasoning as validated search over partial solution chains. A task validator labels generated continuations and failures, and the resulting search trees are converted into supervised targets for three actions: continue, finish, and backtrack, with optional traces that summarize abandoned branches. We evaluate Pyligent on a hidden directed graph task designed to isolate delayed-failure recovery, and on structured reasoning domains with exact validators, including $4{\times}4$ Sudoku, Sudoku with reasoning traces, and Blocksworld. Compared with gold-only supervised fine-tuning, Pyligent improves solve rate by $72.7$ percentage points on hidden graphs, by $17$ and $18$ points on mixed and expert Sudoku, by $27$ and $14$ points on mixed and expert Sudoku with reasoning traces, and by $13$ points on Blocksworld. These results suggest that explicit failed-branch supervision can teach useful recovery behavior beyond imitation of polished solution chains.

</details>


### [106] [Do LLM-Generated Skills Make Better AI Data Scientists? A Component Ablation Across Data-Science Workflows](https://arxiv.org/abs/2607.07504)
*Wei-Jung Huang*

Main category: cs.AI

TL;DR: LLM-generated skill files for data-science tasks fail to improve performance over simple task prompts, with extensive ablation (7,560+ runs) showing no significant gains across any component.


<details>
  <summary>Details</summary>
Motivation: Product data scientists face recurring execution tasks (data cleaning, SQL, statistics, formatting) and use reusable skill files to avoid prompting from scratch. Expert-written skills are high-quality but costly to maintain; the authors explore whether automatically generated skills offer a useful low-curation alternative.

Method: Tested one generated skill per lifecycle stage (data preparation, extraction, statistical analysis, reporting). Main ablation: 56 tasks × 9 model configurations × 3 providers = 7,560 runs. Token-matched control added 1,512 runs comparing Full skills to task-irrelevant skill-formatted content.

Result: No reliable improvement from full generated skills over No-Skill prompting. No ablated variant significantly improved performance (all p-values ≥ 0.396, spread only 1.2 pp). Full skills performed similarly to task-irrelevant skill-formatted content.

Conclusion: Using one LLM-generated skill per data-science workflow as a default single-shot prompting strategy is not recommended; the approach does not yield benefits over simpler prompting.

Abstract: Product data scientists often ask LLM-based agents to help with recurring execution tasks such as cleaning data, writing SQL, choosing statistical tests, and formatting results. Reusable skill files are meant to avoid prompting from scratch by packaging guidance for a task family. Expert-written skills can encode high-quality guidance, but writing and maintaining them across many data-science task families creates a manual bottleneck. We ask whether LLM-generated skills offer a useful low-curation alternative: do they improve performance over the task prompt alone? We test this question across four lifecycle stages: data preparation, data extraction, statistical analysis, and reporting, using one generated skill per stage. We find no reliable improvement from full generated skills over No-Skill prompting. We then ask whether any part of the skill is useful by ablating different skill components. The main ablation covers 56 tasks, nine model configurations, and three providers, yielding 7,560 runs. Compared with prompting using the task alone, neither the full generated skill nor any ablated skill variant significantly improves performance; all p-values are at least 0.396, and the total spread across variants is only 1.2 pp. A supplemental token-matched control adds 1,512 runs and finds that Full skills perform similarly to task-irrelevant skill-formatted content. The results caution against using one LLM-generated skill per data-science workflow as a default single-shot prompting strategy.

</details>


### [107] [RL Post-Training Builds Compositional Reasoning Strategies](https://arxiv.org/abs/2607.07646)
*Azwar Abdulsalam,Nishil Patel,Andrew Saxe*

Main category: cs.AI

TL;DR: This paper investigates whether RL post-training can compose primitive skills into novel higher-level strategies, not just amplify existing ones. Using a rewrite-grammar environment, they show RL discovers sequential and parallel compositions that the base model cannot achieve through scaling alone, and that selectivity (not exploration volume) is the key advantage of RL over rejection fine-tuning.


<details>
  <summary>Details</summary>
Motivation: To determine whether RL post-training can genuinely compose primitive skills into new strategies, as opposed to merely amplifying latent abilities already present in the pretrained model.

Method: Pretrain a Transformer on primitive symbol-rewrite chains, then post-train with RL (using binary final-answer reward) on a Trace-based reasoning task. Compare against rejection fine-tuning (RFT) and conduct trace analysis to examine compositional mechanisms.

Result: RL solves held-out problems rarely solved by the pretrained model even with larger sampling budgets. RL reorganizes competence via phased compositional mechanism: strengthening primitive reductions, then discovering valid composed procedures (sequential and parallel compositions). RFT plateaus and produces many invalid shortcut-like rewrites.

Conclusion: RL enables genuine composition of primitive skills into higher-level strategies, not mere amplification. The key differentiator is selectivity of exploration, not volume. Emergence of compositional strategies depends on pretraining organizing primitive competence into reduction procedures that RL can compress.

Abstract: Does RL post-training merely amplify primitive skills already latent in a base model, or can it compose primitive skills into new higher-level strategies? We study this question in a fully observable rewrite-grammar environment where the pretraining distribution is known and every generated rewrite can be audited. A Transformer is pretrained on primitive symbol-rewrite chains and post-trained on a Trace-based reasoning task with only a binary final-answer reward. RL solves held-out problems that remain rarely solved by the pretrained model even under much larger sampling budgets, while rejection fine-tuning improves early but plateaus. Trace analysis shows that RL reorganizes primitive competence through a phased compositional mechanism: it first strengthens primitive reductions, then discovers valid composed procedures. These include sequential compositions, which collapse ordered chains of primitive contractions, and parallel compositions, which combine independent primitive contractions in a single step. The composed procedures are not isolated samples; they are reused and consolidated into a stable repertoire. Comparing RL with rejection fine-tuning shows that the key difference is not exploration volume but selectivity: RFT produces many shortcut-like rewrites, much of them invalid, whereas RL concentrates exploration into valid reusable structure. Pretraining ablations show that the emergence of compositional strategies is gated not by primitive exposure alone, but by whether pretraining organizes primitive competence into reduction procedures that RL can later compress. The base model provides weak procedural ingredients; RL builds them into reliable higher-level strategies.

</details>


### [108] [Recursive Self-Improvement in AI: From Bounded Self-Refinement to Autonomous Research Loops](https://arxiv.org/abs/2607.07663)
*Mingguang Chen,Licheng Wang,Bo Qu*

Main category: cs.AI

TL;DR: A survey of 1,250 arXiv papers (2024-2026) taxonomizing AI self-improvement along axes of what is improved and loop closure degree, finding bounded self-refinement is industrial practice while open-ended RSI remains constrained, with a key focus on the evaluator design space and a verification hierarchy that explains failure modes.


<details>
  <summary>Details</summary>
Motivation: The vocabulary around AI self-improvement (self-refine, self-reward, self-play, self-evolve) conflates fundamentally different ambitions, creating confusion. The authors aim to impose a clear taxonomy distinguishing bounded self-refinement from open-ended recursive self-improvement (RSI), and to identify the critical role of the evaluator signal in determining loop viability.

Method: Survey of 1,250 arXiv papers (2024-2026) categorized along two axes: (1) what is improved (behavior, policy, evaluator, research process), and (2) loop closure degree (human-in-the-loop to fully closed). The authors introduce a "verification hierarchy" from formal verifiers (strongest) to intrinsic self-assessment (weakest), and map observed self-improvement strength and failure modes onto this hierarchy.

Result: Bounded self-refinement (convergent, evaluable) is already industrial practice, while open-ended RSI remains bounded by grounding requirements, collapse dynamics, and compute constraints on every measured axis. Demonstrated self-improvement strength tracks the verification hierarchy. Failure modes (self-confirming loops, model collapse, diversity collapse) follow from violations of this hierarchy. The "research direction-setting" bottleneck keeps humans in the loop at the top of the hierarchy.

Conclusion: The field's most underpopulated niche is governance-grade measurement of self-improvement. The paper connects technical findings to RSI limit theory and safety/governance questions raised by frontier-lab accounts of loop closure.

Abstract: AI systems increasingly participate in their own improvement: revising their outputs, adapting their own harnesses during deployment, training on data they generate, and, increasingly, conducting AI research itself. This literature is described under a vocabulary ("self-refine," "self-reward," "self-play," "self-evolve") that conflates fundamentally different ambitions. We survey 1,250 arXiv papers (2024-2026) along two axes: what the system improves -- its behavior in deployment, its policy through training, its evaluator, or the research process itself -- and the degree of loop closure (human-in-the-loop to fully closed). The taxonomy separates bounded self-refinement -- convergent, evaluable, and already industrial practice -- from open-ended recursive self-improvement (RSI), which remains bounded by grounding requirements, collapse dynamics, and compute constraints on every measured axis. Its distinctive feature is a dedicated category for self-evaluation: every improvement loop is a claim that some signal can substitute for human judgment. We survey the evaluator design space -- judges, process reward models, verifiers, rubrics, meta-evaluation -- order the signals into a verification hierarchy from formal verifiers (strongest) to intrinsic self-assessment (weakest), and observe that demonstrated self-improvement strength tracks this hierarchy, that its failure modes (self-confirming loops, model collapse, diversity collapse) follow from its violations, and that the "research direction-setting" bottleneck keeping humans in the loop sits at the top of that hierarchy. We connect the technical literature to the theory of RSI limits and to the safety and governance questions raised by frontier-lab accounts of closing the loop, and identify governance-grade measurement of self-improvement as the field's most underpopulated niche.

</details>


### [109] [SkillCenter: A Large-Scale Source-Grounded Skill Library for Autonomous AI Agents](https://arxiv.org/abs/2607.07676)
*Tianming Sha,Yue Zhao,Lichao Sun,Yushun Dong*

Main category: cs.AI

TL;DR: SkillCenter is a large open skill library for AI agents with 216,938 structured skills, combining source-grounded skills from academic/technical sources and community skills from GitHub/ClawHub, with traceability to exact source quotations.


<details>
  <summary>Details</summary>
Motivation: Autonomous AI agents lack grounded operational knowledge to ensure their outputs are correct, secure, and maintainable, requiring a large-scale, source-verified skill library.

Method: End-to-end pipeline with multi-source acquisition, LLM-based SkillGate quality filter, template-driven generation, iterative source-grounding, and quality-controlled publishing; skills stored as offline-searchable SQLite FTS5 bundles.

Result: 216,938 structured skills across 24 domain bundles: 114,565 source-grounded skills from peer-reviewed journals/ArXiv/technical sources, plus 102,373 community skills from GitHub/ClawHub.

Conclusion: SkillCenter provides the largest open skill library for agents with verifiable source grounding, enabling more correct and secure autonomous agent outputs.

Abstract: Autonomous AI agents can execute complex tasks with limited human review, yet they often lack the grounded operational knowledge to make their outputs not just executable but correct, secure, and maintainable. We introduce SkillCenter, to our knowledge the largest open skill library for agents by total count: 216,938 structured skills across 24 domain bundles. A SkillGate-filtered pipeline contributes 114,565 source-grounded skills from peer-reviewed journals, ArXiv, and over 24,000 technical sources, integrated with 102,373 community skills from GitHub and the ClawHub marketplace. We present the end-to-end framework that builds the pipeline subset: multi-source acquisition, an LLM-based quality gate (SkillGate), template-driven generation, iterative source-grounding, and quality-controlled publishing. Source grounding is a traceability guarantee: each retained claim maps to an exact quotation in its source. All skills ship as offline-searchable SQLite FTS5 bundles.

</details>


### [110] [Institutional Red-Teaming: Deployment Rules, Not Just Models, Causally Shape Multi-Agent AI Safety](https://arxiv.org/abs/2607.07695)
*Yujiao Chen*

Main category: cs.AI

TL;DR: The paper introduces institutional red-teaming, a methodology to test AI deployment rules by isolating rule changes' causal effects on collective behavior. Using IABench-CA (33,924 games across 228 contexts), it finds rules causally alter safety (22-58pp fatality shifts), no safe default exists, identity-targeting is universally dangerous, and identity salience drives elimination behavior. A safety-case certification workflow is proposed.


<details>
  <summary>Details</summary>
Motivation: To develop a rigorous evaluation methodology for testing how different deployment rules causally affect collective safety outcomes in multi-agent AI systems, addressing the gap of attributing behavioral changes to specific institutional rules rather than confounding factors.

Method: Institutional red-teaming: holds agents, objectives, and task state fixed; varies only one deployment rule. Instantiated in IABench-CA with 228 contexts, 5 canonical rules, 7 model populations (33,924 games), using a cooperative reference and auto-labelled reasoning traces. Includes anonymization ablation experiments and repeated-play analysis.

Result: (1) Deployment rules causally alter safety by 22-58pp mean fatality changes. (2) No universally safe default; regressive identity-targeting is never decisively safest and eliminates the least-resourced agent in 30-87% of games. (3) Identity salience is the mechanism—merely naming the loss bearer in rule text increases targeting from 22% to 81%; anonymization only delays targeting under repeated play.

Conclusion: Institutional red-teaming provides a causal evaluation framework for deployment rules. The findings demonstrate critical risks of identity-targeting rules and the absence of safe defaults across populations, leading to a proposed safety-case workflow with provisional rule regions, residual risks, and monitoring obligations.

Abstract: We introduce institutional red-teaming, an evaluation methodology for testing deployment rules in multi-agent AI: hold the agents, objectives, and task state fixed, vary only one rule, and attribute the resulting change in collective behavior to that rule. We instantiate the methodology in IABench-CA, a consequence-allocation benchmark spanning 228 contexts, five canonical rules, and seven model populations (33,924 games), with a normative cooperative reference and auto-labelled reasoning traces. Three findings emerge. (1) Deployment rules causally alter collective safety: changing only the consequence rule moves mean fatality by 22 to 58 percentage points within every population. (2) There is no safe default, but the targeting hazard is universal: the safest rule, the least-safe rule, and even the direction of the incidence effect vary across populations, yet regressive identity-targeting is never decisively safest in any context for any population, eliminates the least-resourced agent in 30-87% of games everywhere, and is selection-unsafe relative to the cooperative reference for all seven populations. (3) Identity salience is the mechanism: a one-shot anonymization ablation on the most exploitation-prone population (gpt-5.1) shows that merely naming the loss bearer in the rule text drives targeted elimination from 22% to 81% at identical payoffs; under repeated play, anonymization only delays the targeting, as agents re-infer the hidden rule from observed eliminations. We package the methodology as a safety-case workflow that certifies a provisional rule region $Φ(c,P)$ per deployment context and population, with explicit residual risks and monitoring obligations.

</details>


<div id='eess.SP'></div>

# eess.SP [[Back]](#toc)

### [111] [A Note on the Orthogonalization of Real-valued Trigonometrical Basis Functions](https://arxiv.org/abs/2607.06571)
*R. J. Cintra*

Main category: eess.SP

TL;DR: This paper derives three orthogonal real matrices from the Fourier matrix using elementary methods and an intuitive approach.


<details>
  <summary>Details</summary>
Motivation: To provide an intuitive and elementary derivation of orthogonal real matrices from the Fourier matrix.

Method: Uses only elementary methods to derive three orthogonal real matrices based on the Fourier matrix.

Result: Three orthogonal real matrices are derived from the Fourier matrix.

Conclusion: The derivation can be achieved intuitively using only elementary methods.

Abstract: Adopting an intuitive approach, this letter shows the derivation of three orthogonal real matrices based on the Fourier matrix. Only elementary methods are employed.

</details>


### [112] [A Multi-Analyst LLM Pipeline for Auditable Rule Discovery Across 68 Public Physiological Corpora](https://arxiv.org/abs/2607.06802)
*Dovy Paukstys*

Main category: eess.SP

TL;DR: This paper presents a controlled four-analyst LLM workflow that converts 68 public physiological corpora into an auditable library of 436 unique candidate rule shapes for detector design, with 94 components identified as ready for immediate hardware validation.


<details>
  <summary>Details</summary>
Motivation: Open physiological corpora are heterogeneous and do not directly specify which detector rules should be built for new contactless monitoring platforms, motivating the need for a systematic conversion pipeline.

Method: Four independent commercial LLM families analyzed 68 public corpora under controlled prompts, producing 695 candidate rule markers that were deduplicated to 649, audited for sanity violations, and consolidated into 436 unique rule shapes, then gate-tagged against hardware invariants.

Result: 695 candidate rule markers, 649 after deduplication, 51 sanity violations flagged, 436 unique rule shapes, and 94 build-now detector components identified across four detector-family buckets.

Conclusion: The pipeline produces an auditable engineering cascade (not a validated clinical detector) that routes literature-derived rules toward prospective hardware validation through analyst disagreement, threshold checks, curator review, and CI checks.

Abstract: Open physiological corpora are heterogeneous: they use different sensors, labels, sampling rates, recording settings, and clinical endpoints. They can support detector design, but they do not directly specify which detector rules should be built for a new contactless monitoring platform. We report a controlled four-analyst large-language-model (LLM) workflow for converting 68 public physiological corpora, screened for commercial-use compatibility, into an auditable library of candidate rule shapes for prospective validation. Four independent commercial LLM families read the corpus documentation under a controlled prompt and produced 695 candidate rule markers (top-markers). Deduplication retained 649 rule records; a threshold-bounds audit then flagged 51 sanity violations for clamping or curator review. Cross-corpus consolidation produced 436 unique rule shapes. Gate-tagging against two hard invariants, native target-hardware channel availability and no multi-night per-patient personalization, identified 94 build-now detector components across four detector-family buckets. The pipeline does not produce a validated clinical detector. It produces an auditable engineering cascade in which analyst disagreement, threshold checks, curator review, and automated continuous-integration (CI) checks route literature-derived rules toward prospective hardware validation.

</details>


### [113] [Iterative Optimization of Reconfigurable Intelligent Surface Aided Single-Carrier Spatial Modulation](https://arxiv.org/abs/2607.06859)
*Kentaro Nagaya,Shinya Sugiura*

Main category: eess.SP

TL;DR: This paper combines RIS with spatial modulation in CP-SC transmission, formulates DCMC capacities, derives closed-form gradients for RIS phase shifts, and uses gradient-ascent for iterative optimization, outperforming conventional RIS benchmarks in frequency-selective fading channels.


<details>
  <summary>Details</summary>
Motivation: To improve spectral efficiency and capacity in frequency-selective fading channels by integrating reconfigurable intelligent surfaces (RIS) with spatial modulation in a cyclic-prefixed single-carrier transmission scheme.

Method: Proposes a RIS-aided CP-SC spatial modulation scheme; formulates DCMC capacity; derives closed-form gradients of capacity w.r.t. RIS phase shifts; applies gradient-ascent algorithm for iterative phase optimization.

Result: The proposed iterative gradient-based optimization enhances DCMC capacity and outperforms conventional RIS benchmarks; convergence behavior and sensitivity of the algorithm are analyzed.

Conclusion: The proposed RIS-aided CP-SC spatial modulation with gradient-ascent optimization effectively improves capacity, offering a promising approach for RIS-assisted wireless communications in frequency-selective environments.

Abstract: This paper proposes a novel cyclic-prefixed single-carrier transmission scheme that amalgamate a reconfigurable intelligent surface (RIS) with spatial modulation in the frequency-selective fading channel. The discrete-input continuous-output memoryless channel's~(DCMC) capacities of the proposed schemes are formulated, while their gradients with respect to the RIS phase shifts are derived in the closed form. Then, the gradients are used for iteratively and efficiently optimizing the proposed RIS-aided schemes with the aid of the gradient-ascent algorithm. Our performance results demonstrate that the proposed iterative algorithm enhances the DCMC capacity of the schemes, while outperforming the conventional RIS benchmarks. Moreover, the convergence behavior and sensitivity in the proposed iterative algorithm are analyzed.

</details>


### [114] [Performance Limits of FRIS Systems in Nakagami-$m$ Fading](https://arxiv.org/abs/2607.06861)
*Masoud Khazaee,Felipe A. P. de Figueiredo,Rausley A. A. de Souza,Farshad Rostami Ghadi,Kai-Kit Wong,Luciano L. Mendes,Fernando D. Almeida García*

Main category: eess.SP

TL;DR: This letter develops a rigorous analytical framework for FRIS-assisted wireless systems over arbitrarily correlated Nakagami-$m$ fading, deriving the first strict outage probability lower bounds that outperform CLT approximations in the high-SNR regime.


<details>
  <summary>Details</summary>
Motivation: Fluid reconfigurable intelligent surfaces (FRIS) enhance wireless link reliability via spatial decorrelation, but performance analysis is challenging due to the sum-product structure of cascaded channels, motivating the need for a rigorous analytical framework.

Method: The authors introduce a physically consistent correlation model for Nakagami-$m$ fading, derive tractable statistical characterizations for the cascaded channel, and compute rigorous lower bounds for outage probability.

Result: The proposed outage probability lower bound is the first strict bound reported for FRIS under arbitrarily correlated Nakagami-$m$ fading, and it yields tighter characterization than CLT approximation in the high-SNR regime.

Conclusion: The proposed bound provides both rigorous performance guarantees and improved accuracy, serving as a reliable benchmark for FRIS system design.

Abstract: Fluid reconfigurable intelligent surfaces (FRIS) have recently emerged as a promising technology for enhancing wireless link reliability through spatial decorrelation. However, their performance analysis remains challenging due to the sum-product structure of the cascaded channel. This letter develops a rigorous analytical framework for FRIS-assisted wireless systems over arbitrarily correlated Nakagami-$m$ fading channels. Specifically, we introduce a physically consistent correlation model for Nakagami-$m$ fading and derive tractable statistical characterizations for the cascaded channel. These results lead to rigorous lower bounds for the outage probability (OP), with a simplified expression also obtained for the independent and identically distributed case. To the best of our knowledge, these are the first strict OP lower bounds reported for an FRIS-aided wireless system under arbitrarily correlated Nakagami-$m$ fading. CLT- and Gamma-based approximations are included as benchmark methods. Notably, the numerical results show that the proposed OP bound not only provides rigorous performance guarantees but also yields a noticeably tighter OP characterization than the CLT approximation in the high-SNR regime.

</details>


### [115] [Bessel Beam Optimization for Near-Field THz Communications under UE Location Uncertainty](https://arxiv.org/abs/2607.07069)
*Aditya Jolly,Vitaly Petrov,Gábor Fodor,Emil Björnson*

Main category: eess.SP

TL;DR: This paper proposes low-complexity closed-form approximations for optimal Bessel-like near-field beam configurations in THz MIMO systems under imperfect UE location knowledge, achieving near-optimal performance (within 0.1% of exhaustive search) with O(1) complexity.


<details>
  <summary>Details</summary>
Motivation: At THz frequencies, conventional far-field assumptions break down, and many UEs operate in the radiative near-field zone. Novel near-field beam synthesis methods are needed, especially under imperfect UE location knowledge.

Method: The authors formulate a spectral efficiency maximization problem with respect to the "Bessel cone angle" and derive closed-form approximations for optimal Bessel beam configuration under three scenarios: (i) deterministic UE location, (ii) Gaussian error, and (iii) uniform error in UE location.

Result: Simulations across multiple frequencies, UE locations, and array sizes show the proposed closed-form approximations match exhaustive search performance within 0.1% difference, while reducing configuration complexity to O(1).

Conclusion: The proposed simple closed-form approximations achieve near-optimal performance with drastically reduced complexity, making them practical for real-world THz near-field beamforming.

Abstract: To achieve the desired coverage and capacity levels, future terahertz (THz) wireless systems are envisioned to utilize extremely large antenna arrays. At THz frequencies, the combination of short wavelengths and large array apertures often makes many of the conventional far-field assumptions invalid in practice. As a result, many UEs operate in the radiative near-field zone, where novel near-field beam synthesis methods become viable. This paper studies phase-only Bessel-like near-field beam configurations for downlink THz multiple-input multiple-output links under imperfect UE location knowledge. We first formulate a spectral efficiency maximization problem with respect to the "Bessel cone angle''. We then derive low-complexity closed-form approximations for the optimal Bessel beam configuration for: (i)deterministic UE location; (ii)Gaussian and (iii)uniform error in the UE location. Finally, through extensive simulations across multiple signal frequencies, UE locations, and array sizes, we show that our proposed simple closed-form approximations closely match (under 0.1% difference) the best performance achieved via exhaustive search, while simultaneously reducing the configuration complexity down to as low as O(1).

</details>


### [116] [Flow-PIN: A Two-Stage Power-Flow-Guided Method for System-Wide Multivariate Profile Inpainting in Distribution Networks](https://arxiv.org/abs/2607.07250)
*Zhenghao Zhou,Yiyan Li,Yike Guo,Xinyi Ma,Zheng Yan,Mo-Yuen Chow*

Main category: eess.SP

TL;DR: Flow-PIN is a two-stage physics-guided framework using conditional flow matching and power-flow-guided refinement for high-fidelity multivariate profile inpainting in power distribution systems, outperforming ten baselines in accuracy, temporal fluctuation capture, and spatial topological correlation preservation.


<details>
  <summary>Details</summary>
Motivation: Existing deep generative models (GAN, Diffusion) for missing data restoration lack physical guarantees, producing "realistic" but not "accurate" results for power distribution system measurement data.

Method: A two-stage framework: (1) conditional flow matching with topological/correlation graph conditioning and physical penalty in loss; (2) topology-aware power-flow-guided refiner using Laplacian positional encoding and differentiable correlation alignment with AC power flow equations.

Result: Flow-PIN achieves high-fidelity profile inpainting across numerical accuracy, temporal fluctuation capture, and spatial topological correlation preservation, outperforming ten baselines on an active distribution network benchmark.

Conclusion: Integrating physical laws into generative models (via flow matching and power-flow refinement) significantly improves measurement data inpainting quality in power distribution systems.

Abstract: High-quality system measurement data is critical for power distribution system operation. As deep generative models (e.g., GAN, Diffusion, etc.) have been widely studied to solve the missing data restoration problem to enhance the data quality, their results may look "realistic" but not sufficiently "accurate" due to lacking physical guarantees. To address this limitation, a two-stage physics-guided framework, Flow-PIN, is proposed in this paper for system-wide multivariate profile inpainting. The first stage employs a conditional flow matching model, conditioned on topological and correlation graphs, to generate candidate values. A physical penalty is integrated into the loss function to constrain the generative vector field based on grid physical laws. The second stage introduces a topology-aware power-flow-guided refiner that utilizes Laplacian positional encoding to inject topology information into node embeddings. By coupling alternating current power flow equations with a differentiable correlation alignment mechanism, this refiner further corrects numerical deviations. Evaluations on an active distribution network dataset benchmark the proposed framework against ten representative baselines. The results show that Flow-PIN achieves high-fidelity profile inpainting across three dimensions: maximizing numerical accuracy, capturing temporal fluctuation, and preserving spatial topological correlations.

</details>


### [117] [Blockage-Robust Beamforming for Near-Field Communications: From Single-Airy to Multi-Airy](https://arxiv.org/abs/2607.07278)
*Yi Wang,Linglong Dai*

Main category: eess.SP

TL;DR: The paper proposes a multi-Airy beamforming scheme that uses multiple coordinated Airy beams from sub-arrays to overcome the sensitivity of single-Airy beamforming to geometry estimation errors in near-field communications, achieving more robust blockage circumvention and higher achievable rates.


<details>
  <summary>Details</summary>
Motivation: Single-Airy beamforming is highly sensitive to estimation errors of transmitter-obstacle-receiver geometry; even small errors cause mismatched trajectories and severe performance loss.

Method: Reveals the sensitivity mechanism of single-Airy beamforming, then extends to a coordinated multi-Airy generation method by deriving phase offsets for coherent combining. The transmit array is partitioned into sub-arrays, each configured with a tailored Airy beam such that multiple curved trajectories coherently combine at the target user.

Result: Simulations verify the sensitivity of single-Airy beamforming and the robustness of multi-Airy beamforming under geometry estimation errors. The proposed scheme also achieves higher achievable rates than single-Airy beamforming in blocked scenarios even without estimation errors.

Conclusion: Multi-Airy beamforming is a promising approach for blockage-robust near-field communications, offering both robustness to geometry errors and improved performance over single-Airy methods.

Abstract: High-frequency communications strongly depend on the line-of-sight (LoS) path, and obstacle blockage can severely degrade the received signal power and achievable rate. Near-field Airy beams with curved trajectories can circumvent obstacles, offering a promising way to alleviate blockage. However, since an Airy beam carries most useful energy along a single curved trajectory, existing Airy beamforming methods are highly sensitive to estimation errors of transmitter-obstacle-receiver geometry. That is to say, even a small error in the estimated geometry may cause the mismatched Airy trajectory, leading to severe performance loss. To address this problem, we propose a multi-Airy beamforming scheme for blockage-robust near-field communications. Specifically, we first reveal and analyze the sensitivity mechanism of single-Airy beamforming. This mechanism motivates us to extend the single-Airy generation method to a coordinated multi-Airy generation method by deriving the phase offsets required to coherently combine multiple Airy beams at the target user. Based on this coordinated generation method, we partition the transmit array into multiple sub-arrays and configure a tailored Airy beam for each sub-array, so that the resulting Airy beams formed by multiple curved trajectories can be coherently combined at the target user. Simulation results verify the sensitivity of single-Airy beamforming and the robustness of multi-Airy beamforming under estimation errors of transmitter-obstacle-receiver geometry. Moreover, the proposed scheme achieves higher achievable rates than single-Airy beamforming in blocked scenarios without geometry estimation errors.

</details>


### [118] [Design and Deployment Guidelines for UAV-Mounted RIS Under Position Uncertainty](https://arxiv.org/abs/2607.07298)
*Kevin Weinberger,David Müller,Martin Mönnigmann,Aydin Sezgin*

Main category: eess.SP

TL;DR: This paper develops a Gaussian uncertainty model (GUM) framework for UAV-mounted RIS channels, showing that phase uncertainty from UAV positioning errors causes exponential coherence loss, and introduces a performance-driven coherence threshold (PCT) to define the boundary where incoherent combining degrades performance. The PCT is then used to derive optimal UAV-mounted RIS placement, which deviates from conventional RIS placement intuition.


<details>
  <summary>Details</summary>
Motivation: UAV-mounted RIS offers dynamic wireless control for 6G, but UAV positioning uncertainties distort channel phase alignment, degrading coherent combining. This work aims to model and quantify this uncertainty impact.

Method: A GUM-based uncertainty propagation framework maps UAV position uncertainty through a geometric Tx-RIS-Rx model into the complex cascaded channel. A closed-form stochastic propagation model captures nonlinear phase uncertainty effects, and a performance-driven coherence threshold (PCT) is introduced.

Result: Phase uncertainty induces exponential coherence loss, dominating performance degradation. Monte Carlo simulations and analytical scaling laws confirm the PCT's accuracy in capturing the coherence transition.

Conclusion: The validated PCT enables optimal UAV-mounted RIS placement, which under realistic positioning conditions significantly deviates from the conventional RIS intuition of placing RIS close to either Tx or Rx.

Abstract: UAV-mounted reconfigurable intelligent surfaces (RIS) are a promising enabler for 6G networks, offering dynamic control of wireless propagation for coverage enhancement, integrated sensing and communication (ISAC), and localization. By exploiting UAV mobility, RIS can maintain favorable line-of-sight links, improving channel quality in dynamic environments. However, UAV positioning uncertainties introduce channel distortions that degrade RIS phase alignment and coherent combining. This work develops a GUM-based uncertainty propagation framework for UAV-mounted RIS channels, mapping UAV position uncertainty through the geometric Tx-RIS-Rx model into the complex cascaded channel. We derive a closed-form stochastic propagation model capturing nonlinear phase uncertainty effects and quantify their impact on channel coherence. The results show that phase uncertainty induces exponential coherence loss, dominating performance degradation. To characterize this transition, we introduce a performance-driven coherence threshold (PCT) that defines the boundary where incoherent combining results in a predetermined performance loss. Results based on analytical scaling laws and Monte Carlo simulations confirm the tightness of the PCT in accurately capturing the coherence transition. This validated threshold is then leveraged to derive optimal UAV-mounted RIS placement, revealing that realistic positioning conditions significantly deviate from the conventional RIS intuition, which typically favors placement close to either the transmitter or receiver.

</details>


### [119] [Spatial Limits of Fluid Antenna Systems](https://arxiv.org/abs/2607.07333)
*Amy S. Inwood,Peter J. Smith,Rajitha Senanayake,Michail Matthaiou*

Main category: eess.SP

TL;DR: This paper analyzes Continuous Fluid Antenna Systems (CFAS) and derives scaling laws for high SNR/SINR probability, showing performance gains with increased dimensionality and region size, even under interference.


<details>
  <summary>Details</summary>
Motivation: To understand fundamental performance limits of fluid antenna systems by examining the high SNR probability (HSP) as a tractable metric, and to derive closed-form expressions that reveal how spatial diversity scales with system dimensions.

Method: Derives dimensional scaling laws for the HSP of a single-user CFAS, then applies the law to a two-antenna, two-user CFAS with MMSE combining, analyzing how performance changes with region size and dimensionality.

Result: Performance gains increase consistently with both dimensionality and region size. The scaling law remains accurate in the two-user case, and the per-user high SINR probability actually exceeds the single-user HSP despite added interference.

Conclusion: The scaling law shows that additional dimensions' influence is dominated by CFAS size and threshold; CFAS can outperform single-user bounds even with interference due to spatial diversity benefits.

Abstract: Continuous fluid antenna systems (CFASs) represent an upper bound on the spatial diversity performance of fluid antenna systems (FASs), achieved when antennas may be positioned anywhere within a defined spatial region. This article examines the fundamental relationships governing CFAS performance. The focus is on the probability that the signal-to-noise ratio (SNR) exceeds a prescribed high threshold, termed the high SNR probability (HSP). This is among the few FAS performance metrics that admit the derivation of closed-form expressions. Following a survey of recent analytical advances in FAS performance limits, a dimensional scaling law derived for the HSP of a single-user, single-antenna CFAS is examined. This law is then applied to the per-user high signal-to-interference-plus-noise ratio (SINR) probability of a two-antenna, two-user CFAS employing minimum mean-squared error (MMSE) combining. For both scenarios, performance gains are shown to increase consistently with both dimensionality and region size. Remarkably, the scaling law remains accurate in the two-user case, showing that, in both scenarios, the influence of additional dimensions is dominated by the CFAS size and considered threshold. Moreover, the per-user high SINR probability of the two-user system exceeds the single-user HSP, despite the addition of inter-user interference.

</details>


### [120] [Dual-Mapping Sparse Vector Coding for Phase Noise-Resilient Short-Packet Transmission](https://arxiv.org/abs/2607.07454)
*Yanfeng Zhang,Xi'an Fan,Xu Zhu,Yujie Liu,Jinkai Zheng,Weiwei Yang,Wen Wu,Tom H. Luan,Yong Liang Guan*

Main category: eess.SP

TL;DR: This paper proposes a dual-mapping sparse vector coding (DM-SVC) scheme that handles phase noise in short-packet communications by using distinct sparsity patterns for pilot and data symbols, achieving near-ideal performance with improved spectral efficiency.


<details>
  <summary>Details</summary>
Motivation: Existing sparse vector transmission (SVT) schemes assume negligible phase noise (PN), which is unrealistic in practical wireless systems. There is a need for robust short-packet communication under PN.

Method: DM-SVC maps pilot symbols onto multiple non-zero blocks and data symbols onto isolated non-zero elements within a single sparse vector, enabling pilot-data separation via sparsity patterns. It uses a basis expansion model for PN representation and an iterative joint PN estimation and data decoding algorithm.

Result: Simulation results show DM-SVC achieves block error rate performance close to perfect PN compensation, with improved spectral efficiency and reduced codebook storage overhead compared to state-of-the-art SVT schemes.

Conclusion: DM-SVC effectively handles phase noise in short-packet communications without explicit resource partitioning, offering both performance and efficiency gains over existing approaches.

Abstract: Sparse vector transmission (SVT) has emerged as a promising technique for ultra-reliable low-latency short-packet communications. However, existing SVT schemes typically assume negligible phase noise (PN), an assumption that rarely holds in practical wireless systems. In this paper, a dual-mapping sparse vector coding (DM-SVC) scheme is proposed for short-packet communications subject to PN. In DM-SVC, pilot symbols are mapped onto multiple non-zero blocks and data symbols onto isolated non-zero elements within a single sparse vector, thereby enabling pilot-data separation through distinct sparsity patterns rather than explicit resource partitioning. Moreover, the indices of pilot blocks convey additional information bits, further improving spectral efficiency. A basis expansion model is adopted to represent the PN process, substantially reducing the number of parameters to be estimated. Furthermore, an iterative joint PN estimation and data decoding algorithm is developed, where pilot block indices are first detected exploiting block-sparse priors, after which PN estimation and data decoding proceed iteratively. Simulation results show that DM-SVC could achieve block error rate performance close to that of perfect PN compensation, while offering improved spectral efficiency and reduced codebook storage overhead compared to state-of-the-art SVT schemes.

</details>


### [121] [Semantic Communications in the THz Band](https://arxiv.org/abs/2607.07455)
*Fatima Ismail,Hadi Sarieddeen,Jihad Fahs*

Main category: eess.SP

TL;DR: This paper combines deep learning-based semantic communication (DeepSC) with terahertz (THz) channels, showing that DeepSC models generalize well to THz fading channels with receiver compensation, and proposes a lightweight neural detector that outperforms traditional systems without requiring channel state information.


<details>
  <summary>Details</summary>
Motivation: To explore the integration of semantic communication with THz-band communications as algorithmic and spectral enablers for future wireless networks, addressing the challenge of THz channel impairments.

Method: DeepSC models trained under AWGN are tested on THz block- and fast-fading channels with receiver-side compensation; a lightweight neural detector without CSI is proposed as a fully data-driven reception method.

Result: At 0.3 THz, DeepSC outperforms a throughput-matched traditional coded system over 0-12 dB SNR, achieving over 50 percentage-point higher BLEU-1 score. The pilot-free detector outperforms MMSE equalization with both perfect and imperfect CSI and remains robust to frequency offsets up to 50 MHz.

Conclusion: Semantic communication demonstrates strong resilience to THz channel impairments, and the proposed CSI-free neural detector offers a robust, lightweight alternative for THz-band communications.

Abstract: Semantic and terahertz (THz)-band communications are algorithmic and spectral enablers of future wireless networks. This work investigates deep learning-based semantic communication (DeepSC) over THz channels. We show that DeepSC models trained solely under additive white Gaussian noise generalize well to the tested THz block- and fast-fading channels when receiver-side compensation is applied. To enable fully data-driven reception, we propose a lightweight neural detector that does not require channel state information (CSI). At 0.3 THz, DeepSC outperforms a throughput-matched traditional coded communication system baseline over 0-12 dB signal-to-noise ratio (SNR), achieving more than 50 percentage-point higher Bilingual Evaluation Understudy unigram (BLEU-1) score. The proposed pilot-free detector outperforms minimum mean square error (MMSE) equalization with both perfect and imperfect CSI and remains robust to frequency offsets up to 50 MHz, highlighting the resilience of semantic communication to THz channel impairments.

</details>


### [122] [5G Positioning Reference Signal impact assessment in Non-Terrestrial Networks communication service](https://arxiv.org/abs/2607.07466)
*Alejandro Gonzalez-Garrido,Ottavio M. Picchi,Francesco Menzione*

Main category: eess.SP

TL;DR: This paper analyzes coexistence of NR Positioning Reference Signals (PRS) with broadband data in 5G Non-Terrestrial Networks. It derives a statistical model for inter-satellite differential delays, validates it with orbit simulations, and shows PRS-based PNT can coexist with data downlink under proper PRS periodicity design.


<details>
  <summary>Details</summary>
Motivation: NTNs using LEO/MEO satellites can provide both broadband communications and PNT services via NR PRS, but large inter-satellite differential propagation delays may cause PRS from non-serving satellites to interfere with serving-satellite data streams, threatening coexistence.

Method: Derives a statistical model for slant-range distribution over visible spherical cap, extends to dual-shell constellations via mixture formulation yielding closed-form CDF of differential delay. Validates using 10-day orbit simulation of dual-shell European NTN constellation. Evaluates effective carrier-to-noise density ratio and uncoded BER for FR1/FR2 configurations via Monte Carlo simulations.

Result: Probability of simultaneous multi-PRS overlap remains below a few percent. PRS detection feasible despite data interference. When PRS is ~25 dB below data signal, impact on uncoded BER is negligible over wide range of repetition periods; BER degradation increases with PRS duty cycle.

Conclusion: NR-PRS-based PNT can coexist with broadband downlink in NTN under appropriate PRS periodicity design.

Abstract: 5G New Radio (NR) Non-Terrestrial Networks (NTNs) extend cellular connectivity through Low Earth Orbit (LEO) and Medium Earth Orbit (MEO) satellite constellations while enabling the reuse of downlink NR Positioning Reference Signals (PRS) to provide Positioning, Navigation, and Timing (PNT) services alongside broadband communications. However, the large inter-satellite differential propagation delays inherent to NTN geometry may cause PRS transmissions from non-serving satellites to overlap with the serving-satellite data stream. This paper analyzes this coexistence by deriving a statistical model for the slant-range distribution over the visible spherical cap and extending it to dual-shell constellations through a mixture formulation, yielding a closed-form cumulative distribution function (CDF) of the differential delay. The model is validated using a 10-day orbit simulation representative of a dual-shell European NTN constellation. Detection limits of non-serving satellite PRS under interference from the serving-satellite data stream are characterized in terms of the effective carrier-to-noise density ratio. The impact of periodic PRS transmissions on the uncoded bit error rate (BER) is also evaluated for standardized NR Frequency Range 1 (FR1) and Frequency Range 2 (FR2) configurations. Monte Carlo simulations show that the probability of simultaneous multi-PRS overlap remains below a few percent, depending on PRS duration and repetition period, while PRS detection remains feasible despite data interference. When the PRS is received about 25 dB below the data signal, its impact on uncoded BER is negligible over a wide range of repetition periods, whereas BER degradation increases with PRS duty cycle. These results demonstrate that NR-PRS-based PNT can coexist with broadband downlink in NTN under appropriate PRS periodicity design.

</details>


### [123] [Stability of Flow Models for Graph Signals](https://arxiv.org/abs/2607.07510)
*Martin Schmidt,Gonzalo Mateos*

Main category: eess.SP

TL;DR: This paper analyzes the stability of continuous normalizing flow models parameterized by GNNs for graph signal generation, proving permutation equivariance and deriving stability bounds; a regularized flow matching strategy that penalizes the Lipschitz constant is introduced and validated on synthetic and real-world fMRI data.


<details>
  <summary>Details</summary>
Motivation: While GNNs have known stability properties, it's unclear how structural errors propagate through continuous generative flow models for graph signals. The paper aims to bridge this gap.

Method: The authors theoretically prove permutation equivariance of continuous-time ODEs and their discrete approximations in GNN-based normalizing flows. They derive stability bounds on generated distributions and propose a regularized flow matching strategy that penalizes the spatial Lipschitz constant of the vector field.

Result: Experiments on synthetic SBM graph signals and real-world fMRI brain connectome data show the proposed bound-oriented regularization yields generative models more robust to structural noise without sacrificing output quality.

Conclusion: The stability-promoting regularization effectively improves robustness of graph signal generative models to structural perturbations while maintaining generation quality.

Abstract: Generating signals on graphs requires permutation-equivariant models that exhibit stability with respect to relative structural perturbations. While favorable stability properties of Graph Neural Networks (GNNs) have been well documented, it is unclear how structural errors propagate through the dynamics of continuous generative flow models that are gaining traction for graph signal generation. In this paper, we analyze continuous normalized flow models parameterized by GNNs and show that permutation equivariance is preserved for both the resulting continuous-time ordinary differential equations and their discrete numerical approximations used as graph signal samplers. Our primary contribution is to derive explicit stability bounds on the generated probability distributions, which quantify how relative graph perturbations affect the final sampled signals. Motivated by these theoretical bounds, we introduce a stability-promoting regularized flow matching strategy that actively penalizes the spatial Lipschitz constant of the vector field during model training. Experiments using synthetic smooth signals on stochastic block model graphs and real-world fMRI signals on brain connectomes demonstrate that this bound-oriented approach yields generative models that are more robust to structural noise, without sacrificing output quality.

</details>


### [124] [Energy Efficiency Optimization in Distributed MIMO vRAN via Cross-Layer Link Abstraction](https://arxiv.org/abs/2607.07520)
*Jaebum Park,Chan-Byoung Chae,Robert W. Heath*

Main category: eess.SP

TL;DR: This paper proposes an energy efficiency optimization framework for virtualized RANs with distributed MIMO, using joint modulation, rank, and power selection plus a traffic-aware mode-switching strategy, achieving 24% wider SIMO energy-saving range and higher EE than always-on MIMO baselines.


<details>
  <summary>Details</summary>
Motivation: vRAN runs compute-intensive MIMO baseband on shared servers, making energy efficiency a primary concern. Existing power models often overlook the combined effects of vDU processing, fronthaul transport, and radio unit operation.

Method: Builds a three-component power model (vDU baseband, fronthaul, radio-unit); formulates joint modulation, rank, and per-subcarrier power optimization with convex constraints via exponential effective SNR mapping; extends to traffic-aware switching among MIMO, SIMO, and sleep modes using real user trajectories from MATSim.

Result: RF-only models underestimate SIMO power-saving spectral efficiency range; the proposed power model extends this range by 24%. The traffic-aware strategy achieves higher average EE than always-on MIMO while maintaining comparable peak-hour throughput, with one-time offline calibration eliminating further link-level simulation.

Conclusion: Joint optimization of modulation, rank, and power with traffic-aware mode switching significantly improves vRAN energy efficiency, validated on 3GPP NR compliant channels with realistic traffic simulations.

Abstract: Virtualized radio access networks (vRAN) run the compute-intensive multiple-input multiple-output (MIMO) baseband as software on shared servers, which makes energy efficiency (EE) a primary design objective. Distributed MIMO vRAN consumes power across virtualized distributed unit (vDU) baseband, fronthaul transport, and per-radio-unit operation. We build a power model that resolves these three components. We then develop a framework that jointly selects modulation, transmission rank, and per-subcarrier power to maximize system EE. Exponential effective SNR mapping induces a convex per-subcarrier power constraint, which yields a convex power minimization problem with a closed-form waterfilling-like solution. We show that radio frequency-only models underestimate the spectral efficiency range where single-input multiple-output (SIMO) transmission saves power, and our power model extends this range by 24%. We further extend the framework to a traffic-aware setting with realistic user trajectories from the multi-agent transport simulator. We propose a traffic-aware strategy that switches each radio unit among MIMO, SIMO, and sleep modes based on demand. Simulation results over 3GPP NR compliant fading channels show that, after a one-time offline calibration, the framework predicts link performance without further link-level simulation. The proposed framework achieves higher average EE than a traffic-agnostic always-on MIMO baseline, while maintaining comparable throughput at peak hours.

</details>


### [125] [Six-Pole Dual-band Bandpass Filter for WiMAX Applications](https://arxiv.org/abs/2607.07661)
*Halimat Olamide Yusuf,Augustine O. Nwajana*

Main category: eess.SP

TL;DR: This paper presents a compact six-pole dual-band bandpass filter using Folded-Arms Square Open-Loop Resonator (FASOLR) on microstrip, centered at 2.3 GHz with two passbands at ~2.2 GHz and 2.4 GHz, designed for WiMAX applications.


<details>
  <summary>Details</summary>
Motivation: The growing demand for multi-band wireless communication systems necessitates compact dual-band bandpass filters that can isolate specific frequency sections within broader spectrum, addressing the need for efficient multi-band filtering solutions.

Method: A third-order single bandpass filter is transformed into a sixth-order dual-band bandpass filter using FASOLR microstrip planar structure, designed and simulated in PathWave ADS on Rogers RT/Duroid 6010LM substrate.

Result: The six-pole dual-band filter produces two passbands at ~2.2 GHz and 2.4 GHz with 7% fractional bandwidth, compact footprint of 30.56×20.56 mm², return loss >10 dB (first band) and >19 dB (second band), insertion loss <1.5 dB, with good agreement between circuit-level and EM simulations.

Conclusion: The proposed FASOLR-based dual-band bandpass filter achieves compact size with good electrical performance, validated through simulation agreement, and is suitable for WiMAX applications.

Abstract: Recent advances in multi-band wireless communication systems have driven the increasing need for dual-band bandpass filters. These types of filters are capable of isolating a small section of the frequency spectrum within a broader spectrum. Over the years, coplanar waveguide, microstrip, slotline, stripline, and other planar transmission line technologies have been widely employed in the design of microwave circuits and systems. This work employs a Folded-Arms Square Open-Loop Resonator (FASOLR) microstrip planar structure, designed and simulated using PathWave Advanced Design System (ADS). A third order (three-pole) single bandpass filter is transformed into a sixth order (six-pole) dual-band bandpass filter. The proposed six-pole dual-band bandpass filter is centred at 2.3 GHz, with a fractional bandwidth of 7% and produces two passbands centred at approximately 2.2 GHz and 2.4 GHz. The design is implemented on a commercially available Rogers RT/Duroid 6010LM substrate with a dielectric constant of 10.7, a loss tangent of 0.0023, a substrate thickness of 1.27 mm, and 35 micro meter copper cladding on both sides. The overall filter component has a compact footprint of 30.56 mm by 20.56 mm. The design is validated through comparison of circuit-level and electromagnetic (EM) simulation results, which show good agreement. The EM simulation responses indicate a return loss better than 10 dB on the first band and better than 19 dB on the second, and an insertion loss better than 1.5 dB, demonstrating its suitability for WiMAX applications.

</details>


<div id='eess.IV'></div>

# eess.IV [[Back]](#toc)

### [126] [Reconfigurable Radiology Labels Without Relabeling](https://arxiv.org/abs/2607.06597)
*Jean-Benoit Delbrouck,Dave Van Veen,Akash Pattnaik,Kalina Slavkova,Javid Abderezaei,Harris Bergman,Khan Siddiqui*

Main category: eess.IV

TL;DR: This paper presents a pipeline that converts free-text chest X-ray reports into multi-label matrices, allowing label schema reconfiguration through dictionary edits rather than expensive relabeling passes. Using MIMIC-CXR, they show 43% of studies contain findings outside CheXpert-14, and expanded labels reach 0.78 AUROC on long-tail findings.


<details>
  <summary>Details</summary>
Motivation: Existing CXR datasets use fixed, small label schemas (e.g., CheXpert-14) that miss many clinically relevant findings in free-text reports. Re-labeling corpora for new schemas is expensive and inefficient.

Method: They build a pipeline that converts free-text radiology reports into multi-label matrices via a one-time NLP pass, then enables schema reconfiguration through dictionary edits (keyword/phrase mapping) without re-running inference. Applied to MIMIC-CXR with a 58-label taxonomy.

Result: Reconfiguring MIMIC-CXR (223K reports) takes 196 seconds vs. $6.6K for relabeling with Claude Opus. 43% of CXR studies have findings outside CheXpert-14. Image probes on expanded labels match CheXpert-14 on shared targets and achieve 0.78 AUROC on long-tail labels.

Conclusion: Once reports are structured into label matrices, the label schema becomes a configurable dictionary edit rather than a corpus to relabel, offering a more flexible and cost-effective paradigm for radiology labeling.

Abstract: Public chest-radiograph (CXR) datasets are typically released with small, fixed label schemas such as CheXpert-14. However, the underlying free-text reports describe far more findings -- and which findings matter depends on the task, site, and reader. We release a pipeline that converts free-text reports into multi-label matrices and then reconfigures the label schema through dictionary edits rather than new inference passes, i.e., without relabeling the corpus. After this one-time pass, reconfiguring MIMIC-CXR (223K reports) from cached annotations takes 196 seconds with no API cost, compared to \$6.6K for an equivalent relabeling pass with Claude Opus 4.7. Using a 58-label taxonomy, we show that 43\% of CXR studies contain at least one finding outside CheXpert-14. Image probes trained on these labels match CheXpert-14 probes on shared targets while also reaching 0.78 AUROC on expert-reviewed long-tail labels that CheXpert-14 cannot represent. These results suggest a different unit of work for radiology labeling: once reports are structured, the label schema becomes a configuration to edit, not a corpus to relabel.

</details>


### [127] [Non-contact, Real-time, Heart-rate Measurement using Image Processing with Commodity Cameras and AI Agents](https://arxiv.org/abs/2607.06598)
*Kelly Li,Fulu Li*

Main category: eess.IV

TL;DR: This paper presents a non-contact, real-time heart rate measurement system using commodity cameras and image processing. The system uses face detection with 68 facial landmarks via deep learning, a time sliding window for noise reduction, and signal periodicity to compute heart rate. It is tested against Apple Watch measurements with promising results.


<details>
  <summary>Details</summary>
Motivation: Heart rate monitoring is critical for elderly health care, but traditional methods rely on contact sensors (hospital devices, Apple Watch). The authors aim to develop a non-contact, accessible solution using commodity cameras for real-time health monitoring.

Method: The system has four steps: (1) identify camera FPS (e.g., 30 fps), (2) face detection with 68 facial landmarks using deep learning, (3) time sliding window (TSW) algorithm for signal denoising, and (4) heart rate computation based on signal periodicity.

Result: The prototype was tested against Apple Watch heart rate measurements on the same person at the same time. The difference range was analyzed across multiple rounds, and the mean difference was computed.

Conclusion: The system shows feasibility for non-contact heart rate monitoring. Future work includes further tuning, optimization, and deployment as a personal AI agent for health monitoring.

Abstract: Heart rate measurement is one of the key requirements for real-time health monitoring, in particular for health caring of elderly people. Traditional heart rate measurement relies on contact sensing mechanisms such as some heart rate measurement devices at medical hospitals or some wearable devices with embedded sensors such as Apple Watch, etc. In this paper, we develop a system for non-contact, real-time, heart rate measurement using image processing with commodity cameras such as an embedded camera on a laptop, where we use an innovative algorithm to capture the relevant signals for the computation of heart rate in a time series in real life environments. The presented heart rate computation (HRC) process is composed with four major steps: (a) identify frames per second of the camera in use, i.e., 30 frames per second for a given camera, (b) face detection (FD) with shape predictor of 68 face landmarks using deep learning (DL) method, (c) time sliding window (TSW) algorithm to de-noise the signal by smoothing out the noise, and (d) compute heart rate based on identified signal periodicity. We test and analyze the developed prototypes against heart rate results by Apple Watch and check the difference range in multiple rounds and compute the mean of the difference for the measurement values of the heart rate of the same person at the same time. We will do further tuning and optimization of the present methods and deploy the system as a personal AI agent [6] for health monitoring as our future directions.

</details>


### [128] [Format-Controlled Multi-Scale JPEG Compression Response Analysis for Image-Level Forgery Screening](https://arxiv.org/abs/2607.06615)
*Sujith K Mandala*

Main category: eess.IV

TL;DR: A lightweight, CPU-only image forgery screening pipeline using multi-scale Error Level Analysis and handcrafted features with gradient boosted trees, achieving AUC=0.990 on format-controlled CASIA v2.0 without relying on GPU or deep learning.


<details>
  <summary>Details</summary>
Motivation: Deep-learning forgery localization methods are GPU-heavy and computationally expensive, motivating a lightweight, interpretable, CPU-only alternative for image-level forgery screening.

Method: Proposes a 405-dimensional feature vector combining multi-scale ELA (7 JPEG quality levels), cross-quality ELA ratios, spatial entropy, FFT energy bands, edge density, SRM residuals, and DCT blockiness, classified by gradient boosted trees.

Result: AUC=0.990 (F1=0.905) on JPEG-only CASIA v2.0 subset (5-fold cross-validation); AUC=0.976 under source-aware group split; multi-scale ELA contributes +0.180 AUC gain over single-quality ELA.

Conclusion: The method detects compression-history inconsistencies rather than file-format shortcuts, offers feature-level interpretability, CPU-only deployment, and sub-second inference.

Abstract: Image forgery detection is a critical task in digital forensics, yet many deep-learning localization approaches are typically GPU-accelerated and computationally heavier than handcrafted screening methods. We propose a lightweight, interpretable feature engineering pipeline for image-level forgery screening using only CPU computation and gradient boosted trees. Our method introduces \emph{multi-scale Error Level Analysis} (ELA) computed at seven JPEG quality levels, combined with novel \emph{cross-quality ELA ratio} features that capture double-compression artifacts characteristic of spliced regions, augmented by spatial entropy, FFT energy bands, edge density, SRM residuals, and DCT blockiness, yielding a 405-dimensional feature vector. CASIA v2.0 contains a format confound (60\% of tampered images are TIFF while authentic images are JPEG/BMP and contain no TIFF samples), enabling a trivial \texttt{is\_tiff} classifier to reach 0.80 AUC. We address this through rigorous format-controlled evaluation: on the JPEG-only subset (9,501 images, eliminating the TIFF/JPEG container confound), our method achieves AUC~=~0.990 [95\% CI: 0.988--0.991] and F1~=~0.905 using 5-fold stratified cross-validation. Under a conservative source-aware group split (preventing related images from appearing in both train and test), AUC remains 0.976. An ablation study reveals that multi-scale ELA provides the dominant gain (+0.180 AUC over single-quality on the format-controlled subset), while cross-quality ratios provide complementary double-compression detection. These results support that the method detects compression-history inconsistencies rather than file-format shortcuts -- while offering feature-level interpretability, CPU-only deployment, and sub-second inference.

</details>


### [129] [From Data Completeness to Data Sufficiency: A Task-Driven Imaging Framework for Intraoperative CBCT under Quality-Time-Dose Trade-offs](https://arxiv.org/abs/2607.07039)
*Yi Jia,Rongjun Ge,Yang Chen,Yan Xi,Wenjun Xia*

Main category: eess.IV

TL;DR: This review argues that complete data is mathematically impossible in mobile C-arm CBCT with single circular trajectory, and proposes shifting from "data completeness" to "data sufficiency" to balance image quality, imaging time, and radiation dose for specific clinical tasks.


<details>
  <summary>Details</summary>
Motivation: Current practice mechanically applies fan-beam CT criteria to CBCT, pursuing unattainable "data completeness" while worsening trade-offs among image quality, time, and dose.

Method: A review that reframes CBCT evaluation from "data completeness" to "data sufficiency," synthesizing evidence from multiple clinical scenarios.

Result: Complete data is mathematically unattainable under single circular trajectory CBCT; approximation errors are acceptable when clinical decision-making requirements are met.

Conclusion: Adopting a "data sufficiency" perspective can achieve a better Q-T-D balance in intraoperative CBCT.

Abstract: Mobile C-arm cone-beam computed tomography (CBCT) has been widely used for real-time intraoperative 3D imaging. However, current practice often mechanically applies the fan-beam CT criterion of "180° plus fan angle" in pursuit of "data completeness" in reconstruction. This review argues that, under the single circular trajectory of three-dimensional cone-beam geometry, complete data are mathematically unattainable; moreover, blindly increasing sampling may exacerbate the trade-off among intraoperative image quality (Q), imaging time (T), and radiation dose (D). Against this background, this review reframes the evaluation of intraoperative CBCT around "data sufficiency" rather than "data completeness." This perspective moves beyond the excessive pursuit of absolute mathematical and analytic accuracy, and instead emphasizes task-specific minimum image-quality thresholds required for clinical decision-making. By synthesizing evidence from multiple clinical scenarios, this review suggests that approximation errors can be acceptable when clinical decision-making requirements are satisfied, thereby achieving a Q-T-D balance.

</details>


### [130] [Towards Accurate and Fast Clinical Body Composition: A Resource-Efficient Hierarchical Segmentation Framework for Multi-Source CT](https://arxiv.org/abs/2607.07177)
*Xiaodi Shen,Qingzhu Zheng,Yaoyang Qiu,Cien Fan,Ruonan Zhang,Yangdi Wang,Luyao Wu,Weikai Zheng,Longfei Zhao,Bing Li,Rulin Xu,Qiqi Xu,Ren Mao,Shiting Feng,Xuehua Li*

Main category: eess.IV

TL;DR: A coarse-to-fine hierarchical framework for automated 3D segmentation of muscles and adipose tissue from CT, optimized for CPU-only deployment with low memory usage, achieving high Dice scores (0.924–0.982) and fast inference (44.5s/volume, 4.73 GB peak memory) on multi-source heterogeneous data.


<details>
  <summary>Details</summary>
Motivation: Automated 3D segmentation of muscles and adipose tissue from CT is critical for body composition analysis, but faces challenges from multi-source data heterogeneity and high CPU memory demands that hinder clinical deployment.

Method: A coarse-to-fine hierarchical framework segmenting ten tissue structures, using Dynamic Spacing and Anisotropic Patching for efficiency, Group Inference for low-memory sliding-window processing, and Topology-Aware Asymmetric Resampling for fast post-processing.

Result: Trained on 1,558 CT volumes (7 public + 2 private datasets), evaluated on 105 independent test cases. Per-structure Dice: 0.924–0.982. Eight structures met ±10% relative error clinical limit. CPU-only pipeline: 44.5s/volume, 4.73 GB peak memory on a 12-core workstation.

Conclusion: The framework balances accuracy and efficiency, enabling robust, large-scale body composition analysis on standard CPU workstations without GPU dependency.

Abstract: Background: Automated 3D segmentation of muscles and adipose tissue from CT is vital for body composition analysis, but multi-source data heterogeneity and high CPU memory demands hinder clinical deployment.
  Methods: We propose a coarse-to-fine hierarchical framework to segment ten tissue structures. Efficiency is optimized using Dynamic Spacing and Anisotropic Patching, a Group Inference mechanism for low-memory sliding-window processing, and Topology-Aware Asymmetric Resampling for fast post-processing.
  Results: The framework was trained on 1,558 CT volumes from seven public and two private datasets, and evaluated on an independent test cohort (N=105), per-structure Dice coefficients ranged from 0.924 to 0.982. Eight major structures met the +-10% relative error clinical acceptance limit. On a 12-core CPU workstation, the GPU-free pipeline averaged 44.5 seconds per volume with 4.73 GB peak memory.
  Conclusion: This framework balances accuracy and efficiency, enabling robust, large-scale body composition analysis on standard CPU workstations.

</details>


<div id='cs.IT'></div>

# cs.IT [[Back]](#toc)

### [131] [Near-Optimal Lower Bounds on One-Bit Compressed Sensing of Approximately Sparse Signals](https://arxiv.org/abs/2607.06750)
*Junren Chen,Arya Mazumdar,Ming Yuan*

Main category: cs.IT

TL;DR: This paper provides nearly optimal lower bounds for one-bit compressed sensing of approximately sparse signals, achieving matching bounds of $\widetilde{\Omega}((k/m)^{1/3})$ for $\ell_1$ sparsity and extending to $\ell_q$ balls with smooth interpolation between exact and $\ell_1$ sparsity.


<details>
  <summary>Details</summary>
Motivation: To establish tight lower bounds for one-bit compressed sensing under approximately sparse (not just exactly $k$-sparse) signals, bridging the gap between existing upper bounds and unknown fundamental limits.

Method: Embed a small Euclidean ball into the signal set (using a lifting map for the canonical model, directly for dithered model), then construct two indistinguishable signals separated by Euclidean distance at least $(k/m)^{1/3}$ up to log factors.

Result: First near-optimal lower bounds $\widetilde{\Omega}((k/m)^{1/3})$ for $\ell_1$ sparsity; extended bounds $\widetilde{\Omega}((k/m)^{\frac{2-q}{2+q}})$ for $\ell_q$ balls ($q\in[0,1]$); extensions to sub-Weibull matrices, adversarial bit flips, and matrix recovery.

Conclusion: The lower bounds nearly match known upper bounds, establishing near-optimality; the framework generalizes across sparsity models, noise models, and other recovery settings.

Abstract: This paper provides the first near-optimal lower bounds for one-bit compressed sensing of approximately sparse signals lying in a scaled $\ell_1$ ball, which is a commonly adopted relaxation of the exactly $k$-sparse assumption. In prior works, the best known upper bounds on uniform Euclidean error are of order $\widetilde{O}((k/m)^{1/3})$, where $m$ is the number of measurements. Under sub-Gaussian matrices, we establish nearly matching lower bounds for both the canonical one-bit compressed sensing model and the uniformly dithered model. Our argument is to first embed a small Euclidean ball into the signal set, which is straightforward for the dithered model but relies on a lifting map for the canonical model, and then construct two signals in this small ball that are separated in Euclidean distance by at least $(k/m)^{1/3}$ (up to logarithmic factor) but are indistinguishable from the binary measurements. Moreover, our argument extends to approximately sparse signals that live in a properly scaled $\ell_q$ ball $(q\in [0,1])$, yielding a lower bound $\widetildeΩ((k/m)^{\frac{2-q}{2+q}})$ that smoothly bridges the cases of exact sparsity ($q=0$) and $\ell_1$ sparsity ($q=1$). Finally, we discuss the extensions of our lower bounds to sub-Weibull matrices, adversarial bit flipping, matrix recovery, and characterize the transition to the non-sparse case.

</details>


### [132] [AirPASS: Over-the-Air Federated Learning via Pinching Antenna Systems](https://arxiv.org/abs/2607.06768)
*Seyed Mohammad Azimi-Abarghouyi,Christopher G. Brinton*

Main category: cs.IT

TL;DR: This paper proposes AirPASS, an alternating optimization framework for over-the-air federated learning (AirFL) in systems with multi-waveguide pinching antenna systems (PASS), which jointly optimizes device selection, receive beamforming, and pinching-antenna placement. It uses a homotopy-Riemannian margin-consolidation method and a homotopy-assisted geometry optimization method to handle the nonconvex problem, outperforming conventional MIMO baselines.


<details>
  <summary>Details</summary>
Motivation: To improve over-the-air federated learning performance by leveraging multi-waveguide pinching antenna systems (PASS) while jointly optimizing device selection, beamforming, and antenna placement under nonconvex constraints.

Method: AirPASS: alternating optimization framework with two components—(1) homotopy-Riemannian margin-consolidation for device selection and receive beamforming under fixed PASS configuration; (2) homotopy-assisted geometry optimization for updating pinching-antenna positions under fixed selected devices and beamformer.

Result: AirPASS consistently outperforms conventional co-located MIMO baselines, remains close to ideal FedAvg, and achieves an attractive performance-complexity tradeoff relative to SDR-DC and matching-pursuit scheduling alternatives.

Conclusion: The proposed AirPASS framework effectively addresses the nonconvex joint optimization problem in AirFL with PASS, offering superior performance and practical tradeoffs for wireless federated learning systems.

Abstract: This paper investigates over-the-air federated learning (AirFL) in wireless systems where the access point is equipped with a multi-waveguide pinching antenna system (PASS). We adopt the widely studied learning-oriented AirFL formulation, which seeks to maximize the number of selected devices while keeping the aggregation distortion below a prescribed threshold. The resulting joint optimization of device selection, receive beamforming, and pinching-antenna placement is highly nonconvex due to the intricate coupling among these system variables. To address this challenge, we develop AirPASS, an alternating optimization framework with two main components: a homotopy-Riemannian margin-consolidation method for device selection and receive beamforming under fixed PASS configuration, and a homotopy-assisted geometry optimization method for updating the pinching-antenna positions under fixed selected devices and beamformer. Experiments show that AirPASS consistently outperforms conventional co-located MIMO baselines, remains close to ideal FedAvg, and achieves an attractive performance-complexity tradeoff relative to SDR-DC and matching-pursuit scheduling alternatives.

</details>


### [133] [Four classes of few-weight self-orthogonal codes and their applications for LCD codes and quantum codes](https://arxiv.org/abs/2607.07181)
*Yue Huang,Zhonghao Liang,Chenlu Jia,Yongkang Wan,Qunying Liao*

Main category: cs.IT

TL;DR: This paper constructs projective four-weight self-orthogonal codes and three classes of four-weight self-orthogonal codes using augmented codes from defining sets, then constructs LCD codes and quantum codes from them, with some being almost optimal or AMDS.


<details>
  <summary>Details</summary>
Motivation: Self-orthogonal codes, few-weight codes, LCD codes, and quantum codes have important applications in coding theory and cryptography, motivating the construction of new such codes.

Method: The authors consider two classes of defining sets and their corresponding augmented codes, building upon the augment code notion introduced by Heng et al. (2024) and defining sets by Ding et al. (2007).

Result: Constructed projective four-weight self-orthogonal codes, three classes of four-weight self-orthogonal codes, determined dual code parameters for two classes, and built LCD codes and quantum codes. Some LCD codes have almost optimal dual codes per sphere packing bound; some quantum codes are AMDS per quantum Singleton bound.

Conclusion: The constructed codes are not only self-orthogonal and few-weight, but also yield almost optimal LCD codes and AMDS quantum codes, showing practical value in coding theory and quantum information.

Abstract: Since self-orthogonal codes, few-weight codes, linear complementary dual codes(LCD codes, for short) and quantum codes have nice applications in coding theory and cryptography, they have received continuous attention. In 2024, by introducing the notion of the augment code, Heng et al.[30] constructed several classes of few-weight self-orthogonal codes basing on defining sets, which are introduced by Ding et al.[10] in 2007. In this manuscript, for two classes of defining sets, we consider the corresponding augmented codes, construct a class of projective four-weight self-orthogonal codes and three classes of four-weight self-orthogonal codes. And for two classes of these four-weight self-orthogonal linear codes, we determine the parameters of their dual codes. As applications, we construct two classes of LCD codes and a class of quantum codes. In particular, we prove that there exists a class of these LCD codes whose dual codes are almost optimal LCD codes according to the sphere packing bound, and a class of quantum codes are AMDS according to the quantum Singleton bound.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [134] [TriRoute: Unified Learned Routing for Joint Adaptive Attention, Experts, and KV-Cache Allocation](https://arxiv.org/abs/2607.06601)
*Andrii Balashov,Olena Ponomarova*

Main category: cs.LG

TL;DR: TriRoute is a lightweight controller that jointly coordinates attention mode, expert selection, and KV-cache bit-width per token per layer, outperforming independent MoD+MoE+KV-quantization combinations under matched compute and memory budgets.


<details>
  <summary>Details</summary>
Motivation: Existing conditional computation techniques (MoE, MoD, KV-cache quantization) act independently, but attention resolution, expert selection, and cache bit-width are strongly coupled—a token needing full attention likely also needs high-precision caching, regardless of its expert routing.

Method: A single lightweight controller emits a joint policy per token per layer for three axes (attention mode, FFN experts, KV-cache bit-width), trained end-to-end via heterogeneous Gumbel-Softmax with straight-through estimation, load-balanced top-k gating, and a Lagrangian budget constraint. Addresses cross-axis routing-collapse cascade with per-axis normalization and coupling-aware balancing loss.

Result: On decoder-only models (160M–1.3B parameters), TriRoute Pareto-dominates independent MoD+MoE+KV-quantization at matched FLOPs and memory, while better preserving robustness on rare entities, code, and arithmetic.

Conclusion: Jointly coordinated conditional computation yields better efficiency-quality trade-offs than independent axes. The controller learns interpretable patterns (e.g., full attention + high-precision cache on rare subwords, cheap routing on function words).

Abstract: Conditional computation can decouple language model quality from per-token inference cost, yet leading techniques act on a single axis in isolation: Mixture-of-Experts (MoE) sparsifies the FFN, Mixture-of-Depths (MoD) skips whole transformer blocks, and KV-cache quantization compresses attention memory. We argue these three decisions (attention resolution, expert selection, and cache bit-width) are strongly coupled and should be made jointly: a token rare enough to warrant full attention may also need high-precision caching regardless of which expert processes it. We introduce TriRoute, a single lightweight controller shared across all three axes that, for every token at every layer, emits a coordinated policy: (i) an attention mode (skip/local/full), (ii) a sparse set of FFN experts (with a null expert recovering MoD), and (iii) a KV-cache bit-width. The controller trains end-to-end via a heterogeneous relaxation (Gumbel-Softmax with straight-through estimation for categorical decisions and load-balanced top-k gating for experts) under a Lagrangian budget constraint that turns the average compute and memory cost into a controllable knob. We identify a cross-axis routing-collapse cascade in naive joint training, where collapse on one axis propagates to the others, and address it with per-axis normalization and a coupling-aware balancing loss. On decoder-only models from 160M to 1.3B parameters at compute-optimal token counts, TriRoute Pareto-dominates the best independent MoD+MoE+KV-quantization combination at matched inference FLOPs and memory, while better preserving tail-case robustness on rare entities, code, and arithmetic that pure perplexity optimization erodes. Post-hoc analysis reveals interpretable structure: the controller allocates full attention and high-precision cache to sentence-initial positions, rare subwords, and named entities, while cheaply routing function words.

</details>


### [135] [A Quiet Failure in Calibrated Virtual Screening: Marginal Conformal Prediction Under-Covers the Minority Class, and a Class-Conditional Fix Recovers It](https://arxiv.org/abs/2607.06605)
*Muhammadjon Tursunbadalov,Mustafojon Tursunbadalov*

Main category: cs.LG

TL;DR: Standard conformal prediction can severely undercover minority classes in imbalanced drug discovery datasets (e.g., 4.2% coverage on toxic compounds), but class-conditional conformal prediction reliably restores per-class coverage targets.


<details>
  <summary>Details</summary>
Motivation: Conformal prediction is increasingly used to provide reliable uncertainty estimates in drug discovery, but its behavior under class imbalance—common in real-world chemistry—has been underexplored. The authors aim to reveal and diagnose a dangerous failure mode where marginal coverage guarantees hide catastrophic undercoverage of rare classes.

Method: The authors evaluate standard (marginal) and class-conditional (Mondrian) conformal prediction across four drug-discovery datasets with imbalance, testing three model types (random forest, graph neural network, frozen chemical language model). They derive a conservation identity linking minority shortfall to imbalance ratio, localize failures to generic molecular scaffolds, and propose a cost model for screening utility.

Result: Marginal conformal prediction achieves 90% global coverage but minority coverage drops to 64.8% (blood-brain barrier) and 4.2% (clinical toxicity). The failure is model-agnostic (p<0.001 across all architectures). The conservation identity predicts gaps within 1 percentage point. Class-conditional methods restore minority coverage fully with modest set-size increases. Failures localize to generic scaffolds like benzene and pyridine.

Conclusion: Standard conformal prediction can silently fail on imbalanced drug discovery data; practitioners should use class-conditional (Mondrian) conformal prediction and monitor scaffold-level coverage to ensure reliability across all classes.

Abstract: Conformal prediction is being adopted in drug discovery to put an honest number on model reliability: pick an error rate alpha, and the method returns prediction sets containing the true label with probability at least 1 - alpha. We show this guarantee can be dangerous on imbalanced datasets. Across four datasets, standard (marginal) conformal prediction hits its global 90% coverage target while leaving the minority class badly exposed: realized minority coverage falls to 64.8% on blood-brain-barrier penetration and to 4.2% on clinical-trial toxicity, where the rare class is nearly abandoned. The failure is not tied to one model: a random forest, a graph network, and a frozen chemical language model all reproduce it (p < 0.001 in every case), with severity tracking baseline calibration on rare labels rather than architecture. A conservation identity explains the effect: the minority's shortfall equals the majority's surplus amplified by the imbalance ratio, predicting the measured gap to within one point and ordering severity across datasets. The failure survives realistic scaffold splits and a second conformal score, while aggregate accuracy and overall coverage stay reassuringly high, which is exactly why it is easy to miss. Class-conditional (Mondrian) conformal prediction closes the gap on every dataset, restoring minority coverage to target for a modest increase in prediction-set size. We localize the failures to generic molecular scaffolds - plain benzene and pyridine cores occurring in both classes - propose a one-number diagnostic, and show with a cost model that abstaining on affected compounds flips a screening campaign from net-negative to net-positive utility. Our contribution is demonstrating on real chemistry how severe and invisible this known conformal-theory gap becomes under imbalance, and laying out a practical protocol restoring per-class reliability.

</details>


### [136] [NEST: Tackling Dataset-Level Distribution Shifts via Regime-Oriented Mixture-of-Experts](https://arxiv.org/abs/2607.06607)
*Lanhao Li,Bingshu Xie,Lijun Sun,Xin Xue,Haoyi Zhou,Jianxin Li*

Main category: cs.LG

TL;DR: NEST is a two-phase dense MoE framework that models dataset-level distribution shifts by partitioning time-series into operational regimes via moment-entropy clustering, using regime-oriented routing and specialized expert kernels for state-of-the-art long-term forecasting.


<details>
  <summary>Details</summary>
Motivation: Existing methods focus on local temporal shifts but fail to explicitly model global structural challenges where datasets are composites of distinct operational regimes, leading to inaccurate long-term forecasting.

Method: NEST uses a two-phase dense MoE architecture: (1) unsupervised clustering in moment-entropy space to partition regimes, (2) regime-oriented routing with geometric modulation to centroids, where experts function as specialized kernels with unique variate-attention patterns.

Result: Extensive evaluations on heterogeneous network traffic and physical phenomena benchmarks consistently achieve state-of-the-art performance.

Conclusion: Modeling and recomposing evolving dataset structures through specialized regime-aware experts significantly improves long-term forecasting accuracy under distribution shifts.

Abstract: Accurate long-term forecasting in complex systems is frequently compromised by dataset-level distribution shifts, where diverse underlying behavioral modes and evolving system states drive the dynamic multivariate time-series. While existing methods predominantly focus on local temporal shifts, they fail to explicitly model the global structural challenge where datasets are composites of distinct operational regimes. In this paper, we propose NEST, a specialized framework designed to model and recompose these evolving structures through a two-phase dense MoE architecture. NEST first facilitates structural specialization by partitioning the dataset into distinct operational regimes through unsupervised clustering in a principled moment-entropy space. We introduce a regime-oriented router mechanism that generates initial expert weights based on temporal content, subsequently refined through geometric modulation to regime centroids. Crucially, rather than acting as monolithic predictors, individual experts function as specialized kernels that capture regime-specific dynamics by evolving unique variate-attention patterns. Extensive evaluations on diverse benchmarks, including heterogeneous network traffic and physical phenomena, demonstrate that NEST consistently achieves state-of-the-art performance. Our code and datasets are available at https://github.com/Aaralshin/NEST

</details>


### [137] [D2PO: Optimizing Diffusion Samplers via Dynamic Preference](https://arxiv.org/abs/2607.06609)
*Jinkyu Kim,Jinyoung Choi,Bohyung Han*

Main category: cs.LG

TL;DR: D2PO reformulates diffusion sampler optimization as a preference-based alignment problem using DPO, modeling sampling policies as energy-based models with dynamic self-improving preferences to outperform regression-based schedulers under low-NFE constraints.


<details>
  <summary>Details</summary>
Motivation: Existing student-teacher regression frameworks misalign samplers with perceptual quality because low-NFE student samplers mimic high-NFE teachers and sacrifice high-frequency texture fidelity while preserving coarse structures.

Method: (1) Reformulates sampler optimization as preference-based alignment using DPO; (2) Models sampling policy as an energy-based model (EBM) for tractable energy differences; (3) Introduces a novel energy formulation from pretrained score networks for perturbed spaces; (4) Proposes dynamic preferences where preferred samples self-improve iteratively.

Result: D2PO aligns diffusion samplers with perceptual quality more faithfully, unlocks the full potential of high-quality teachers, and consistently outperforms conventional regression-based schedulers under low-NFE constraints.

Conclusion: Dynamic, preference-guided refinement replaces rigid static teacher supervision, providing progressively stronger alignment signals for diffusion sampling optimization.

Abstract: We propose D2PO (Dynamic Direct Preference Optimization), a principled framework for optimizing diffusion sampling policies with respect to timestep schedules and classifier-free guidance (CFG) weights. Our work is motivated by a fundamental limitation of existing student-teacher regression frameworks; low-NFE student samplers are trained to mimic high-NFEteachers, often sacrificing high-frequency texture fidelity while preserving coarse global structures, thereby misaligning the sampler with perceptual quality. D2PO addresses this challenge by reformulating sampler optimization as a preference-based alignment problem, leveraging the Direct Preference Optimization (DPO) framework. To make DPO applicable to diffusion samplers, we model the sampling policy as an energy-based model (EBM), transforming preference comparisons into tractable energy differences. We further introduce a novel energy formulation derived directly from the pretrained score network, enabling preference evaluation in perturbed spaces that jointly capture structural consistency and fine-grained details. Moreover, we introduce dynamic preferences, where the preferred samples used for alignment progressively improve as the sampling policies are learned. This self-improving mechanism replaces rigid static teacher supervision with an iterative, preference-guided refinement process, providing progressively stronger alignment signals. Extensive experiments demonstrate that D2PO aligns diffusion samplers with perceptual quality more faithfully, unlocking the full potential of high-quality teachers and consistently outperforming conventional regression-based schedulers under low-NFE constraints.

</details>


### [138] [Deep Reinforcement Learning for Reliability Based Bi-Objective Portfolio Optimization](https://arxiv.org/abs/2607.06610)
*Sounaq Das,Tanmay Sen,Raghu Nandan Sengupta,Aditya Gupta*

Main category: cs.LG

TL;DR: Proposes a deep reinforcement learning framework (MORP-DRL) for multi-objective reliability-based portfolio optimization that jointly optimizes return and downside risk using variance, CVaR, and EVaR, incorporating GARCH, EVT, t-copula for heavy-tailed returns, and PPO with transaction costs, outperforming NSGA-II across pre-COVID, COVID, and post-COVID regimes.


<details>
  <summary>Details</summary>
Motivation: Existing reliability-based portfolio optimization uses static frameworks that cannot capture sequential decision making, tail risk, and market frictions like transaction costs.

Method: Deep reinforcement learning (PPO-based) framework jointly optimizing expected return and downside risk via variance, CVaR, and EVaR; uses GARCH(1,1), EVT, and t-copula for uncertainty modeling; generates scenarios via quasi-Monte Carlo simulation; benchmarks against NSGA-II.

Result: Competitive risk-return performance, reduced downside risk during market stress, and scalability to high-dimensional portfolios across three market regimes using ten global equity indices.

Conclusion: MORP-DRL effectively addresses limitations of static reliability-based approaches by capturing sequential decisions, tail risk, and transaction costs, offering robust multi-objective portfolio optimization.

Abstract: Portfolio optimization under uncertainty is inherently a multi-objective decision problem involving complex interactions among return, risk, market dynamics, and practical investment constraints. Existing reliability based portfolio optimization approaches primarily rely on static optimization frameworks and often fail to capture sequential decision making, tail risk, and market frictions such as transaction costs. To address these limitations, we propose a deep reinforcement learning framework for multi-objective reliability based portfolio optimization (MORP-DRL). The proposed framework jointly optimizes expected return and downside risk using three complementary risk measures: variance, Conditional Value-at-Risk (CVaR), and Entropic Value-at-Risk (EVaR). To model uncertainty and heavy-tailed market behavior, asset returns are represented using GARCH(1,1), Extreme Value Theory, and a t-copula dependence structure, while realistic scenarios are generated through quasi-Monte Carlo simulation. A Proximal Policy Optimization (PPO) based strategy is developed under practical constraints including transaction costs and portfolio bounds, and is benchmarked against NSGA-II. Experiments on ten global equity indices across pre-COVID, COVID, and post-COVID market regimes demonstrate that MORP-DRL achieves competitive risk-return performance, reduced downside risk during periods of market stress, and scalability to high-dimensional portfolio settings.

</details>


### [139] [Prior-matched evaluation of operational Earth-observation classifiers: a three-number reporting method demonstrated on Sentinel-1 internal-wave detection](https://arxiv.org/abs/2607.07146)
*Joao Pinelo,Joao Goncalves,Arun Shukla,Adriana Santos-Ferreira*

Main category: cs.LG

TL;DR: The paper identifies a systematic gap between balanced-test and real-world precision for internal solitary wave detection in Sentinel-1 data due to mismatched class priors, and proposes a prior-matched reporting method that improves operational precision from 0.192 to 0.927 while maintaining recall at 0.80.


<details>
  <summary>Details</summary>
Motivation: The operational detection rate for internal solitary waves is roughly 1-in-20 scenes, but the classifier was trained and reported at a 1-to-1 class balance. This causes balanced-test precision (0.794) to vastly overstate real operational precision (0.192), and this gap is invisible to commonly reported metrics.

Method: The authors identify the issue as an evaluation problem (not a training one), show prior correction and calibration cannot fix precision at fixed recall, and propose a prior-matched reporting method using three figures: balanced-test, operational-prior, and real post-deployment. They then apply a precision-first, leakage-controlled development cycle with pre-registered margins, holding recall at 0.80 and certifying against a sealed lockbox.

Result: The promoted model achieves 0.927 precision at the operational prior; only feature aggregation provided real lift, while added capacity and calibration did not clear the margin. An out-of-time check confirmed discrimination transfers but a fixed operating point does not.

Conclusion: Prior-matched reporting—beginning with balanced metrics and transitioning to the operational prior as the stream reveals it—transfers to any Earth-observation service bootstrapping a rare-event detector under an unknown prior.

Abstract: The Internal Waves Service screens the Sentinel-1 Wave-mode archive for internal solitary waves, routing detections to experts whose adjudication time is the resource the effort exists to conserve. Because attention is the cost of error, precision leads. Its classifier was trained and reported at a one-to-one class balance, fixed before the operational rate could be known. That rate has since emerged at roughly one scene in twenty, and a balanced-test score badly overstates the precision a validator meets. A model that scores 0.794 balanced-test precision scores 0.192 in real operation: the gap is a systematic artefact of reporting at the wrong prior, invisible to the metric most work quotes. We show the mismatch to be an evaluation problem in the costume of a training one at a fixed recall, prior correction and calibration cannot move precision, and answer it with a prior-matched reporting method based on three figures: balanced-test, operational-prior, and real post-deployment, whose contrast is the honest measure. A precision-first, leakage-controlled development cycle then improves the classifier lever by lever, each promoted only against a pre-registered margin; added capacity not clearing it, calibration inert, feature aggregation the one real lift, so the honest negatives are as much a result as the gain. Holding recall at a floor of 0.80 and certifying against a sealed, single-read lockbox, the promoted model reports 0.927 precision at the operational prior; an out-of-time check confirms discrimination transfers to unseen periods while a fixed operating point does not. Prior-matched reporting, begin balanced, then move to the prior as the stream reveals it, transfers to any operational Earth-observation service bootstrapping a rare-event detector under a prior it has yet to discover.

</details>


### [140] [STAGformer: A Spatio-temporal Agent Graph Transformer for Micro Mobility Demand Forecasting](https://arxiv.org/abs/2607.06614)
*Ye Zihao*

Main category: cs.LG

TL;DR: STAGformer is a Spatio-Temporal Agent Graph Transformer for bike-sharing demand forecasting that achieves linear computational complexity via a two-step agent attention mechanism, outperforming state-of-the-art models on NYC Citi-Bike and Chicago Divvy-Bike datasets.


<details>
  <summary>Details</summary>
Motivation: Accurate station-level demand forecasting is essential for bike-sharing systems but remains challenging due to complex spatio-temporal dependencies and large-scale urban networks.

Method: STAGformer integrates four core modules: (1) spatio-temporal encoder fusing dynamic node features with external factors, (2) graph propagation for spatial neighbor aggregation, (3) temporal convolution for local pattern extraction, and (4) agent attention mechanism (two-step: learnable agent tokens aggregate global info then broadcast back) achieving O(NT) complexity.

Result: Consistently outperforms state-of-the-art baselines on NYC Citi-Bike and Chicago Divvy-Bike datasets across multiple prediction horizons, with significant RMSE and MAE improvements. Ablation studies confirm each component's contribution.

Conclusion: The proposed agent attention mechanism is critical for efficient global spatio-temporal dependency modeling, enabling superior forecasting accuracy with linear computational complexity.

Abstract: Accurate station-level demand forecasting is essential for the efficient operation of bike-sharing systems, yet it remains challenging due to complex spatio-temporal dependencies and the large scale of urban networks. This paper presents STAGformer, a Spatio-Temporal Agent Graph Transformer that achieves efficient global modeling with linear computational complexity. The model introduces a two-step agent attention mechanism, where a small set of learnable spatial and temporal agent tokens first aggregate global information and then broadcast it back to individual stations and time steps, effectively capturing long-range interactions while reducing the quadratic cost of standard self-attention to O(NT). STAGformer integrates four core modules: a spatio-temporal encoder that fuses dynamic node features with external contextual factors (weather, time, points of interest), a graph propagation module for spatial neighbor aggregation, a temporal convolution module for local pattern extraction, and the agent attention module for global dependency modeling. Extensive experiments on two real-world datasets -- NYC Citi-Bike and Chicago Divvy-Bike -- demonstrate that STAGformer consistently outperforms state-of-the-art baselines across multiple prediction horizons, achieving significant improvements in both RMSE and MAE. Ablation studies validate the contribution of each component, with the agent attention mechanism proving critical for modeling global spatio-temporal dependencies.

</details>


### [141] [WHERE to Generate Matters: Budget-Aware Synthetic Augmentation for Label Skewed Federated Learning](https://arxiv.org/abs/2607.06616)
*Sangwoo Lee,Sunghwan Park,Jaewoo Lee*

Main category: cs.LG

TL;DR: FedEAS is an entropy-adaptive budget allocation policy for synthetic data augmentation in federated learning that reduces generation cost by 94.1% while recovering most accuracy gains of full class balancing.


<details>
  <summary>Details</summary>
Motivation: Label skew in federated learning causes client drift and degrades global accuracy. Existing synthetic data augmentation for class balancing is computationally expensive.

Method: FedEAS assigns each client an entropy-adaptive per-class generation budget based on its local label distribution, dynamically deciding both how much each client generates and where the samples go.

Result: Reduces generation budget by 94.1% while recovering most accuracy gains of full class balancing. Outperforms Uniform allocation by up to 18.82% at the same total budget on CIFAR-10 and CIFAR-100.

Conclusion: Entropy-adaptive budget allocation is an efficient and effective strategy for mitigating label skew in federated learning without the high cost of full class balancing.

Abstract: Label skew in federated learning (FL) causes client drift and degrades global accuracy. Synthetic data augmentation can reduce this imbalance; however, full class balancing requires substantial computation cost. We propose FedEAS, a policy that assigns each client an entropy-adaptive per-class generation budget computed from its local label distribution. The budget jointly decides \emph{how much} each client generates and \emph{WHERE} the samples go. Accordingly, the total generation budget follows from the per-client budgets rather than being fixed in advance. FedEAS recovers most of the accuracy gain of full class balancing while reducing the generation budget by 94.1\%. At the same total generation budget, it outperforms Uniform allocation by up to 18.82\% across CIFAR-10 and CIFAR-100.

</details>


### [142] [Inertia-1: An Open Exploration of Wearable Motion Foundation Models](https://arxiv.org/abs/2607.06617)
*Zongzhe Xu,Aakarsh Anand,Sarah Jiang,Chuntung Zhuang,Zitao Shuai,Sriram Sankararaman,Yuzhe Yang*

Main category: cs.LG

TL;DR: Inertia-1 is a fully open exploration of wearable motion foundation models using 18.2M+ hours of accelerometer data, systematically studying data, model, and training choices across 15 datasets to provide a comprehensive cookbook for wearable motion representation learning.


<details>
  <summary>Details</summary>
Motivation: Wearable motion sensing offers a scalable window into human behavior and health, but the pretraining and scaling principles for foundation models in this domain remain poorly understood, with prior work studying isolated design choices under narrow settings that fail to capture real-world sensing diversity.

Method: Built a controlled framework using massive accelerometer corpora (18.2M+ hours) to systematically study the full lifecycle: data choices (modality, placement, sampling rate, window length), model choices (architectures, size), and training choices (pretraining objective, data scale). Evaluated across 15 datasets covering activity recognition, freezing-of-gait detection, and disease prediction.

Result: Revealed intriguing findings for building motion foundation models that generalize across tasks and sensing conditions, achieving state-of-the-art recipes for diverse downstream tasks.

Conclusion: Inertia-1 serves as a comprehensive, practical, and open cookbook for wearable motion representation learning, providing empirically grounded guidelines for the entire modeling pipeline.

Abstract: Wearable motion sensing provides a continuous and scalable window into human behavior and health, making it a natural fit for foundation models, yet its pretraining and scaling principles remain poorly understood. Prior work studies isolated design choices, such as sensor placement or sampling frequency, often under fixed settings and narrow downstream tasks that fail to capture real-world sensing diversity. We introduce Inertia-1, a fully open exploration of wearable motion foundation models. Using massive corpora of accelerometer data from global sources spanning more than 18.2M hours, we build a controlled framework for studying the full lifecycle of wearable motion foundation models, covering data choices such as sensor modality, device placement, sampling rate, window length; model choices such as architectures and model size; and training choices such as pretraining objective and data scale. Extensive evaluations across 15 datasets spanning human activity recognition, freezing-of-gait detection, and disease prediction reveal intriguing findings for building motion foundation models that generalize across tasks and sensing conditions. Collectively, Inertia-1 not only presents state-of-the-art recipes for diverse downstream tasks, but also serves as a comprehensive, practical, and open cookbook for wearable motion representation learning.

</details>


### [143] [Fingerprint, Not Blueprint: How Positional Schemes Set the Default Spectral Algebra of Attention](https://arxiv.org/abs/2607.06621)
*Li Hengyu*

Main category: cs.LG

TL;DR: This paper analyzes the spectral properties of attention head operators M = W_q^T W_k, finding that pre-softmax scores are bilinear forms. Across seven pretrained models, the eigenspectrum encodes positional scheme information: rotational under RoPE, non-rotational under learned-absolute/ALiBi. Dynamic analysis shows spectra evolve from random-matrix nulls and consolidate after circuit formation. Causal experiments show no spectral channel is strictly necessary, as training reroutes around bans, though symmetry constraints impose costs. The positional scheme sets the default spectral algebra as a post-hoc fingerprint, not a hard constraint.


<details>
  <summary>Details</summary>
Motivation: To understand what the complex eigenspectrum of the non-symmetric (non-normal) attention operator M encodes across different attention heads, positional schemes, and training dynamics, at static, dynamic, and causal levels.

Method: Spectral analysis of M = W_q^T W_k across seven pretrained models (three positional schemes: RoPE, learned-absolute, ALiBi). Dynamic analysis over Pythia checkpoints. Causal interventions including zeroing phase channels and imposing symmetry, with toy-scale constrained training.

Result: Strongest previous-token heads show perfect spectral separation by positional scheme (p=0.029). Zeroing RoPE phase eliminates induction. Spectra originate from random-matrix null and consolidate after function emerges. No single spectral channel is necessary; training reroutes. Symmetry costs differ by scheme (2.9x slowdown for learned-absolute).

Conclusion: The positional scheme sets the default spectral algebra of an attention head's solution, but this is a fingerprint sculpted after function, not a hard constraint upon it.

Abstract: The pre-softmax score of an attention head is a bilinear form $score(i,j) = x_i^T M x_j$ in a learned operator $M = W_q^T W_k$. Because M is generally non-symmetric, hence non-normal, it has a complex eigenspectrum and non-orthogonal eigenvectors, the regime where non-Hermitian and random-matrix tools apply. We ask what this spectrum encodes, at three levels for previous-token and induction circuits. Statically, across seven pretrained models spanning three positional schemes, the strongest previous-token heads are spectrally rotational under RoPE and non-rotational, or content-like, where position enters outside QK (learned-absolute and ALiBi); the model-level separation is perfect at every top-k examined (exact permutation $p=0.029$), and zeroing the per-frequency RoPE phase $Im(M_t)$ eliminates induction on a pre-identified previous-token head in all three RoPE models. Dynamically, over public Pythia checkpoints every head originates at the random-matrix (Ginibre) null; the rotational signature emerges with the behavior, not before it, and the population-median suppression that yields the final profile follows circuit formation, so the profile is a consolidated fingerprint, not a precursor. Causally, and at toy scale, no spectral channel is necessary: constrained two-layer training reroutes around every ban with capability intact, albeit at a significant formation delay (four pre-registered contrasts, $q_BH <= 0.016$). The cost structure exposes each scheme's default: imposing symmetry slows learned-absolute models by a factor of 2.9, whereas a RoPE head with a fully symmetric static M still routes directionally via the phase channel, impossible under absolute positions. Within the settings examined, the positional scheme sets the default spectral algebra of an attention head's solution: a fingerprint sculpted after function, not a hard constraint upon it.

</details>


### [144] [LLM-Guided Task-Semantic Field Factorization for Industrial Process Forecasting](https://arxiv.org/abs/2607.06623)
*Youcheng Zong,Runda Jia,Mingxuan Ren,Dakuo He*

Main category: cs.LG

TL;DR: TSF is an LLM-guided framework that integrates semantic information from process documents into time-series forecasting for industrial soft-sensing, achieving 6.4% average MAE reduction with minimal overhead (1.8-3.0k extra parameters, <0.008 ms/step).


<details>
  <summary>Details</summary>
Motivation: Process industries face scarce labeled data, frequent operating regime changes, and costly model retraining. Existing time-series models treat inputs as anonymous numerical columns, ignoring rich semantic information available in variable tables and process documents. Text-enhanced methods fail to make semantic-logical relations between input variables and prediction targets available within each numerical window.

Method: TSF builds a task-semantic field from task protocols and variable documents using LLM offline. During online training and inference, the current numerical window activates variable semantics, allowing semantic information to participate in each prediction. The LLM is used only for offline semantic construction, while online training/inference uses conventional time-series backbones.

Result: On multiple complex industrial forecasting and soft-sensing tasks, TSF reduces MAE by 6.4% on average, with the largest reduction reaching 25.5%. It adds only about 1.8-3.0k parameters with less than 0.008 ms/step of additional online inference overhead.

Conclusion: TSF effectively turns existing process documents into measurable forecasting gains across different backbones and semantic generators while remaining lightweight for deployment.

Abstract: Process industries rely on time-series forecasting and soft sensing to estimate quality variables that are hard to measure online. Labeled data are scarce, operating regimes change frequently, and retraining models or rebuilding alignment pipelines for each scenario is costly. Such settings often provide variable tables and process documents that record variable names, units, physical meanings, and process roles. However, standard time-series backbones usually treat inputs as anonymous numerical columns. Existing text-enhanced methods also rarely make the semantic-logical relations between input variables and the prediction target available to the model within each numerical window. To address this problem, this article proposes Task-Semantic Field Factorization (TSF), a large language model (LLM)-guided framework. TSF builds a task-semantic field from task protocols and variable documents before training and uses the LLM only for offline semantic construction. Online training and inference remain with conventional time-series backbones. During training and inference, the current numerical window activates variable semantics, so semantic information participates in each prediction and supports adaptation to different prediction targets and operating shifts. On multiple complex industrial forecasting and soft-sensing tasks, TSF reduces MAE by 6.4\% on average in improved settings, with the largest reduction reaching 25.5\%. It adds only about 1.8--3.0k parameters, with less than 0.008 ms/step of additional online inference overhead. These results show that TSF turns existing process documents into measurable forecasting gains across backbones and semantic generators while remaining lightweight for deployment.

</details>


### [145] [Open-Ended Scenario Reasoning for Specialist Model Adaptation](https://arxiv.org/abs/2607.06625)
*Youcheng Zong,Runda Jia,Ranmeng Lin,Mingxuan Ren,Dakuo He*

Main category: cs.LG

TL;DR: ROAM is a framework that uses LLM reasoning to adapt frozen industrial specialist models to new scenarios without retraining, achieving >20% MAE reduction with minimal overhead.


<details>
  <summary>Details</summary>
Motivation: Industrial specialist models degrade in new scenarios due to sensor drift, feedstock variation, and regime switching, but retraining is costly and existing adaptation methods require modifying model parameters with sufficient labeled data.

Method: ROAM freezes specialist models, confines corrections to a low-dimensional latent space, fuses LLM-generated scenario judgments with online observations under a probabilistic framework, and uses a risk-constrained mechanism to suppress corrections when LLM evidence is unreliable.

Result: On mineral thickening and IndPenSim datasets, ROAM reduces MAE by over 20% in major shift settings with only 839 additional parameters and under 0.02ms per-step overhead.

Conclusion: LLM reasoning can be conservatively turned into an adaptation signal for industrial models already in service, enabling rapid response without retraining.

Abstract: Process industries have accumulated validated specialist models, yet sensor drift, feedstock variation, and regime switching cause these models to degrade systematically in new scenarios. Collecting new labeled data and retraining is costly, while continuing with the original model incurs persistent bias. Existing adaptation methods require modifying model parameters with sufficient labeled data, making rapid response on deployed systems difficult. Using LLMs as direct predictors risks hallucinations and uncontrollable outputs. Such predictors also cannot incorporate unstructured scenario knowledge from the field. To address these limitations, this article proposes Reasoning-Driven Open Adaptation for Specialist Models (ROAM), a framework that uses LLM world knowledge and reasoning to adapt frozen specialist models to unseen scenarios without retraining. ROAM confines all corrections to a low-dimensional, semantically interpretable latent space. LLM-generated scenario judgments and online observations are fused under a unified probabilistic framework. A risk-constrained mechanism suppresses corrections under unreliable LLM evidence or abrupt scenario shifts and falls back to the original frozen model when evidence is insufficient. Experiments on a mineral thickening process and the public IndPenSim penicillin fermentation dataset show that ROAM reduces MAE by over 20\% in major shift settings such as hidden shifts with only 839 additional parameters and under 0.02\,ms per-step overhead. These results indicate that LLM reasoning can be turned into a conservative adaptation signal for industrial models already in service.

</details>


### [146] [Reward Valuation in Vision Language Models: Causal Mechanisms Underlying Anhedonia](https://arxiv.org/abs/2607.06626)
*Melika Honarmand,Samin Mahdipour Aghabagher,Martin Schrimpf*

Main category: cs.LG

TL;DR: Vision-Language Models contain reward-anticipatory units functionally analogous to the human Nucleus Accumbens; perturbing these units induces anhedonia-like behavioral deficits mirroring clinical symptoms in depression.


<details>
  <summary>Details</summary>
Motivation: To test whether Vision-Language Models (VLMs) exhibit alignment with human reward valuation mechanisms, using a clinical neuroscience framework originally designed to assess anhedonia and motivational deficits in major depressive disorder.

Method: Identified reward-anticipatory units in VLMs analogous to human Nucleus Accumbens (NAc) function; applied targeted perturbations to these units; evaluated behavioral changes on effort-based decision-making tasks and clinical anhedonia scales (DARS, MAP-SR).

Result: Perturbing NAc-selective units caused the model to shift toward low-effort, low-reward options, mirroring human anhedonia. This effect was specific to reward valuation/anticipation, not general task capability, as baseline performance was preserved when reward-based choice was removed. Behavioral changes aligned with clinical anhedonia and motivation scales.

Conclusion: VLMs possess reward valuation circuits that functionally parallel those in the human brain, suggesting deep architectural alignment between artificial and biological reward processing systems.

Abstract: Recent Vision-Language Models capture increasingly complex aspects of human cognition. Here we ask whether this alignment extends to reward valuation, which we assess in a mechanistic framework built on clinical tests that were developed to evaluate anhedonia and motivational deficits in major depressive disorder. In the brain, anhedonia is frequently linked to dysregulation in the Nucleus Accumbens (NAc) and the broader dopaminergic reward system. While neuroimaging has localized these deficits, establishing a causal link between NAc activity and specific behavioral symptoms remains a challenge. We use these ideas from neuroscience to functionally identify reward-anticipatory units in vision language models, and test their causal role via targeted perturbations. Perturbing NAc-selective units induces behavioral effects that mirror human anhedonia: the model shifts toward low-effort, low-reward options in effort-based decision-making tasks. Crucially, our results reflect a specific deficit in reward valuation and anticipation rather than a loss of task capability: the perturbed model maintains baseline performance when reward-based choice is removed. This induced vulnerability further aligns with clinical anhedonia and motivation scales, including DARS and MAP-SR. Taken together, these results reveal reward valuation circuits in AI models that parallel those in humans.

</details>


### [147] [Cross-Trajectory Chimera Interventions Reveal Dissociable Roles of Weight Magnitude and Direction in Grokking](https://arxiv.org/abs/2607.06628)
*Truong Xuan Khanh*

Main category: cs.LG

TL;DR: Direction vectors from one trained network can causally transfer circuit identity to another independently trained network, while norm vectors only modulate training speed, not identity.


<details>
  <summary>Details</summary>
Motivation: To determine which properties of a partially trained network (weight norm vs. direction) are causally portable across different, independently trained neural networks, rather than merely necessary within a single training run.

Method: Cross-trajectory chimera interventions: split weight vectors into norm and unit direction from two runs (different seeds), recombine one run's norm with the other's direction, continue training. Tested on modular-arithmetic tasks exhibiting grokking.

Result: Direction carries donor-specific circuit identity (40/40 transfer success), predicted by recipient's norm with perfect class separation (p=1.9e-4). Norm only causes modest delay effects with no identity signal. Transfer threshold localized to ±1/64 precision.

Conclusion: Direction indexes which solution a trajectory approaches; norm governs susceptibility to identity overwriting. These components dissociate in function and causal portability across runs.

Abstract: Which properties of a partially trained network are causally portable to a different, independently trained network? Single-trajectory interventions show necessity within one run, not portability across runs. We introduce cross-trajectory chimera interventions: given two runs from different seeds, we split each weight vector into a norm and a unit direction, recombine one run's norm with the other's direction, and continue training. On two modular-arithmetic tasks that grok, the components dissociate. Direction carries a transferable, donor-specific circuit identity: implanting a donor's direction at the recipient's norm drives the run to the donor's circuit in 40/40 cases, while an angle-matched random control yields no shift. The transfer is threshold-like, and its location is predicted by the recipient's norm, separating perfectly by norm class over all 20 pairs (joint permutation probability 1.9e-4). Norm carries only a modest, distributed delay effect and no identity signal. An adaptive bisection procedure localizes the threshold to +/-1/64. Direction indexes which solution a trajectory approaches; norm governs how susceptible that identity is to being overwritten.

</details>


### [148] [STST-JEPA: Shallow-Target Spatio-Temporal Joint Embedding Prediction Architecture For EEG Self-Supervised Learning](https://arxiv.org/abs/2607.06629)
*Roy Segal,Yoni Svechinsky,Tomer Fekete*

Main category: cs.LG

TL;DR: STST-JEPA is a self-supervised transformer for EEG brain-age prediction, pretrained on ~48K sessions (ages 5-81). It achieves 3.06 years MAE in age regression, rank-1 on multiple EEG benchmarks, and its age residuals correlate with cognitive efficiency.


<details>
  <summary>Details</summary>
Motivation: EEG brain-age models face challenges including cross-site montage heterogeneity, small labeled datasets, and subject-level non-stationarity. No existing EEG foundation model delivers competitive age regression across the full pediatric-to-older-adult range needed for real-world deployment.

Method: STST-JEPA uses a latent-prediction objective (predicting masked-token representations against an EMA-of-tokenizer target) plus an auxiliary signal-reconstruction term, applied to 30-second multi-channel windows under spatiotemporal block masks. Pretrained on 47,703 sessions from brain.space and HBN corpora.

Result: Best held-out MAE of 3.06 years (r=0.924) for age regression on 3,367 sessions. Rank-1 on NeuralBench EEG leaderboard for sex classification (0.911 balanced accuracy), age prediction (r=0.749), and psychopathology regression (r=0.215). Age-prediction residual negatively correlates with cognitive efficiency.

Conclusion: STST-JEPA is a powerful EEG foundation model capable of accurate age regression across the lifespan, and its age residuals serve as a biomarker correlated with cognitive performance.

Abstract: Brain age -- the age inferred from a physiological recording -- is an emerging biomarker whose deviation from chronological age tracks neurological and psychiatric burden, and EEG is an attractive substrate for it because it is cheap, portable, and temporally rich. Yet EEG brain-age models must contend with cross-site montage heterogeneity, small labelled cohorts, and dominant subject-level non-stationarity, and few EEG foundation models have been shown to deliver competitive age regression across the full pediatric-to-older-adult range in which such a biomarker would actually be deployed. We introduce STST-JEPA, a self-supervised transformer for resting-state and task EEG, pretrained on 47,703 sessions spanning ages 5-81 from the brain.space and Healthy Brain Network (HBN) corpora. The model combines a latent-prediction objective - predicting masked-token representations against an EMA-of-tokenizer target - with an auxiliary signal-reconstruction term, applied to 30-second multi-channel windows under spatiotemporal block masks. A lightweight attentive probe trained on frozen pretrained embeddings achieves a best held-out-validation mean absolute error of 3.06 years (r = 0.924) for age regression on 3,367 sessions, against a predict-the-mean baseline of approximately 10 years MAE. With light task-specific fine-tuning of the model's final layers, the same pretrained encoder achieves rank-1 placements - with the model's native 30-second windows - on the public NeuralBench x brain.space EEG leaderboard for sex classification (balanced accuracy 0.911), age prediction (r = 0.749), and psychopathology composite regression (r = 0.215). We further show that the model's age-prediction residual is negatively correlated with cognitive efficiency over several tasks we examined.

</details>


### [149] [When Certificates Fail: A Unified Safety Framework for Embedded Neural Interface Models](https://arxiv.org/abs/2607.06630)
*Jasmeet Singh Bindra*

Main category: cs.LG

TL;DR: This paper identifies and demonstrates three critical alignment failures between mathematical certification and operational safety in neural interfaces: verification insufficiency (certificates pass while task accuracy drops 25.7%), proxy-fidelity divergence (task optimization damages neural signal structure), and latent information exfiltration (private attributes like subject identity are recoverable at 48.1% vs 6.7% chance). The authors propose an empirical audit framework and show these gaps persist across multiple architectures (EEGNet, CSP+LDA, FBCSP+LDA), arguing operational safety auditing is necessary beyond certificate verification.


<details>
  <summary>Details</summary>
Motivation: To expose and formalize the dangerous gap between mathematical robustness certificates and actual operational safety in neural interfaces, where certification can pass while task performance collapses and user welfare is compromised.

Method: The authors propose a unified empirical audit framework organized around three failure modes: verification insufficiency, proxy-fidelity divergence, and latent information exfiltration. They validate on BCI Competition IV 2a and SEED-IV datasets using multiple EEG decoders (EEGNet, CSP+LDA, FBCSP+LDA) with official session-level validation, null controls, and paired statistical tests.

Result: At perturbation budget e=0.25, EEGNet classification accuracy drops 25.7% under PGD attack while Lipschitz certificate remains valid for all 9 subjects. Subject identity is recoverable at 48.1% accuracy (vs 6.7% chance). The verification gap is architecture-independent, persisting across all tested models. Time-domain auxiliary objective reduces reconstruction MSE by 0.1132 but worsens spectral log-MSE.

Conclusion: Mathematical certificate verification alone is insufficient for responsible neural-interface deployment. Operational safety auditing is necessary to detect alignment failures that formal guarantees miss, as certification gaps are architecture-independent and persist across different model families.

Abstract: Formal robustness certificates for embedded neural-interface models can pass while task accuracy collapses: at perturbation budget e=0.25, EEGNet classification accuracy drops by 25.7% under projected-gradient attack while the Lipschitz-style certificate remains valid for all 9 tested subjects. We argue that this gap between mathematical certification and operational safety is one instance of a broader alignment failure in neural interfaces, where training objectives diverge from user welfare. We propose a unified empirical audit framework organized around three such failures: verification insufficiency, in which certificates pass while task behavior degrades; proxy-fidelity divergence, in which task-optimized representations damage neural signal structure (a time-domain auxiliary objective reduces reconstruction MSE by 0.1132 while worsening spectral log-MSE); and latent information exfiltration, in which public-task embeddings retain private attributes (subject identity recoverable at 48.1% versus 6.7% chance). We instantiate the framework on BCI Competition IV 2a and SEED-IV using multiple deep and classical EEG decoders, official session-level validation, null controls, and paired statistical tests. The verification gap persists across EEGNet, CSP+LDA, and FBCSP+LDA, and is therefore architecture-independent. Our results establish that operational safety auditing, not certificate verification alone, is necessary for responsible neural-interface deployment.

</details>


### [150] [Does Demand Response Increase Vulnerability to Cyber Attacks by Adversarial Data Modifications?](https://arxiv.org/abs/2607.06632)
*Clemens Kortmann,Eike Cramer*

Main category: cs.LG

TL;DR: This paper investigates how adversarial attacks on electricity price forecasts can harm industrial demand response decision-making, finding that while such attacks can erode profits, limited perturbations preserve ~90% of demand response financial advantage, and attack impact depends more on perturbation orientation than magnitude.


<details>
  <summary>Details</summary>
Motivation: While adversarial attacks have been studied in energy systems focusing on grid-side problems (e.g., forecasting, state estimation), few studies have analyzed their impact on the demand side. This paper addresses that gap by examining how manipulated price forecasts affect industrial demand response decision-making.

Method: The authors design adversarial attacks targeting electricity price forecasting models, then solve scheduling optimization problems for energy-intensive production processes using distorted price forecasts. They use a generalized process model to investigate vulnerability across production scheduling problems with varying levels of process flexibility.

Result: Adversarial attacks can erode profits from demand response. However, with limited perturbations (hard to detect by humans), demand response retains ~90% of its financial advantage. The impact depends more on perturbation orientation than magnitude.

Conclusion: Attack analyses should explicitly incorporate sensitivities of scheduling optimization models into attack design to enable more rigorous assessments of decision-making under adversarial attacks.

Abstract: Adversarial attacks are crafted data manipulations that aim to deteriorate the outcomes of prediction or decision-making algorithms. In the energy systems literature, adversarial attacks have been studied with a focus on problems regarding the electricity grid. Such problems include forecasting and grid state estimation, where adversarial attacks are also known as false data injection attacks. Only few studies have analyzed the potential impact that adversarial attacks have on the demand side. We analyze how manipulated price forecasts impact the decision-making in industrial demand response. To this end, we design adversarial attacks that aim to deteriorate the output of electricity price forecasting models and solve scheduling optimization problems of energy-intensive production processes using the distorted price forecasts. We make use of a generalized process model to investigate the vulnerability to adversarial attacks for a range of production scheduling problems with different levels of process flexibility. We find that adversarial attacks can erode the profits gained from demand response. However, when perturbations are limited in extent (so that they are hard to detect by the human user), demand response preserves about 90\% of its financial advantage compared to steady-state process operation. Further, we find that the impact of adversarial attacks on demand response does not only depend on the magnitude of the perturbations but rather on the orientation of the adversarial perturbations. Therefore, we argue that attack analyses should explicitly incorporate the sensitivities of scheduling optimization models into the attack design to enable more rigorous assessments of decision-making under adversarial attacks.

</details>


### [151] [When Do Geometric Algebra Layers Beat Scalarization? A Controlled Study on SO(3)-Equivariant Vector Laws](https://arxiv.org/abs/2607.06634)
*Fabien Polly*

Main category: cs.LG

TL;DR: Clifford algebra networks outperform invariant scalarization baselines only when the target involves deep composition of group operations (e.g., chains of rotations), but offer no advantage for single-stage laws or invariant magnitude prediction.


<details>
  <summary>Details</summary>
Motivation: To determine whether the geometric algebra structure of Cl(3,0) networks contributes anything beyond exact SO(3)-equivariance, by comparing against a minimal invariant scalarization baseline and other equivariant models.

Method: Compared Cl(3,0) network against scalarization baseline (invariant dot products + MLP outputting equivariant basis coefficients), Vector Neurons, e3nn, and multiplicative coefficient networks on single-stage and compositional 3D vector laws, with ablations on rotation chain depth and composition type.

Result: On single-stage laws, scalarization matches or beats Cl(3,0) at fraction of cost. On compositional targets with nested group operations, Cl(3,0) beats scalarization by an order of magnitude in low-data regime (100 vs 3000 samples). No model extrapolates invariant magnitudes.

Conclusion: Geometric algebra layers are not a general shortcut for low-data 3D learning but become useful specifically when targets compose group elements in depth.

Abstract: Compact networks built from Clifford algebra Cl(3,0) primitives are exactly SO(3)-equivariant and learn synthetic 3D vector laws from few samples. We ask whether the geometric algebra structure itself contributes anything beyond exact equivariance. We compare against a minimal scalarization baseline: invariant dot products fed to a small MLP that outputs coefficients on the equivariant basis {v_i, v_i x v_j}, which is also exactly equivariant. On single-stage laws (rotation by axis-angle, cross product, central force), scalarization matches or beats the Cl(3,0) network at a fraction of the training cost, so the geometric algebra adds nothing there. On compositional targets whose computation graph nests group operations (apply R2 R1 to a point; map a local force through an orientation, then take a torque), the Cl(3,0) network beats scalarization by an order of magnitude in the low-data regime, reaching with 100 samples what the baseline needs 3000 for, and the gap survives strengthening the baseline with the triple-product invariant and 17x more parameters, external Vector Neurons and e3nn baselines, and a multiplicative coefficient network. Ablations show the required network depth tracks the rotation chain length, and scalarization falls below the constant predictor on chains of four rotations. The advantage is not composition per se: on a rotation-free nested cross product, which flattens into polynomial invariant coefficients, scalarization wins by 24x. No tested model, equivariant or not, extrapolates invariant magnitudes: on radius and separation shifts every model is worse than a constant predictor once errors are normalized. We conclude that geometric algebra layers are not a general shortcut for low-data 3D learning, but become useful precisely when the target composes group elements in depth.

</details>


### [152] [Generative Diffusion Models of Stochastic Graph Signals](https://arxiv.org/abs/2607.06833)
*Yiğit Berkay Uslu,Samar Hadou,Sergio Rozada,Shirin Saeedi Bidokhti,Alejandro Ribeiro*

Main category: cs.LG

TL;DR: This paper proposes a unified denoising diffusion framework for conditional graph signal generative modeling, using a novel U-GNN architecture (generalizing U-Net to graphs) to sample from conditional distributions, demonstrated on stock price forecasting and wireless resource allocation.


<details>
  <summary>Details</summary>
Motivation: Existing approaches for sampling stochastic signals on graphs rely on application-specific designs that often regress to conditional means rather than sampling from the full conditional distribution, lacking a unified generative framework.

Method: A denoising diffusion framework with a reverse process parameterized by graph neural networks (GNNs). The key architecture is U-GNN, which performs multi-resolution encoder-decoder processing with learned node selection (pooling/unpooling) using nested selection matrices and zero-padded lifting, while maintaining graph convolutions on the original graph with strided hop reach.

Result: Demonstrated effectiveness on two tasks: stock price forecasting and optimal wireless resource allocation, with extensive numerical results validating the approach.

Conclusion: The proposed conditional diffusion framework with U-GNN successfully unifies diverse conditional graph signal generation problems into a single approach, outperforming regression-based alternatives by sampling from the conditional distribution rather than just predicting the conditional mean.

Abstract: Sampling stochastic signals supported on a graph underlies many graph machine learning tasks, including recommender systems, forecasting in financial markets, and wireless network optimization. In these settings, the target signals are realizations of unknown conditional distributions. However, prevailing approaches rely mostly on intricate, application-tailored designs that often regress to a conditional mean instead of sampling from the conditional law. This paper unifies such problems as conditional graph signal generative modeling and tackles them with a single denoising diffusion framework. We learn a reverse diffusion process, parametrized by graph neural networks (GNNs), that draws graph signals conditioned directly on the graph topology and on node-feature side information. The reverse process is realized by a novel architecture, the U-Graph Neural Network (U-GNN), which generalizes the image-convolutional U-Net to graph-structured signals. The U-GNN performs multi-resolution encoder--decoder processing in which pooling and unpooling reduce to a learned node selection, expressed by nested selection matrices, and a zero-padded lifting of coarse signals back to the full node set. The graph convolutions are carried out on the original graph, with a stride that sets their hop reach, so the U-GNN bypasses explicit graph coarsening at every resolution. We demonstrate our method on two generative tasks: stock price forecasting and optimal wireless resource allocation, with extensive numerical results in both domains.

</details>


### [153] [Optimized Instance Alteration for Explaining and Assessing Robustness of Classifiers](https://arxiv.org/abs/2607.06637)
*Evgenii Kuriabov,David Miller,Jia Li*

Main category: cs.LG

TL;DR: The paper proposes a unified optimization framework with an explainability-aware L0 penalty that both diagnoses misclassification and assesses robustness of black-box classifiers by generating sparse, interpretable modifications to flip predicted labels, validated on image and tabular data.


<details>
  <summary>Details</summary>
Motivation: Existing methods for diagnosing misclassification and assessing robustness are often treated separately, lacking a unified approach that simultaneously provides interpretability for why misclassifications occur and quantifies how susceptible a classifier is to small perturbations.

Method: An optimization framework combining an explainability-aware L0 (XA-L0) penalty for sparse/interpretable modifications with a classifier loss objective; introduces the TOR-Confusion Matrix to measure class-to-class transition probabilities under tolerance-bounded perturbations.

Result: Validated on both image and tabular datasets, the method successfully delivers joint interpretability and robustness assessment.

Conclusion: The proposed unified approach effectively bridges interpretability and robustness evaluation for black-box classifiers, offering a practical tool for understanding and testing classifier behavior.

Abstract: In this work, we propose a unified approach for diagnosing misclassification and assessing the robustness of black-box classifiers. Central to our method is an optimization framework that modifies an instance so that the classifier predicts a specified target label, while ensuring that the modification remains easily explainable. The objective function contains two components: an explainability-aware $L_0$ (XA-$L_0$) penalty that promotes sparse and interpretable modifications, and a classifier loss objective that steers the perturbed instance toward the desired output. This integrated optimization formulation is used both to identify the underlying causes of misclassification and to evaluate robustness by determining how an instance can change within a tolerance region before being reassigned to another class. To quantify robustness, we introduce the Tolerance Region Confusion Matrix (TOR-Confusion Matrix), which measures a classifier's susceptibility by modeling the class-to-class transition probabilities induced by tolerance-bounded perturbations. We validate the proposed method on both image and tabular datasets, demonstrating its ability to jointly deliver interpretability and robustness assessment.

</details>


### [154] [FourierQK: Spectral Preprocessing of Query-Key Projections Improves Transformer Attention](https://arxiv.org/abs/2607.07478)
*Athanasios Zeris*

Main category: cs.LG

TL;DR: Spectral preprocessing of Q/K projections via FFT with learned frequencies (4 multi-scale filters) reduces character-level language modelling perplexity by 79% over standard dot-product attention, while causal filters fail and benefit is unique to frequency-domain mixing (not random projections).


<details>
  <summary>Details</summary>
Motivation: To improve transformer attention on character-level language modelling, where standard dot-product attention struggles with fine-grained tokenization.

Method: Apply FFT-based spectral preprocessing to query and key projections, learning frequencies that span paragraph to word scales (e.g., 49, 27, 10, 6 tokens/cycle), while preserving the full attention score structure.

Result: Four learned frequencies achieve val=0.309 (Delta=+1.166), 79% reduction over standard attention. Single frequency confirms across seeds (mean val=0.236, std=0.019). Random projections show no improvement. Causal filters fail.

Conclusion: Bilateral (non-causal) spectral attention is uniquely effective for character-level modelling, defining an architectural boundary from causal spectral attention for word-scale tasks; the benefit stems from global frequency-domain mixing, not metric distortion.

Abstract: FFT-based spectral preprocessing of learned query-key (Q/K) projections substantially improves transformer attention on character-level language modelling. On TinyShakespeare: a fixed random spectral filter achieves val=1.031 (Delta=+0.443); a single learned frequency at paragraph scale achieves val=0.608 (Delta=+0.867); and four learned frequencies spanning paragraph to word scale achieve val=0.309 (Delta=+1.166), a 79% reduction over standard dot-product attention. The single-frequency result is confirmed across three random seeds (mean val=0.236, std=0.019). The four frequencies converge to a near-geometric multi-scale ordering (49, 27, 10, 6 tokens/cycle) corresponding to paragraph, sub-paragraph, phrase, and word scales. The gain is specific to spectral preprocessing: random orthogonal and non-orthogonal projections of Q/K produce no measurable improvement, suggesting the benefit comes from global frequency-domain mixing rather than metric distortion. All results are verified by a shuffled-validation diagnostic against positional leakage. Causal filters (Gaussian, Mexican Hat, Morlet) do not improve over standard attention at character-level tokenisation: the bilateral FFT kernel is structurally non-causal, coupling every position to future tokens. This defines an architectural boundary between bilateral spectral attention (this paper) and genuinely causal spectral attention at word-scale tokenisation (companion paper MorletQK). This work is architecturally distinct from FNet (Lee-Thorp et al., 2021), which replaces attention with Fourier mixing of token embeddings. Here, spectral preprocessing applies only to Q/K projections while the full attention score structure is preserved.

</details>


### [155] [UASPL: Uncertainty-Aware Self-Paced Learning with Evidential Neural Networks](https://arxiv.org/abs/2607.06638)
*Yifan Zhang,Yuxin Hu,Zhuobin Hao,Xiaozhuan Gao,Lipeng Pan*

Main category: cs.LG

TL;DR: The paper proposes UASPL, an uncertainty-aware self-paced learning method using evidential neural networks, which integrates predictive reliability into sample selection to improve classification performance, interpretability, and generality across datasets.


<details>
  <summary>Details</summary>
Motivation: Traditional SPL selects samples based on loss values, but smaller loss does not always mean simpler/reliable samples. The authors aim to integrate uncertainty estimation to improve sample selection reliability.

Method: UASPL integrates predictive reliability into sample selection via a general loss function within the Subjective Logic framework, leveraging evidential neural networks for uncertainty estimation. The loss function couples sample selection preference for interpretability.

Result: Experimental results on multiple datasets demonstrate that UASPL outperforms other SPL methods in classification performance, interpretability, and generality.

Conclusion: Uncertainty-aware sample selection is more reliable than loss-based selection in SPL, and the proposed framework is both effective and generalizable across different SPL variants.

Abstract: Self-paced learning (SPL) is an effective learning paradigm that simulates the human learning process by progressing from easy to difficult samples based on the value of the loss function during the learning process. It has shown great potential in improving model performance and training efficiency. However, the prediction results of samples with smaller loss values are not necessarily reliable, indicating that such samples are not always simple samples for the model. Hence, this article proposes an uncertainty-aware self-paced learning based on evidential neural networks, termed UASPL, which integrates predictive reliability into sample selection through a general loss function within the Subjective Logic framework. This loss function incorporates uncertainty estimation and can be extended to different variants of SPL. Moreover, this loss function couples a sample selection preference, thereby ensuring the interpretability of the sample selection process. Finally, the experimental results on multiple datasets show that UASPL outperforms other SPL methods in terms of classification performance, interpretability, and generality. The source code is available at: https://github.com/treelife979/UASPL.

</details>


### [156] [At-Grok Is Not Converged:A Measurement-Validity Audit for Grokking Representation Metrics](https://arxiv.org/abs/2607.06639)
*Truong Xuan Khanh*

Main category: cs.LG

TL;DR: This paper shows that neural network embedding compression in modular arithmetic tasks continues long after generalization, with compression lagging accuracy by thousands of steps. The authors develop an auditing toolkit to properly measure this phenomenon and find that LayerNorm significantly reduces the compression lag.


<details>
  <summary>Details</summary>
Motivation: To understand the relationship between network embedding compression and the grokking phenomenon, specifically challenging the assumption that compression coincides with generalization.

Method: Measuring effective rank of embeddings at the grokking transition across MLPs and transformers; performing one-variable ablations with LayerNorm; developing an auditing suite to separate onset from compression and detect censoring.

Result: Compression lags accuracy transition by ~10,000+ steps; effective rank at grokking overstates converged value by 3-5x (MLP) and 1.3-1.5x (transformer); LayerNorm reduces compression fraction from 0.87 to 0.25; depth law fails generality test on transformers.

Conclusion: Compression is not synonymous with generalization; the authors provide an auditing toolkit for properly measuring compression dynamics, and caution against naive interpretation of representation metrics at grokking onset.

Abstract: On modular arithmetic, a network's embedding keeps compressing for tens of thousands of steps after it has already generalized. Reading effective rank at the grokking transition overstates the converged value by 3-5x on an MLP, and by 1.3-1.5x on a transformer trained to convergence; on the MLP it also erases which cells compress at all. Compression lags the accuracy transition by an amount on the order of the time-to-grok, at least 10,000 steps, rather than coinciding with it. A one-variable ablation shows what sets the lag size: adding LayerNorm to an otherwise identical transformer moves the fraction of compression done by the grok step from 0.87 to 0.25, and a pre-registered control rules out scale invariance as the mechanism. We package this as an audit that separates onset from compression, flags censoring, excludes boundary cells that never fully generalize, and checks that the reference floor has plateaued, with an adversarial suite that caught a false-confidence bug in our own branch. A secondary, MLP-specific depth law linking norm budget to converged floor fails a generality test on a transformer and flips sign under free weight decay. Code and the toolkit are released.

</details>


### [157] [The Rank-One Corner: How Much Value Equivalence Does a Task Need from a World Model?](https://arxiv.org/abs/2607.06640)
*Donna Vakalis*

Main category: cs.LG

TL;DR: A world model's latent representation installs only as much predictive structure as the dimensionality of its training objective. With a scalar (value) objective, closure recovery is poor (R²=0.10→0.76); sweeping objective dimensionality from 1 to 4 installs exactly that many predictive directions. Value equivalence is dimensional, not all-or-nothing.


<details>
  <summary>Details</summary>
Motivation: To challenge the assumption that learned world models are uniformly "good" or "bad," and instead show that what a model captures about task structure depends narrowly on the dimensionality of the objective it is trained to predict.

Method: Using a DreamerV3 stack in a controlled environment with known ground-truth closure, the authors sweep the dimensionality of the training objective (scalar → full objective) and measure how much predictive structure is recoverable through linear probes and the model's own value head.

Result: A scalar objective installs only a one-dimensional projection (R² from 0.10 to 0.76). Sweeping objective dimensionality from 1 to 4 installs exactly that many predictive directions. The effect is dimensional, not an artifact of head form. On closed-loop tasks where structure is observable frame-by-frame, reconstruction suffices and scalar objective works.

Conclusion: Value equivalence is dimensional rather than all-or-nothing. The familiar single-reward objective is its rank-one corner; a model installs as much task structure as the objective it is asked to predict.

Abstract: A learned world model is usually judged by how faithfully it reconstructs its observations or predicts reward, as though quality were something the model simply has or lacks. But what a task actually needs from a model is narrower: the few predictive coordinates its queries depend on, which we call the closure. We show that how much of that closure a latent comes to represent is set not by the model's capacity or its observations but by the dimensionality of the objective it is trained against, and we measure this directly on a DreamerV3 stack in a controlled environment with known ground-truth closure. An aligned scalar value signal -- the objective at the heart of value equivalence -- installs only a one-dimensional projection of a closure that needs several dimensions: read through a single linear probe, the recoverable structure rises from R^2=0.10 to 0.76 as the scalar is replaced by the full objective. Sweeping the objective's dimensionality from one to four installs exactly that many predictive directions through an auxiliary head, and the same staircase appears -- at attenuated magnitude but the same rank -- through the model's own value head, so the dissociation is dimensional rather than an artifact of head form. Capacity-matched comparisons and in-situ pressure checks rule out the obvious alternatives. The law governs a regime, and we measure its boundary: on a companion closed-loop task whose structure is observable frame by frame, reconstruction installs that structure and the scalar objective suffices -- the objective decides what a latent represents exactly where cheaper training signals cannot already recover it. Value equivalence is thus not all-or-nothing but dimensional: the familiar single-reward objective is its rank-one corner, and a model installs as much of a task's structure as the objective it is asked to predict.

</details>


### [158] [The Approximation Ratio for the Risk of Myopic Bayesian Active Learning for Linear Regression](https://arxiv.org/abs/2607.06642)
*Stephen Mussmann*

Main category: cs.LG

TL;DR: This paper proves a tight (up to constant) approximation ratio for the greedy algorithm in active learning for linear regression, where the ratio is linear in the maximum initial leverage score (MILS).


<details>
  <summary>Details</summary>
Motivation: To understand the theoretical guarantees of the greedy algorithm (myopic Bayesian active learning) in optimal experimental design for linear regression, replacing long-term planning with one-step optimal choices.

Method: Deriving a tight approximation ratio for the greedy algorithm's risk, using the newly identified quantity MILS (maximum initial leverage score).

Result: A first-of-its-kind approximation ratio for the greedy algorithm that is tight up to an absolute constant, linear in MILS.

Conclusion: The greedy algorithm's performance is fundamentally governed by MILS; theoretical guarantees are validated via numerical simulations.

Abstract: Active learning studies the fundamental question: what data should we choose to observe? The greedy algorithm in optimal experiment design is a common heuristic and also equivalent to myopic Bayesian active learning for linear regression, the common framework where long-term planning is replaced with the one-step optimal choice. In this work, we prove a first-of-its-kind approximation ratio for the greedy algorithm's risk that is tight up to an absolute constant. The approximation ratio is linear in the maximum initial leverage score (MILS), a newly identified quantity fundamental to the greedy algorithm's performance. Finally, we illustrate the results with simple numerical simulations.

</details>


### [159] [Diffusion enabled Optimal Transport distances for graph matching](https://arxiv.org/abs/2607.06646)
*Iman Seyedi,Francesco Archetti*

Main category: cs.LG

TL;DR: DsrFGW improves graph comparison by integrating diffusion processes into optimal transport, outperforming traditional methods especially on noisy or partially observed graphs.


<details>
  <summary>Details</summary>
Motivation: Traditional Gromov-Wasserstein and semi-relaxed variants (srGW, srFGW) capture graph structure but struggle with sparse, noisy, or partially observed graphs.

Method: DsrFGW incorporates diffusion processes into the semi-relaxed Fused Gromov-Wasserstein framework, enabling information propagation across nodes to capture both local and global structural patterns.

Result: On 36 synthetic tasks, DsrFGW consistently outperforms srFGW (0-20 pp accuracy gains), achieves positive ARI where srFGW yields negative ARI in medium scenarios, and improves clustering quality in 92% of tasks under severe noise.

Conclusion: DsrFGW is a robust framework for graph comparison under structural uncertainty, with diffusion scales adapting to problem difficulty.

Abstract: This paper introduces Diffusion Semi-Relaxed Fused Gromov-Wasserstein (DsrFGW), a novel method for graph comparison that unifies node features and structural connectivity through optimal transport. While traditional Gromov-Wasserstein and semi-relaxed variants (srGW, srFGW) capture graph structure, they often struggle with sparse, noisy, or partially observed graphs. Inspired by Graph Diffusion Distance, which posits graphs are similar if they enable similar information transmission patterns, DsrFGW incorporates diffusion processes allowing information propagation across nodes, capturing local and global structural patterns while reducing sensitivity to noise or missing edges. An extensive evaluation on 36 synthetic pairwise graph matching tasks (easy, medium, hard) demonstrates consistent superiority over srFGW, achieving accuracy improvements of 0-20 percentage points and dramatic Adjusted Rand Index (ARI) gains: in medium-difficulty scenarios, srFGW often achieves negative ARI (worse than random) while DsrFGW offers better performance in terms of both internal and external clustering quality measures (i.e., Adjusted Rank Index and Accuracy with respect to the true underlying clusters, respectively). Even under severe noise, DsrFGW improves clustering quality in 92% of the synthetic tasks with optimal diffusion scales adapting to problem difficulty, establishing DsrFGW as a robust framework for graph comparison under structural uncertainty.

</details>


### [160] [Final Checkpoints Are Not Enough: Analyzing Latent Reasoning Faithfulness Along Training Trajectories](https://arxiv.org/abs/2607.06648)
*Hengyu Jin,Shu Yang,Di Wang*

Main category: cs.LG

TL;DR: This paper tracks how faithfulness of latent reasoning evolves during training, finding that causal contribution of latent steps decays over training and depends on answer format (binary vs. open-ended).


<details>
  <summary>Details</summary>
Motivation: Prior work found unfaithful behaviors in latent reasoning at converged checkpoints (e.g., replacing latent steps without changing answers), but it was unknown how these behaviors emerge during training.

Method: The authors track faithfulness across saved checkpoints for different latent reasoning paradigms using (1) counterfactual edits on input and (2) noise-ablation activation patches on latent reasoning steps.

Result: (i) At output level, methods look similarly unfaithful at convergence but follow divergent trajectories; (ii) at activation level, causal contribution of latent steps decays across training, correlated with output flips; (iii) activation-level trajectory diverges by answer format—decaying on binary choice, rising on open-ended decoding.

Conclusion: Latent reasoning faithfulness depends on training stage and answer format, not just convergence behavior.

Abstract: Latent reasoning methods perform multi-step inference entirely in the model's continuous hidden states, promising more compact and efficient reasoning. However, these opaque hidden states raise a question of faithfulness: whether these latent reasoning steps causally drive the final answer. Prior work investigates this question at converged checkpoints and reports several unfaithful behaviors, such as latent reasoning steps that can be replaced without changing the answer, but leaves how these behaviors form during training unexamined. We instead track how faithfulness evolves across saved checkpoints for different latent reasoning paradigms, applying a verifiable counterfactual edit on the input and a noise-ablation activation patch on the latent reasoning steps. We find that (i) at the output level, latent reasoning methods can look similarly unfaithful at convergence under counterfactual edits while following qualitatively divergent trajectories; (ii) at the activation level, the causal contribution of latent reasoning steps to the final answer decays across training for both paradigms, with the examples that flip on the output side in (i) also being the examples on which this contribution decays; and (iii) the activation-level trajectory diverges by answer format, decaying on binary choice and rising on open-ended decoding. These findings highlight that latent reasoning faithfulness depends on training stage and answer format.

</details>


### [161] [Entropy-Guided Tensor Compression for Multimodal Federated Learning on Edge Devices](https://arxiv.org/abs/2607.06651)
*Quoc Bao Phan,Tuy Tan Nguyen*

Main category: cs.LG

TL;DR: MESH-FL is an entropy-guided matrix product state compression framework for modality-heterogeneous federated learning that adaptively allocates compression ranks across layers and devices, achieving up to 56.8× compression with improved accuracy over uncompressed FedAvg on heterogeneous Raspberry Pi clusters.


<details>
  <summary>Details</summary>
Motivation: Existing FL compression schemes apply uniform policies across layers and devices, failing to account for modality-specific differences in spectral structure and compressibility in multimodal FL over heterogeneous mobile/edge devices.

Method: MESH-FL estimates spectral entropy of layer-wise updates via truncated SVD, adaptively allocates MPS compression ranks across layers/modalities/devices under per-client payload budgets, and solves a convex surrogate rank-allocation problem with theoretical convergence guarantees.

Result: On a 15-node heterogeneous Raspberry Pi 4/5 cluster, MESH-FL achieves up to 56.8× compression, surpasses uncompressed FedAvg by up to 2.01% in final accuracy, and reduces total transmitted data to convergence by up to 66×.

Conclusion: Entropy-guided adaptive rank allocation effectively exploits modality-specific compressibility in heterogeneous FL, achieving both higher compression and better accuracy than uniform approaches.

Abstract: Federated learning (FL) over mobile and edge devices increasingly involves multimodal models in which clients differ in both sensing capability and computational capacity. Existing update compression schemes typically apply uniform policies across layers and devices, without accounting for modality-specific differences in spectral structure and compressibility. We propose MESH-FL, an entropy-guided matrix product state (MPS) update-compression framework for modality-heterogeneous FL on resource-constrained devices. MESH-FL estimates the spectral entropy of each layer-wise update via truncated singular value decomposition and allocates MPS compression ranks adaptively across layers, modalities, and devices under per-client payload budgets. We show that higher spectral entropy necessitates a higher reconstruction rank under the majorization order on singular-value energy distributions. Building on this result, we prove that the proposed entropy-guided allocation solves a convex surrogate rank-allocation problem, preserves monotonicity under the exact payload model, and achieves convergence with an explicit compression-dependent error term. Experiments on a 15-node heterogeneous Raspberry Pi~4/5 cluster with modality-heterogeneous clients show that MESH-FL achieves up to $56.8\times$ compression while surpassing the uncompressed FedAvg baseline in final accuracy by up to 2.01%, and reduces total transmitted data to reach convergence by up to $66\times$.

</details>


### [162] [From Jumps to Signatures: a Generative Method for Temporal Point Processes](https://arxiv.org/abs/2607.06652)
*Niels Cariou-Kotlarek,Vasileios Lampos*

Main category: cs.LG

TL;DR: The paper introduces sigTPP, a signature-based generative model for Temporal Point Processes that uses interarrival embeddings to extend rough path signatures to event sequences, enabling path-level training loss and distributional discrepancy measures for evaluation.


<details>
  <summary>Details</summary>
Motivation: Rough path signatures cannot be directly applied to cadlag paths of Temporal Point Processes (TPPs), limiting signature methods for event sequences. Additionally, neural TPP models lack global sequence-level loss and proper distributional discrepancy measures for evaluating variable-length event sequences.

Method: Proposes interarrival embedding, a stable injective lift from jump paths to continuous bounded-variation paths, extending signature methods to discrete event sequences. Develops sigTPP, the first signature-based generative model for TPPs with path-level training loss, and derives three distributional discrepancies for evaluating generative TPP models.

Result: sigTPP achieves the best average rank across eight complementary metrics, outperforms or is within a standard error of the strongest baseline in 64% of dataset-metric pairs, and improves against every baseline by at least 19% on average.

Conclusion: The proposed pathwise framework successfully extends signature methods to TPPs, enabling both improved generation quality and principled evaluation of event sequence models.

Abstract: Rough path signatures are a universal feature map for continuous paths and, via the expected signature, characterise path distributions. These guarantees do not directly extend to cadlag paths of Temporal Point Processes (TPPs), limiting the use of signature methods for event sequences. Furthermore, neural TPP models, including recent generative approaches, optimise per-event objectives with no global sequence-level loss, while evaluation of variable-length event sequences lacks distributional discrepancy measures. This paper proposes a common pathwise framework for addressing these limitations. We introduce the interarrival embedding, a stable, injective lift from jump paths to continuous paths of bounded variation, extending signature methods to discrete event sequences. Our theoretical contributions give rise to sigTPP, the first signature-based generative model for TPPs, trained using a path-level loss on complete trajectories. We further analyse the space of counting paths and derive three distributional discrepancies, providing mathematically justified tools for evaluating generative TPP models. Across synthetic and real-world datasets, sigTPP achieves the best average rank based on eight complementary metrics, outperforms or is within a standard error of the strongest baseline in 64% of the dataset-metric pairs, and according to a relative score, improves against every baseline by at least 19% on average.

</details>


### [163] [Dual Attention Heads for Personalized Federated Learning in ECG Classification](https://arxiv.org/abs/2607.06653)
*Kien Le,Joseph Lindley,Quoc Bao Phan,Tuy Tan Nguyen*

Main category: cs.LG

TL;DR: FedDualAtt is a personalized federated learning approach for ECG classification that splits transformer attention heads into globally aggregated and locally kept branches to handle data heterogeneity across institutions.


<details>
  <summary>Details</summary>
Motivation: ECG data heterogeneity across healthcare providers makes federated learning challenging; existing FL methods struggle with institution-level recording differences.

Method: Splits transformer attention heads into global branches (aggregated via FedAvg for shared patterns) and local branches (client-specific for institution-level adaptation).

Result: Outperforms existing FL and personalized FL methods on the FedCVD benchmark for cardiovascular disease detection; different clients benefit from different global-local head ratios.

Conclusion: Personalized architectural split (global vs. local attention heads) effectively handles ECG data heterogeneity in federated learning.

Abstract: Federated learning (FL) enables collaborative model training across institutions without sharing sensitive patient data. However, the inherent heterogeneity of electrocardiogram (ECG) data across healthcare providers presents significant technical challenges for robust classification. We propose FedDualAtt, a personalized federated learning approach that splits transformer attention heads into global and local branches. Global heads are aggregated via FedAvg to capture shared cross-site patterns, while local heads remain client-specific to adapt to institution-level recording characteristics. Experiments on FedCVD, an FL benchmark for cardiovascular disease detection, demonstrate that FedDualAtt outperforms existing FL and personalized FL methods in ECG classification tasks. Analysis of global-local head ratios reveals that different clients benefit from varying levels of architectural personalization.

</details>


### [164] [Robust Human-AI Complementarity under Uncertainty](https://arxiv.org/abs/2607.06656)
*Yewon Byun,Bryan Wilder*

Main category: cs.LG

TL;DR: This paper investigates why human decision makers often fail to achieve complementary gains from AI predictions, identifying error correlation structure as a key factor. When AI errors are negatively correlated with human errors, robust strategies can guarantee improvements in expected utility.


<details>
  <summary>Details</summary>
Motivation: To understand why human decision makers routinely fail to realize complementary gains from AI predictions, despite models being designed to augment rather than replace human judgment.

Method: The authors study asymmetric information about the quality of information available to human vs. AI decision makers, and analyze how the error correlation structure between human and AI predictions impacts complementary value extraction.

Result: When AI prediction errors are negatively correlated with human errors, decision makers can construct robust strategies that guarantee improvements in expected utility.

Conclusion: The conditions for complementarity (negative error correlation) were empirically investigated using real-world forecasting benchmarks, suggesting practical relevance of this theoretical finding.

Abstract: Machine learning models are often intended to augment rather than replace human decision makers, by providing information that is complementary to human judgement.
  Yet, in practice, human decision makers routinely fail to realize such complementary gains, even when models provide useful signal.
  In this work, we study how asymmetric information about the quality of information available to a human decision maker vs. an AI impacts the ability of a decision maker to extract complementary value from AI predictions.
  We show that a key factor is the error correlation structure between human and AI predictions. In particular, when the AI's prediction errors are \textit{negatively correlated} with those of the human, the decision maker can construct robust strategies which guarantee improvements in expected utility. We empirically investigate whether these conditions for complementarity arise in practice, using real-world forecasting benchmarks.

</details>


### [165] [Trees from Marginals: Autoregressive drafting with factorized priors](https://arxiv.org/abs/2607.06763)
*Yuma Oda,Ryan Mathieu,Roman Knyazhitskiy,Artur Chakhvadze*

Main category: cs.LG

TL;DR: Weaver improves speculative decoding by using a lightweight autoregressive adapter with proposal trees from top-K marginals, achieving 4.37x speedup over autoregressive decoding and 24.7% over DFlash.


<details>
  <summary>Details</summary>
Motivation: Factorized draft models suffer from degrading acceptance rates as speculative budget grows due to their independence assumption, limiting interactivity gains.

Method: Weaver introduces a lightweight autoregressive adapter that constructs proposal trees from top-K marginals of a factorized drafter, restoring conditional dependencies without full-vocabulary projection, plus a rollback-free tree-verification algorithm with optimized CUDA kernels in SGLang.

Result: 4.37-fold speedup over autoregressive decoding, 24.7% improvement over DFlash baseline.

Conclusion: Weaver effectively addresses the independence assumption limitation of factorized draft models, achieving significant speedups through both model and systems optimizations.

Abstract: Speculative decoding greatly increases the interactivity of autoregressive language models by trading off computation for extra tokens generated in a single forward pass. Factorized draft models are especially efficient because they predict future-token marginals in parallel, but their independence assumption causes acceptance rates to degrade sharply as the speculative budget grows. We analyze this limitation and introduce Weaver, a lightweight autoregressive adapter that constructs proposal trees from the top-K marginals of a factorized drafter. Weaver restores conditional dependencies between proposed tokens while avoiding a full-vocabulary projection. To support fast verification for models with Gated Delta Net layers, we derive a rollback-free tree-verification algorithm and implement optimized CUDA kernels in SGLang. By combining these model and systems contributions we achieve a 4.37-fold speedup over autoregressive decoding, and outperform a highly optimized DFlash baseline by 24.7%.

</details>


### [166] [Efficient Long-Horizon Learning for Learned Optimization](https://arxiv.org/abs/2607.06772)
*Xiaolong Huang,Benjamin Thérien,James Harrison,Eugene Belilovsky*

Main category: cs.LG

TL;DR: ELO proposes an efficient meta-training algorithm that reallocates compute to longer failure regimes and uses decoupled progressive expert supervision to improve learned optimizers, enabling them to outperform AdamW across language modeling and image classification tasks with minimal meta-training cost.


<details>
  <summary>Details</summary>
Motivation: Current learned optimizers (LOs) suffer from two main limitations: (1) inefficient scaling of meta-training to long-horizon inner problems, and (2) often failing to surpass hand-designed optimizers like Adam.

Method: ELO (Efficient Long-hOrizon learning) employs two key strategies: (1) reallocating redundant meta-training compute to longer failure regimes for efficient long-horizon learning, and (2) enforcing decoupled progressive expert supervision to provide stable meta-learning signals and improve LO generalization.

Result: ELO substantially improves long-unroll performance and out-of-distribution generalization of base LOs. ELO-Celo2 consistently outperforms well-tuned AdamW on GPT-2 (124M/350M), ViT-B/16, and ResNet-50 tasks, and remains competitive with Muon on language modeling. All baselines require less than 7 H100 GPU-hours.

Conclusion: ELO effectively addresses scalability and performance limitations of learned optimizers, achieving superior results over hand-designed optimizers with minimal computational cost, suggesting a promising direction for practical learned optimization.

Abstract: Learned optimization aims to improve upon hand-designed optimizers (e.g., Adam and Muon) by meta-learning small neural network optimizers over a distribution of tasks. While recent work has greatly advanced the architectural design and inductive biases of learned optimizers (LOs), current meta-training approaches still suffer from two main difficulties: (1) they cannot efficiently scale meta-training to long-horizon inner problems and (2) they often fail to surpass comparable hand-designed optimizers. To address these limitations, we propose Efficient Long-hOrizon (ELO) learning, an efficient meta-training algorithm that (1) reallocates redundant meta-training compute to longer failure regimes, achieving efficient long-horizon learning, and (2) enforces decoupled progressive expert supervision, providing stable meta-learning signals that additionally improve the generalization of LOs. Our empirical study evaluates ELO for meta-training both element-wise and matrix-based LOs. Across downstream language modeling (GPT-2-124M/350M on FineWeb) and image classification (ViT-B/16, ResNet-50 on ImageNet-1K) tasks, ELO substantially improves the long-unroll performance and out-of-distribution generalization of the base LOs. In particular, ELO-Celo2 consistently outperforms well-tuned AdamW across all evaluated tasks, while remaining competitive with Muon on language modeling. \textit{Notably, all ELO baselines require less than 7 H100 GPU-hours for meta-training.}

</details>


### [167] [Efficient Bayesian Deep Ensembles via Analytic Predictive Inference](https://arxiv.org/abs/2607.06776)
*Sina Aghaee Dabaghan Fard,Marie Maros,Jaesung Lee*

Main category: cs.LG

TL;DR: A Bayesian deep ensemble method that uses low-dimensional representations, closed-form Bayesian aggregation, and independent training to achieve interpretable and calibrated predictive regression with computational efficiency.


<details>
  <summary>Details</summary>
Motivation: To enhance interpretability of deep ensemble methods for predictive regression while maintaining competitive performance, calibrated uncertainty estimates, and computational efficiency — enabling use as a standalone predictor or component in larger learning systems.

Method: Three key components: (i) low-dimensional ensemble representation using a small number of trained neural predictors; (ii) closed-form Bayesian linear regression to aggregate ensemble predictions with interpretable posterior weights; (iii) independent training of multiple neural networks to ensure diversity and robust uncertainty calibration.

Result: Competitive predictive performance on standard regression benchmarks with reliable uncertainty estimates across settings.

Conclusion: The proposed method achieves a balance between interpretability, predictive performance, computational efficiency, and well-calibrated uncertainty, making it practical for both standalone use and integration into broader learning systems.

Abstract: We introduce an efficient Bayesian deep ensemble method for predictive regression designed to enhance interpretability while maintaining competitive predictive performance and computational efficiency. Our method combines the statistical rigor of Bayesian inference with the scalability of deep ensembles, providing calibrated uncertainty estimates that enable its use not only for standalone prediction but also as a component within broader learning systems. To achieve these goals, our work relies on three key design components: (i) low-dimensional ensemble representation: predictions are expressed as a combination of a small number of trained neural predictors, enabling scalable inference whose cost depends on ensemble size rather than dataset size; (ii) closed-form Bayesian aggregation: ensemble predictions are combined using Bayesian linear regression, yielding interpretable posterior weights and calibrated uncertainty without approximate inference; and (iii) Independent ensemble training: multiple neural networks are trained separately, producing diverse predictive representations that improve robustness and uncertainty calibration. Empirical results on standard regression benchmarks demonstrate that the proposed approach achieves competitive predictive performance while maintaining reliable uncertainty estimates across settings.

</details>


### [168] [On Explicit Super-Expressive Approximation for Neural Networks](https://arxiv.org/abs/2607.06781)
*Feng-Lei Fan,Ze-Yu Li,Chen-Yu Wang,Jian-Jun Wang*

Main category: cs.LG

TL;DR: This paper introduces the Chinese Remainder Theorem as a constructive encoding mechanism for fixed-architecture neural networks, achieving explicit parameter-error trade-offs for Lipschitz and Hölder-smooth functions with bounded parameter magnitudes.


<details>
  <summary>Details</summary>
Motivation: Prior work showed super-expressive approximation using fixed-size networks but lacked quantitative and non-asymptotic characterizations of parameter magnitude with respect to approximation error. This work resolves that gap.

Method: Uses the Chinese Remainder Theorem as a constructive encoding mechanism. Constructs two types of fixed-architecture networks: (1) width max{D,4}, depth 5 for Lipschitz functions with explicit parameter-error trade-offs; (2) width max{2D, D+5N+1}, depth r+9 for Hölder-smooth functions, achieving parameter magnitude log₂𝒫 = O(ε^{-2D/(r+γ)} log(1/ε)).

Result: Achieves explicit parameter-error trade-offs in fixed-architecture networks. For Hölder-smooth functions, the parameter magnitude is bounded by log₂𝒫 = O(ε^{-2D/(r+γ)} log(1/ε)), which is the dual result compared to the parameter-bounded and architecture-unbounded paradigm.

Conclusion: The paper establishes a novel dual perspective: instead of fixing parameter budgets and allowing architecture to grow, they fix the architecture and provide tight bounds on parameter growth, using the Chinese Remainder Theorem as a key encoding tool.

Abstract: In this work, we investigate the fixed-architecture neural network approximation with explicit parameter bounds and elementary activations. While prior work demonstrated super-expressive approximation using fixed-size networks, they lack quantitative and non-asymptotic characterizations of parameter magnitude with respect to the approximation error. We resolve this issue by introducing the Chinese Remainder Theorem as a constructive encoding mechanism. For Lipschitz continuous functions on $[0,1]^D$, we construct a width-$\max\{D,4\}$, depth-$5$ network with explicit parameter-error trade-offs. For Hölder-smooth functions in $C^{r,γ}_A\left([0,1]^D\right)$, our fixed network of width $\max\{2D,\ D+5N+1\}$ and depth $r + 9$ achieves the parameter magnitude $\mathcal{P}$ bounded by $\log_2 \mathcal{P}=\mathcal{O}\bigl(\varepsilon^{-2D/(r+γ)}\log(1/\varepsilon)\bigr)$. This is the dual result compared to those in the parameter-bounded and architecture-unbounded paradigm.

</details>


### [169] [Enhancing deep learning models for time series classification via knowledge distillation](https://arxiv.org/abs/2607.06796)
*Javidan Abdullayev,Maxime Devanne,Jonathan Weber,Germain Forestier*

Main category: cs.LG

TL;DR: This paper investigates Knowledge Distillation (KD) for Time Series Classification (TSC) using FCN, Inception, and ConvTran architectures on the UCR Archive. Results show KD effectively benefits intermediate-complexity student models, with parameter reductions of 38x (FCN) and 42% (Inception), and improved attention-based models.


<details>
  <summary>Details</summary>
Motivation: To address the high computational and memory demands of deep learning models for time series classification in resource-limited environments by applying Knowledge Distillation to create efficient student models without significant performance loss.

Method: Knowledge Distillation applied to three TSC architectures (FCN, Inception, ConvTran). Modified architectural components (convolutional filters, Inception modules, attention heads) and evaluated on the UCR Archive benchmark datasets.

Result: KD benefits intermediate-complexity student models most. Distilled FCN: 38x parameter reduction. Distilled Inception: near-teacher performance with 42% fewer parameters. Distilled ConvTran with 2 attention heads showed most significant improvement.

Conclusion: Knowledge Distillation is effective for Time Series Classification, particularly for models of intermediate complexity, enabling significant parameter reduction while maintaining competitive performance. Implementation is publicly available.

Abstract: Deep learning has achieved remarkable success in various domains including time series analysis, computer vision and natural language processing. However, high computational and memory demands of state-of-the-art architectures pose challenges for deployment in resource-limited environments. Knowledge Distillation (KD) addresses this by transferring knowledge from a large teacher model to a smaller, more efficient student model while maintaining competitive performance. In this work, we investigate the effectiveness of KD for Time Series Classification (TSC) across three architectures: the classical Fully Convolutional Network (FCN), the convolutional Inception model and the transformer-based ConvTran model. We evaluate our approach on UCR Archive, the largest benchmark repository of time series datasets, by modifying architectural components such as convolutional filters, Inception modules and attention heads across the three architectures. Our results consistently show that KD most effectively benefits student models of intermediate complexity across all three architectures, with the distilled FCN student reducing parameters by a factor of 38, the distilled Inception student achieving nearly the same performance as the teacher with 42% fewer parameters and the distilled ConvTran student with 2 attention heads showing the most significant improvement through distillation. To encourage further research and reproducibility, we provide our implementation at https://github.com/MSD-IRIMAS/KD-4-TSC.

</details>


### [170] [What Predicts Correctness in Text-to-SQL? A Selective-Prediction Study](https://arxiv.org/abs/2607.06799)
*Robert Richardson*

Main category: cs.LG

TL;DR: This paper evaluates which signals best predict correctness of AI-generated SQL queries. Black-box signals (self-consistency, executability) achieve ~0.61-0.68 AUROC; LLM judges reach 0.72-0.82 AUROC, with two-provider ensembles performing best. Fine-tuned verifiers work well in-domain (~0.77-0.79) but fail to generalize across schemas (~0.66), suggesting cross-schema verification requires large frozen reasoning models rather than fine-tuning.


<details>
  <summary>Details</summary>
Motivation: To systematically evaluate which signals reliably predict whether AI-generated SQL queries are correct, especially on complex multi-table text-to-SQL benchmarks.

Method: Compared black-box signals (string/structure/execution self-consistency, schema relevance, executability), white-box log-probabilities, LLM judges, and fine-tuned verifiers (encoder and generative) using AUROC on BIRD and Spider benchmarks.

Result: Black-box signals max at ~0.68 AUROC; LLM judges achieve 0.72-0.78; two-provider ensemble reaches 0.82 AUROC with low calibration error (0.03). Fine-tuned verifiers reach 0.77-0.79 in-domain but drop to ~0.66 on unseen schemas. Scaling, schema diversity, and distillation failed to close the generalization gap.

Conclusion: Uncertainty estimation for SQL correctness depends on reasoning-based signals. Fine-tuned verifiers are effective in-domain but cannot generalize across schemas; cross-schema verification currently requires large frozen reasoning models.

Abstract: Evaluating uncertainty in AI-generated SQL queries requires estimating whether a query is correct, where correct means it executes to the same result as a human-written reference. We study which signals predict correctness on hard multi-table text-to-SQL, using AUROC to measure how well each ranks correct queries above incorrect ones. On BIRD and Spider, black-box signals such as string, structural, and execution self-consistency, a schema-relevance score, and query executability all fall between about 0.61 and 0.68 AUROC, with string self-consistency strongest at 0.675; white-box log-probability is similar (0.67). The signals that move past this ceiling are verification-based: an LLM judge scores from 0.72 (GPT-4o-mini) to 0.78 (Claude). Judges from different providers make different errors, so a two-provider ensemble reaches 0.82 AUROC with a well-calibrated probability (expected calibration error 0.03) and supports useful abstention frontiers (for example, answering 27% of questions at 24% selective risk) where self-consistency offers no valid low-risk subset. The pattern holds across two benchmarks, two generators, and two judge providers. We also ask whether a verifier can be trained. Fine-tuned verifiers, both encoder and generative, reach about 0.77 to 0.79 AUROC in-distribution but fall to about 0.66 on unseen schemas; scaling to 7B, adding schema diversity, distilling a strong judge's rationales, and cross-benchmark training all fail to close that gap. Cross-schema transfer appears to track model scale and reasoning rather than fine-tuning. In practice, correctness uncertainty for text-to-SQL lives in reasoning-based signals: a fine-tuned verifier is a good in-domain tool, but a verifier that generalizes across schemas currently means a large frozen reasoning model.

</details>


### [171] [LEMUR 2: Unlocking Neural Network Diversity for AI](https://arxiv.org/abs/2607.06839)
*Tolgay Atinc Uzun,Waleed Khalid,Saif U Din,Sai Revanth Mulukuledu,Akashdeep Singh,Chandini Vysyaraju,Raghuvir Duvvuri,Avi Goyal,Yashkumar Rajeshbhai Lukhi,Muhammad A. Hussain,Krunal Jesani,Usha Shrestha,Yash Mittal,Roman Kochnev,Pritam Kadam,Mohsin Ikram,Harsh R. Moradiya,Alice Arslanian,Dmitry Ignatov,Radu Timofte*

Main category: cs.LG

TL;DR: LEMUR 2 is a large-scale benchmark framework with 14,000+ architectures and 750,000+ training records, covering diverse generation methods (mutation, evolution, fractal, LLM-guided), multimodal tasks, and real-device deployment on mobile/VR platforms, enabling cross-domain and hardware-aware NAS research.


<details>
  <summary>Details</summary>
Motivation: Existing NAS benchmarks are too narrow, task-specific, and lack cross-domain or deployment-aware evaluation, limiting architectural diversity and real-world applicability.

Method: LEMUR 2 generates architectures via AST-based code mutation, genetic/RL evolution, fractal architectures, and LLM-guided synthesis (including NN-RAG using 900+ PyTorch modules). It uses NN-VR and NN-Lite pipelines for automated deployment and latency benchmarking on mobile/VR platforms.

Result: 14,000+ architectures, 750,000+ structured training records covering multimodal tasks (image captioning, text-to-image, language modeling) with real-device performance metadata.

Conclusion: LEMUR 2 provides a new data foundation for LLM-driven AutoML, reproducible AI design, and architectural generalization across modalities and hardware platforms.

Abstract: Existing NAS benchmarks (e.g., NAS-Bench, NATS-Bench) cover only narrow, task-specific regions of the architectural design space and lack cross-domain or deployment-aware evaluation. LEMUR 2 introduces a large-scale, extensible framework unifying generative, evaluative, and deployment pipelines to unlock neural-network diversity. It comprises over 14,000 distinct architectures and more than 750,000 structured training records documenting model performance, hyperparameters, and task outcomes. These models were produced through AST-based code mutation, genetic and reinforcement-learning evolution, generation of fractal architectures, and synthesis guided by a Large Language Model (LLM). This includes deep models generated with the retrieval-augmented system NN-RAG, which derived and used architectural motifs from over 900 PyTorch modules extracted from public repositories. LEMUR 2 further employs NN-VR and NN-Lite pipelines for automated deployment and latency benchmarking on heterogeneous mobile and Unity-based VR platforms, providing real-device performance metadata. It spans multimodal tasks, image captioning, text-to-image synthesis, and language modeling, supporting cross-domain analysis of architectural transferability. By linking diverse architectures, tasks, and deployment data, LEMUR 2 provides the data foundation for LLM fine-tuning and coupling diverse architectural origins with large-scale, cross-platform empirical validation. This dataset defines a new basis for reproducible and data-driven AI design, advancing the emerging paradigm of LLM-driven AutoML and architectural generalization across modalities and hardware.

</details>


### [172] [A Gold-Standard Study of What Makes a Lightweight Game-Playing Agent Strong](https://arxiv.org/abs/2607.06854)
*Nima Kelidari,Mohammadsaeed Haghi,Mahdi Salmani*

Main category: cs.LG

TL;DR: This paper develops a lightweight, game-agnostic reinforcement learning recipe for imperfect-information card games by using a strong rule-based expert as a fixed yardstick (not for training). The authors identify key techniques that improve agent performance (trust region updates, well-aimed reward, curriculum learning, warm starting, best checkpoint) and several that do not (reward shaping, learned embeddings, imitation learning, LLM opponents). They find network capacity is not the limiting factor—information is.


<details>
  <summary>Details</summary>
Motivation: Reinforcement learning agents for imperfect-information card games are only as strong as their training opponents and are hard to evaluate because they beat random opponents easily but only tie copies of themselves. A reliable evaluation yardstick is needed.

Method: The authors build a strong fixed rule-based expert for Gin Rummy used only as an evaluation yardstick. They conduct over a hundred runs isolating which techniques (trust region updates, reward shaping, curriculum learning, warm starting, checkpoint selection, various neural architectures) improve lightweight agents. They also test on Leduc Hold'em where the optimum is computable.

Result: The rule-based expert beats every trained agent 70-99% of the time. Stacking effective techniques lifts a self-play champion from ~30% to ~36% win rate against the expert. Trust region updates, well-aimed reward, curriculum learning, warm starting, and best checkpoint selection help. Reward shaping, learned embeddings, imitation/DAgger, and LLM opponents do not. Network architecture comparisons (MLP, CNN, set-based, attention, recurrent) show extra capacity does not break the performance ceiling.

Conclusion: The paper presents a lightweight, game-agnostic recipe that trains competitive agents without needing to train against a strong expert. The performance ceiling is information-limited rather than capacity-limited. The approach generalizes to Leduc Hold'em and is released as a reusable package.

Abstract: Reinforcement learning agents for imperfect-information card games are only as strong as the opponents they train against, and they are hard to grade, since they beat a random opponent over 99 percent of the time and only tie copies of themselves. So we build a strong, fixed, rule-based expert for Gin Rummy and use it only as a yardstick, never for training. It beats every agent we trained 70 to 99 percent of the time. Across more than a hundred runs, we isolate what makes a lightweight agent stronger. Trust region updates, a well-aimed reward, a curriculum of tougher opponents, warm starting, and keeping the best checkpoint all help, and stacking them lifts a self-play champion from about 30 to 36 percent against the expert. Several ideas did not pay off. Short-term and longer-term reward shaping, learned state embeddings, imitation and DAgger, and a live large language model opponent were each unhelpful, too slow, or too heavy to train at scale. Comparing MLP, convolutional, set-based, attention, and recurrent encoders shows that extra capacity does little to break the ceiling, suggesting the limit is information rather than network size. We add standard baselines (neural fictitious self-play and information set Monte Carlo search) and confirm the approach carries over to Leduc Hold'em, where the optimum is computable. The result is a lightweight, game-agnostic recipe that trains competitive agents without training on the expert, for any game a small model can handle, reported with robust statistics and released as a reusable package.

</details>


### [173] [Geometric Self-Distillation for Reasoning Generalization](https://arxiv.org/abs/2607.06855)
*Josip Jukić,Ivan Titov*

Main category: cs.LG

TL;DR: GeoSD is a geometric self-distillation method that counters predictive drift in on-policy distillation by using Hellinger loss weighting and Fisher-Rao proximal regularization, improving OOD reasoning by 5.7-8.6 points across model scales.


<details>
  <summary>Details</summary>
Motivation: On-policy self-distillation provides dense supervision but causes drift that degrades out-of-distribution reasoning because the teacher's privileged context makes it confident about continuations the student cannot justify, and the strongest pulls occur where disagreement is largest.

Method: GeoSD uses a Hellinger loss (scaling teacher preferences by student's existing overlap) to attenuate pulls on unsupported tokens, plus a Fisher-Rao proximal term that penalizes prediction drift from a recent checkpoint. Both use natural-gradient updates in the geometry of next-token distributions.

Result: Across mathematical reasoning benchmarks and three model families (1.7B to 32B), GeoSD preserves in-distribution gains of self-distillation while improving average OOD accuracy by 5.7-8.6 points over the base model, whereas standard matching degrades OOD performance.

Conclusion: Standard matching fails OOD because it drains probability mass from alternatives at high-entropy states, producing confident wrong answers. GeoSD keeps alternatives in reach, preventing this collapse.

Abstract: On-policy distillation is a practical post-training recipe for large language models, supplying dense teacher supervision on the student's own trajectories. In privileged-context self-distillation, teacher and student are the same model conditioned on the same prefix, but the teacher also sees a hint or the full solution trace. This makes supervision abundant but harder to trust: the teacher can be confident about continuations its privileged view makes obvious but the student cannot yet justify. The distillation pull is strongest where teacher and student disagree most, and over many updates it accumulates into drift that degrades out-of-distribution (OOD) reasoning. We introduce GeoSD, a geometric self-distillation objective that treats this drift as movement in the student's predictive behavior and counters it in two complementary ways. A Hellinger loss scales each teacher preference by the overlap the student already shares with it, attenuating the pull on tokens the student cannot yet support. Since these pulls still compound over training, a proximal term penalizes how far the student's predictions drift from a recent checkpoint, measured as a Fisher-Rao distance. Both are distances in the same geometry of next-token distributions, and a natural-gradient update takes its steps in that geometry rather than in parameter space. Across mathematical reasoning benchmarks and three model families, GeoSD preserves the in-distribution gains of self-distillation while improving average OOD accuracy by 5.7-8.6 points over the base model, with gains holding across model scales from 1.7B to 32B. Analyzing why standard matching fails out of distribution, we find it wins agreement with the teacher by draining mass from alternatives at high-entropy states, resulting in confident agreement on wrong answers, whereas GeoSD keeps those alternatives in reach.

</details>


### [174] [Best-Arm Identification with Generative Proxy](https://arxiv.org/abs/2607.06879)
*Tianyi Ma,Hanzhang Qin,Ruihao Zhu,Jierui Zuo*

Main category: cs.LG

TL;DR: PROBE is a phase-elimination algorithm for best-arm identification that leverages cheap proxy scores (e.g., from ML/LLMs) correlated with costly rewards. It uses control-variate adjustment to reduce sample complexity by factor 1-ρ², maintains valid confidence via ordinary least squares certificates, and achieves near-oracle performance even with unknown correlation ρ.


<details>
  <summary>Details</summary>
Motivation: In many decision-making applications, reward observations are costly, but cheap predictions (from ML/LLMs) are increasingly available. The paper aims to leverage these cheap proxy scores to reduce the number of costly reward pulls needed for best-arm identification, while handling the unknown correlation between proxy and reward.

Method: PROBE (PRoxy OLS for Best-arm Exploration): a phase-elimination algorithm that pairs each costly reward pull with a cheap proxy score, applies control-variate adjustment to reduce variance by factor 1-ρ², and maintains an upper certificate on residual variance using ordinary least squares with exact chi-square distribution guarantees (valid regardless of unknown correlation).

Result: The algorithm is δ-PAC and achieves the known-correlation oracle sample complexity up to a constant multiplicative factor plus a constant additive calibration cost. Extends to (ε,δ)-PAC with minimal changes. Numerical experiments on synthetic data and auto-loan pricing replay with LLM/tabular proxies confirm sample savings scale with correlation strength as theoretically predicted.

Conclusion: PROBE effectively exploits cheap proxy signals to reduce costly reward pulls in best-arm identification, with rigorous statistical guarantees that hold even when the reward-proxy correlation is unknown and must be learned online. The method is both theoretically sound and empirically validated.

Abstract: Best-arm identification is a canonical model for data-driven decision-making, but in many applications each reward observation is costly. Motivated by the growing availability of cheap predictions from machine learning and large language models, we study fixed-confidence best-arm identification in which each costly reward pull is paired with a cheap but correlated proxy score. The marginal mean of the proxy can be estimated offline and is treated as known, whereas its correlation $ρ$ with the reward, which governs how much the proxy helps, is unknown and must be learned online in pair with real rewards. We show that a control-variate adjustment turns this model into a heteroscedastic identification problem whose oracle sample complexity improves by residual variance $1-ρ^2$. The central difficulty is that the correlation must be learned from the same costly samples that identification consumes online, and that a plug-in estimate of the residual variance is anti-conservative and can compromise correctness. We propose PROBE (PRoxy OLS for Best-arm Exploration), a phase-elimination algorithm that directly maintains an upper certificate on the residual variance with an ordinary least squares fit, whose exact chi-square law keeps the certificate valid regardless of the unknown correlation. We prove that PROBE is $δ$-PAC and attains the known-correlation oracle sample complexity up to a constant multiplicative factor and a constant additive calibration cost. The guarantee extends to the $(ε,δ)$-PAC setting under minimal changes to the algorithm. Numerical experiments on synthetic instances and on an auto-loan pricing replay with large language model and tabular proxies confirm that the sample savings of PROBE scale with the strength of the reward-proxy correlation, exactly as the theory predicts.

</details>


### [175] [Converge to Surprise: Evolutionary Self-supervised Image Clustering](https://arxiv.org/abs/2607.06887)
*Canlin Zhang,Xiuwen Liu*

Main category: cs.LG

TL;DR: Proposes a self-supervised image clustering framework that replaces gradient-descent-based loss optimization with a "surprise score" maximization approach, using an evolution-strategy outer loop and periodic gradient-descent inner loop, achieving state-of-the-art results in non-parametric clustering.


<details>
  <summary>Details</summary>
Motivation: Current self-supervised clustering methods rely on gradient descent requiring clearly defined targets (contrastive splits, masks, pseudo-labels, etc.). The authors seek to drop this requirement entirely.

Method: Uses Maximum Entropy Principle as null hypothesis (H0: pixels are i.i.d.), defines a "surprise score" measuring how unlikely model output is under H0. Optimizes via a "converge-to-surprise" scheme: ES outer loop maximizes surprise score without gradients, periodic gradient-descent inner loop uses discovered clusters as surrogate targets.

Result: Achieves new state-of-the-art results on standard image benchmarks in non-parametric self-supervised image clustering (strictest deep-clustering setting where ground-truth class count is not given).

Conclusion: A surprise-score-based optimization framework that does not require per-step differentiable loss can outperform traditional gradient-descent-based self-supervised clustering methods.

Abstract: Most self-supervised image clustering models, actually almost all deep learning approaches, are based on gradient descent: In order to calculate the loss, every optimization step requires a clearly defined target, whether a contrastive split, a masked patch or entity, an EMA-teacher output, a pseudo-label, or a differentiable information-theoretic functional. We propose a self-supervised framework that drops this requirement for image clustering. Without any prior knowledge, we have to assume that each pixel is i.i.d. according to the Principle of Maximum Entropy. Taking this as our null hypothesis H0, we define a "surprise score" that measures how unlikely the model's output representation would be under H0. Maximizing the surprise score forces the deep learning model to reject H0 - equivalently, to discover non-random feature from data. Also, here is our fundamental assumption: a surprise score cannot, in general, be reduced to a per-step loss. Hence, we propose the "converge-to-surprise" scheme to optimize our model: an evolution-strategy (ES) outer loop, which directly maximizes the surprise score without needing its gradient, paired with a periodic gradient-descent inner loop, which uses the surprising clusters already discovered by ES as surrogate targets. On standard image benchmarks, our framework achieves new state-of-the-art results in non-parametric self-supervised image clustering - the strictest deep-clustering setting, in which the number of ground-truth classes is not given to the model.

</details>


### [176] [Latency-Constrained DNN Architecture Learning for Edge Systems using Zerorized Batch Normalization](https://arxiv.org/abs/2607.06922)
*Shuo Huai,Di Liu,Hao Kong,Weichen Liu,Ravi Subramaniam,Christian Makaya,Qian Lin*

Main category: cs.LG

TL;DR: This paper proposes a latency-oriented neural network learning method that optimizes DNN models for high accuracy while meeting strict latency constraints. It uses a universal hardware-customized latency predictor with one-shot training. Experiments show it can meet hard latency constraints (e.g., 34ms) with minimal accuracy loss or even improvement on edge devices like Jetson Nano and TX2.


<details>
  <summary>Details</summary>
Motivation: Deep learning on edge devices faces strict real-time latency constraints, but conventional neural network optimization methods do not directly control the temporal cost of model inference. Deciding the optimal number of neurons for latency-critical edge systems is not intuitive, motivating a latency-oriented optimization approach.

Method: The authors propose a latency-oriented learning method that optimizes DNNs for accuracy while satisfying a given latency constraint. It includes a universal hardware-customized latency predictor to estimate inference time, enabling a one-shot training process to meet latency requirements without iterative tuning.

Result: On Jetson Nano with ImageNet-100, GoogLeNet's latency reduced from 40.32ms to 34ms with only 0.14% accuracy drop (0.04% with quantization). On Jetson TX2, VGG-19 compressed from 119.98ms to 34ms with 0.5% accuracy improvement; GoogLeNet scaled up from 20.27ms to 34ms with 0.78% higher accuracy.

Conclusion: The proposed latency-oriented method effectively optimizes DNNs to meet hard latency constraints on edge devices with minimal accuracy degradation or even accuracy improvements. The framework is open-sourced for reproducibility and practical use.

Abstract: Deep learning applications have been widely adopted on edge devices, to mitigate the privacy and latency issues of accessing cloud servers. Deciding the number of neurons during the design of a deep neural network to maximize performance is not intuitive. Particularly, many application scenarios are real-time and have a strict latency constraint, while conventional neural network optimization methods do not directly change the temporal cost of model inference for latency-critical edge systems. In this work, we propose a latency-oriented neural network learning method to optimize models for high accuracy while fulfilling the latency constraint. For efficiency, we also introduce a universal hardware-customized latency predictor to optimize this procedure to learn a model that satisfies the latency constraint by only a one-shot training process. The experiment results reveal that, compared to state-of-the-art methods, our approach can well-fit the 'hard' latency constraint and achieve high accuracy. Under the same training settings as the original model and satisfying a 34 ms latency constraint on the ImageNet-100 dataset, we reduce GoogLeNet's latency from 40.32 ms to 34 ms with a 0.14% accuracy reduction on the NVIDIA Jetson Nano. When coupled with quantization, our method can be further improved to only 0.04% drop for GoogLeNet. On the NVIDIA Jetson TX2, we compress VGG-19 from 119.98 ms to 34 ms and even improve its accuracy by 0.5%, and we scale GoogLeNet up from 20.27 ms to 34 ms and achieve higher accuracy by 0.78%. We also open source this framework at https://github.com/ntuliuteam/ZeroBN

</details>


### [177] [Intrinsic-Noise Consolidation: A Doob-Barrier-Conditioned Diffusion Turns Analog Device Noise into a Continual-Learning Resource](https://arxiv.org/abs/2607.06924)
*Gunner Levi Howe*

Main category: cs.LG

TL;DR: This paper proposes using intrinsic noise in analog neuromorphic hardware for memory consolidation, rather than treating it as a liability. The authors frame per-synapse consolidation as a Doob h-transform, conditioning weight dynamics to avoid crossing memory-critical barriers. They show that increasing noise can improve sequential-task retention in an inverted-U pattern, validated on Split-MNIST and on BrainScaleS-2 silicon hardware.


<details>
  <summary>Details</summary>
Motivation: Current neuromorphic systems treat intrinsic device noise as an accuracy cost. The authors challenge this assumption, asking whether noise can instead be harnessed to consolidate memories — turning a hardware liability into a functional asset.

Method: The paper casts per-synapse memory consolidation as a Doob h-transform: conditioning each weight's stochastic dynamics to never cross a barrier around its consolidated value. This adds a noise-amplified restoring drift σ² d/dw log h that diverges at the barrier. The rule combines this barrier-conditioning term with an anchored drift −s(w−μ). Experiments use Split-MNIST with 8 seeds and BrainScaleS-2 hardware.

Result: On Split-MNIST, the rule achieves 10.9 points higher retention at an interior noise optimum (p=0.004), while anchored-drift methods (OU, EWC, MESU) show monotonic degradation. On BrainScaleS-2, barrier-conditioning retains a prior task 15.6 points better than the matched control at equal average accuracy. The inverted-U effect survives ablation tests, different task streams, and noise in the forward pass.

Conclusion: Intrinsic analog noise, typically a disadvantage, can be repurposed as a consolidation resource. The Doob h-transform provides a principled framework for this, yielding a falsifiable prediction (inverted-U retention) that anchored-drift methods cannot produce. This suggests analog neuromorphic hardware offers a fundamental advantage over digital accelerators for memory consolidation.

Abstract: On analog neuromorphic hardware, intrinsic device noise is normally an accuracy tax. We ask whether it can instead consolidate memories. We cast per-synapse consolidation as a Doob h-transform: condition each weight's stochastic dynamics on never crossing a memory-critical barrier around its consolidated value. The conditioned diffusion gains an extra drift sigma^2 d/dw log h, a restoring force amplified by the noise variance itself that diverges at the barrier. We are explicit about novelty: the anchored drift -s(w-mu) our rule also contains is not ours (the limit of OUA, MESU, and EWC), and we surrender it. We claim only the conjunction of (a) the Doob barrier-conditioning as a synaptic rule, to our knowledge unclaimed (every h-transform use we found is generative modeling, none synaptic), and (b) a falsifiable prediction: increasing intrinsic noise non-monotonically improves sequential-task retention, an inverted-U that anchored-drift methods cannot produce. We pre-registered this as a go/no-go gate; it passes. On single-head Split-MNIST (8 seeds) the rule lifts retention 10.9 points at an interior optimum (paired Wilcoxon p=0.004), while matched OU/EWC/MESU anchors are monotone. Ablating the conditioning removes the effect; the optimum tracks the barrier; the inverted-U survives a second task stream and the realization where noise enters the forward pass. We then measure the intrinsic noise on real BrainScaleS-2 silicon (additive, trial-to-trial independent, tunable via on-chip averaging) and run the rule on the chip with its noise in the training loop: barrier-conditioning retains a prior task 15.6 points better than the matched control at matched average accuracy, a stability-plasticity shift, not a net-accuracy win (single seed; retention measured, energy modelled). Intrinsic analog noise thus becomes a consolidation dividend a digital accelerator must spend energy to generate.

</details>


### [178] [Imputation Meets Clustering: Exploiting Latent Subgroup Structure for Missing Data Recovery](https://arxiv.org/abs/2607.06930)
*Chuyao Zhang,E Li,Taochen Chen,Yiqun Zhang,Yuzhu Ji,Shuping Zhao,Peng Liu,Yiu-ming Cheung*

Main category: cs.LG

TL;DR: CAGI is a cluster-aware generative imputation framework that jointly optimizes clustering and imputation through a mutually reinforcing co-optimization process, outperforming 15 baselines on 14 benchmark datasets.


<details>
  <summary>Details</summary>
Motivation: Existing imputation methods overlook population heterogeneity in real-world datasets, where multiple subgroups with distinct distributions exist. Without structural guidance, they produce generic estimates that blur subgroup boundaries. Incorporating subgroup information faces a circular dependency: reliable clustering needs complete data, while data completion is the goal.

Method: CAGI employs a "Partition-Guide-Restore" strategy where dynamic cluster assignments serve as local priors to condition a GAN. An iterative feedback loop progressively refines both cluster structures and imputed values. A multi-level optimization objective combines instance-level reconstruction with distribution-level regularization.

Result: Extensive experiments on 14 benchmark datasets with 15 representative baselines demonstrate the superiority of CAGI over existing methods.

Conclusion: CAGI effectively resolves the circular dependency between clustering and imputation by treating them as a mutually reinforcing co-optimization process, achieving superior imputation quality by preserving subgroup-level distributional fidelity.

Abstract: Missing data is prevalent in practical applications, making effective imputation an essential preprocessing step for downstream analysis. Real-world datasets often exhibit complex latent structures composed of multiple subgroups with distinct distributions. However, existing methods often overlook such population heterogeneity. Without explicit structural guidance, these methods tend to produce generic estimates that blur subgroup boundaries and lack instance-level fidelity. While incorporating subgroup information offers a remedy, it faces a circular dependency: reliable subgroup identification requires complete data, while data completion is the imputation objective itself. To resolve this, we propose CAGI (Cluster-Aware Generative Imputation), a framework that reformulates clustering and imputation as a mutually reinforcing co-optimization process. CAGI employs a ``Partition-Guide-Restore'' strategy where dynamic cluster assignments act as local priors to condition a Generative Adversarial Network. An iterative feedback loop is established to progressively refine both cluster structures and imputed values toward faithful subgroup distributions. To ensure distributional stability, CAGI further employs a multi-level optimization objective combining instance-level reconstruction with distribution-level regularization. Extensive experiments on 14 benchmark datasets with 15 representative baselines demonstrate the superiority of CAGI. The source code is available at: https://github.com/supercocachii/CAGI

</details>


### [179] [Physical activities enable scalable foundation modelling for broad-spectrum health prediction](https://arxiv.org/abs/2607.06954)
*Zhenghuang Wu,Yuyao Zhu,Songlin Xu*

Main category: cs.LG

TL;DR: StepFM is a foundation model using only step counter data for broad-spectrum health prediction, offering privacy-preserving and computation-efficient alternative to high-frequency sensor data, achieving strong performance across 20+ health risk tasks.


<details>
  <summary>Details</summary>
Motivation: Existing wearable sensor models are disease-specific and lack transferability; high-frequency raw sensor data raises privacy, computational, and scalability concerns. A more generalizable, privacy-preserving approach is needed.

Method: StepFM uses a scalable pre-training framework on large-scale step counter sequences to capture temporal dynamics and behavioral patterns, enabling transfer learning across diverse health risk prediction tasks.

Result: Strong performance across 20+ health risk prediction tasks spanning diverse devices, regions, and disease types, with robustness in heterogeneous settings.

Conclusion: Step-based sensing (step counter data) is a viable foundation for scalable, real-world health monitoring, revealing interpretable relationships between physical activity patterns and health risks.

Abstract: Wearable and mobile sensing technologies have demonstrated strong potential for health inference; however, most sensor models are designed for specific disease types, limiting their transferability across different health risks. Wearable foundation models offer a more generalizable approach in diverse health risk types. Nevertheless, most existing methods rely on high-frequency raw sensor data, raising concerns about privacy, computational overhead, and scalability across devices and populations. In this paper, we propose StepFM, a foundation model built solely on step counter data for broad-spectrum health prediction. Leveraging the ubiquity and low-dimensional nature of step data, StepFM provides a practical, privacy-preserving, and computation-efficient alternative to traditional sensor-based models. We design a scalable pre-training framework that captures temporal dynamics and behavioral patterns from large-scale step sequences, enabling transfer across more than 20 health risk prediction tasks spanning diverse devices, new regions, and novel disease types. Extensive experiments demonstrate that StepFM achieves strong performance compared to existing methods while maintaining robustness across heterogeneous settings. Furthermore, our analysis reveals interpretable and generalizable relationships between physical activity patterns and various health risks, offering new insights into activity-based health modeling. Our work establishes step-based sensing as a viable foundation for scalable and real-world health monitoring.

</details>


### [180] [Rethinking Multimodal Time-Series Forecasting Evaluation](https://arxiv.org/abs/2607.06973)
*Haoxin Liu,Yichen Zhou,Rajat Sen,B. Aditya Prakash,Abhimanyu Das*

Main category: cs.LG

TL;DR: TimesX is a new multimodal time series forecasting benchmark with rich textual contexts, diverse domains, and automated data generation, revealing that simple ensemble methods leveraging text outperform strong baselines while many existing approaches fail.


<details>
  <summary>Details</summary>
Motivation: To address three key limitations of existing multimodal forecasting benchmarks: poor generalization due to small/synthetic data, limited types of textual contexts, and inability to mitigate data leakage in evaluation.

Method: TimesX is built via an automated data generation pipeline that provides high-quality real-world time series across diverse domains, enriched with multiple types of textual contexts.

Result: Many approaches that perform well on existing benchmarks fail on TimesX; simple ensemble methods leveraging rich textual contexts outperform strong baselines.

Conclusion: Existing multimodal forecasting methods may not generalize well, and incorporating diverse textual context via simple ensembles is a promising direction for zero-shot forecasting.

Abstract: We introduce a new context-enriched, multimodal time series forecasting benchmark, TimesX. TimesX contains a wide selection of high-quality real-world time series with diverse domains and textual contexts obtained from an automated data generation pipeline, which helps address three main issues of existing multimodal forecasting benchmarks: (1) poor generalization due to the small scale and synthetic nature of benchmark data, (2) very limited types of textual contexts in the benchmarks, and (3) an inability to mitigate data leakage in evaluation. We conduct a thorough empirical study of zero-shot multimodal forecasting approaches on TimesX. Our results suggest that many approaches that perform well on existing benchmarks may fail on TimesX. In contrast, simple ensemble methods that leverage rich textual context accompanying time-series can outperform strong baselines on TimesX.

</details>


### [181] [Hybrid Least Squares/Gradient Descent Methods for MIONets](https://arxiv.org/abs/2607.06976)
*Jun Choi,Chang-Ock Lee,Minam Moon*

Main category: cs.LG

TL;DR: Proposes an efficient hybrid least squares/gradient descent (LSGD) method for MIONets to accelerate training by optimizing last-layer parameters via alternating least squares.


<details>
  <summary>Details</summary>
Motivation: To accelerate the training of MIONets, which are computationally expensive due to their multi-branch architecture.

Method: Generalizes LSGD from DeepONets to MIONets; treats MIONet as a multilinear function w.r.t. last-layer parameters of branch networks; uses alternating least squares with Kronecker/Khatri-Rao products and tensor permutation matrices to handle large system matrices.

Result: Compatible with general L2 loss and regularization; enables efficient optimization of last-layer parameters by factoring large matrices into smaller ones.

Conclusion: The LSGD method can effectively accelerate MIONet training while supporting regularization and linear operators on outputs.

Abstract: In this paper, we propose an efficient hybrid least squares/gradient descent (LSGD) method for MIONets to accelerate training. This method generalizes the LSGD method for DeepONets. Since MIONet is the sum of the entrywise product of multiple branch networks and a trunk network, it can be viewed as a multilinear function with respect to the last layer parameters of each branch network. These sets of parameters can be optimized using the alternating least squares method, where we solve the LS system for a single branch network in turn. To handle the large-sized system matrix, we introduce Kronecker and Khatri-Rao products and tensor permutation matrices to factor the large matrix into small ones. Our method is compatible with a general type of $L^2$ loss with regularization terms for the last layer parameters of each branch, where linear operators can be applied to the MIONet output in each loss term.

</details>


### [182] [Safe Reinforcement Learning using Ideas from Model Predictive Control](https://arxiv.org/abs/2607.07252)
*Georg Schäfer,Jakob Rehrl,Stefan Huber,Simon Hirlaender*

Main category: cs.LG

TL;DR: Combines deep reinforcement learning with model predictive control to enforce hard safety constraints during RL exploration by projecting actions onto a precomputed feasible space using a safety filter.


<details>
  <summary>Details</summary>
Motivation: Ensuring strict, hard safety constraints during active RL learning in physical systems is critical, as violating mechanical limits can cause irreversible damage.

Method: Uses offline MPC to compute a globally verified feasible state-action space, then projects the RL agent's actions onto this safe set via a safety filter during training and deployment.

Result: Evaluated on a non-linear 1-DoF laboratory testbed, demonstrating successful safe exploration and stable policy convergence on physical hardware.

Conclusion: The proposed framework effectively integrates DRL's adaptability with MPC's formal safety guarantees for real-world CPS applications.

Abstract: Reinforcement learning (RL) enables the synthesis of control policies directly from data, making it highly appealing for complex cyber-physical systems (CPSs) and robotics. A persistent challenge, however, is ensuring strict, hard safety constraints during the active learning phase. In real-world physical systems, violating mechanical limits can cause irreversible damage, necessitating that exploration remains strictly within safe operational regions. We propose a generalized framework that combines the adaptive, high-performance nature of deep reinforcement learning (DRL) with the formal safety guarantees of model predictive control (MPC). Using a mathematical model of the system dynamics, offline MPC computations define a feasible state-action space, representing all safe combinations of system states and control inputs that guarantee constraint satisfaction. During training and deployment, the RL agent's instantaneous actions are projected onto this globally verified feasible set via a safety filter. We systematically evaluate our generalized approach on a non-linear 1-DoF laboratory testbed, demonstrating successful exploration and stable policy convergence on physical hardware.

</details>


### [183] [Robust Federated Learning Under Real-World Client Churn](https://arxiv.org/abs/2607.06979)
*Dhruv Garg,Neha Lakhani,Debopam Sanyal,Myungjin Lee,Alexey Tumanov,Ada Gavrilovska*

Main category: cs.LG

TL;DR: FeLiX is an FL orchestration framework that reduces wall-clock time-to-target accuracy by up to 2.37X through streaming-aware availability tiers, fresh-utility selection, and delay-robust aggregation, addressing transient availability, dynamic data heterogeneity, and prediction-outcome delays.


<details>
  <summary>Details</summary>
Motivation: Production FL deployments suffer from slow, multi-day refresh cycles, making models stale and unresponsive to rapidly shifting data distributions in applications like feed ranking and ad targeting. Existing FL systems overlook transient client availability, dynamic data heterogeneity, and delays between predictions and observable outcomes.

Method: FeLiX introduces three primitives: (i) streaming-aware availability tiers using lightweight telemetry; (ii) fresh-utility selection prioritizing statistically valuable updates from clients meeting tight deadlines; (iii) informativeness-aware, delay-robust aggregation incorporating late high-value updates without biasing toward stale distributions.

Result: FeLiX reduces wall-clock time-to-target accuracy by up to 2.37X while reducing communication bandwidth by 1.30X compared to state-of-the-art synchronous and asynchronous FL baselines on CIFAR-10, Google Speech, and realistic low-availability traces.

Conclusion: FeLiX achieves near-oracular performance without requiring unrealistic oracular knowledge of client availability, bridging the gap between existing FL systems and the demands of real-time applications.

Abstract: Federated Learning (FL) enables training shared models on private, on-device data, but production deployments remain constrained to slow, multi-day refresh cycles due to the complexity of coordinating massive client populations. For applications such as feed ranking, ad targeting, and personalized recommendation, model freshness: the ability to rapidly adapt to new user-local data is critical for maximizing objectives like click-through rate. This lag leaves models stale and unresponsive to volatile data distributions driven by viral trends and shifting user intent. Bridging this gap requires addressing three challenges overlooked by existing FL systems: transient client availability, dynamic data heterogeneity, and delays between model predictions and observable outcomes. We present FeLiX, an FL orchestration framework that minimizes wall-clock time-to-target accuracy on live interaction streams. FeLiX introduces three primitives: (i) streaming-aware availability tiers that leverage lightweight telemetry to identify ready clients at scale; (ii) fresh-utility selection, a dual-tier mechanism that prioritizes statistically valuable updates from devices able to meet tight refresh deadlines; and (iii) informativeness-aware, delay-robust aggregation that incorporates late, high-value updates containing ground-truth outcomes without biasing the global model toward stale distributions. Unlike prior systems that rely on unrealistic oracular knowledge of client availability, FeLiX achieves near-oracular performance in real-world settings. Across CIFAR-10, Google Speech, and realistic low-availability traces, FeLiX reduces wall-clock time-to-target accuracy by up to 2.37X while reducing communication bandwidth by 1.30X compared to state-of-the-art synchronous and asynchronous FL baselines.

</details>


### [184] [UP: Unbounded Positive Asymmetric Optimization for Breaking the Exploration-Stability Dilemma](https://arxiv.org/abs/2607.06987)
*Chongyu Fan,Pengfei Liu,Jingjia Huang,Sijia Liu,Yi Lin*

Main category: cs.LG

TL;DR: The paper proposes Unbounded Positive Asymmetric Optimization (UP), a plug-and-play objective that resolves the exploration-stability dilemma in RL-based LLM training by allowing unbounded positive updates for exploration while clipping negative ones for stability.


<details>
  <summary>Details</summary>
Motivation: Current RL-based LLM training suffers from an exploration-stability dilemma: importance sampling causes instability, but clipping mechanisms constrain policy updates, stifling exploration—especially for correct but low-confidence reasoning paths.

Method: The authors formalize "Probability Capacity" to diagnose the issue, then propose UP—an asymmetric objective that uses stop-gradient to anchor the policy, enabling unbounded positive gradients for exploration while retaining clipping for negative advantages to ensure stability.

Result: UP enhances exploration capacity and achieves superior reasoning accuracy across diverse RL algorithms (DAPO, GSPO, GRPO), model architectures (Dense, MoE, vision-language), and training modalities (language and multimodal).

Conclusion: UP is a universal, plug-and-play enhancement that effectively resolves the exploration-stability dilemma in RL-based LLM training.

Abstract: Reinforcement learning (RL) has become the standard paradigm for enhancing the complex reasoning capabilities of large language models (LLMs). To achieve sample efficiency, modern RL frameworks rely on importance sampling (IS). However, these algorithms suffer from an exploration-stability dilemma. Pure IS often leads to catastrophic training instability, while standard clipping mechanisms used to mitigate this instability strictly constrain the policy update budget. By formalizing the concept of Probability Capacity (Cap), we reveal that conservative clipping structurally stifles exploration by prematurely truncating the update budget for correct but low-confidence reasoning paths. To break free from these constraints, we propose Unbounded Positive Asymmetric Optimization (UP), a universal and plug-and-play objective. UP theoretically restructures the optimization process by anchoring the policy to its current state via the stop-gradient operator. This asymmetric design unleashes unclipped, stable gradients for positive advantages to maximize exploration, while maintaining standard clipping safeguards for negative advantages to prevent training instability. Furthermore, our formulation readily extends across different optimization granularities, including token-level (GRPO, DAPO) and sequence-level (GSPO) frameworks. Extensive experiments demonstrate that UP enhances exploration capacity and achieves superior reasoning accuracy across diverse RL algorithms (DAPO, GSPO, and GRPO), model architectures (Dense, MoE, and vision-language), and training modalities (language and multimodal), validating UP as a truly universal plug-and-play enhancement for RL-based training.

</details>


### [185] [Physics-guided spatiotemporal neural models for fuel density prediction](https://arxiv.org/abs/2607.06999)
*Tolga Caglar,Jaynil Jaiswal,Saqib Azim,Yudhir Gala,Mai H. Nguyen,Ilkay Altintas*

Main category: cs.LG

TL;DR: This paper proposes a physics-guided machine learning framework that integrates physics constraints into deep learning models (ConvLSTM, AFNONet, ViViT) for predicting fuel density evolution, achieving better accuracy and stability than purely data-driven baselines for fire forecasting.


<details>
  <summary>Details</summary>
Motivation: To improve fuel density prediction accuracy and stability for adaptive prescribed burn management by incorporating physics constraints into deep learning models, overcoming limitations of purely data-driven approaches.

Method: Three deep learning architectures (ConvLSTM, AFNONet, ViViT) are explored with a physics-guided loss function containing differentiable physics-informed terms: mass-conserving fuel transport term and rate-of-spread estimation.

Result: The proposed PGML framework outperforms purely data-driven baselines in both accuracy and stability, as demonstrated by experimental results averaged across multiple independent trials.

Conclusion: The PGML framework enables computationally efficient, physically plausible fire forecasting that supports adaptive prescribed burn management.

Abstract: This paper presents a physics-guided machine learning (PGML) framework for fuel density prediction, integrating physics constraints and domain knowledge into deep learning models to enhance model accuracy and stability. We explore three deep learning architectures -- ConvLSTM, Adaptive Fourier Neural Operator (AFNONet), and Video Vision Transformer (ViViT) -- to model the spatiotemporal evolution of fuel density. Our approach incorporates differentiable physics-informed terms in the loss function, including a mass-conserving fuel transport term and a rate-of-spread estimation. Experimental results, averaged across multiple independent trials, demonstrate that the proposed PGML framework outperforms purely data-driven baselines without physics constraints in both accuracy and stability. This framework enables computationally efficient, physically plausible fire forecasting to support adaptive prescribed burn management.

</details>


### [186] [Dissociating the Internal Representations of Sycophancy in LLMs](https://arxiv.org/abs/2607.07003)
*Anthony Baez,Sheer Karny,Pat Pataranutaporn*

Main category: cs.LG

TL;DR: This paper dissociates sycophancy in LLMs into factual and opinion subtypes, finding that different models represent these subtypes with varying degrees of shared vs. distinct neural representations.


<details>
  <summary>Details</summary>
Motivation: Sycophancy is often treated as a single behavior, but it manifests in distinct ways. The authors aim to understand whether these different manifestations share internal mechanisms or rely on separate representations.

Method: The authors train linear probes and construct steering vectors on activations from one sycophancy subtype (factual vs. opinion) and evaluate cross-transfer to the other subtype to measure representational similarity.

Result: Different LLMs represent factual and opinion sycophancy subtypes differently — some models show more unified representations while others exhibit distinct and causally interfering representations.

Conclusion: This dissociation method provides a useful framework for studying the representational structure of complex behaviors in LLMs, highlighting that seemingly unified behaviors can have heterogeneous internal mechanisms.

Abstract: Large Language Models (LLMs) frequently exhibit sycophancy, where they agree with a user's statement even when incorrect. While sycophancy is often treated as a single defined behavior, it can manifest in substantially distinct ways and circumstances, raising the question of whether this multi-faceted nature is reflected in its internal mechanisms. To address this gap, we dissociate the representations of sycophancy into factual and opinion subtypes -- motivated by the distinction between verifiable claims and subjective beliefs. We train linear probes and construct steering vectors on activations of one subtype and evaluate their transfer to the other subtype to measure to what extent they share representations. We find evidence that different LLMs represent these subtypes differently, with either more unified or more distinct and causally interfering representations. This method of dissociation offers a promising framework for studying the representational structure of complex model behaviors.

</details>


### [187] [Multimodal Spatiotemporal-Frequency Fusion with Peak Enhancement for Cellular Traffic Forecasting](https://arxiv.org/abs/2607.07016)
*Qingzhong Li,Yue Hu,Hui Ma,Yajun Zhang,Xinjun Pei,Ming Yan,Fei Xing*

Main category: cs.LG

TL;DR: MSPF-Net is a multimodal cellular traffic forecasting framework that integrates external news context with traffic patterns (spatiotemporal, frequency, burst) to improve prediction accuracy, validated on three real-world datasets.


<details>
  <summary>Details</summary>
Motivation: Existing spatiotemporal traffic forecasting methods focus on intrinsic patterns within a single modality and fail to model burst behavior triggered by external urban events (e.g., news events), leading to poor prediction under sudden traffic spikes.

Method: MSPF-Net comprises four modules: (1) Spatiotemporal-Frequency Traffic Encoder for capturing temporal, spatial, and spectral traffic patterns; (2) Peak Enhancement Module for burst-aware spike representations; (3) News Context Representation Module for encoding urban news streams into exogenous contextual embeddings; (4) Dynamic Fusion Prediction Module for adaptively integrating heterogeneous signals.

Result: Experiments on Milano, Trento, and LTE traffic datasets demonstrate that jointly modeling traffic dynamics, burst patterns, and news contextual signals effectively improves forecasting performance.

Conclusion: Integrating multimodal signals (traffic, burst patterns, and news context) significantly enhances cellular traffic forecasting, validating the importance of exogenous information for handling bursty dynamics.

Abstract: Accurate forecasting of cellular network traffic is essential for network planning, resource allocation, and quality-of-service assurance in modern mobile communication systems. Real-world traffic often exhibits bursty endogenous dynamics and disturbances triggered by external urban events, which makes reliable prediction highly challenging. Most existing spatiotemporal traffic forecasting methods primarily focus on intrinsic traffic patterns or structural relationships within a single modality, and rarely model burst behavior together with exogenous contextual signals. To address this issue, we propose \textbf{MSPF-Net}, a multimodal cellular traffic forecasting framework that integrates external contextual information. Specifically, MSPF-Net consists of a Spatiotemporal-Frequency Traffic Encoder for capturing temporal, spatial, and spectral traffic patterns, a Peak Enhancement Module for extracting burst-aware representations of sudden spikes, a News Context Representation Module for encoding urban news streams into exogenous contextual embeddings, and a Dynamic Fusion Prediction Module for adaptively integrating these heterogeneous signals to generate forecasts. Experiments on the Milano, Trento, and LTE traffic datasets demonstrate that jointly modeling traffic dynamics, burst patterns, and news contextual signals can effectively improve forecasting performance.

</details>


### [188] [Online Data Selection Is Implicit Alignment](https://arxiv.org/abs/2607.07023)
*Aoxiong Zeng,Yuxin Yang,Xiangquan Yang*

Main category: cs.LG

TL;DR: This paper shows that online data selection during supervised fine-tuning (SFT) acts as an implicit alignment mechanism, shifting model behaviors (e.g., refusal rate, verbosity, sycophancy) based on what data is selected, even without explicit preference optimization. The authors formalize this, introduce protocols for auditing alignment drift (ADA) and constraining it (AAS), and demonstrate that selectors with similar task accuracy can produce very different behavioral profiles.


<details>
  <summary>Details</summary>
Motivation: Current practice treats SFT as a capability step and alignment as a separate phase, but online data selection during SFT already biases model behavior. The authors aim to study and formalize this overlooked implicit alignment effect.

Method: The authors compare random, loss-based, quality-based, and diversity-based online data selectors under the same budget; formalize online selection as a reweighted SFT objective; and propose Alignment Drift Auditing (ADA) and Alignment-Aware Selection (AAS) to quantify and constrain behavioral drift along safety and style axes.

Result: Selectors indistinguishable in task accuracy diverge sharply in refusal rate, verbosity, and sycophancy. The direction of behavioral shift is predictable from the attribute mixture of selected data.

Conclusion: Online data selection during SFT is an implicit alignment mechanism; the choice of data matters for safety and style, and practitioners should audit and constrain selection-induced drift.

Abstract: Supervised fine-tuning (SFT) is often treated as a capability-adaptation step, while alignment is attributed to later preference optimization or reinforcement learning. This separation is incomplete: when examples are scored and kept online during fine-tuning, the choice of which data to train on already changes the model's behavioral preferences. We study online data selection as an implicit alignment mechanism. Given the same base model, optimizer, and selected-token budget, we compare random, loss-based, quality-based, and diversity-based online selectors and measure the behavioral drift they induce without any preference optimization. The proposed evaluation tracks helpfulness, refusal rate, verbosity, truthfulness, sycophancy, calibration, and jailbreak robustness, together with diagnostics for which behavioral modes are over-represented in the selected data. We formalize online selection as a reweighted SFT objective whose weights define an implicit preference over response styles and safety postures, so that an online scorer plays the role usually assigned to a reward model. This view predicts that high-scoring data can systematically favor longer, more assertive, more compliant, or more refusal-prone behaviors depending on how the online score is defined. Empirically, selectors that are statistically indistinguishable in task accuracy diverge sharply in refusal rate, verbosity, and sycophancy, and we show that the direction of the shift is predictable from the attribute mixture of the selected data. We introduce Alignment Drift Auditing (ADA), a controlled protocol for quantifying selection-induced behavioral movement, and Alignment-Aware Selection (AAS), a diagnostic online selector that retains data efficiency while constraining drift along safety and style axes.

</details>


### [189] [Constrained Decoding for Diffusion Language Models via Efficient Inference over Finite Automata](https://arxiv.org/abs/2607.07026)
*Meihua Dang,Stefano Ermon*

Main category: cs.LG

TL;DR: This paper proposes an exact and tractable algorithm for constrained decoding in diffusion language models, using finite automata as graphical models to efficiently sample from constrained mean-field posteriors. It achieves logarithmic depth reduction and shows significant accuracy gains on multiple tasks (function calling, planning, text-to-SQL, math reasoning) with minimal overhead.


<details>
  <summary>Details</summary>
Motivation: Existing constrained decoding methods are designed for autoregressive models with left-to-right generation, but diffusion language models break this assumption by sampling multiple positions simultaneously. There is a need for a constrained decoding algorithm tailored to the parallel, fully-factorized nature of diffusion models.

Method: The authors view finite automata as graphical models to obtain tractable representations of the constrained mean-field posterior distribution. They apply depth-reduction techniques from arithmetic circuit theory to reduce sampling depth from linear to logarithmic in sequence length. The approach supports greedy and sampling-based decoding, parallel/block-wise decoding, and arbitrary remasking schedules.

Result: Evaluations on Dream-7B and LLaDA-8B show substantial accuracy gains across function calling (xLAM, BFCL), planning (Sudoku, Countdown), text-to-SQL (Spider), and math reasoning (GSM-Symbolic) with under 5% wall-clock overhead. For BFCL-Live, Dream-7B's accuracy improved from 63.9% to 71.5% (greedy) and 22.3% to 69.0% (stochastic).

Conclusion: The proposed algorithm enables exact constraint satisfaction for diffusion language models with minimal inference overhead, bridging the gap between constrained decoding capabilities of autoregressive and diffusion-based LLMs across diverse tasks.

Abstract: Constrained decoding is essential for serving LLMs, ensuring that generated outputs follow specific structures such as JSON schema-formatted function calls. Existing systems are designed for autoregressive models and assume left-to-right generation, masking out invalid next tokens at each step. Diffusion language models, however, break this assumption: they sample multiple positions simultaneously from a fully-factorized mean-field distribution at each denoising step. In this paper, we present an exact and tractable algorithm for sampling from the constrained mean-field posterior under any constraint expressible as a finite automaton. Viewing finite automata as graphical models, we obtain tractable representations of the constrained distribution that enable efficient inference. The approach guarantees constraint satisfaction by construction, supports both greedy and sampling-based decoding, and is compatible with parallel and block-wise decoding under arbitrary remasking schedules. Applying depth-reduction techniques from arithmetic circuit theory, we further reduce sampling depth from linear to logarithmic in the sequence length. Empirical evaluations on Dream-7B and LLaDA-8B show substantial accuracy gains across various tasks including function calling (xLAM, BFCL), planning (Sudoku, Countdown), text-to-SQL (Spider), and math reasoning (GSM-Symbolic), with little inference overhead relative to unconstrained decoding. For example, on BFCL-Live, our approach improves Dream-7B's greedy decoding accuracy from 63.9% to 71.5%, and stochastic sampling accuracy from 22.3% to 69.0%, where the unconstrained baseline collapses, with under 5% wall-clock overhead.

</details>


### [190] [Latent graph encoding of multimodal neuroimaging features with generative AI architectures](https://arxiv.org/abs/2607.07027)
*Ishaan Batta,Meenu Ajith,Vince Calhoun*

Main category: cs.LG

TL;DR: This paper designs a multimodal graph VAE (gMMVAE) that outperforms other generative models (VAEs, GANs, transformers, diffusion models) for encoding and reconstructing structural and functional MRI features, with modality-aware graph encoding yielding superior results.


<details>
  <summary>Details</summary>
Motivation: To develop optimal architectural frameworks with appropriate encoding and latent space processes for studying structural and functional properties of the brain using generative models on multimodal neuroimaging data.

Method: Systematic evaluation of encoding strategies, latent multimodal fusion, and generative model selection using GMV and sFNC features from a large neuroimaging dataset. Compare VAEs, transformers, GANs, and diffusion models, with a proposed multimodal graph VAE (gMMVAE).

Result: Modality-aware graph encoding of functional connectivity into lower-dimensional latent space outperforms vectorized encoders or direct data space approaches. gMMVAE surpasses alternative generative variants across generation fidelity, reconstruction quality, efficiency, and latent space discriminability.

Conclusion: The multimodal graph VAE (gMMVAE) shows strong potential for robust multimodal neuroimaging analysis, outperforming other generative frameworks.

Abstract: While generative models enable encoding of complex neuroimaging data for feature generation and reconstruction, developing optimal architectural frameworks with appropriate encoding and latent space processes is crucial for studying structural and functional properties of the brain. We design a multimodal generative framework for structural and functional magnetic resonance imaging (MRI) features through systematic evaluation of encoding strategies, latent multimodal fusion, and generative model selection. Using structural gray matter volume (GMV) and static functional network connectivity (sFNC) features from a large neuroimaging dataset, we analyze generative frameworks involving variational autoencoders (VAEs), transformers, generative adversarial networks (GANs), and diffusion models. Architectures that employ modality-aware graph encoding of functional connectivity into a lower-dimensional latent space outperform vectorized encoders or direct data space approaches. The proposed multimodal graph VAE (gMMVAE) surpasses alternative generative variants across multiple metrics for generation fidelity, reconstruction quality, efficiency, and latent space discriminability, highlighting its potential for robust multimodal neuroimaging analysis.

</details>


### [191] [Gimitest: A Comprehensive Tool for Testing Reinforcement Learning Policies](https://arxiv.org/abs/2607.07029)
*Dennis Gross,Quentin Mazouni,Helge Spieker,Arnaud Gotlieb*

Main category: cs.LG

TL;DR: A comprehensive framework (Gimitest) for testing RL policies across diverse environments, algorithms, and scenarios.


<details>
  <summary>Details</summary>
Motivation: RL policies are unsafe and vulnerable to attacks; existing testing methods are limited to specific environments, scenarios, and algorithms.

Method: Proposes a comprehensive framework supporting single- and multi-agent RL testing with an open-source implementation (Gimitest) compatible with various gym frameworks and customizable components.

Result: Effectively tested multiple RL policies in Farama Gymnasium and PettingZoo environments.

Conclusion: Gimitest provides a versatile, open-source solution for RL policy testing across diverse conditions.

Abstract: Reinforcement learning (RL) policies can be unsafe and vulnerable to attacks. Ensuring their reliability is often a pain point as existing automated testing methods target only selected environments, testing scenarios, and RL algorithms. To address this, we propose a comprehensive framework for testing single- and multi-agent RL policies under varying conditions. Our implementation of this framework, Gimitest, is an open-source tool that supports various gym frameworks and allows for modifications of their integrated components. This article describes the framework and details Gimitest's functionality and architecture. It showcases its effectiveness in testing multiple RL policies in environments such as the official Farama Gymnasium and PettingZoo.

</details>


### [192] [Gauge-Invariant Learnable Spectral Positional Encodings for Directed Graphs via Hermitian Block Krylov Subspaces](https://arxiv.org/abs/2607.07032)
*Jiaqing Xie,Yuxin Wang*

Main category: cs.LG

TL;DR: The paper proposes learnable spectral positional encodings for directed graphs using magnetic operators and random probes, computed efficiently via Hermitian block Krylov methods, achieving gauge-invariance by construction and strong theoretical guarantees.


<details>
  <summary>Details</summary>
Motivation: Existing spectral PEs for directed graphs suffer from costly O(n³) Hermitian eigendecompositions and complex eigenvectors that are only defined up to unitary gauge, requiring basis-invariant architectures that are restrictive.

Method: Proposes learnable spectral PEs of form h_θ(A_q) R where A_q is a normalized magnetic operator, h_θ a learnable spectral response, and R a block of random probes. Computed via Hermitian block Krylov subspace from sparse matrix-vector products, with provably O(log(1/ε)) convergence for heat-resolvent families.

Result: On directed SBM, direction-blind PEs stay at chance while magnetic Krylov PEs converge to exact eigendecomposition oracle as depth increases. Achieves 1/√s Monte-Carlo error for pairwise features. Undirected q=0 case improves heterophilous benchmarks over no-PE and polynomial baselines.

Conclusion: The proposed gauge-invariant, efficiently computable spectral PEs via Krylov methods offer a scalable and theoretically grounded alternative to expensive eigendecompositions for directed graphs.

Abstract: Spectral positional encodings (PEs) for \emph{directed} graphs face two obstacles: magnetic Laplacians require an $O(n^3)$ Hermitian eigendecomposition per potential, and their complex eigenvectors are defined only up to unitary gauge, which prior work handles with basis-invariant architectures. We propose learnable spectral PEs of the form $h_θ(A_q)\,R$, where $A_q$ is a normalized magnetic operator, $h_θ$ a learnable scalar spectral response, and $R$ a block of random probes. Because the PE is a \emph{matrix function} of the operator, it is gauge-invariant by construction. We compute it in a Hermitian block Krylov subspace from sparse matrix--vector products only, prove that $k = O(\log(1/\varepsilon))$ block steps suffice uniformly over heat--resolvent response families, and give a covering-number argument for why low-dimensional structured families generalize where free per-eigenvalue weights overfit. On a directed SBM whose symmetrization is uninformative by construction, direction-blind PEs stay at chance while magnetic Krylov PEs converge to the exact-eigendecomposition oracle as the depth grows. The same probes yield gauge-invariant pairwise features with $1/\sqrt{s}$ Monte-Carlo error, and the undirected $q{=}0$ case improves heterophilous benchmarks over no-PE and polynomial baselines.

</details>


### [193] [Intrinsic Green's Learning: Supervised Learning on Manifolds via Inverse PDE](https://arxiv.org/abs/2607.07034)
*Alexandre Quemy*

Main category: cs.LG

TL;DR: IGL learns a source function and integrates it against a Green's kernel to model a target on a manifold, using low-rank tensor decomposition and coordinate gating to scale linearly with intrinsic dimension.


<details>
  <summary>Details</summary>
Motivation: To model functions on manifolds by solving linear PDEs without directly approximating the target, overcoming the curse of dimensionality in high-dimensional data.

Method: (1) Learn a source term from data; (2) Integrate via Green's kernel; (3) Use an encoder to discover low-dimensional coordinate chart; (4) Decompose source and kernel as low-rank tensors, reducing high-dimensional integrals to independent 1D integrals; (5) Two-stage algorithm (coordinate discovery then source fitting) to avoid dimensional collapse; (6) Learnable gates to automatically discover intrinsic dimension.

Result: Validated on synthetic manifolds and MNIST, achieving near-optimal classification and automatic recovery of intrinsic dimension.

Conclusion: IGL offers a principled, scalable approach for manifold learning that integrates PDE theory with neural representation, achieving both high accuracy and interpretability via intrinsic dimension discovery.

Abstract: We introduce Intrinsic Green's Learning (IGL), a framework that models a target function on a manifold as the solution to a linear PDE whose source term is learned from data. Rather than approximating the target directly, IGL learns a source and integrates it against a Green's kernel. An encoder discovers a low-dimensional coordinate chart on the manifold where both the source and the kernel decompose as low-rank tensors, collapsing a high-dimensional integral into independent one-dimensional integrals with cost linear in the intrinsic dimension. A two-stage algorithm separates coordinate discovery from source fitting, a near-convex linear solve, preventing the dimensional collapse of joint training. Learnable gates on each coordinate automatically discover the intrinsic dimension of the manifold. We validate IGL on synthetic manifolds and on MNIST, where it simultaneously achieves near-optimal classification and automatic recovery of the intrinsic dimension.

</details>


### [194] [On the Principles of Deep Feedforward ReLU Networks](https://arxiv.org/abs/2607.07035)
*Changcun Huang*

Main category: cs.LG

TL;DR: This paper explains the mechanism of deep feedforward ReLU networks by showing how paths (relationships between paths) enable units to form piecewise linear manifolds rather than hyperplanes, generalizing two-layer principles to deeper networks and thereby revealing the "black box" of training solutions.


<details>
  <summary>Details</summary>
Motivation: To uncover the internal mechanism of deep feedforward ReLU networks, which are a key but poorly understood component of many deep learning architectures.

Method: Systematic study using path-based analysis; shows that units form piecewise linear manifolds; generalizes principles from two-layer ReLU networks (strict partial orders, continuity restriction) to deeper cases.

Result: The training solutions obtained by back-propagation can be explained through path relationships; deeper networks use hidden units to produce both linear functions and input space partitions more efficiently than shallow networks.

Conclusion: The black box of deep feedforward ReLU networks is revealed through a combination of simple, generalizable principles centered on path relationships.

Abstract: The architecture of deep feedforward neural networks is ubiquitous in deep learning, either as a whole system or as a subnetwork of other architectures, and thus its mechanism is a key ingredient of the black box of neural networks. On the basis of the simplest two-layer ReLU network, this paper systematically studies the mechanism of deep feedforward ReLU networks with multiple hidden layers and successfully explains the training solution obtained by the back-propagation algorithm. The concept of a path, especially in terms of the relationships between paths, plays a central role in uncovering the mystery of the black box. It is shown that a unit of a deep ReLU network can form a piecewise linear manifold to divide the input space, instead of a hyperplane of the two-layer case. How to efficiently use the hidden-layer units to produce both linear functions and partitions of the input space is also a central problem. The principles of a two-layer ReLU network can be generalized to the deeper case to a large extent, such as multiple strict partial orders and continuity restriction. The combination of the basic and simple principles proposed can yield complicated instantiations including the training solutions, and in this sense the black box of deep feedforward ReLU networks is revealed.

</details>


### [195] [Complexity-Budgeted, Interaction-Aware Interpretable Model for Tabular Data](https://arxiv.org/abs/2607.07060)
*Srikumar Krishnamoorthy*

Main category: cs.LG

TL;DR: IAIML is a new interpretable ML framework for tabular data that captures pairwise feature interactions, achieving near-gradient-boosting accuracy with far fewer explanation components.


<details>
  <summary>Details</summary>
Motivation: Existing interpretable classifiers discard features whose predictive value only emerges through joint configurations with other variables, missing important interactions.

Method: Three mechanisms: (1) adaptive per-feature discretization, (2) finite-grid pairwise interaction scoring, (3) partitioned explanation budget. Detected interactions either relax screening filters or construct explicit pair terms for sparse downstream classifiers.

Result: Mean AUC within 1.4 points of gradient boosting; 14-28× fewer components; outperforms baselines on interaction-heavy data; comparable to RuleFit; degrades on higher-order interactions beyond pairwise scope.

Conclusion: IAIML is a compact, interaction-aware framework suitable when bounded explanation size and controlled interaction handling are required.

Abstract: Inherently interpretable classifiers for tabular data typically rely on sparse features, rules, or patterns that users can inspect directly. The marginal feature-screening step common to these methods can discard variables whose predictive value emerges only through joint configurations with other variables. We present Interaction Aware Interpretable Machine Learning (IAIML), a framework that addresses this limitation through three coordinated mechanisms: adaptive per-feature discretization, finite-grid pairwise interaction scoring, and a partitioned explanation budget. Detected interactions are routed through one of two strategies: relaxing the screening filter so that interaction-supported variables enter the pattern search, or constructing explicit pair terms for a sparse downstream classifier. On a 40-dataset panel comprising 24 real-world tabular benchmarks and 16 synthetic interaction stress tests, evaluated under nested cross-validation, IAIML achieves mean AUC within 1.4 points of tuned gradient-boosted ensembles while requiring roughly 14--28 times fewer fitted explanation components. On datasets with strong pairwise interaction structure and low marginal signal, IAIML outperforms all baselines. Among compact interpretable methods, IAIML is comparable to RuleFit in AUC and component count and is less expensive to tune. EBM obtains a small but significant AUC advantage across the full panel, with a substantially larger lookup-table footprint. Performance degrades on datasets requiring higher-order interactions beyond the pairwise scope. Component-isolated ablations confirm that adaptive discretization and interaction-aware admission each contribute incrementally. These results support IAIML as a compact, interaction-aware framework appropriate for settings where bounded explanation size and controlled treatment of feature interactions are design requirements.

</details>


### [196] [Multiplication Beyond Groups: Stratified Fourier Mechanisms in Transformer Circuits](https://arxiv.org/abs/2607.07066)
*Zitong Andrew Chen,Junaid Hasan,Akhil Srinivasan,Hemkesh Bandi,Jarod Alper*

Main category: cs.LG

TL;DR: Transformers learn modular multiplication over composite moduli by partitioning input space into local algebraic regions where group-like structure survives, extending representation-theoretic mechanisms beyond groups.


<details>
  <summary>Details</summary>
Motivation: Previous mechanistic analyses of transformers focused on globally invertible operations (e.g., cyclic addition, group composition). The authors investigate non-invertible operations (modular multiplication with zero-divisors) to understand if and how group-based representation mechanisms extend to more general algebraic structures.

Method: Proposes "monoid extension," a localized generalization of Group Composition via Representation (GCR). Trains small transformers on square-free modular multiplication. Analyzes embedding organization, attention patterns (class-sensitive routing, low-rank write directions), and local character features' contribution to output logits.

Result: Embeddings organize around local hierarchical algebraic regions. Attention exhibits class-sensitive routing and low-rank write directions. Local character features explain a large fraction of output logits.

Conclusion: Representation-theoretic mechanisms previously identified for group operations can extend beyond groups to more general algebraic structures like monoids.

Abstract: Transformers have demonstrated a remarkable ability to learn algorithmic reasoning, yet mechanistic analyses have mostly focused on globally invertible operations such as cyclic addition and group composition. In this work, we investigate how small transformers learn modular integer multiplication over composite moduli, a fundamentally non-invertible operation due to the presence of zero-divisors. We propose the monoid extension: a localized generalization of Group Composition via Representation (GCR) that suggests the learned computation does not rely on a single global representation space. Instead, the model partitions the input space into local hierarchical algebraic regions, where group-like structure survives and Fourier mechanisms can be applied. In transformers trained on square-free modular multiplication, we find that embeddings organize around these regions, attention exhibits class-sensitive routing and low-rank write directions, and local character features explain a large fraction of the model's output logits. Our results suggest that representation-theoretic mechanisms previously identified for group operations can extend beyond groups to more general structures.

</details>


### [197] [An Hybrid Quantum-Classical Diffusion Model for Image Generation](https://arxiv.org/abs/2607.07072)
*Qipeng Qian,Keli Deng,Yuntao Qian*

Main category: cs.LG

TL;DR: This paper proposes a hybrid quantum-classical generative model that combines a classical autoencoder for dimensionality reduction with a mixed-state quantum denoising diffusion probabilistic model (MSQuDDPM) in latent space, using an analytic reverse update rule, and demonstrates it on MNIST.


<details>
  <summary>Details</summary>
Motivation: Quantum diffusion models are constrained by high qubit cost for encoding classical high-dimensional data and computational burden of simulating large density operators.

Method: Classical autoencoder compresses data into compact latent codes embeddable into small-qubit Hilbert space; MSQuDDPM learns generative distribution over latent density operators; reverse dynamics simplified by predicting clean state ρ₀ and using analytic backward propagation rule instead of learning explicit predictor for ρ_{t-1}.

Result: Demonstrated on MNIST image generation; shown to be practically feasible under realistic qubit budgets.

Conclusion: Mixed-state quantum diffusion can serve as a practical backbone for hybrid quantum-classical generative modeling with limited qubit resources.

Abstract: Quantum diffusion models provide a physics-consistent route to generative learning by formulating noising and denoising directly on quantum states. However, applying such models to classical high-dimensional data is constrained by the qubit cost of state encoding and the computational burden of simulating large density operators. We propose a scalable hybrid generative pipeline that combines a classical autoencoder for dimensionality reduction with a mixed-state quantum denoising diffusion probabilistic model (MSQuDDPM) operating in the learned latent space. The autoencoder compresses data into compact latent codes that can be embedded into a small-qubit Hilbert space, after which the quantum diffusion model learns a generative distribution over latent density operators and decodes samples back to the original domain. Algorithmically, we simplify the reverse dynamics by predicting an estimate of the clean state $ρ_0$ at timestep $t$ and computing the one-step reverse update via an analytic backward propagation rule, rather than learning an explicit predictor for $ρ_{t-1}$. We demonstrate the proposed approach on MNIST image generation and discuss how mixed-state quantum diffusion can serve as a practical backbone for hybrid quantum--classical generative modeling under realistic qubit budgets.

</details>


### [198] [Structural Adversarial Attacks on Relational Deep Learning under Integrity Constraints](https://arxiv.org/abs/2607.07089)
*Alan Gany,Bogdan Cautis,Silviu Maniu*

Main category: cs.LG

TL;DR: This paper studies the adversarial robustness of Relational Deep Learning (RDL) pipelines, considering a white-box attacker who can only modify foreign-key references in the upstream database while preserving integrity constraints. The authors propose seven attack heuristics (two random, five gradient-guided) and evaluate them on the RelBench benchmark, finding gradient-based attacks outperform random ones on regression tasks but show limited gains on classification.


<details>
  <summary>Details</summary>
Motivation: RDL has become a standard methodology for ML on relational databases, but its adversarial robustness has not been studied. The authors aim to understand how vulnerable these pipelines are to attacks that respect database integrity constraints.

Method: The authors formulate a white-box attack setting where an attacker can rewire FK references under integrity constraints. They propose seven attack heuristics: two random sampling baselines and five gradient-guided variants using differentiable edge masks, evaluated on the RelBench rel-f1 benchmark.

Result: Gradient-based attacks consistently outperform random baselines on regression tasks, while gains on classification are smaller due to low label-flip rates and greater local stability of classification outputs.

Conclusion: The study reveals that RDL pipelines are vulnerable to adversarial attacks on the upstream database, especially for regression tasks, highlighting the need for robust defenses in relational learning systems.

Abstract: Relational Deep Learning (RDL) has become a standard methodology for machine learning on relational databases: the database is encoded as a heterogeneous temporal graph in which tuples become nodes and primary-key to foreign-key (PK-FK) dependencies become typed edges, over which a graph neural network is trained for downstream prediction. We study the adversarial robustness of this pipeline. We consider a white-box attacker who knows how the graph is built and the model is trained, reasons about perturbations on the graph, but can only act on the upstream database, by rewiring foreign-key references while preserving the integrity constraints of the schema (foreign-key validity, the degree-one FK constraint, and functional dependencies). This restricts the attacker to a constrained, combinatorial set of admissible edits under a global perturbation budget, which is intractable to explore exhaustively and made non-additive by GNN message passing. We investigate seven attack heuristics - two random sampling baselines and five gradient-guided variants that exploit differentiable edge masks - and evaluate them on the RelBench rel-f1 benchmark. Gradient-based attacks consistently outperform random baselines on regression tasks, whereas gains on classification are smaller, which we attribute to low label-flip rates and greater local stability of classification outputs.

</details>


### [199] [A knowledge-augmented dataset of high-risk driving scenarios with LLM annotations for autonomous driving](https://arxiv.org/abs/2607.07103)
*Heye Huang,Jingguang Li,Zhiyuan Zhou,Paul Liang,Mingyu Wu,Kitae Jang,Jianqiang Wang*

Main category: cs.LG

TL;DR: K-Risk is a knowledge-augmented dataset combining 20 trajectory datasets with LLM-generated semantic annotations, curating 31,398 high-risk events (including 1,036 near-collision cases) with structured triplets of trajectories, metadata, and language descriptions, plus causal analyses and action recommendations validated via closed-loop simulation.


<details>
  <summary>Details</summary>
Motivation: Safe autonomous driving requires handling both common high-risk events and rare long-tail safety scenarios, but existing datasets lack high-risk event labels, semantic annotations, and verifiable safety signals due to severe under-representation in naturalistic driving data.

Method: Combined 20 human-driven and AV trajectory datasets from EU, China, US (highways, urban freeways, intersections, roundabouts); applied a unified risk-centric extraction pipeline; used LLMs to generate structured scenario descriptions, abnormal-behavior notifications, causal analyses, and action recommendations; validated a subset via closed-loop simulator with iterative reflection.

Result: Curated 31,398 high-risk events and a 1,036-event extreme subset of near-collision cases, each released as synchronized trajectory, metadata, and language triplets with multi-dimensional risk annotations.

Conclusion: K-Risk bridges structured traffic trajectories, semantic reasoning, and decision supervision, providing a standardized foundation for developing and evaluating next-generation risk-aware autonomous driving agents.

Abstract: Safe autonomous driving requires both rapid responses to common high-risk events and deeper reasoning over rare, extreme long-tail scenarios in traffic safety. These scenarios are severely under-represented in naturalistic driving data, and existing trajectory and language-augmented datasets seldom provide high-risk event labels, semantic annotations, and verifiable safety signals. Here we present K-Risk, a knowledge-augmented dataset that combines structured driving trajectories with large language model generated semantic annotations for safety-critical driving scenarios. K-Risk integrates 20 human-driven and autonomous-vehicle trajectory datasets from Europe, China, and the United States, covering highways, urban freeways, intersections, and roundabouts. Using a unified risk-centric extraction pipeline, K-Risk curates 31,398 high-risk events, together with a 1,036-event extreme subset of near-collision cases. Each event is released as a synchronized trajectory, metadata, and language triplet containing structured scenario descriptions, abnormal-behavior notifications, and, for a representative subset, causal risk analyses and action recommendations validated through a closed-loop simulator with iterative reflection. By combining multi-dimensional risk annotations, interpretable language supervision, and verifiable decisions, K-Risk bridges structured traffic trajectories, semantic reasoning, and decision supervision, providing a standardized foundation for developing and evaluating next-generation risk-aware autonomous driving agents.

</details>


### [200] [Distributed Sparse Interventions in Language Models](https://arxiv.org/abs/2607.07128)
*Maximilian S. Ernst,Lorenz Linhardt,Aaron Peikert,Oliver Eberle*

Main category: cs.LG

TL;DR: DSI is a neuron-level intervention method that identifies sparse neuron sets (~0.01%) to activate task behaviors in LLMs, capturing nonlinear effects missed by global steering approaches.


<details>
  <summary>Details</summary>
Motivation: Prior model steering methods assume task representations are approximately linear and additive in activation space, missing neuron-specific nonlinear effects on outputs.

Method: Distributed Sparse Interventions (DSI) identifies sparse sets of neurons across layers using nonlinear, interaction-aware interventions, intervening on as few as 0.01% of neurons.

Result: DSI successfully activates task behavior in instruction-tuned language models with very sparse interventions (0.01% of neurons), outperforming linear steering methods.

Conclusion: Sparse, distributed neuron-level interventions enable fine-grained control, localization of task-relevant circuitry, and insights into task composition in LLMs.

Abstract: Language models perform a wide range of tasks at varying levels of abstraction with the capacity to flexibly infer tasks from context, execute multiple tasks simultaneously, and select among competing tasks. To study the role of model components in task behaviour, their causal influence can be investigated through interventions. Prior work on model steering has largely focused on interventions along global directions in activation space, modeling task representations as approximately linear and additive. By studying interventions at the neuron level, we find substantial, neuron-specific nonlinear effects on model outputs that are not captured by current steering approaches. We introduce Distributed Sparse Interventions (DSI), an intervention approach that considers nonlinearities and interactions between neurons across layers to identify sparse sets of neurons that elicit task-relevant computations. Across a range of tasks, we demonstrate that DSI can activate task behaviour in instruction-tuned language models by localising and intervening on as few as 0.01% of neurons, highlighting the effectiveness of sparse, distributed interventions in the neuron basis. Additionally, adopting a set-based perspective enables computations over the identified neuron sets, offering insights into the roles of individual neurons by analysing their effects across tasks. Through sparse interventions, DSI enables fine-grained control over model behaviour, localisation of task-relevant neuron sets, and furthers our understanding of task composition.

</details>


### [201] [Fractal KV-Cache Archives: Lossless Symbolic Storage with In-Place Retrieval for Long-Context LLM Inference](https://arxiv.org/abs/2607.07144)
*Vladimir Gusev*

Main category: cs.LG

TL;DR: This paper proposes a contractive iterated-map code archive format for quantized KV caches that supports lossless compression, O(1) random access, O(1) amortized append, and enables direct approximate substring search on stored vectors without materializing surrounding text. The method achieves 36-54x cache compression on GPT-2 with 1024-token contexts at 11-15% perplexity cost.


<details>
  <summary>Details</summary>
Motivation: The KV cache dominates memory cost in long-context autoregressive inference. While prior work focuses on compression via quantization, eviction, or offloading, this paper asks a complementary question: once KV states are quantized to codebook indices, how should the resulting symbol stream be stored, and can the storage layer do more than just store?

Method: The paper revisits contractive iterated-map codes that serialize a symbol sequence into low-dimensional real vectors, forming an archive format for quantized KV caches. It conducts a controlled study on GPT-2 with 1024-token contexts, using per-head residual vector quantization with a small exact window (4 attention sinks + 32 recent tokens) and archiving the rest.

Result: The archive compresses the KV cache by 36-54x relative to fp16 at 11-15% perplexity cost. A sharp key/value asymmetry is quantified: quantizing keys is ~4x more damaging than quantizing values. The archive also serves as a search index enabling approximate substring queries directly on stored vectors.

Conclusion: The proposed archive format is a natural storage solution for quantized KV caches, offering lossless compression, efficient access patterns, and dual functionality as a search index. All code is released and reproducible from a single command on a laptop CPU.

Abstract: The key-value (KV) cache dominates the memory cost of long-context autoregressive inference, and a growing body of work compresses it through quantization, eviction, or offloading. We study a complementary question: once a position's KV state has been quantized to codebook indices, how should the resulting symbol stream be stored, and can the storage layer do more than store? A family of contractive iterated-map codes that serialize a symbol sequence into a sequence of low-dimensional real vectors is revisited, and it is shown that they form a natural archive format for a quantized KV cache with the following features. The method provides exactly the access pattern a growing cache requires. It is lossless, it runs in linear time, and supports O(1) random access and O(1) amortized append. A controlled study of the quantizer feeding this archive is conducted on GPT-2 with 1024-token contexts. Keeping a small exact window (4 attention sinks + 32 recent tokens) and archiving the rest, per-head residual vector quantization reduces the archived cache by 36-54x relative to an fp16 cache at a perplexity cost of 11-15%, and we quantify a sharp key/value asymmetry -- quantizing keys is roughly 4x more damaging than quantizing values, consistent with prior low-bit KV work -- and use it to allocate bits in a hybrid scheme. Finally, we show the archive is simultaneously a search index: approximate substring queries execute directly on the stored vectors, and matched context is decoded from the matched vector without ever materializing the surrounding text. We release all code; every number reproduces from a single command on a laptop CPU.

</details>


### [202] [Information Allocation Dynamics in Neural Network Optimization](https://arxiv.org/abs/2607.07156)
*Zhang Gongyue,Liu Donghan,Ren Weihong,Sheng Yixuan,Wang Zhiyong,Liu Honghai*

Main category: cs.LG

TL;DR: This paper proposes an information allocation dynamics perspective to understand optimizer implicit bias, interpreting it as the relative allocation of training signals between weight-like and bias-like parameter pathways, controlled by a continuous preconditioning exponent p. The analysis shifts focus from solution-space geometry to update dynamics during training.


<details>
  <summary>Details</summary>
Motivation: Existing studies analyze optimizer biases from the geometry of the final solution, but how optimizer bias forms during training lacks a clear internal mechanism.

Method: Analyzes update contributions of weight and bias to the same residual signal in a minimal linear model. Shows weight correction preserves input-dependent residuals while bias correction preserves residual mean direction. Introduces preconditioning exponent p to control relative strength of these pathways.

Result: Optimizer implicit bias is reflected in the relative write-in ratio of training signals across different parameter pathways (weight vs. bias-like), not just in final solution geometry or global training trajectory.

Conclusion: The relative update allocation between weight and bias-like parameters is an important dynamical mechanism affecting parameter trajectories and generalization behavior, moving analysis from solution-space geometry to update dynamics.

Abstract: Different optimizers have different update biases, but these biases are usually implicit. Existing studies mainly analyze or control such biases from the geometry of the final solution. However, how optimizer bias forms during training still lacks a clear internal mechanism.
  This paper proposes an information allocation dynamics perspective. It interprets optimizer implicit bias as the relative allocation of training signals between weight-like and bias-like parameter pathways. This allocation can be described and adjusted by a continuous preconditioning exponent \(p\).
  To characterize this mechanism, we first analyze the update contributions of weight and bias to the same residual signal in a minimal linear model. The weight correction term preserves input-dependent residual signals, while the bias correction term preserves the residual mean direction. They therefore correspond to different projection pathways of the residual signal. After substituting the preconditioned update into the residual update equation, the optimizer can change the relative strength of the weight correction term and the bias correction term through different preconditioning factors. Therefore, optimizer implicit bias is not only reflected in the final solution or the global training trajectory. It is also reflected in the relative write-in ratio of training signals across different parameter pathways.
  Overall, this paper moves the analysis of optimizer implicit bias from solution-space geometry to update dynamics during training. It reveals that the relative update allocation between weight and bias-like parameters is an important dynamical mechanism that affects parameter trajectories and generalization behavior.

</details>


### [203] [Entropy Pacing Policy Optimization for Multi-Task Agentic Reinforcement Learning](https://arxiv.org/abs/2607.07178)
*Zetian Hu,Shunyu Liu,Junjie Zhang,Yongcheng Jing,Ting-En Lin,Yongbin Li,Dacheng Tao*

Main category: cs.LG

TL;DR: This paper identifies a phenomenon called "exploration-exploitation pace mismatch" in multi-task agentic RL for LLMs, where easier and harder tasks interfere with each other through entropy crossovers. The authors propose Entropy Pacing Policy Optimization (EPPO), which uses a task-wise dynamic clipping mechanism to adaptively adjust entropy levels across tasks, outperforming existing methods on multi-task agentic benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing RL methods for LLMs focus on single-task settings, but real-world deployment requires multi-task generalist agents. The paper identifies an underexplored phenomenon where different tasks exhibit exploration-exploitation pace mismatch, causing interference and instability in multi-task learning.

Method: EPPO introduces a task-wise dynamic clipping mechanism that replaces the fixed clipping threshold in GRPO with a task entropy-aware adaptive bound. It tightens updates for over-confident (low-entropy) tasks and relaxes them for under-explored (high-entropy) ones, coordinating entropy across tasks to stabilize multi-task optimization.

Result: Experiments on multi-task agentic benchmarks show that EPPO yields superior results compared to counterpart methods.

Conclusion: The proposed EPPO effectively addresses the exploration-exploitation pace mismatch in multi-task agentic RL for LLMs, achieving better performance by dynamically coordinating entropy across tasks.

Abstract: Recent breakthroughs of Reinforcement Learning (RL) have highlighted its potential for complex agentic Large Language Model (LLM) tasks. However, existing efforts largely focus on single-task settings, whereas real-world deployment necessitates a generalist agent capable of solving multiple tasks simultaneously. In this work, we identify a critical yet underexplored phenomenon in multi-task agentic RL: different tasks can exhibit exploration-exploitation pace mismatch. Specifically, easier tasks may converge early to low-entropy policies that hinder learning on harder tasks, while harder tasks can, in turn, push easier tasks back toward high-entropy exploration. This back-and-forth interaction creates inter-task entropy crossovers and frequent entropy spikes. Inspired by this observation, we introduce Entropy Pacing Policy Optimization (EPPO) for multi-task agentic LLMs, which coordinates entropy across tasks to stabilize multi-task optimization. At the core of EPPO is a task-wise dynamic clipping mechanism that replaces the fixed clipping threshold in Group Relative Policy Optimization (GRPO) with a task entropy-aware adaptive bound, tightening updates for over-confident tasks while relaxing them for under-explored ones. Experiments on the multi-task agentic benchmarks demonstrate that the proposed EPPO yields results superior to its counterparts.

</details>


### [204] [Predicting LLM Safety Before Release by Simulating Deployment](https://arxiv.org/abs/2607.07184)
*Marcus Williams,Hannah Sheahan,Cameron Raymond,Tomek Korbak,Deng Pan,Peilin Yang,Leon Maksin,Ningyi Xie,Phillip Guo,Ian Kivlichan,Micah Carroll*

Main category: cs.LG

TL;DR: The paper proposes "deployment simulation" — using de-identified conversation prefixes from previous deployments and regenerating responses with a candidate model — to estimate real-world misbehavior rates before releasing new AI models, showing it outperforms traditional evaluations across GPT-5-series deployments.


<details>
  <summary>Details</summary>
Motivation: Traditional pre-deployment safety evaluations are limited: they have insufficient coverage, are unrepresentative of real-world use, and are easily recognized as tests, providing poor evidence about how often undesired model behavior will actually occur in deployment.

Method: Starting from de-identified conversations from a previous model deployment, the authors fix the initial conversation prefix and regenerate the next response using a candidate model. The responses are then audited for misalignments and used to estimate misbehavior prevalence before deployment.

Result: Deployment simulation produced informative estimates of post-deployment misbehavior rates, outperformed baselines based on adversarially selected production data, and provided point estimates much closer to production traffic than traditional evaluations. The approach also worked when seeded from public chat datasets.

Conclusion: Deployment simulation helps evaluators forecast real-world language model behavior and supports more quantitative assessment of deployment risk, with potential for external researchers to run deployment-grounded evaluations without access to private production logs.

Abstract: Pre-deployment safety evaluations aim to inform the downstream risks of releasing a new AI model. Yet most evaluations provide limited evidence about how often undesired model behavior will occur in deployment: they generally have insufficient coverage, are unrepresentative, and are generally recognizable as tests. To address these concerns, we study a simple way to simulate a model deployment: starting from de-identified conversations from a previous model deployment, we hold fixed the initial conversation prefix and regenerate the next response using a candidate model. The resulting responses can then both be audited for novel misalignments and used to estimate the prevalence of model misbehavior before deployment. We evaluate deployment simulation across four GPT-5-series deployments, using registered, outcome-blinded predictions for GPT-5.4 and retrospective analyses of three earlier releases. We find that deployment simulation produces informative estimates of post-deployment misbehavior rates and outperforms baselines based on adversarially selected production data; its evaluation-awareness point estimates were also much closer to production traffic than those from traditional evaluations. We also identify the realism of tool resampling as a central challenge for further improving predictions and share results suggesting that this challenge is surmountable even in complex tool-use settings. Finally, we show that deployment simulation can be seeded from public chat datasets and remain informative about production misbehavior rates, suggesting a path for external researchers to run deployment-grounded evaluations without access to private production logs. Overall, deployment simulation helps evaluators forecast how language models will behave in the real world and supports more quantitative assessment of deployment risk.

</details>


### [205] [Geometric--Nongeometric Optimizer Calculus: A Modular Language for Reachable Gradient Methods](https://arxiv.org/abs/2607.07206)
*Zavier Li*

Main category: cs.LG

TL;DR: The paper introduces a modular "geometric–nongeometric optimizer calculus" for formally auditing and classifying adaptive optimizers, with a direction-expressivity theorem and Pareto-style design framework over module budgets.


<details>
  <summary>Details</summary>
Motivation: To provide a formal, modular language for understanding and comparing adaptive optimizers beyond empirical benchmarks, by decomposing them into geometric (preconditioning) and nongeometric (memory, noise, discretization, etc.) components.

Method: The authors propose a geometric–nongeometric decomposition: a positive cometric family (geometry) maps covectors to descent directions, while nongeometric modules (information, memory, control, operator, noise, target, discretization) govern availability and usage. They prove a direction-expressivity theorem and define restricted direction residuals for diagonal/block geometries, then lift pointwise residuals to trajectory-level complexity.

Result: Full positive-definite geometry exactly expresses all strict descent directions away from critical points. Diagonal and block geometries have provably limited expressivity. The framework supports Pareto optimization over module budgets. A full-metric probe solves deterministic quadratic benchmarks; a Muon-style PyTorch candidate shows small-scale auditability.

Conclusion: The paper presents a theoretical language and benchmark framework for optimizer auditing, not a claim of large-scale SOTA. It separates direction-level diagnostics from geometric complexity, enabling principled design trade-offs.

Abstract: Adaptive optimizers mix several mechanisms: a metric or preconditioner maps gradients to descent directions, while estimation, memory, step-size control, constraints, stochasticity, target modification, and discretization determine which directions are available and how they are used. We introduce geometric--nongeometric optimizer calculus, a modular language for auditing reachable gradient methods under explicit oracle, budget, state, and rule constraints. The geometric module is a positive cometric family that maps covectors to parameter-space directions; the nongeometric modules are information, memory, control, operator, noise, target, and discretization mechanisms. The main formal result is a direction-expressivity theorem: away from critical points, full positive-definite geometry expresses exactly the strict descent directions. We then define restricted direction residuals for admissible metric families, prove exact expressivity conditions for diagonal and block geometries, and separate this direction-level diagnostic from condition-number geometric complexity. The resulting design problem is a Pareto optimization over module budgets, not a single universal optimizer ordering. We also lift pointwise residuals to a trajectory-level residual complexity that couples direction mismatch with the variation of the explaining geometry. We include diagnostic prototypes only as evidence for the language: a high-information full-metric probe solves deterministic quadratic benchmarks to numerical precision, while a practical Muon-style PyTorch candidate gives small-scale evidence that matrix-operator updates can be audited by the calculus. The paper is a theory and benchmark-language manuscript; it does not claim large-scale optimizer state-of-the-art performance.

</details>


### [206] [HPG-Diff: Hierarchical physics-guided diffusion with differentiable connectivity constraints for topology optimization](https://arxiv.org/abs/2607.07233)
*Jinbo Yang,Mingyue Yuan,Boyuan Zhang,Yoshifumi Kitamura,Shikai Jing*

Main category: cs.LG

TL;DR: HPG-Diff is a physics-guided diffusion model for topology optimization that uses hierarchical physics features and a floating material suppression loss (based on virtual heat propagation) to improve generalizability and connectivity, achieving low compliance errors (0.87% in-distribution, 5.29% out-of-distribution) and minimal floating material ratios (~2.5-2.9%).


<details>
  <summary>Details</summary>
Motivation: Deep generative models for topology optimization lack intrinsic physics guidance, leading to poor generalizability under unseen boundary conditions and the formation of floating material artifacts.

Method: Proposes HPG-Diff with (1) a hierarchical physics-guided strategy aligning precomputed physics features with the denoising process, and (2) a floating material suppression loss based on virtual heat propagation from load positions to penalize disconnected material.

Result: Average compliance errors: 0.87% (in-distribution), 5.29% (out-of-distribution). Floating material ratios: 2.90% and 2.44%, respectively. LoRA fine-tuning shows adaptation potential for rectangular non-square domains.

Conclusion: HPG-Diff effectively embeds physics guidance into diffusion-based topology optimization, improving generalizability and connectivity while maintaining low compliance error.

Abstract: Deep generative models offer a promising paradigm for topology optimization, enabling rapid design exploration. However, these approaches lack intrinsic physics guidance, often leading to poor generalizability across unseen boundary conditions and the formation of floating material artifacts. To address these limitations, we propose Hierarchical Physics-Guided Diffusion (HPG-Diff), a novel diffusion framework that enforces physics consistency through two synergistic mechanisms. First, we introduce a hierarchical physics-guided strategy that aligns different precomputed physics features with the denoising process, guiding material distribution toward optimal load paths to enhance generalizability. Second, we propose a floating material suppression loss as a differentiable connectivity constraint inspired by thermal conduction to improve topological connectivity. By simulating a virtual heat propagation process from load positions, this mechanism explicitly penalizes floating material during training. Quantitative evaluations demonstrate that HPG-Diff achieves average compliance errors of 0.87% (in-distribution) and 5.29% (out-of-distribution), while reducing floating material ratios to 2.90% and 2.44%, respectively. Furthermore, case studies on a 3:1 rectangular domain, including cantilever and bridge benchmarks, provide preliminary evidence that lightweight LoRA fine-tuning with a small dataset can support the adaptation of HPG-Diff to rectangular non-square domains.

</details>


### [207] [ORCAID: Oblique Rule-Based Continuous-Action Interpretation for Deep RL Policies](https://arxiv.org/abs/2607.07235)
*Ignacio D. Lopez-Miguel,Ezio Bartocci,Thomas Eiter,Martin Tappler*

Main category: cs.LG

TL;DR: ORCAID extracts interpretable rule-based policies from RL agents in continuous-action environments using oblique decision trees with a three-stage split search and leaf merging.


<details>
  <summary>Details</summary>
Motivation: Explainability in RL remains a key challenge, especially in continuous action spaces where distilling interpretable policies is difficult.

Method: ORCAID uses an oblique decision tree with three-stage split search (random initialization, local refinement, backward elimination) and adjacent leaf merging to produce concise, interpretable rules.

Result: The extracted rule-based policies maintain strong performance with few parameters and can even improve the original deep RL policy's performance.

Conclusion: ORCAID effectively bridges interpretability and performance in continuous-action RL environments.

Abstract: Explainability remains a key issue in reinforcement learning (RL). Distilling an interpretable policy from an agent trained in a complex environment is particularly challenging when the action space is continuous. We introduce ORCAID, a novel method for extracting interpretable rule-based policies from RL agents operating in mixed continuous-discrete environments with continuous action spaces. Our main contribution is an efficient oblique decision tree training algorithm that partitions the state space by hyperplanes and fits local linear models. The key idea lies in a three-stage split search: efficient random initialization, local refinement, and backward elimination. Finally, adjacent leaves are merged to yield a concise set of interpretable rules describing a given deep RL policy. We evaluate ORCAID across multiple RL environments, demonstrating that the extracted rule-based policies maintain strong performance with a low number of parameters and can even be used to improve the performance of the original deep RL policy.

</details>


### [208] [FMMVCC: Fuzzy Mamba-based Multi-View Contrastive Clustering for Univariate Time Series](https://arxiv.org/abs/2607.07258)
*Donato Cerciello,Leonardo Schiavo,Angel Panizo-LLedot,Javier Huertas Tato,David Camacho*

Main category: cs.LG

TL;DR: FMMVCC is a Mamba-based deep clustering framework for time series that uses state space sequence modeling for efficient temporal representation learning and multi-view self-supervised learning, outperforming baselines on 15 benchmark datasets.


<details>
  <summary>Details</summary>
Motivation: Time series data often lack annotations, making supervised learning impractical. Existing deep clustering methods struggle with capturing long-range temporal dependencies or suffer from high computational costs.

Method: FMMVCC uses a Mamba-based architecture (state space sequence modeling) with linear complexity for temporal representation learning, combined with multi-view self-supervised learning using temporal masking and augmentations.

Result: Evaluated on 15 benchmark datasets, FMMVCC achieved the best performance in 29 out of 60 metric evaluations and the highest average rank across all tested scenarios, consistently outperforming state-of-the-art baselines.

Conclusion: FMMVCC effectively addresses limitations of existing deep clustering methods for time series by balancing long-range dependency capture with computational efficiency, demonstrating superior clustering performance.

Abstract: In many realistic scenarios, large volumes of time series data are generated with limited or expensive annotations. This limitation makes supervised learning methods difficult to apply and leads to the use of unsupervised approaches capable of discovering meaningful structures directly from raw data. Clustering therefore plays a crucial role in organizing time series into groups that share similar temporal patterns, enabling exploratory analysis and downstream tasks without requiring manual labeling. However, existing deep clustering methods often struggle to capture long-range temporal dependencies or rely on architectures with high computational cost. This paper introduces FMMVCC, a Mamba-based deep clustering framework for time series that leverages state space sequence modeling to efficiently learn temporal representations with linear complexity. Additionally, it utilizes multi-view self-supervised learning with temporal masking and augmentations. Experimental evaluation in 15 benchmark datasets proves that FMMVCC consistently outperforms state-of-the-art baselines, achieving the best overall performance in 29 of 60 total metric evaluations and the highest average rank in all tested scenarios.

</details>


### [209] [BubbleSH: A Dataset of Rising Bubbles with Deformable Interfaces](https://arxiv.org/abs/2607.07275)
*Rachna Ramesh,Kiet Bennema ten Brinke,Douwe Orij,Ivo Roghair,Vlado Menkovski*

Main category: cs.LG

TL;DR: The paper introduces BubbleSH, a dataset of 3D bubbly flow dynamics from direct numerical simulations, featuring time-resolved bubble trajectories, velocities, and shape evolution represented via spherical harmonics. It provides evaluation metrics and benchmarks for data-driven models, particularly generative models, on deformable multiphase systems.


<details>
  <summary>Details</summary>
Motivation: Bubbly flows exhibit complex multiscale dynamics with deformable bubbles interacting through the surrounding liquid, creating strongly coupled kinematic and morphological behavior. A compact yet physically expressive dataset is needed to enable data-driven modeling where shape deformation and bubble-bubble interactions are central.

Method: High-fidelity direct numerical simulations of bubbles rising in a periodic domain generate transient, 3D bubble-swarm dynamics. Bubble morphology is compactly represented using spherical harmonics. The dataset provides time-resolved trajectories, velocities, and shape evolution. Evaluation metrics for trajectory and shape prediction are introduced.

Result: The dataset is characterized by bubble kinematics, morphology, and interaction patterns. A permutationally and translationally equivariant probabilistic emulator is evaluated on BubbleSH using the proposed metrics, establishing a benchmark for data-driven models of deformable, chaotic multiphase systems.

Conclusion: BubbleSH serves as a lightweight, high-fidelity dataset and benchmark for developing and evaluating data-driven models (especially generative models) for deformable, chaotic multiphase systems, with sensitivity to local perturbations making it well suited for learning distributions over future trajectories.

Abstract: Bubbly flows exhibit complex multiscale dynamics, with deformable bubbles interacting through the surrounding liquid and giving rise to strongly coupled kinematic and morphological behavior. We present BubbleSH, a bubbly flows dataset consisting of transient, three-dimensional bubble-swarm dynamics obtained from high-fidelity direct numerical simulations of bubbles rising in a periodic domain. The dataset provides time-resolved bubble trajectories, velocities, and shape evolution, with bubble morphology compactly represented using spherical harmonics. Designed to be lightweight yet physically expressive, the dataset enables data-driven modeling of bubbly flow simulators where shape deformation and bubble-bubble interactions play a central role. We characterize the dataset with bubble kinematics, morphology, and interaction patterns, and introduce evaluation metrics for both trajectory and shape prediction. The sensitivity of bubble-swarm dynamics to local perturbations makes BubbleSH particularly well suited to generative models that learn distributions over possible future trajectories. We evaluate a permutationally and translationally equivariant probabilistic emulator on BubbleSH given the proposed metrics. Therefore, we establish a compact, high-fidelity dataset and a benchmark for developing and evaluating data-driven models of deformable, chaotic multiphase systems.

</details>


### [210] [Nonlinear Bandit](https://arxiv.org/abs/2607.07304)
*Tianshuo Zheng,Ting Wu,Zhi-Hua Zhou,Keqin Liu*

Main category: cs.LG

TL;DR: This paper studies generalized linear bandits under heavy-tailed noise and piecewise constant contexts, proposing the EHM algorithm (based on online mirror descent and adaptive Huber loss) that achieves near-optimal regret without needing a key parameter. It extends the method to piecewise constant contexts (PGLB-EHM) and nonlinear bandits (NB-EHM) with sublinear regret guarantees.


<details>
  <summary>Details</summary>
Motivation: Heavy-tailed noise is common in real-world applications (recommendation, finance, medical treatments), yet existing GLB solutions often assume sub-Gaussian noise. The authors aim to provide robust algorithms that handle heavy-tailed distributions without requiring knowledge of certain distribution parameters.

Method: (1) EHM algorithm: online mirror descent with adaptive Huber loss, achieving one-pass O(1) per-round complexity. (2) PGLB-EHM: modified for piecewise constant contextual characteristics. (3) NB-EHM: uses bisection method with special restrictions for nonlinear bandits, combined with affine lifting for general NB problems.

Result: EHM achieves near-optimal regret Õ(T^{1/(1+ε)}). PGLB-EHM maintains the same regret order under piecewise constant contexts. NB-EHM achieves sublinear regret for nonlinear bandits via affine lifting.

Conclusion: The proposed algorithms effectively handle heavy-tailed noise in GLB and its variants while eliminating the need for prior knowledge of a commonly used parameter, with computationally efficient updates and provable regret guarantees.

Abstract: In this paper we first study the problem of generalized linear bandit (GLB) under heavy-tailed noise. The characteristics of heavy-tailed distributions are widely observed in real-world applications such as personalized recommendation, financial markets, and medical treatments. Based on the online mirror descent (OMD) method, we propose an algorithm EHM that extends the adaptive Huber loss method (Wang et al., 2025) with one-pass update ($\mathcal{O}(1)$ computational complexity with respect to current round $t$ and the time horizon $T$), which simultaneously achieves an almost optimal regret of $\widetilde{\mathcal{O}}(T^{\frac{1}{1+ε}})$ where $T$ is the time horizon. In addition, by utilizing a special property of some link function (Sawarni et al., 2025), our algorithm eliminates the need to know a commonly used parameter. Next, we study the GLB problem under the case when contextual characteristic becomes piecewise constant, and we slightly revised former algorithm to obtain the PGLB-EHM algorithm. After theoretical analysis, we prove that the regret upper bound order stays the same. Furthermore, we look deeper into a special case of nonlinear bandit (NB) and present the NB-EHM algorithm with bisection method and special restriction. Eventually we utilize the affine lifting approach and show that the general NB problem can be applied with NB-EHM to achieve a sublinear regret bound.

</details>


### [211] [FedCVESA: Taking Away Training Data in Federated Learning via Correlation Value Encoding and Segmented Aggregation](https://arxiv.org/abs/2607.07314)
*Chongkai Li,Bang Zhang,Wenjian Luo*

Main category: cs.LG

TL;DR: This paper proposes FedCVESA, a white-box attack where a malicious FL server actively encodes target clients' private training data into selected model parameters (carrier parameters) using Pearson-correlation regularization, and uses segmented aggregation to prevent parameter overwriting, enabling successful training data theft in federated learning.


<details>
  <summary>Details</summary>
Motivation: While centralized TATD attacks exploit deep model memorization to recover training data, this threat has not been systematically studied in federated learning because multi-client averaging could overwrite encoded data. The authors aim to investigate whether FL can be exploited as a parameter-level memorization channel for active TATD attacks.

Method: FedCVESA extends the Correlation Value Encoding Attack (CVEA) to FL by: (1) adding a Pearson-correlation regularizer to target clients' loss functions to gradually encode private data into carrier parameters; (2) introducing segmented aggregation over dispersed carrier parameters to reduce overwriting during server aggregation while maintaining standard averaging on other parameters.

Result: Experiments on MNIST, Fashion-MNIST, and CIFAR-10 with Dirichlet non-IID partitions demonstrate that the method can steal semantically meaningful private training images from the trained model while maintaining acceptable main-task utility in a controlled proof-of-concept setting.

Conclusion: FL can serve as a parameter-level memorization channel for active TATD attacks under the studied white-box malicious-server setting, revealing a significant privacy vulnerability in federated learning systems.

Abstract: Federated learning (FL) avoids explicit data exposure by keeping raw data on local clients, yet privacy risks remain in the training process and the learned model itself. Recently, centralized Taking Away Training Data (TATD) attacks have shown that malicious training could abuse the memorization capacity of deep models to store and later recover training data. However, this memorization-based threat has not been systematically studied under FL environments, where multi-client averaging could overwrite encoded training data. In this paper, we study a white-box TATD attack in which a malicious server selects n target clients from K participating clients and actively writes private training data into the global model during federated training. We propose FedCVESA, a federated variant of Correlation Value Encoding Attack (CVEA), by adding a Pearson-correlation regularizer to the loss function of target clients, so that private training data are gradually encoded into selected model parameters, referred to as carrier parameters. To reduce the overwriting of carrier parameters during server aggregation, we further propose segmented aggregation over dispersed carrier parameters, preserving selected carrier parameters while keeping standard averaging on the remaining parameters. Experiments on MNIST, Fashion-MNIST, and CIFAR-10 under Dirichlet non-IID partitions show that the proposed method can steal semantically meaningful private training images from the trained model while maintaining acceptable main-task utility in a controlled proof-of-concept setting. These results demonstrate that FL can become a parameter-level memorization channel for active TATD attack under the studied white-box malicious-server setting.

</details>


### [212] [Mechanistic Interpretability for Neural Networks: Circuits, Sparse Features and Symbolic Reasoning](https://arxiv.org/abs/2607.07316)
*Pranav Sawant,Jakub Krejčí*

Main category: cs.LG

TL;DR: This paper provides a comprehensive overview of mechanistic interpretability, covering techniques to reverse-engineer neural networks, including Transformer circuit analysis, sparse autoencoders for addressing superposition, steering vectors for model control, and connections to neurosymbolic AI.


<details>
  <summary>Details</summary>
Motivation: Traditional explainable AI methods only capture surface-level input-output correlations, leaving neural networks as opaque "black boxes." This is dangerous for safety and auditability in high-stakes deployments, motivating the need for deeper, mechanistic understanding.

Method: The paper surveys several methods: (1) Transformer circuit analysis examining residual stream, attention, and induction heads; (2) Sparse Autoencoders (SAEs) and transcoders to decompose superposition into interpretable features; (3) Steering vectors and causal interventions for model control; (4) Neurosymbolic AI frameworks to translate neural representations into logical rules.

Result: The paper presents a structured taxonomy of mechanistic interpretability techniques, showing how each method addresses specific challenges like superposition (SAEs), in-context learning (induction heads), and model control (steering vectors). It bridges interpretability with symbolic reasoning.

Conclusion: Mechanistic interpretability offers a promising path toward fully understanding neural networks by reverse-engineering their internal algorithms, with concrete tools now available for circuit analysis, feature decomposition, and model steering—ultimately enabling safer and more auditable AI systems.

Abstract: This article offers a comprehensive overview of mechanistic interpretability, an emerging field that seeks to reverse-engineer the internal algorithms of modern neural networks. While traditional explainable AI methods often stop at surface-level input-output correlations, this approach directly addresses the opaque "black box" nature of machine learning models, which is essential for ensuring safety and auditability in high-stakes deployments. The paper provides a detailed examination of Transformer circuit analysis, exploring how internal components like the residual stream, attention mechanisms, and induction heads drive complex tasks and in-context learning. It subsequently tackles the core challenge of superposition and polysemanticity, demonstrating how tools like Sparse Autoencoders (SAEs) and transcoders can decompose tangled network activations into distinct, human-interpretable features. Furthermore, the paper explores methods for actively controlling and modifying model behavior through steering vectors and causal interventions. Finally, it connects these mechanistic insights with neurosymbolic AI frameworks designed to translate neural representations into explicit, executable logical rules.

</details>


### [213] [Hypergraph Neural Stochastic Diffusion: An SDE Framework for Uncertainty Estimation](https://arxiv.org/abs/2607.07330)
*Zhiheng Zhou,Mengyao Zhou,Dengyi Zhao,Xingqin Qi,Guiying Yan*

Main category: cs.LG

TL;DR: HyperNSD proposes a stochastic differential equation framework for uncertainty estimation on hypergraphs, modeling representations as stochastic processes over node-hyperedge structures to capture uncertainty evolution during learning.


<details>
  <summary>Details</summary>
Motivation: Predictive uncertainty in hypergraphs remains underexplored. Unlike pairwise graphs, hypergraph uncertainty arises from noisy attributes, ambiguous labels, and variations in incidence structures. Existing methods rely on post-hoc confidence or expensive ensembles/Bayesian inference, failing to capture uncertainty evolution during representation learning.

Method: A stochastic differential equation (SDE) framework where hypergraph representations evolve as stochastic processes over node-hyperedge incidence structures. A learnable drift captures deterministic higher-order diffusion, while a learnable stochastic forcing function characterizes structural ambiguity and noise. Uncertainty is quantified via variability of stochastic trajectories.

Result: Experiments on multiple hypergraph benchmarks show HyperNSD achieves reliable uncertainty estimation for OOD and misclassification detection while maintaining competitive prediction accuracy.

Conclusion: Provides a principled stochastic-dynamical framework for trustworthy higher-order representation learning, with theoretical guarantees on well-posedness, stability, equivariance, and convergence.

Abstract: Hypergraph neural networks have shown powerful capability in modeling higher-order relations, yet their predictive uncertainty remains underexplored. Unlike pairwise graphs, uncertainty in hypergraphs arises not only from noisy attributes and ambiguous labels, but also from variations in node-hyperedge incidence structures and complex higher-order dependencies. Existing approaches mainly estimate uncertainty from final predictions or rely on computationally expensive ensembles and Bayesian inference, limiting their ability to capture uncertainty evolution during representation learning. In this paper, we propose Hypergraph Neural Stochastic Diffusion(HyperNSD), a stochastic differential equation framework for uncertainty estimation on hypergraphs. HyperNSD models hypergraph representations as stochastic processes evolving over node-hyperedge incidence structures. A learnable drift function captures deterministic higher-order diffusion dynamics, while a learnable stochastic forcing function characterizes structural ambiguity and representation noise. Predictive uncertainty is directly quantified through the variability of stochastic representation trajectories, providing an intrinsic uncertainty measure beyond post-hoc confidence scores. We formulate HyperNSD with neural drift and diffusion networks, enabling joint learning of prediction and uncertainty propagation. Theoretical analyses establish well posedness, perturbation stability,permutation equivariance, and numerical convergence of the proposed stochastic dynamics. Experiments on multiple hypergraph benchmarks demonstrate that HyperNSD achieves reliable uncertainty estimation for out-of-distribution and misclassification detection while preserving competitive prediction accuracy. These results provide a principled stochastic-dynamical framework for trustworthy higher-order representation learning.

</details>


### [214] [Latency-Aware Bid Acceptance under Operational Feasibility: A Public Benchmark with Hindsight Ceilings](https://arxiv.org/abs/2607.07343)
*Aswin Chandrasekaran*

Main category: cs.LG

TL;DR: FreightBidBench is a new public benchmark for online truckload bid acceptance, featuring explicit, reproducible feasibility/economics parameters and proposing tighter hindsight bounds (Lagrangian-per-truck) plus efficient surrogate-rollout cascade policies that recover ~98% of rollout profit at ~40-56% of its latency.


<details>
  <summary>Details</summary>
Motivation: Existing benchmarks for dynamic truckload acceptance are scarce; routing benchmarks are static, and dynamic-fleet studies rely on private operator data, lacking reproducibility and public calibration.

Method: The authors build a closed-loop benchmark (FreightBidBench) from public Freight Analysis Framework and USDA truck rate data, define two hindsight ceilings (LP relaxation and tighter Lagrangian-per-truck relaxation), and develop a parametric surrogate-rollout cascade with boundary-band and scarcity-pressure escalation triggers.

Result: The Lagrangian-per-truck relaxation is 20.7% tighter (tight capacity) and 39.3% tighter (scarce capacity) than LP. Best simple policy retains 91.0%/86.5% of rollout profit; cascade recovers ~98% on both scenarios at 40-56% of rollout's latency and is statistically indistinguishable from the rollout teacher on tight scenarios.

Conclusion: FreightBidBench enables reproducible research on online truckload acceptance. The Lagrangian-per-truck bound and cascade policy offer practical, efficient solutions closely matching optimal rollout performance.

Abstract: Online truckload bid acceptance is a closed-loop stochastic decision problem in which a carrier or broker must, in real time, accept or reject a tendered load subject to operational feasibility, fleet repositioning costs, and opportunity cost against future demand. Public, reproducible benchmarks for this problem are scarce: existing routing benchmarks are static, while dynamic-fleet studies typically rely on private operator data. We introduce FreightBidBench, a public-calibrated, dependency-free, closed-loop benchmark in which feasibility (pickup reach, appointment windows, simplified hours-of-service, stochastic yard delays) and economics (service-failure penalty, terminal fleet value, daily price-premium window) are explicit, versioned, and reproducible from public Freight Analysis Framework and U.S. Department of Agriculture truck rate data. We develop two full-horizon hindsight ceilings: a simple LP style relaxation and a tighter Lagrangian-per-truck information relaxation that retains per-truck hours-of-service and sequencing structure and is 20.7% tighter than the LP relaxation on a tight-capacity scenario and 39.3% tighter on a scarce-capacity scenario. We introduce a parametric surrogate-rollout cascade with boundary-band and scarcity-pressure escalation triggers. On ten-seed tight and scarce scenarios, the best simple policy retains 91.0% and 86.5% of rollout profit and the standard-library surrogate 94.2% and 89.3%; a cascade at a single escalation band recovers roughly 98% on both at 40-56% of rollout's mean decision latency, and on the tight scenario is statistically indistinguishable from the rollout teacher (paired-bootstrap 95% CI on the profit delta spans zero).

</details>


### [215] [Multi-Agent AI Control: Distributed Attacks Hamper Per-Instance Monitors](https://arxiv.org/abs/2607.07368)
*Oliver Makins,Orazio Angelini,Zohreh Shams,Mary Phuong*

Main category: cs.LG

TL;DR: This paper empirically studies multi-agent AI control, introducing FakeLab (a synthetic AI-lab codebase) and finding a "fragmentation effect" where distributed attacks by multiple coordinated agents become harder for per-agent monitoring to detect.


<details>
  <summary>Details</summary>
Motivation: Real AI deployments run many agents over shared infrastructure, and severe risks (model-weight exfiltration, training-run poisoning) require multiple agents acting in concert. Existing AI control research mostly focuses on single agents in single trajectories, leaving distributed multi-agent attacks understudied.

Method: The authors develop FakeLab (9 services, 86 benign tasks, 4 attack objectives), formalize distributed attacks where multiple agents jointly pursue malicious goals, and evaluate single-agent monitoring against these attacks by varying agent count, coordination mechanisms, model capabilities, and monitoring configurations.

Result: Key findings: (1) Fragmentation effect: more coordinated attackers reduce per-agent monitoring's catch rate. (2) This effect is not driven by benign-to-malicious code ratio, but likely by model capability. (3) An explicit planner amplifies fragmentation, boosts attack completion up to 7x, and sharpens executors' poor monitor-theory-of-mind. (4) Weak trusted monitors miss most attacks; stronger monitors reduce undetected success by >10x but remain imperfect.

Conclusion: Multi-agent distributed attacks pose a qualitatively different challenge for AI control that warrants further study; the fragmentation effect underscores the insufficiency of per-agent monitoring alone.

Abstract: AI control is a family of techniques to prevent an AI with malicious goals from subverting its operator's intent. AI Control usually studies a single agent in one trajectory, but real deployments run many agents over shared infrastructure, and the most severe risks (model-weight exfiltration, training-run poisoning) plausibly need several agents acting in concert. We initiate the empirical study of multi-agent AI control, formalising distributed attacks in which several agents jointly aim for a malicious goal. We develop FakeLab: a synthetic AI-lab codebase (9 services, 86 benign tasks, 4 attack objectives). We evaluate single agent monitoring against distributed attacks, varying the number of agents, their coordination, model capabilities and precise monitoring configuration.
  Our central finding is the fragmentation effect: as more agents coordinate to attack, per-agent monitoring becomes less likely to catch any of the attackers. Secondly, this is not driven by the ratio of benign to malicious code -- we argue this is likely dependent on model capability. Third, an explicit planner amplifies the fragmentation effect, raises the rate of completing the attack up to sevenfold, and mildly sharpens executors' poor monitor-theory-of-mind. Finally: a weak "trusted" monitor misses most attacks, while a stronger one cuts undetected success by more than an order of magnitude but remains imperfect. We are happy to share FakeLab, our control setting, with safety researchers on request.

</details>


### [216] [On Adversarial Vulnerability of Vision-Language Models through the Lens of Intermediate Spectral Subspaces](https://arxiv.org/abs/2607.07375)
*Chethan Krishnamurthy Ramanaik,Tobias Callies,Michael Hecht,Eirini Ntoutsi*

Main category: cs.LG

TL;DR: This paper explores adversarial vulnerability in DNNs by examining the spectral structure of intermediate linear transformations, particularly in transformer-based vision-language models (VLMs). The authors propose SSGRA, a white-box attack that aligns intermediate representations with the subspace of bottom right singular vectors, showing improved attack effectiveness and offering spectral insights into adversarial robustness.


<details>
  <summary>Details</summary>
Motivation: Existing studies on adversarial vulnerability focus on decision-boundary geometry, feature robustness, input-output Jacobians, and inverse problem instability, but the spectral structure of intermediate linear transformations in DNNs remains unexplored. The authors aim to fill this gap, especially for vision-language models where linear layers admit interpretable spectral decompositions.

Method: The authors propose SSGRA (Spectral-Subspace-Guided Attack), a white-box method that aligns intermediate representations with the subspace spanned by the bottom right singular vectors of intermediate linear transformations in transformer-based VLMs.

Result: SSGRA demonstrates improved attack effectiveness compared to existing baselines on transformer-based vision-language models.

Conclusion: The spectral interpretation offers new insights into adversarial vulnerability in VLMs, which can inform future strategies for improving model robustness.

Abstract: Adversarial vulnerability in deep neural networks (DNNs) has been studied from the perspectives of decision-boundary geometry, feature robustness, input-output Jacobians, and the instability of inverse problems. Here, we focus on the spectral structure of intermediate linear transformations that propagate information through modern DNNs, an unexplored mechanism of adversarial vulnerability. Specifically, we investigate transformer-based vision-language models, whose linear layers admit interpretable spectral decompositions and whose widespread adoption makes understanding their robustness increasingly important. We propose a white-box spectral-subspace-guided attack (SSGRA) that aligns intermediate representations with the subspace spanned by the bottom right singular vectors. Our experiments show improved attack effectiveness over existing baselines. In addition, SSGRA offers a spectral interpretation of adversarial vulnerability in VLMs, providing insights for improving their robustness.

</details>


### [217] [Generalist Vision-Language Models for Fast Radio Burst detection: a zero-shot benchmark against a specialized detector](https://arxiv.org/abs/2607.07382)
*Raiff H. Santos,Amilcar R. Queiroz,Tharcisyo S. S. Duarte,K. E. L. de Farias,Rafael A. Batista*

Main category: cs.LG

TL;DR: Small, open-weight VLMs (Gemma 4 2B/4B) achieve 93.65% accuracy detecting FRBs in a zero-shot, prompt-only regime, matching state-of-the-art SwinYNet (92.90%) with significantly lower false-positive rates on RFI and noise, without any fine-tuning.


<details>
  <summary>Details</summary>
Motivation: Current deep learning FRB detectors require large task-specific training datasets and cannot be redefined without retraining, motivating evaluation of generalist VLMs that can work zero-shot with only prompt engineering.

Method: Evaluated Gemma 4 2B and 4B VLMs in zero-shot, prompt-only regime on 2000 simulated L-band dynamic spectra (FRB vs non-FRB), compared against specialized SwinYNet detector. Also tested three-class classification (FRB/RFI/noise) on 3000 spectra via prompt rewriting.

Result: Gemma 4 2B achieved 93.65% accuracy (vs SwinYNet 92.90%), with significantly lower false-positive rates on structured RFI (6.4% vs 25.0%) and zero false positives on pure noise. SwinYNet had perfect ROC-AUC (1.0000 vs 0.9482). Three-class classification reached 86% accuracy with zero false FRBs.

Conclusion: Small generalist VLMs can competitively detect FRBs in a zero-shot setting without any task-specific training, offering advantages in reduced false-positive rates and flexibility via prompt reconfiguration, though specialized detectors still excel at probabilistic ranking.

Abstract: Fast Radio Bursts (FRBs) are millisecond-duration radio transients whose automated detection increasingly relies on highly specialized deep learning models. These detectors achieve exceptional performance, but they require large task-specific training datasets and cannot be redefined without retraining. In this work, we evaluate whether small, open-weight, locally run generalist Vision-Language Models (VLMs) can detect FRBs in dynamic spectra under a zero-shot, prompt-only regime, with no fine-tuning and no labeled examples, returning structured decisions with a natural-language justification. From a controlled set of 3000 simulated L-band dynamic spectra containing FRBs, structured Radio Frequency Interference (RFI), and noise, we draw a balanced binary benchmark of 2000 samples and compare two such VLMs (Gemma 4 2B and 4B), sample by sample, against the state-of-the-art specialized detector SwinYNet. At the default threshold, Gemma 4 2B reaches an accuracy of 93.65%, with no statistically significant difference from SwinYNet (92.90%), while showing a significantly lower false-positive rate on structured RFI (6.4% vs. 25.0%) and no false positives on pure noise. SwinYNet retains a perfect probabilistic ranking on this benchmark (ROC-AUC of 1.0000 vs. 0.9482), a ceiling that the zero-shot VLM approaches from general-purpose pretraining alone. Rewriting the prompt alone reconfigures the same models for three-class FRB/RFI/noise classification on the full set of 3000 spectra, where they reach up to 86% accuracy without a single false FRB.

</details>


### [218] [Sparse Delta Memory: Scaling the State of Linear RNNs through Sparsity](https://arxiv.org/abs/2607.07386)
*Loïc Cabannes,Pierre-Emmanuel Mazaré,Gergely Szilvasy,Matthijs Douze,Maria Lomeli,Ilze Amanda Auzina,Justin Carpentier,Gabriel Synnaeve,Hervé Jégou*

Main category: cs.LG

TL;DR: SDM scales linear attention's hidden state to much higher capacity via sparse memory addressing, improving long-context recall and reasoning under isoFLOP constraints.


<details>
  <summary>Details</summary>
Motivation: Linear attention models suffer from limited state size, which hurts long-context recall compared to softmax-attention transformers. Increasing state size improves recall but increases FLOPs.

Method: SDM extends Gated DeltaNet by replacing dense key-value outer products with sparse reads and writes to a large explicit memory, enabling much higher state capacity without proportional FLOP increase.

Result: Under isoFLOP and same parameter count, higher state memory capacity significantly improves in-context learning and long-context retrieval. Learning the initial memory state further boosts common-knowledge and reasoning tasks.

Conclusion: Sparse memory addressing is an effective way to scale linear RNN state capacity, bridging the long-context recall gap with softmax transformers while maintaining computational efficiency.

Abstract: Linear attention models allow a fixed state size and a fixed amount of compute per token. However, due to their limited state size, linear attention models fall behind in long-context recall compared to softmax-attention-based transformer architectures. Increasing the state size of linear attention improves recall performance but at the cost of higher FLOPs. In this work, we introduce Sparse Delta Memory (SDM), an architecture that scales the hidden state of gated linear RNNs to orders of magnitude higher capacity using a sparse addressing scheme. SDM extends the Gated DeltaNet architecture by replacing the dense key-value outer product with sparse reads and writes to a large explicit memory. We show that, under an isoFLOP constraint and with an identical number of parameters, a higher state memory capacity significantly improves performance on in-context learning and long-context retrieval tasks. Moreover, by learning the initial state of the SDM memory and therefore using it as a parametric memory, we show that the model further improves on a wide range of common-knowledge and reasoning tasks.

</details>


### [219] [The Optimal Sample Complexity of Learning Autoregressive Chain-of-Thought](https://arxiv.org/abs/2607.07423)
*Zhiyuan Li*

Main category: cs.LG

TL;DR: The paper proves that sample complexity for exact-trace PAC learning of Chain-of-Thought traces is bounded by the multiclass rate of the local next-token class (governed by DS dimension), despite one wrong action making the entire trace incorrect. The bound has no dependence on rollout length, and is worst-case optimal. The proof introduces "parity dimension" as a rollout-stable refinement of DS dimension.


<details>
  <summary>Details</summary>
Motivation: To understand the sample complexity of learning full autoregressive Chain-of-Thought (CoT) traces under exact-trace loss, where a single wrong action invalidates the entire trace. The key question is whether the rollout length causes sample complexity to blow up, or whether it remains governed by the complexity of the local next-token prediction class.

Method: The paper introduces a new complexity measure called parity dimension, which is a rollout-stable refinement of the Daniely-Shalev-Shwartz (DS) dimension based on even pseudo-cubes. It uses a low-coordinate spanning theorem on finite restrictions to control one-inclusion density, and proves that parity dimension (unlike DS dimension) does not increase under autoregressive rollout.

Result: The PAC sample complexity for exact-trace learning of Rollout(H) is O((DSdim(H) + log(1/δ))/ε), with no dependence on rollout length. This is worst-case optimal since one-step stopping recovers ordinary multiclass learning of H. The paper also shows that DS dimension can increase under rollout, justifying why the parity dimension detour is necessary.

Conclusion: Exact-trace CoT learning is not harder than learning the local next-token predictor in terms of sample complexity, despite the harsh exact-trace loss. The parity dimension provides a stable complexity measure for autoregressive rollouts, and the bounds are tight in the worst case.

Abstract: We prove that, in the realizable PAC setting, the sample complexity of exact-trace learning for full autoregressive Chain-of-Thought traces is upper bounded by the standard multiclass rate of the local next-token class, where this rate is governed by the Daniely--Shalev-Shwartz dimension. Under exact-trace loss, one wrong action makes the whole trace incorrect; nevertheless, for every stopping rule $\mathtt{halt}$ and every pointwise $\mathtt{halt}$-halting local class $\mathrm{H}$, $n_{\mathrm{PAC}}^{\varepsilon,δ}(\operatorname{Roll}_{\mathtt{halt}}(\mathrm{H}))=O((\operatorname{DSdim}(\mathrm{H})+\log(1/δ))/\varepsilon)$, with no dependence on rollout length. The dependence on $\operatorname{DSdim}(\mathrm{H})$ is worst-case optimal, since one-step stopping recovers ordinary multiclass learning of $\mathrm{H}$. The proof introduces parity dimension, a rollout-stable refinement of DS dimension based on even pseudo-cubes. It controls one-inclusion density via a low-coordinate spanning theorem on finite restrictions and, unlike DS dimension itself, does not increase under autoregressive rollout. We also show why this detour is necessary: DS dimension can increase under rollout.

</details>


### [220] [RLVP: Penalize the Path, Reward the Outcome](https://arxiv.org/abs/2607.07435)
*Bojie Li,Noah Shi*

Main category: cs.LG

TL;DR: This paper argues that real-world agents must learn online from costly interactions, requiring both path-sensitive constraint compliance and few-shot efficiency. The authors show that RLVR fails on both counts and propose a "penalize the path, reward the outcome" approach with four design rules for effective penalties, achieving high success with near-zero violations.


<details>
  <summary>Details</summary>
Motivation: Real-world agents (e.g., phone-calling agents) face costly, irreversible interactions where (1) deployability depends on path constraints (e.g., not calling repeatedly) that outcome-based rewards cannot capture, and (2) sample efficiency is critical. RLVR is blind to both challenges.

Method: The authors propose a dual-signal approach: "penalize the path, reward the outcome." They introduce verifiable path penalties (cheap to detect) alongside outcome rewards, provide four design rules for effective penalties, and warn against the "inaction trap" when penalties are used alone.

Result: The proposed method achieves high task success with near-zero constraint violations, whereas outcome-only training violates constraints on nearly every episode.

Conclusion: Outcome-only RL is insufficient for real-world agentic tasks. Path penalties provide a cheap, verifiable dense signal that reliably supplies variance the outcome lacks, making "penalize the path, reward the outcome" an effective recipe.

Abstract: Agents acting on our behalf in the real world (e.g. placing phone calls) must learn online from costly, often irreversible interactions rather than cheap simulator steps. Two things follow. First, deployability depends on the path, not only the outcome. An agent must respect outcome-neutral constraints such as not repeatedly calling an unresponsive user, respecting business hours, or completing required authentication constraints that outcome-based rewards cannot express, since violating them frequently improves apparent success. Second, because each interaction is expensive, the agent must learn efficiently from very few examples.
  Reinforcement learning from verifiable rewards (RLVR) is blind to both challenges: it optimizes solely on the outcome and wastes expensive rollouts on all-fail groups where group-relative advantage collapses to zero. Attempts to densify supervision by rewarding progress target the hard-to-verify direction. In contrast, real agentic environments can cheaply detect bad moves. Since group-relative advantage is equivalent to within-group variance, a dense signal helps only when it supplies variance the outcome lacks. A verifiable penalty on the path meets this condition reliably, while a progress potential helps only where partial progress is reachable. The resulting recipe "penalize the path, reward the outcome" achieves high task success with near-zero violations, where outcome-only training violates constraints on nearly every episode. We provide four design rules for effective penalties, including avoidance of the inaction trap that arises when a penalty is used in isolation.

</details>


### [221] [Where to Intervene? Benchmarking Fairness-Aware Learning on Differentially Private Synthetic Tabular Data](https://arxiv.org/abs/2607.07471)
*Vinícius Gabriel Angelozzi,Héber H. Arcolezi*

Main category: cs.LG

TL;DR: First systematic evaluation of fairness interventions on differentially private synthetic tabular data, using AIM as the state-of-the-art DP synthesizer across four datasets and multiple fairness metrics.


<details>
  <summary>Details</summary>
Motivation: DP and fairness objectives can conflict—DP often amplifies demographic disparities, and it's unclear whether established fairness interventions remain effective under DP constraints.

Method: Benchmark centered on AIM (marginal-based DP synthesizer); evaluates four pipeline configurations (Baseline, DP-only, Fair-only, DP+Fair) across pre-processing, in-processing, and post-processing fairness strategies under various privacy budgets.

Result: DP alone degrades both utility and fairness; applying fairness interventions partially restores equity; post-processing methods offer the most stable fairness-utility trade-offs across privacy budgets.

Conclusion: Post-processing fairness interventions are most effective for achieving equitable outcomes under DP constraints while preserving competitive utility; open-source artifacts released for reproducibility.

Abstract: Machine learning models are increasingly deployed in high-stakes domains, raising concerns about both privacy and fairness. Differential Privacy (DP) has become a gold standard for privacy-preserving data analysis, while fairness-aware mechanisms aim to mitigate discrimination against underrepresented groups. However, these objectives can conflict: DP often amplifies disparities across demographic groups, and little is known about whether established fairness interventions remain effective under DP constraints. In this work, we present, to our knowledge, the first systematic evaluation of fairness interventions on differentially private synthetic tabular data. Our benchmark centers on the Adaptive Iterative Mechanism (AIM), identified as the state-of-the-art marginal-based DP synthesizer (Cormode et al. 2025). We thus evaluate fairness interventions across four datasets, multiple group fairness metrics, and three categories of mitigation strategies (pre-processing, in-processing, and post-processing) under a wide range of privacy budgets. We compare four pipeline configurations: (Baseline) training on original data; (DP-only) training on DP synthetic data; (Fair-only) applying fairness mechanisms on original data; and (DP+Fair) combining fairness mechanisms with DP synthetic data. Our results demonstrate that while DP alone can degrade both utility and fairness, applying fairness interventions can partially restore equitable outcomes. Among them, post-processing methods tend to provide more stable fairness-utility trade-offs across privacy budgets and synthesizers, achieving strong fairness improvements while preserving competitive utility relative to other intervention stages. We release all code, data, and experimental artifacts in an open-source repository to ensure full reproducibility and to support future research on the privacy-fairness-utility trade-off.

</details>


### [222] [Reward-Adaptive Iterative Discovery: A Case Study on Automated Game Testing for NHL26](https://arxiv.org/abs/2607.07498)
*Florian Fuchs,Jessy Gosselin-Grant,Boris Skuin,Michele Petteni,Alessandro Sestini,Joakim Bergdahl,Amir Baghi,Linus Gisslén*

Main category: cs.LG

TL;DR: The paper presents RAID, an iterative RL approach that automatically finds behavioral exploits in goalie AI for EA SPORTS NHL 26, discovering six exploit strategies matching those found by human playtesters.


<details>
  <summary>Details</summary>
Motivation: Testing goalie AI in game development is labor-intensive; the authors aim to reduce re-testing effort after each game or behavior modification by automating exploit discovery.

Method: RAID (Reward-Adaptive Iterative Discovery) uses an iterative Reinforcement Learning approach with a population of goal scoring agents, adding a simple extension to existing RL algorithms to find multiple diverse high-quality solutions and avoid overfitting.

Result: In a single experiment, RAID found six hockey scoring exploit strategies that were qualitatively similar to those discovered by human playtesters in hours-long manual testing sessions.

Conclusion: RAID effectively automates exploit discovery in game AI testing, reducing reliance on manual playtesting while maintaining comparable quality of results.

Abstract: Testing is a major effort for the gaming industry, requiring a significant part of development budget and people power. We present a case study on a development version of the ice hockey game EA SPORTS NHL 26, for which human playtesters test the goalie AI for behavioral exploits. To reduce the effort of re-testing the goalie AI after every game or behavior modification in the development phase, we propose Reward-Adaptive Iterative Discovery (RAID), a novel approach to automatically find exploits using an iterative Reinforcement Learning (RL) approach that trains a population of goal scoring agents. While previous approaches can already successfully find exploits, RL algorithms tend to overfit to a single solution. We introduce a simple extension on top of existing RL algorithms, such that they find multiple diverse high-quality solutions. For our first deployment of this approach, within a single experiment we were able to find six hockey scoring exploit strategies that were qualitatively similar to those that playtesters had found in hours-long manual testing sessions.

</details>


### [223] [TimEE: End-to-end Time Series Classification via In-Context Learning](https://arxiv.org/abs/2607.07500)
*Jaris Küken,Shi Bin Hoo,Martin Mráz,Frank Hutter,Lennart Purucker*

Main category: cs.LG

TL;DR: TimEE is a 4.5M-parameter foundation model for end-to-end time series classification via in-context learning, meta-trained solely on synthetic data, achieving state-of-the-art performance on the UCR benchmark without any real data pre-training.


<details>
  <summary>Details</summary>
Motivation: The two-stage paradigm in TSC (feature encoder + classifier) has limitations: it decouples representation learning from the classification objective, requires per-dataset training, and cannot exploit label information during inference.

Method: TimEE follows the prior-data fitted network (PFN) framework, meta-trained exclusively on synthetic TSC tasks where class identities arise from structured distributional shifts in the generative process. It takes a labeled support set and a query time series, and directly outputs a predicted class distribution in a single forward pass.

Result: TimEE ranks first in ROC AUC and third in accuracy on the UCR benchmark among all compared methods, including foundation models and supervised deep learning baselines, despite seeing no real data during pre-training.

Conclusion: End-to-end ICL with synthetic priors is a compelling, largely unexplored direction for TSC. Future improvements include scaling, prior design, and richer generation mechanisms.

Abstract: Time series classification (TSC) is dominated by a two-stage paradigm: train a feature encoder -- either from scratch on the target dataset or via pretraining on large corpora -- and then fit a task-specific classifier on top. While effective, this decoupling optimizes representation learning independently of the classification objective, requires per-dataset training, and prevents the model from exploiting label information during inference. We introduce TimEE, a 4.5M-parameter foundation model for end-to-end TSC via in-context learning. Given a labeled support set and a query time series, TimEE directly outputs a predicted class distribution in a single forward pass with no per-dataset training required. Following the prior-data fitted network (PFN) framework, TimEE is meta-trained exclusively on synthetic TSC tasks, where each task contains time series with distinct class identities arising from structured distributional shifts in the generative process. Despite seeing no real time series during pre-training, TimEE ranks first in ROC AUC (and third on accuracy) on the UCR benchmark among all compared methods, which include both foundation models and supervised deep learning baselines. To our knowledge, TimEE is the first purely synthetic-pretrained model to reach state-of-the-art performance on the UCR benchmark. These results establish end-to-end ICL with synthetic priors as a compelling, largely unexplored direction for TSC, with scaling, prior design, and richer generation mechanisms as natural avenues for improvement. Code is publicly available at http://github.com/automl/timee.

</details>


### [224] [Single-Rollout Asynchronous Optimization for Agentic Reinforcement Learning](https://arxiv.org/abs/2607.07508)
*Zhenyu Hou,Yujiang Li,Jie Tang,Yuxiao Dong*

Main category: cs.LG

TL;DR: SAO (Single-rollout Asynchronous Optimization) is a novel asynchronous RL framework for LLM post-training that replaces group-wise sampling with single-rollout sampling, introduces double-side token-level clipping, and consistently outperforms GRPO on agentic coding and reasoning benchmarks, successfully deployed in training the GLM-5.2 model.


<details>
  <summary>Details</summary>
Motivation: Existing asynchronous RL systems for LLMs emphasize throughput but neglect training stability and task effectiveness. The widely-used GRPO framework's group-wise sampling does not naturally fit asynchronous agentic training, leading to stability and off-policy challenges that need to be addressed.

Method: (1) Single-rollout sampling: replaces GRPO's group-wise sampling with one rollout per prompt to reduce off-policy effects and improve generalization. (2) Practical value-model training designs to enhance the single-rollout strategy. (3) Strict double-side token-level clipping strategy to improve optimization stability.

Result: SAO trains stably for one thousand steps and consistently outperforms GRPO and its variants on SWE-Bench Verified, BeyondAIME, and IMOAnswerBench benchmarks. It is also effective in simulated online learning environments and successfully deployed in the GLM-5.2 (750B-A40B) model training pipeline.

Conclusion: Single-rollout asynchronous RL (SAO) effectively addresses stability and off-policy challenges in asynchronous RL for LLMs, achieving superior performance over GRPO on agentic tasks and enabling stable long-horizon training for large-scale models.

Abstract: Reinforcement learning (RL) is becoming increasingly important for post-training large language models (LLMs). Previous RL pipelines for LLMs were mostly synchronous and batch-interleaved, which is inefficient for long-horizon agentic tasks. Recently, asynchronous RL has emerged as a more efficient alternative by updating the model as rollouts arrive. However, existing asynchronous RL systems often emphasize throughput, while leaving training stability and task effectiveness largely underexplored. For example, a key challenge is that group-wise sampling in the widely-used GRPO framework does not naturally fit asynchronous agentic training. In this paper, we present Single-rollout Asynchronous Optimization (SAO) to address the stability and off-policy challenges in asynchronous RL. To reduce off-policy effects and improve generalization, we replace group-wise sampling with single-rollout sampling, that is, using one rollout per prompt. We further improve this single-rollout strategy with practical value-model training designs. To improve optimization stability, we introduce a strict double-side token-level clipping strategy. SAO is able to train stably for one thousand steps and consistently outperform GRPO and its variants on agentic coding and reasoning benchmarks, such as SWE-Bench Verified, BeyondAIME, and IMOAnswerBench. We also demonstrate that single-rollout RL is particularly effective in a simulated online learning setting, where the model must adapt to changing evolving environments. To this end, SAO is successfully deployed in the agentic RL pipeline for training the open GLM-5.2 model (750B-A40B).

</details>


### [225] [Fast Rates for Semi-Supervised Learning via Data-Augmentation Graph Regularization](https://arxiv.org/abs/2607.07513)
*Adam M. Oberman*

Main category: cs.LG

TL;DR: This paper provides a theoretical explanation for why self-supervised learning (SSL) is label-efficient, proving that data augmentation creates a similarity graph enabling graph-Laplacian-regularized learning with a fast O(1/n_L) transductive rate—much better than supervised O(1/√n_L)—while also introducing a streamlined loss that recovers ideal top-K features.


<details>
  <summary>Details</summary>
Motivation: Self-supervised learning empirically matches supervised accuracy with far fewer labels, but the theoretical basis for this label efficiency was lacking. The paper aims to explain why SSL needs so few labeled examples.

Method: The authors frame data augmentation as inducing a similarity graph on unlabeled data, connecting SSL to graph-Laplacian-regularized learning. They adapt the leave-one-out stability analysis from Johnson and Zhang (2007) to this augmentation graph, proving a fast transductive rate. They also introduce a streamlined loss without projector, negative samples, or orthogonality constraints that still recovers the top-K eigenspace of the augmentation kernel.

Result: The expected error is bounded by C/n_L + R_DA(y), where R_DA(y) is the data-augmentation alignment error (graph-cut mass of augmentations crossing label boundaries). This yields an O(1/n_L) rate versus supervised O(1/√n_L), and explains the accuracy-versus-label-count curve rather than just bounding a generalization gap.

Conclusion: The paper provides the first theoretical explanation of SSL's label efficiency, showing that good augmentations (low cross-boundary cut mass) drastically reduce the required labels. The analysis is more realistic than prior limit-based approaches and matches empirical observations.

Abstract: Self-supervised learning matches supervised accuracy from a fraction of the labels, but the labeled-sample efficiency behind this has lacked a theoretical explanation. We provide one. Data augmentation induces a similarity graph on the unlabeled data, so downstream learning on that graph is graph-Laplacian-regularized learning. We prove a fast transductive rate, $O(1/n_L)$ in the number of labels, in place of the supervised $O(1/\sqrt{n_L})$, by carrying the leave-one-out stability apparatus of Johnson and Zhang (JMLR 2007) over to the augmentation graph, and without the unrealistic assumptions of limit-based analyses (exact kernel, generalizing features). The bound makes augmentation quality explicit: the expected error is at most $C/n_L + R_{\mathrm{DA}}(y)$, where the data-augmentation alignment error $R_{\mathrm{DA}}(y)$ is the graph-cut mass of augmentations that cross a label boundary, so good augmentations let few labels suffice. The analysis uses a streamlined loss that drops the projector, negative-sample, and orthogonality overhead of standard objectives yet still recovers the top-$K$ ideal features in the infinite-data limit, the augmentation-kernel eigenspace studied by Zhai et al. The result explains the observed accuracy-versus-label-count curve rather than only bounding a generalization gap.

</details>


### [226] [Gradient-free Riemannian Langevin Sampler](https://arxiv.org/abs/2607.07519)
*Ricardo Baptista,Olivier Zahm*

Main category: cs.LG

TL;DR: GRiLS is a gradient-free MCMC sampler that uses a Riemannian metric and ensemble-estimated mean/covariance to efficiently sample multimodal distributions, outperforming existing methods on benchmarks.


<details>
  <summary>Details</summary>
Motivation: Standard MCMC methods suffer from poor mixing and mode trapping when sampling multimodal probability distributions, especially when gradients are unavailable or computationally expensive.

Method: GRiLS introduces a Riemannian metric that reshapes local geometry to facilitate mode transitions, requiring only the mean and covariance of the target density (estimated via an ensemble of interacting particles), without needing gradient evaluations.

Result: Empirical results on multimodal benchmarks show GRiLS achieves improved mixing compared to both gradient-based and gradient-free MCMC approaches.

Conclusion: GRiLS is an effective gradient-free alternative for complex, computationally expensive multimodal sampling problems where gradients are impractical.

Abstract: We address the problem of efficiently sampling multimodal probability distributions, where standard Markov Chain Monte Carlo methods often suffer from poor mixing and mode trapping. To mitigate these issues, we propose Gradient-free Riemannian Langevin Sampler (GRiLS), a novel proposal that improves exploration without requiring gradient evaluations of the target density. Our approach introduces a Riemannian metric which reshapes the local geometry in order to facilitate transitions across modes. The resulting gradient-free MCMC algorithm is particularly suitable for complex, computationally expensive targets where derivatives are unavailable or impractical. The GRiLS proposal requires knowing the mean and covariance of the target density, which we estimate using an ensemble of interacting particles. Empirical results on multimodal benchmarks demonstrate that GRiLS achieves improved mixing compared to existing gradient-based and gradient-free MCMC approaches.

</details>


### [227] [Avoiding unsafe sets when training with Langevin Dynamics](https://arxiv.org/abs/2607.07538)
*Adam M. Oberman*

Main category: cs.LG

TL;DR: The paper studies noise-induced failure probabilities in noisy gradient descent training, modeled as overdamped Langevin dynamics. For strongly convex losses, it derives bounds on trajectory probabilities entering failure regions, showing equilibrium mass is exponentially small in dimension d, while transient probabilities can bulge dramatically if the failure region is not geometrically isolated. A local relaxation rate is introduced to control such transient swelling.


<details>
  <summary>Details</summary>
Motivation: To understand safety risks in training neural networks with noisy gradient descent, specifically bounding the probability that training trajectories enter undesirable failure regions.

Method: Models training as overdamped Langevin dynamics on a smooth, strongly convex loss landscape. Uses spectral gap analysis, chi-squared divergence, and introduces a local relaxation rate via spectral measure of centered indicator functions to bound trajectory probabilities.

Result: Three bounds: (1) equilibrium mass is exponentially small in d with energy-barrier rate; (2) shape-free bound shows relaxation to static value after burn-in time O(d); (3) for geometrically isolated regions, local relaxation rate exceeds global rate, proportionally reducing burn-in time and capping trajectory probability uniformly.

Conclusion: Strong convexity determines training relaxation speed, but the geometric shape of the unsafe set dictates whether transient probability bulging occurs. Local spectral analysis can mitigate such risks.

Abstract: Training a model with noisy gradient descent can be idealized as overdamped Langevin dynamics on the loss landscape, and a natural safety question is to bound the probability $ν_t(\mathcal{A}_H) = \mathbb{P}(Q_t \in \mathcal{A}_H)$ that the trajectory lies in a designated failure region $\mathcal{A}_H$. We study this for a smooth, strongly convex loss in $d$ dimensions and a failure region separated from the minimizer by an energy gap. Three bounds emerge. At the end of training, the equilibrium mass $π(\mathcal{A}_H)$ is exponentially small in $d$, with a complementary energy-barrier rate when the noise is small. Along the trajectory, a shape-free bound $ν_t(\mathcal{A}_H) \le π(\mathcal{A}_H)(1 + \sqrt{χ_0^2/π(\mathcal{A}_H)}\,e^{-mt})$ shows that the in-set probability relaxes to (twice) the static value after a burn-in time of order $d$, using only the global spectral gap $m$ of the loss. A worked Ornstein-Uhlenbeck example shows this burn-in is necessary: an angular slice of the equilibrium shell can transiently swell by a factor exponential in $d$, even though its equilibrium mass is tiny. To rule such swelling out we introduce a local relaxation rate attached to the failure region, defined through the spectral measure of its centered indicator rather than a Dirichlet-form Rayleigh quotient. For geometrically isolated regions this rate exceeds the global one, shrinking the burn-in proportionally, and combined with a maximum-principle ceiling it caps the trajectory probability uniformly in time. The picture is that strong convexity sets how fast training relaxes, but the shape of the unsafe set decides whether the trajectory bulges through it on the way home.

</details>


### [228] [Collaborative Synthetic Data Generation for Knowledge Transfer in Federated Learning](https://arxiv.org/abs/2607.07565)
*Maximilian Andreas Hoefler,Karsten Mueller,Wojciech Samek*

Main category: cs.LG

TL;DR: FedKT-CSD is a one-shot federated learning framework that uses pretrained autoencoders to compress client data into latent statistics, aggregates them with differential privacy, and decodes synthetic datasets for global model training, achieving strong privacy guarantees while matching or exceeding non-private baselines.


<details>
  <summary>Details</summary>
Motivation: Existing OSFL methods (e.g., synthetic dataset/distillate-based aggregation) lack formal privacy guarantees. The authors aim to jointly achieve low communication overhead, robustness to data heterogeneity, and rigorous differential privacy in a single round of federated learning.

Method: Clients use publicly pretrained autoencoders to encode private data in one forward pass, compute class-conditional latent statistics, and send them to the server. The server aggregates via secure aggregation, adds calibrated DP noise, then decodes a synthetic dataset to train a global model.

Result: FedKT-CSD provides formal (ε,δ)-differential privacy, keeps client computation/communication lightweight, and is competitive with or outperforms non-private baselines across diverse datasets and heterogeneity settings, while scaling to many clients.

Conclusion: The framework successfully closes the gap between communication-efficient one-shot FL, robustness to heterogeneous data, and rigorous privacy guarantees, enabled by neural image compression-inspired design with pretrained autoencoders.

Abstract: One-shot federated learning (OSFL) addresses the communication overhead of federated learning by limiting training to a single round, but doing so without sacrificing model quality is non-trivial, particularly when client data distributions diverge. Recent work has addressed this challenge by aggregating client knowledge on the server through the construction of transferable synthetic datasets or distillates. However, most of these methods lack formal privacy guarantees, leaving a gap in jointly achieving low communication, robustness to heterogeneity, and rigorous privacy. We propose FedKT-CSD (Federated Knowledge Transfer via Collaborative Synthetic Data), a framework inspired by neural image compression that closes this gap by leveraging publicly pretrained autoencoders as a shared latent space. Each client encodes its private data in a single forward pass, computes class-conditional latent statistics, and transmits these to the server. The server aggregates these statistics via secure aggregation, adds calibrated differential privacy noise, and decodes a synthetic dataset for training a global model and further downstream tasks. This design provides formal $(\varepsilon,δ)$-differential privacy by construction, while keeping client-side computation and communication lightweight. Despite operating under privacy constraints, FedKT-CSD is competitive with and even outperforms non-private baselines across diverse datasets and heterogeneity settings, and scales to a large number of clients. Our code is available at: https://github.com/an7123/FedKT-CSD

</details>


### [229] [Multi-Class vs. Multi-Label BERT for CVE-to-CWE Mapping: How Taxonomy Structure Shapes the Errors](https://arxiv.org/abs/2607.07573)
*Ana Schwengber Kelm,Christian Bockermann,Jörg Frochte*

Main category: cs.LG

TL;DR: This paper studies CWE-to-CVE assignment as a text classification problem, comparing multi-class vs. multi-label formulations using three transformer encoders (BERT, SecureBERT, CySecBERT) across three label spaces. Multi-class generally outperforms multi-label, but post-hoc threshold optimization closes the gap on smaller label spaces. Error patterns are dominated by CWE hierarchy structure, not encoder choice. CySecBERT performs best overall.


<details>
  <summary>Details</summary>
Motivation: Assigning CWE categories to CVE records is important but remains largely manual. The authors aim to automate this step and understand which modeling formulation (multi-class vs. multi-label) and which transformer encoder works best for this vulnerability analysis task.

Method: The authors formulate CWE assignment as a text classification problem, comparing multi-class (single CWE per CVE) and multi-label (multiple CWEs allowed) approaches. They evaluate three transformer encoders (BERT Base, SecureBERT, CySecBERT) on three nested label spaces (83, 47, 25 classes). Post-hoc threshold optimization is applied to multi-label models, and confusion analysis examines error patterns against the CWE hierarchy.

Result: Multi-class training achieves higher macro-F1 across all settings, but the gap to multi-label narrows from 21 to 2 percentage points on smaller label spaces. Post-hoc threshold optimization closes the gap on the 25-class setting. Error patterns follow the CWE hierarchy (Pearson r > 0.92 across encoders). Hierarchy-relaxed evaluation raises macro-F1 from ~81% to ~90%. CySecBERT achieves the strongest overall results, especially in multi-label settings.

Conclusion: The study demonstrates that error structure in CWE classification is driven more by taxonomy design than encoder choice. Strict metrics understate branch-level classifier quality, and CySecBERT is the recommended encoder for this task, particularly in multi-label settings.

Abstract: Assigning Common Weakness Enumeration (CWE) categories to Common Vulnerabilities and Exposures (CVE) records remains an important but largely manual step in vulnerability analysis. We study this task as a text classification problem and compare two modelling choices: a \emph{multi-class} formulation that predicts a single CWE per CVE and a \emph{multi-label} formulation that allows multiple assignments. Three transformer encoders (BERT Base, SecureBERT, and CySecBERT) are evaluated on three nested label spaces (83, 47, and 25 classes). Multi-class training achieves higher macro-F1 across all settings, although the gap to multi-label narrows from 21 to 2 percentage points as the label space shrinks. Post-hoc threshold optimisation on the multi-label side closes this gap on the 25-class setting. Confusion analysis shows that the dominant misclassification patterns follow the CWE hierarchy and are shared across all three encoders (Pearson $r > 0.92$), which suggests that the error structure is driven more by taxonomy design than by encoder choice. A hierarchy-relaxed evaluation that forgives within-family confusions raises macro-F1 from ${\sim}$81\% to ${\sim}$90\%, indicating that strict metrics understate branch-level classifier quality. CySecBERT achieves the strongest results overall, with statistically significant gains concentrated in the multi-label setting.

</details>


### [230] [Asymmetric Focal Loss Improves Graph Neural Network Prediction of Drug-Drug Interactions](https://arxiv.org/abs/2607.07611)
*Faranak Hatami,Mousa Moradi*

Main category: cs.LG

TL;DR: Asymmetric focal loss improves drug-drug interaction prediction by emphasizing difficult positive examples, increasing accuracy from 0.699 to 0.892 with a 64.1% relative reduction in classification error.


<details>
  <summary>Details</summary>
Motivation: Standard binary cross-entropy training allocates equal capacity to well-classified and difficult examples in polypharmacy side effect prediction, potentially missing clinically significant interactions.

Method: ClinicalFocal loss was integrated into a relation-aware graph convolutional network using molecular fingerprints, physicochemical descriptors, and learned embeddings, evaluated on TWOSIDES with five-fold cross-validation under identical conditions.

Result: ClinicalFocal loss increased accuracy from 0.699 to 0.892 (+19.3 pp), F1 from 0.700 to 0.894 (+19.4 pp), AUROC from 0.766 to 0.914, AUCPR from 0.714 to 0.860; false-negative rate dropped from 29.8% to 9.1%; overall error reduced by 64.1%.

Conclusion: Asymmetric focal optimization improves DDI prediction without architectural changes; loss-function design is a direct, tunable lever for improving graph-based DDI prediction.

Abstract: Background: Graph neural networks improve computational prediction of polypharmacy side effects, but standard binary cross-entropy training allocates equal capacity to well-classified and difficult examples, potentially missing clinically significant interactions. We evaluated whether an asymmetric focal objective could improve multi-relational drug-drug interaction (DDI) prediction by emphasizing difficult positive interactions. Methods: ClinicalFocal loss was integrated into a relation-aware graph convolutional network using molecular fingerprints, physicochemical descriptors, and learned embeddings. The model was evaluated on TWOSIDES using five-fold cross-validation with identical experimental conditions (architecture, features, data partitions, hyperparameters, and random seeds) for ClinicalFocal loss and binary cross-entropy baseline. Results: ClinicalFocal loss increased accuracy from 0.699 to 0.892 (+19.3 percentage points) and F1 score from 0.700 to 0.894 (+19.4 percentage points). AUROC increased from 0.766 to 0.914, and AUCPR increased from 0.714 to 0.860. The false-negative rate decreased from 29.8% to 9.1%, while specificity increased from 69.6% to 87.5%. Overall classification error decreased from 30.1% to 10.8%, corresponding to a 64.1% relative reduction. Improvements were consistent across all five folds. Conclusions: Asymmetric focal optimization improved classification and ranking performance while achieving 90.9% recall for observed interaction triples, without modifying the underlying architecture. Loss-function design is a direct, tunable lever for improving graph-based DDI prediction.

</details>


### [231] [Selective Timestep Weighting and Advantage-Based Replay for Sample-Efficient Diffusion RLHF](https://arxiv.org/abs/2607.07693)
*Eric Zhu,Abhinav Shrivastava,Soumik Mukhopadhyay*

Main category: cs.LG

TL;DR: This paper proposes two strategies—per-timestep weighting and prioritized trajectory replay—to improve feedback efficiency in diffusion RLHF, achieving up to 6× sample efficiency gains over baselines.


<details>
  <summary>Details</summary>
Motivation: Existing RLHF methods for diffusion models are highly feedback-inefficient, requiring large amounts of human or reward model evaluations, which limits their practicality in real-world settings where feedback is the primary bottleneck.

Method: (1) A per-timestep weighting scheme that reweights denoising steps during PPO optimization, theoretically grounded in optimal convergence properties. (2) A prioritized trajectory replay mechanism that reuses informative past samples instead of repeatedly querying new rewards.

Result: Under identical hyperparameter settings, the approach achieves up to a 6× improvement in sample efficiency compared to widely used diffusion RLHF baselines.

Conclusion: Reward information in diffusion trajectories is unevenly distributed; emphasizing informative timesteps and trajectories yields more effective gradient updates and substantially improves feedback efficiency while preserving generalization to unseen prompts.

Abstract: Reinforcement learning from human feedback (RLHF) has emerged as a powerful paradigm for aligning generative models with human preferences. However, applying RLHF to diffusion models remains highly feedback inefficient, as existing approaches typically require large amounts of human or reward model evaluations. This limitation reduces the practicality of diffusion RLHF in realworld settings where feedback is the primary bottleneck. In this paper, we propose two complementary strategies that substantially improve the feedback efficiency of diffusion RLHF while preserving generalization to unseen prompts. Our key observation is that reward information in diffusion trajectories is unevenly distributed: not all denoising timesteps or trajectories contribute equally to learning from a reward signal. By emphasizing informative timesteps and trajectories during optimization, we obtain more effective gradient updates. First, we introduce a per-timestep weighting scheme that reweights denoising steps during policy optimization. We theoretically connect this weighting to the optimal convergence properties of proximal policy optimization (PPO) and approximate the resulting weighting trend empirically. Second, we introduce a replay mechanism that prioritizes informative trajectories, enabling the model to reuse past samples instead of repeatedly querying new rewards. Together, these strategies significantly improve the feedback efficiency of diffusion RLHF. Under identical hyperparameter settings, our approach achieves up to a 6$\times$ improvement in sample efficiency compared to widely used diffusion RLHF baselines.

</details>


### [232] [Higher-Order Geometric Updates for Levenberg-Marquardt Method via Riemann Normal Coordinates](https://arxiv.org/abs/2607.07623)
*Jianing Liu,Dong H. Zhang*

Main category: cs.LG

TL;DR: The paper proposes Riemann-normal-coordinate Levenberg-Marquardt (RNC-LM), a method that improves standard LM optimization by using Riemann normal coordinates to correct parameter-effects curvature, achieving better convergence and robustness on nonlinear least-squares problems including PINNs and large-scale ML tasks.


<details>
  <summary>Details</summary>
Motivation: Standard Levenberg-Marquardt suffers from parameter-effects curvature nonlinearity because its tangent-space step is applied as a straight update in parameter coordinates, and existing geodesic acceleration only removes this curvature exactly in the infinitesimal-step limit.

Method: RNC-LM reformulates the geodesic equation to extend geodesic acceleration to arbitrary-order corrections, constructing finite-step updates with progressively higher reparameterization consistency via Riemann normal coordinates. A line search along the RNC curve controls step distance while keeping computational cost close to standard LM.

Result: On classical benchmarks, RNC-LM improves convergence in curved valleys and rank-deficient problems. On a reaction-diffusion PINN benchmark, it reduces relative L2 error to ~1e-3. On large-scale ML potential-energy-surface fitting, it achieves 34× speedup over standard LM.

Conclusion: RNC-LM effectively eliminates tangential residual acceleration order by order, making objective reduction more consistent with LM's linear model prediction, leading to superior convergence and robustness across diverse nonlinear least-squares problems.

Abstract: Nonlinear least-squares optimization is central to regression, physics-informed neural networks, and other machine-learning tasks. Such problems have a natural geometric interpretation, model predictions form a manifold in data space, while the chosen parameterization can introduce parameter-effects curvature that becomes a dominant source of nonlinearity. This exposes a limitation of the Levenberg-Marquardt (LM) method, its tangent-space step is applied as a straight update in parameter coordinates. Geodesic acceleration gives a second-order correction, but its removal of parameter-effect curvature is exact only in the infinitesimal-step limit. We propose a Riemann-normal-coordinate Levenberg-Marquardt method (RNC-LM) to improve this consistency for finite optimization steps. By reformulating the geodesic equation, RNC-LM extends geodesic acceleration to arbitrary-order corrections and constructs finite-step updates with progressively higher reparameterization consistency. A line search along the resulting RNC curve controls the traveled distance while keeping the cost close to standard LM. The method eliminates the tangential component of residual acceleration order by order in a moving tangent frame, making the actual objective reduction more consistent with the linear model prediction of LM. On classical nonlinear least-squares benchmarks, RNC-LM improves convergence and robustness in curved valleys and rank-deficient problems. On a reaction-diffusion PINN failure-mode benchmark, it reduces the relative L2 error to the order of 1e-3 and recovers a physically meaningful solution. On a large-scale machine-learning potential-energy-surface fitting task, it achieves a 34-fold speedup over standard LM.

</details>


### [233] [An optimal control approach for neural network architecture adaptation with a posteriori error estimation](https://arxiv.org/abs/2607.07637)
*C G Krishnanunni,Thomas Scott,Tan Bui-Thanh*

Main category: cs.LG

TL;DR: This paper proposes a depth-adaptive neural network method based on a posteriori error estimation, treating training as a continuous optimal control problem. It uses dual weighted residual (DWR) techniques to insert layers where error is highest, and outperforms existing methods on scientific benchmarks like Navier-Stokes.


<details>
  <summary>Details</summary>
Motivation: To develop a principled, mathematically rigorous way to adapt neural network depth automatically, moving beyond heuristic layer selection and addressing how approximation error distributes across layers.

Method: Formulates neural network training as a continuous-time optimal control problem; uses dual weighted residual (DWR) methodology from finite element analysis to derive computable upper bounds on functional error; decomposes total approximation error into interval-wise contributions; inserts new layers at locations of maximum estimated error; treats weights and biases as piecewise linear functions across layers.

Result: The method consistently outperforms existing architecture adaptation methods in generalization performance, demonstrated on scientific datasets including the observable-to-parameter map for the Navier-Stokes equation.

Conclusion: The proposed error-driven depth adaptation framework provides a rigorous and effective basis for automated neural network architecture design, with strong empirical results on complex scientific problems.

Abstract: This work presents a novel approach for adapting neural network architecture along the depth based on a posteriori error estimation. By formulating neural network training as a continuous-time optimal control problem, we derive rigorous error estimates that quantify how approximation error distributes across network layers. This error decomposition enables a principled depth adaptation strategy: new layers are inserted at locations of maximum estimated error, allowing the network to efficiently capture complex, nonlinear variations in the underlying problem. Our framework introduces a novel network architecture that treats weights and biases as piecewise linear functions varying across layers, with the error estimator bounding the discrepancy between this discrete representation and the true continuous optimal control solution. The approach leverages dual weighted residual methodology from finite element analysis to derive computable upper bounds on the functional error. A key theoretical contribution is the derivation of explicit error bounds that decompose the total approximation error into interval-wise contributions, providing a rigorous basis for targeted architecture refinement. We demonstrate the effectiveness of our method on scientific datasets, including learning the observable-to-parameter map for the Navier-Stokes equation. Numerical results reveal that our approach consistently outperforms existing architecture adaptation methods in terms of generalization performance.

</details>


### [234] [ALER-TI: Aligned Latent Embedding Retrieval for Time Series Imputation](https://arxiv.org/abs/2607.07640)
*Xuan-Thong Truong,Trung-Kien Le,Tung Kieu,Thi-Thu Nguyen,Nhat-Hai Nguyen*

Main category: cs.LG

TL;DR: ALER-TI is a retrieval-augmented time series imputation framework that uses historical pattern retrieval and latent embedding alignment to improve missing value reconstruction, outperforming strong baselines across six real-world datasets.


<details>
  <summary>Details</summary>
Motivation: Existing deep learning time series imputation methods rely heavily on localized temporal context within corrupted sequences, which fails in scenarios with non-stationary dynamics, weak temporal correlations, or infrequent patterns that cannot be reliably reconstructed from nearby observations.

Method: ALER-TI proposes a retrieval-augmented framework with two key components: (1) retrieving historical complete patterns to supplement degraded local context, and (2) Latent Embedding Alignment (LEA) that applies post-hoc masking in latent space to align representation between corrupted queries and complete historical candidates, allowing pre-computed caching for efficiency.

Result: Extensive experiments on six real-world datasets under various missing rates show ALER-TI consistently improves strong baseline models and enhances robustness across diverse imputation settings.

Conclusion: ALER-TI demonstrates that retrieval-augmented approaches with representation alignment can effectively address limitations of local-context-dependent imputation, offering a model-agnostic plug-in that enhances any imputation backbone.

Abstract: Deep learning has significantly advanced time series imputation, yet most existing architectures primarily rely on localized temporal context within the corrupted input sequence. This reliance can be limiting in real-world scenarios, where time series often exhibit non-stationary dynamics, weak temporal correlations, and infrequent patterns that are difficult to reconstruct from nearby observations alone. In this paper, we propose ALER-TI, Aligned Latent Embedding Retrieval for Time Series Imputation, a retrieval-augmented framework that explicitly leverages historical patterns to supplement degraded local context for more reliable missing-value reconstruction. The core of ALER-TI is Latent Embedding Alignment (LEA), which mitigates the representation mismatch between corrupted queries and complete historical candidates. By applying post-hoc masking in the latent space, LEA aligns candidates with the query's missingness pattern while allowing historical embeddings to be pre-computed and cached for efficient retrieval. ALER-TI is model-agnostic and can be integrated with various imputation backbones through a lightweight adaptation module. Extensive experiments on six real-world datasets under different missing rates demonstrate that ALER-TI consistently improves strong baseline models and enhances robustness across diverse imputation settings.

</details>


### [235] [Guidance Breaks the Fitted Operator: A Terminal-Fitted Repair for Classifier-Free Guidance](https://arxiv.org/abs/2607.07665)
*Shiheng Zhang*

Main category: cs.LG

TL;DR: The paper identifies that Classifier-Free Guidance (CFG) in diffusion models causes instability at high guidance due to a solver artifact (not the continuous ODE), and proposes a simple one-coefficient fix: replace CFG's w(r-1) with r^(1+w)-r, which stabilizes high-guidance sampling at no extra cost, validated on CIFAR-10 and Stable Diffusion.


<details>
  <summary>Details</summary>
Motivation: CFG at high guidance values causes oversaturation and instability (requiring more steps or schedule tricks), and the root cause is not well understood from a numerical analysis perspective.

Method: The authors analyze CFG through asymptotic-preserving numerical analysis. They show that guidance re-stiffens the discriminative subspace to exponent 1+w, causing DDIM to no longer be a fitted operator. They prove a guided clock barrier with three step-size thresholds, and propose a repair: replace w(r-1) with r^(1+w)-r on the guidance direction.

Result: The proposed fix removes CFG's sigma_min-divergent blow-up, achieves first-order accuracy against the exact guided flow. On CIFAR-10 and Stable Diffusion 1.5 DDIM, it gives 9/9 point-FID wins over CFG, cuts residual amplification and saturation, and preserves classifier-proxy accuracy.

Conclusion: The fix is a high-guidance stabilizer at no extra cost, but not a universal quality improvement; it is not uniformly better than dense vanilla-CFG as an integrator of that field.

Abstract: Classifier-free guidance (CFG) is the standard way to strengthen class-conditioning in diffusion and flow-matching samplers, yet at large guidance it oversaturates and destabilizes, symptoms practitioners suppress with more steps or limited-interval schedules. We analyze CFG through an asymptotic-preserving, numerical-analysis lens. Building on a recent result that the deterministic DDIM step is the unique fitted operator for the unguided terminal layer, exact on the final small-sigma stretch of sampling, we show that guidance re-stiffens exactly the discriminative subspace to an anomalous exponent 1+w. DDIM is therefore no longer fitted there, and on coarse meshes its guided residual diverges as sigma_min goes to zero. We prove a guided clock barrier with three ordered step-size thresholds, and read one-step oversaturation as its endpoint: a solver artifact on the calibration model rather than the continuous guided law. The same analysis yields a one-coefficient, zero-extra-NFE repair: replace CFG's w(r-1) by r^(1+w)-r on the guidance direction. On the calibration model's discriminative crossover, this removes CFG's sigma_min-divergent blow-up and is first-order accurate against the exact guided flow as sigma_min goes to zero. On learned CIFAR-10 checkpoints, and as a cross-domain smoke test on Stable Diffusion 1.5 DDIM, it acts as a high-guidance stabilizer at no extra cost rather than a universal quality knob: it cuts residual amplification and saturation, gives 9/9 point-FID wins over CFG on the tested grid, and preserves classifier-proxy target accuracy in the hard-cell blocks. We report the limits alongside: it is not a universal image-quality win, and against a dense vanilla-CFG reference it is not a uniformly better integrator of that field.

</details>


### [236] [PeTeR: Post-Training Robustification of Probabilistic Circuits](https://arxiv.org/abs/2607.07671)
*Adrian Ciotinga,Yeming Dai,YooJung Choi*

Main category: cs.LG

TL;DR: PeTeR is a data-free post-training framework that robustifies pre-trained probabilistic circuits against distribution shifts without retraining, achieving competitive performance against data-dependent robust learning methods.


<details>
  <summary>Details</summary>
Motivation: Standard likelihood-based PC learning suffers from overfitting and fragile generalization under data noise, small samples, or distribution shifts. Existing distributionally-robust optimization methods require retraining from scratch, which is computationally expensive.

Method: PeTeR is a novel, data-free post-training framework that robustifies pre-trained PCs against distribution shifts without retraining from scratch, using distributionally-robust optimization principles within a Wasserstein ball.

Result: PeTeR effectively robustifies baseline models against both random and adversarial perturbations, achieving competitive or superior performance compared to data-dependent robust learning baselines across multiple density estimation benchmarks.

Conclusion: PeTeR provides an efficient alternative to training robust PCs from scratch, offering data-free post-training robustification that generalizes well under distribution shifts.

Abstract: Probabilistic circuits (PCs) can model complex joint distributions while supporting exact and efficient computation of many inference queries. However, standard likelihood-based PC learning is vulnerable to overfitting and fragile generalization when confronted with data noise, small sample sizes, or distribution shifts. This can be mitigated using distributionally-robust optimization which consider worst-case distributions within a Wasserstein ball of the empirical distribution, but current methods are limited to training a model from scratch in this framework. Instead, we propose PeTeR: a novel, data-free post-training framework designed to robustify pre-trained PCs against distribution shifts without retraining from scratch. Empirical evaluations across multiple density estimation benchmarks demonstrate that PeTeR effectively robustifies baseline models against both random and adversarial perturbations, achieving competitive or superior performance to data-dependent robust learning baselines.

</details>


### [237] [Max Out GRPO Signal: Adaptive Trace Prefix Control for Hard Reasoning Problems](https://arxiv.org/abs/2607.07674)
*Vladislav Beliaev*

Main category: cs.LG

TL;DR: GRPO stalls on hard problems where no rollout succeeds. AdaPrefix-GRPO uses a feedback controller to dynamically adjust prefix length from reference solutions, maintaining ~50% success rate for maximal gradient signal, then removes assistance. This more than doubles accuracy on hard math (2.1x on 0.6B models, 1.6x on Qwen3-1.7B, 1.7x on AIME) while halving trace length, with minimal implementation overhead.


<details>
  <summary>Details</summary>
Motivation: GRPO's group-relative advantages vanish when no rollout in a group succeeds, providing no gradient signal on the hardest frontier problems that are most valuable for learning.

Method: AdaPrefix-GRPO prepends correct prefixes of reference solutions as a difficulty knob, then uses a feedback controller to dynamically adjust prefix length per problem throughout training, targeting ~50% success rate for maximal gradient signal, and eventually withdraws assistance entirely.

Result: At matched training FLOPs: 2.1x accuracy on held-out problems for 0.6B models, 1.6x on Qwen3-1.7B, 1.7x on AIME, with roughly half the trace length. Smaller models show larger gains.

Conclusion: AdaPrefix-GRPO effectively addresses the gradient starvation problem in GRPO on hard problems with a simple, practical method (data preparation + loss mask) that improves both accuracy and efficiency.

Abstract: Group Relative Policy Optimization (GRPO) stalls on a model's hardest problems: when no rollout in a group succeeds, the group-relative advantages vanish and the problem contributes no gradient, wasting the frontier examples we most want to learn from. Prepending a correct prefix of a reference solution raises the success rate, making prefix length a continuous knob on difficulty. Concurrent methods set the knob once; AdaPrefix-GRPO turns it into a feedback controller: throughout training it adjusts how much of the solution each problem gets, holding its success rate near 50%, where GRPO's gradient signal is largest, then withdraws the assistance entirely, so the deployed model solves problems unaided. On hard math, at matched training FLOPs, it more than doubles GRPO's accuracy on held-out problems from the training distribution for a 0.6B model (2.1x), with 1.6x on Qwen3-1.7B and 1.7x on AIME, while roughly halving trace length. The method is implemented in data preparation plus a loss mask on prefix tokens; the trainer is otherwise stock. The smaller the model, the larger the gain.

</details>


### [238] [How Data Shapes RoPE Frequency Usage: From Positional Scale Matching to Length Generalization](https://arxiv.org/abs/2607.07678)
*Xinyi Wu,Siyuan Liu,Ali Jadbabaie*

Main category: cs.LG

TL;DR: This paper explains why transformers use RoPE frequencies non-uniformly, proposing a data-centered "frequency-matching principle": optimal frequencies scale with the width of the data's dependency profile. It connects this to length generalization, showing that frequency scaling works when longer-context dependencies are approximate dilations of training ones.


<details>
  <summary>Details</summary>
Motivation: Understanding why trained models use RoPE frequencies highly non-uniformly despite being provided a fixed grid, and what determines this emergent frequency selection.

Method: Formalizes a field-resolution tradeoff, proposes a frequency-matching principle (optimal frequency ∝ 1/W where W is dependency width), validates on synthetic and text data, and connects to position-interpolation-based length generalization.

Result: RoPE frequencies are selected to match the relative-distance structure of training data; mid-low frequency bands in LMs arise from multi-scale dependency structure of natural language; natural language exhibits approximate self-similarity across positional scales.

Conclusion: Long-context generalization depends on two forms of scale matching: between learned frequencies and training-time dependencies, and between frequency scaling and how dependencies extend to longer contexts.

Abstract: Rotary Position Embeddings (RoPE) provide transformers with a fixed grid of positional frequencies, yet trained models use these frequencies highly non-uniformly. We study what determines this frequency usage and propose a data-centered explanation: RoPE frequencies are selected to match the relative-distance structure of the training data. Viewing each frequency as a positional lens, we formalize a field-resolution tradeoff and show that, for a data-induced dependency profile of width $W$, the optimal frequency scales as $1/W$. This frequency-matching principle explains controlled observations on synthetic and text-based data, and suggests that the mid-low frequency bands observed in language models arise from the multi-scale dependency structure of natural language. We further connect frequency selection to position-interpolation-based length generalization: scaling frequencies down expands the effective field while reducing resolution. This helps when longer-context dependencies are approximate dilations of those seen during training, but can fail when relevant dependencies do not scale with context length. Empirically, we show that natural language exhibits approximate self-similarity across positional scales, explaining why test-time frequency scaling can support long-context generalization. Overall, our results identify a data-driven mechanism behind emergent RoPE frequency usage and show that long-context generalization depends on two forms of scale matching: between learned frequencies and training-time dependencies, and between frequency scaling and how those dependencies extend to longer contexts.

</details>


### [239] [Neural Operator-enabled Topology-informed Evolutionary Strategy for PDE-Constrained Optimization](https://arxiv.org/abs/2607.07682)
*Xiangming Huang,Guannan Zhang,Lu Lu,Raphaël Pestourie*

Main category: cs.LG

TL;DR: NOTES integrates DeepONet neural operators with CMA-ES for efficient, high-dimensional inverse design, reducing nanophotonic design dimensionality from 256 to 25 and achieving >95% efficiency, plus structural optimization compliance of 246.


<details>
  <summary>Details</summary>
Motivation: Inverse design of physical systems is computationally demanding due to high dimensionality and non-convexity. Generative models lack robustness, while evolutionary strategies struggle in high-dimensional spaces.

Method: Couples a DeepONet-based neural operator with CMA-ES for global optimization in a compact, topology-aware latent space, decoupling topology learning from governing physics.

Result: Reduces nanophotonic design dimensionality from 256 to 25; achieves >95% efficiency, outperforming baselines; structural optimization compliance reaches 246.

Conclusion: NOTES provides a flexible, transferable, and robust framework for inverse design across different physical systems governed by PDEs.

Abstract: The inverse design of physical systems governed by partial differential equations is computationally demanding due to the high dimensionality and non-convexity of design spaces. Generative models for inverse design often lack robustness and transferability, whereas evolutionary strategies are robust but struggle in high-dimensional spaces. This paper introduces a Neural Operator-enabled Topology-informed Evolutionary Strategy (NOTES) that integrates dimensionality reduction, representation learning, and evolutionary optimization for efficient and transferable inverse design. NOTES couples a DeepONet-based neural operator with the Covariance Matrix Adaptation Evolution Strategy (CMA-ES) to perform global optimization in a compact latent space that encodes topology-aware priors while discovering high-performance designs for unseen operating conditions. Applied to nanophotonic beam-deflector inverse design governed by Maxwell's equations, NOTES reduces the design dimensionality from 256 to 25 and consistently achieves over 95 percent efficiency, outperforming CMA-ES, topology optimization, and other baselines. Applied to structural optimization, NOTES discovers designs that achieve compliance down to 246. By decoupling topology learning of a DeepONet from the governing physics in a PDE solver, NOTES provides a flexible and transferable framework for the inverse design of physical systems.

</details>


### [240] [ECGLight: Compute-Light Framework For Paper ECG Digitization and Myocardial Infarction Screening](https://arxiv.org/abs/2607.07683)
*Shreyasvi Natraj,Cyrus Achtari,Felice Gragnano,Andrea Milzi,Marco Valgimigli,Diego Paez-Granados*

Main category: cs.LG

TL;DR: This paper presents a lightweight, CPU-only pipeline that digitizes paper ECG printouts from smartphone photos and diagnoses Myocardial Infarction using AI, with SHAP interpretability, achieving ~95% accuracy while running in under 30 seconds per ECG without internet or high-end hardware.


<details>
  <summary>Details</summary>
Motivation: Remote clinics still rely on paper ECG printouts due to limited connectivity and computational resources. These physical ECGs cannot be accessed by modern AI-based decision support systems, leading to overlooked acute coronary occlusion and delayed reperfusion therapy. Previous work addressed digitization and diagnosis separately but lacked a compute-light, on-device end-to-end framework.

Method: An end-to-end lightweight on-device pipeline that converts a smartphone photo/scan of a paper ECG into a calibrated 12-lead signal and screens for MI pathologies, using SHAP for interpretability. Trained on 21,799 ECGs from PTB-XL and validated on a hospital-acquired ECG-Matrix dataset. Runs on CPU-only resources in under 30 seconds per ECG.

Result: Achieves 95.51% accuracy (F1=0.9519) for MI detection on PTB-XL and 88.89% accuracy (F1=0.8862) for OMI detection on ECG-Matrix. Complete system runs in under 30 seconds per ECG on CPU-only resources.

Conclusion: Legacy paper records can be reliably democratized anywhere in the world, providing scalable decision support when digital ECG export, connectivity, or high-end compute are unavailable.

Abstract: Electrocardiography (ECG) is one of the most widely used tests for diagnosing cardiovascular disease. Yet several remote clinics still utilize paper ECG printouts for their analysis due to limited connectivity and computational capacity. As a result, vast numbers of physical ECGs obtained in remote areas still remain incapable of being accessed by contemporary artificial-intelligence (AI)-based decision support as they require high computational resources or strong high-speed internet connectivity. This causes several cases where conditions like acute coronary occlusion (ACS) is overlooked and reperfusion therapy delayed. Although prior work has tackled digitization and diagnosis separately, and utilized advanced AI models for them, there still remains a lack of a compute-light, on-device framework that reconstructs paper ECGs at high fidelity, while accurately supporting multiple clinically relevant endpoints. We address this need with an end-to-end lightweight on-device digitization-to-diagnosis pipeline that converts a smartphone photo or scan of a paper ECG into a calibrated 12-lead signal and screens for Myocardial Infarction (MI) pathologies, with SHapley Additive exPlanations (SHAP) to support interpretability. Trained and evaluated on 21,799 ECGs from the PTB-XL dataset and further validated on hospital-acquired ECG-Matrix dataset, the complete system runs in <30 s per ECG on CPU-only resources, achieving 95.51% accuracy (F1 = 0.9519) for MI detection on PTB-XL and 88.89% accuracy (F1 = 0.8862) for OMI detection on ECG-Matrix. This work showcases that legacy paper records can be reliably democratized in any part of the world, providing a scalable decision support when digital ECG export, connectivity, or high-end compute are unavailable

</details>


### [241] [Agon: Competitive Cross-Model RL with Implicit Rival Grading of Reasoning](https://arxiv.org/abs/2607.07690)
*Vladislav Beliaev*

Main category: cs.LG

TL;DR: Agon introduces a two-model training framework where competing models serve as each other's graders, rewarding out-solving rather than just final-answer correctness, which doubles GRPO's performance on hard math problems without process labels or reward models.


<details>
  <summary>Details</summary>
Motivation: Reinforcement learning from verifiable rewards (e.g., GRPO) only grades the final answer, which on hard problems trains models to write more rather than to think better, since the reasoning trace is never graded and no label for good thinking exists.

Method: Two competing models attempt the same problem in alternating roles: one drafts a solution while the other reads it and solves simultaneously. Each is rewarded for out-solving the other, creating implicit reasoning judgment during training without process labels or reward models. Both models are optimized, so each faces a progressively stronger rival.

Result: On the hard split of DeepMath with Qwen3, Agon doubles GRPO's pass@1, achieving roughly eight times the improvement of an untrained Mixture-of-Agents approach. Results replicate on competitive programming code and across model families (Qwen3.5, Gemma 4).

Conclusion: The two-model competitive training paradigm effectively improves reasoning without explicit process supervision, and the next step is to let models reason together in latent space rather than through text.

Abstract: Reinforcement learning from verifiable rewards (e.g. GRPO) is the engine behind today's reasoning models, yet it grades only the final answer. On hard problems this trains models to write more rather than to think better, since the trace itself is never graded and no label for good thinking exists. We introduce Agon, which makes two competing models each other's graders. Both attempt the same problem; in alternating roles, one drafts a solution and the other reads it while solving, and each is rewarded for out-solving the other. To win, a model must out-reason a rival that has seen its work, so reasoning is judged implicitly during training, with no process labels and no reward model. Because both models are optimized, each faces a progressively stronger rival, which single-model RL cannot provide. The two need only be comparably strong and behaviorally different. At inference the pair deploys as it trains, a two-stage cascade in which one model drafts and the other answers after reading the draft. On the hard split of DeepMath with Qwen3, this doubles GRPO's pass@1, roughly eight times the gain of an untrained Mixture-of-Agents pass over the same base. The ordering replicates on competitive-programming code and across model families (Qwen3.5, Gemma 4). For now the models talk in text; the next step is to let them reason together in latent space.

</details>


### [242] [The Key to Going Linear: Analysis-Driven Transformer Linearization](https://arxiv.org/abs/2607.07706)
*Anna Kuzina,Paul N. Whatmough,Babak Ehteshami Bejnordi*

Main category: cs.LG

TL;DR: This paper investigates post hoc linearization of causal self-attention to reduce quadratic costs in long-context transformer inference. By isolating state update designs in a frozen-backbone regime, the authors show softmax uses key-dependent rank-1 orthogonal projections, explain why delta-style networks outperform gated accumulation, identify approximation error sources, and introduce structural interventions (sink tokens, short convolutions, fixed-budget cache routing). The approach scales to 32B LLaMA/Qwen models, outperforming prior baselines on MMLU and matching complex adaptive-caching methods on long-context retrieval.


<details>
  <summary>Details</summary>
Motivation: To reduce the quadratic computational cost of causal self-attention in long-context transformer inference, and to systematically understand which components of post hoc linearization pipelines preserve model quality.

Method: The authors isolate state update design in a frozen-backbone regime, analyze softmax's reliance on key-dependent rank-1 orthogonal projections, identify approximation error sources, and propose structural interventions: sink tokens, short convolutions, and fixed-budget cache routing.

Result: The linearization approach scales to LLaMA and Qwen models up to 32B parameters, outperforms prior post hoc baselines on MMLU, and matches complex adaptive-caching frameworks on long-context retrieval tasks.

Conclusion: Softmax attention can be effectively linearized post hoc by understanding its projection structure and introducing targeted interventions, enabling efficient long-context inference without retraining.

Abstract: The quadratic cost of causal self-attention severely bottlenecks long-context transformer inference. While numerous post hoc linearization pipelines exist, it is difficult to identify which components preserve model quality. This work isolates the effect of state update design in a strict frozen-backbone regime. We show that softmax relies on key-dependent, rank-1 orthogonal projections, elucidating why delta-style networks outperform purely gated accumulation. We identify a potential source of approximation errors and introduce structural interventions, specifically sink tokens, short convolutions, and fixed-budget cache routing, which reduces the remaining gap. We scale this linearization approach across LLaMA and Qwen models up to 32B parameters, outperforming prior post hoc baselines on MMLU and matching the long-context retrieval of complex adaptive-caching frameworks.

</details>


<div id='cs.RO'></div>

# cs.RO [[Back]](#toc)

### [243] [Communicative Efficiency of Single vs. Multi-Axis Robot Neck Motion](https://arxiv.org/abs/2607.07390)
*Chapa Sirithunge,Haewon Jeong,Qinghua Guan,Fumiya Iida,Josie Hughes*

Main category: cs.RO

TL;DR: This paper studies how robot neck movements communicate information, finding that using 2 degrees of freedom maximizes communicative efficiency, while 3 DoF reduces clarity and increases energy costs — a phenomenon called the morphological information bottleneck.


<details>
  <summary>Details</summary>
Motivation: Nonverbal head/neck movement is crucial for human social signaling, but it's unclear how robotic neck morphology affects the communication of information, especially since robots can move in ways biologically impossible for humans.

Method: Using a robotic neck platform, 84 video stimuli were recorded across 3 rotational DoF with varying amplitude, acceleration, and frequency. Shannon entropy of pixel-change signals was measured alongside energy consumption, and a perceptual study validated communicative interpretations.

Result: Communicative information peaks at 2 DoF and decreases at 3 DoF despite higher energy costs. The optimal configuration achieves 5.26 bits at competitive energy cost. Multi-axis movements reduce perceptual clarity.

Conclusion: Anatomical completeness (more DoF) does not necessarily improve robotic expressiveness. The Motor Information Space framework provides a quantitative basis for designing robot morphology for efficient communication.

Abstract: Nonverbal communication through head and neck movement is fundamental to human social signalling, yet how robotic neck morphology translates motion into communicative information remains poorly understood. We present an information-theoretic framework characterising robot neck movement as a communication channel, quantifying information transmitted and energy expended across varied configurations. Using a robotic neck platform, we recorded 84 video stimuli spanning three rotational degrees of freedom (DoF), varying amplitude, acceleration, and frequency, measuring Shannon entropy of pixel-change signals alongside energy consumption. A perceptual study validated communicative interpretations of each motion. While humans typically engage one axis per gesture, robots are unconstrained by biological architecture, motivating tests up to 3 DoF. Yet communicative information peaks at two DoF and decreases at three despite rising energy cost, a phenomenon we term the morphological information bottleneck. Motion parameter effects were parameter-dependent, some additive, others non-linear. We introduce the Motor Information Space, a framework mapping entropy against energy to expose communicative efficiency across morphologies, in which the optimal configuration achieves 5.26 bits at competitive energy cost. Perception data further confirm multi-axis movements reduce clarity. These findings challenge the assumption that anatomical completeness improves robotic expressiveness, establishing a quantitative basis for morphological design in robots, especially humanoids.

</details>


### [244] [Pelican-VLA 0.5: Attending Before Acting Benefits Generalization](https://arxiv.org/abs/2607.06655)
*Zeyuan Ding,Wenhai Liu,Yang Xu,Jiayu Hu,Yinda Chen,Yi Zhang,Yong Dai,Jian Tang,Xiaozhu Ju*

Main category: cs.RO

TL;DR: Pelican-VLA 0.5 is a unified vision-language-action model that achieves emergent object-level attention without explicit supervision, enabled by learnable Reasoning Slots that create a compact bottleneck between perception and action.


<details>
  <summary>Details</summary>
Motivation: To develop a VLA model that can generalize to unseen scenes and robot embodiments without requiring object annotations, segmentation masks, or task-specific fine-tuning for attention to instruction-relevant objects.

Method: Integrates vision-language understanding, future-frame generation, and action prediction in a single architecture using learnable Reasoning Slots as a bottleneck between perception and action pathways to induce manipulation-centric attention.

Result: Attention-level generalization emerges: the action pathway focuses on instruction-relevant objects and contact regions without explicit supervision, outperforming other open-source VLA baselines. This ability persists across unseen scenes and robot embodiments, and works with different policy structures including MoT-style architectures.

Conclusion: The Reasoning Slot interface is the key enabler of emergent attention generalization, routing task-relevant visual information through a compact bottleneck that induces manipulation-centric attention during pre-training, and remains effective across diverse policy architectures.

Abstract: In this report, we present Pelican-VLA 0.5, a unified VLA model that integrates vision-language understanding, future-frame generation, and action prediction within a single architecture. Pelican-VLA 0.5 achieves attention-level generalization: without object annotations, segmentation masks, attention supervision, or task-specific fine-tuning, its action pathway already focuses on the instruction-relevant object and contact region. This behavior persists across unseen scenes and unseen robot embodiments, and is substantially stronger than in other open-source VLA baselines. We verify that this ability originates from the learnable Reasoning Slots inserted between perception and action: by routing task-relevant visual information through a compact bottleneck, the slot interface induces manipulation-centric attention during pre-training and remains effective across different policy structures, including a MoT-style architecture.

</details>


### [245] [NativeMEM: Native Memory Compression for Long-Horizon Robotic Manipulation](https://arxiv.org/abs/2607.06678)
*Ziye Wang,Modi Shi,Chaojun Ni,Jiazhi Yang,Mengdi Li,Zhizhong Su,Tianwei Lin,Hongyang Li*

Main category: cs.RO

TL;DR: NativeMEM is a VLA policy that uses Native Memory Compression to repurpose the vision encoder to compress historical frames into memory tokens, enabling long-horizon memory with high-frequency updates and negligible latency overhead, achieving 84.0% success in simulation and 98.7% on real robots.


<details>
  <summary>Details</summary>
Motivation: Existing VLA models struggle to retain long-horizon visual histories with high-frequency updates efficiently, as current external memory management restrains either memory horizon or policy reactiveness.

Method: Native Memory Compression repurposes the VLA's own vision encoder to compress historical frames into single tokens appended to the input sequence. A generic memory tokenizer is first developed under a frozen VLA, then the VLA is unfrozen for task-specific fine-tuning.

Result: Boosts success rates from 32.4% to 84.0% in simulation, up to 98.7% on real robots, with low latency and GPU memory usage, and achieves competitive results using only 20% of training data.

Conclusion: NativeMEM effectively enables long-term, real-time updated memory for VLA policies without external modules, demonstrating superior efficiency and performance.

Abstract: How can pretrained Vision-Language-Action (VLA) models retain long-horizon visual histories with high-frequency updates without sacrificing efficiency? Existing approaches rely on external memory management, which restrains either the memory horizon or the reactiveness of pretrained policies. To this end, we present NativeMEM, a VLA policy that features long-term and real-time updated memory. At its core is an efficient memory encoding scheme, Native Memory Compression, which repurposes the VLA's own vision encoder to compress each historical frame from each camera view into a single token. Appended to the input sequence, these memory tokens enable the pretrained VLA to attend over long-term history with negligible latency overhead, requiring neither an external planner nor a freshly initialized memory module. To align the memory tokens with the pretrained policy, we first develop a generic memory tokenizer under the supervision of a frozen VLA on memory-demanding data, and then unfreeze the VLA for task-specific fine-tuning. NativeMEM consistently outperforms prior methods, boosting success rates from 32.4% to 84.0% in simulation and up to 98.7% on real robots, while maintaining low inference latency and GPU memory usage. Notably, NativeMEM exhibits high data efficiency by achieving competitive results with prior arts using only 20% of the training data.

</details>


### [246] [RoboSnap: One-Shot Real-to-Sim Scene Generation for Generalizable Robot Learning and Evaluation](https://arxiv.org/abs/2607.06699)
*Shujie Zhang,Jingkun Yi,Weipeng Zhong,Zirui Zhou,Yangkun Zhu,Hanqing Wang,Xudong Xu,Weinan Zhang,Chunhua Shen*

Main category: cs.RO

TL;DR: RoboSnap turns a single RGB image into a simulation-ready scene using a layered design: collision-aware foreground for robot interaction and 3D Gaussian splatting for background appearance, enabling reliable trajectory replay, synthetic data generation, and policy evaluation.


<details>
  <summary>Details</summary>
Motivation: Recovering real-world scenes as interactive simulations can enable generalizable robot learning and reproducible policy evaluation, but constructing physically stable and visually faithful scenes is slow and expensive.

Method: A layered real-to-sim framework with (1) collision-aware foreground assets refined for stable robot interaction, and (2) a 3D Gaussian splatting visual layer preserving faithful background appearance under novel views.

Result: RoboSnap achieves reliable trajectory replay, supports task-specific synthetic data generation for policy training, and yields meaningful sim-real correlation for policy evaluation on DROID scenes and real-robot tasks.

Conclusion: The value of real-to-sim methods lies not only in high-fidelity visual reconstruction, but in turning real environments into reusable infrastructure for robot learning and evaluation.

Abstract: Recovering real-world scenes as interactive simulation environments can enable generalizable robot learning and reproducible policy evaluation. However, constructing scenes that are both physically stable and visually faithful remains slow and expensive. In this work, we present RoboSnap, a real-to-sim framework that turns a single RGB image into a simulation-ready scene. The key idea is a layered design that separates the physics-critical interaction area from the surrounding visual context: collision-aware foreground assets are refined for stable robot interaction, while a 3D Gaussian splatting visual layer preserves faithful background appearance under novel views. Experiments on DROID scenes and real-robot tasks show that RoboSnap achieves reliable trajectory replay in the recovered scenes, supports task-specific synthetic data generation for policy training, and yields meaningful sim-real correlation for policy evaluation. To further support real-to-sim research, we introduce DROID-Sim, a real-to-sim companion dataset constructed from 564 real-world scenes in DROID. Extensive experiments suggest that the value of real-to-sim methods lies not only in high-fidelity visual reconstruction, but in turning real environments into reusable infrastructure for robot learning and evaluation.

</details>


### [247] [CILC: Cryptographically-secure Inter-agent Loop Closure Candidate Detection for Multi-Agent Collaborative SLAM](https://arxiv.org/abs/2607.06700)
*Andrew Fishberg,Yixuan Jia,Jonathan P. How*

Main category: cs.RO

TL;DR: This paper identifies privacy risks in multi-agent SLAM where compromised agents can reconstruct imagery and trajectory from exchanged global descriptors, and proposes CILC, a secure system using Secure Multi-Party Computation to detect inter-agent loop closures without exposing descriptors, achieving privacy with feasible overhead.


<details>
  <summary>Details</summary>
Motivation: Existing multi-agent SLAM systems exchange global descriptors (GDs) for loop closure detection, but encrypted radios only protect against external eavesdroppers, not compromised swarm members. The authors demonstrate that a corrupted agent can reconstruct an honest agent's imagery and trajectory from public GD broadcasts, showing a concrete privacy threat.

Method: CILC applies Secure Multi-Party Computation (SMPC) specifically to inter-agent loop closure (ILC) candidate detection — i.e., GD similarity comparison — rather than securing the entire CSLAM pipeline. This targeted approach treats the privacy-sensitive yet computationally lightweight step, achieving a favorable privacy-to-overhead trade-off.

Result: Validation through both simulation and hardware experiments shows that CILC remains real-time and communication-feasible across multimodal global descriptors (visual and LiDAR), while effectively mitigating information leakage to a compromised swarm agent.

Conclusion: CILC is the first system to cryptographically secure inter-agent loop closure detection, demonstrating that privacy-preserving collaborative SLAM is achievable without sacrificing real-time performance or communication efficiency.

Abstract: Multi-agent Simultaneous Localization and Mapping (SLAM) and collaborative SLAM (CSLAM) require robots to continuously exchange global descriptors (GDs) to detect inter-agent loop closures (ILCs). While encrypted radios protect this traffic from external eavesdroppers, they offer no protection against a compromised swarm member. We show this threat is concrete by demonstrating how a corrupted agent can reconstruct approximations of an honest agent's imagery and trajectory from its public GD broadcasts. To address this, we propose CILC (Cryptographically-secure Inter-agent Loop Closure candidate detection), a first-of-its-kind system leveraging Secure Multi-Party Computation (SMPC) to detect ILC candidates without exchanging GDs in the clear. Rather than securing the entire CSLAM pipeline, we apply SMPC only to ILC candidate detection (i.e., GD similarity comparison), a privacy-sensitive yet computationally lightweight step, yielding an advantageous privacy-to-overhead trade-off. We validate in both simulation and hardware experiments that CILC remains real-time and communication-feasible across multimodal GDs (visual and LiDAR), while mitigating information leakage to a compromised swarm agent.

</details>


### [248] [Vision Language Action (VLA) Models for Unmanned Aerial Robotics and Bimanual Manipulation: A Review](https://arxiv.org/abs/2607.06706)
*Inkyu Sa,Chanoh Park,Hea-Min Lee,Donghee Noh,Ho Seok Ahn*

Main category: cs.RO

TL;DR: This paper reviews 183 contributions (2017-2026) on Vision Language Action (VLA) models, analyzing their architectures, training, and action representations, while showing how bimanual coordination strategies transfer to UAV navigation and control, and identifying 14 future research directions across both domains.


<details>
  <summary>Details</summary>
Motivation: VLAs have become dominant for learning-based manipulation, with bimanual coordination being the most demanding testbed. Unmanned aerial robotics faces a structurally similar challenge (coordinating thrust, attitude, gripper from visual observations under latency/payload constraints), motivating exploration of cross-domain transfer.

Method: The review organizes 183 contributions along seven dimensions: VLA architectures, training recipes, action representations, bimanual coordination (2022-2026), UAV navigation/control (2017-2026), language grounding, and cross-cutting concerns (memory, world models).

Result: Coordination strategies, training recipes, and action representations developed for bimanual VLAs transfer to unmanned aerial systems. Fourteen research directions across both domains are identified.

Conclusion: Bimanual coordination insights from VLA research can effectively inform UAV system design, suggesting a unified framework for these structurally similar robotics challenges.

Abstract: Vision Language Action (VLA) models unify visual perception, natural-language understanding, and action generation within a single foundation model, allowing a robot to follow instructions such as fold the towel or fly to the red building directly from camera images. Because VLAs inherit world knowledge from internet-scale pre-training, they have become the dominant framework for learning-based manipulation, with bimanual coordination serving as the most demanding testbed: two arms with 7 degrees of freedom each must move in concert to fold, assemble, and reorient objects. Unmanned aerial robotics faces a structurally similar challenge: a drone must coordinate thrust, attitude, and increasingly gripper commands from visual observations under strict latency and payload constraints. This review covers 183 contributions spanning 2017-2026 and organized along seven dimensions: VLA architectures, training recipes, action representations, bimanual coordination (2022-2026), unmanned aerial vehicle (UAV) navigation and control (2017-2026), language grounding, and cross-cutting concerns including memory and world models. We show that the coordination strategies, training recipes, and action representations developed for bimanual VLAs transfer to unmanned aerial systems and identify fourteen research directions across both domains.

</details>


### [249] [EvoPlan: Evolutionary Neuro-Symbolic Robot Planning with Spatio-Temporal Guarantees](https://arxiv.org/abs/2607.06724)
*Bhavya Sai Nukapotula,Samin Moosavi,Haoze Wang,Luke Duncan,Diya Shakkottai,Varun Murali,Srinivas Shakkottai*

Main category: cs.RO

TL;DR: This paper presents a neuro-symbolic framework combining LLM-based robot planners with PDDL planners and Signal Temporal Logic (STL) constraints. It uses offline mining of STL constraints from demonstration data, an evolutionary PDDL planner with LLM proposal/repair, and a constrained execution loop that checks plans against mined constraints. The system runs on open-weight LLMs with no cloud dependency.


<details>
  <summary>Details</summary>
Motivation: LLM-based robot planners generate fluent but not provably executable/safe plans, while classical PDDL planners guarantee properties but require full specification and cannot leverage LLM's contextual understanding. The paper aims to bridge this gap.

Method: Three-part framework: (1) Offline STL constraint mining from demonstrations using counterfactual perturbations and LLM violation generator with evolutionary search; (2) Evolutionary PDDL planner where LLM proposes/repairs plans and programmatic validators filter; (3) Constrained execution loop with waypoint checking and replanning.

Result: Tested on Bench2Drive (driving), HA-VLN-CE (navigation policies), and ALFWorld Text benchmark (planner). The planner beats strong baselines on ALFWorld, remains robust under vocabulary mismatch.

Conclusion: A locally-hosted, cloud-independent neuro-symbolic framework that combines LLM fluency with formal guarantees through mined STL constraints and evolutionary PDDL planning.

Abstract: LLM-based robot planners are fluent but cannot guarantee that their plans are executable or safe. Classical PDDL planners can guarantee these properties, but only after the problem is fully specified, and they make poor use of an LLM's ability to read context and repair plans. This paper presents a neuro-symbolic framework with three parts. All LLM calls use a locally-hosted open-weight model, so the pipeline can be deployed on-robot with no cloud dependency. First, an offline procedure that mines a single global Signal Temporal Logic (STL) constraint on mobility from demonstration data. The procedure recovers codified rules (e.g., stopping at red lights, mined from nuPlan driving logs) or population preferences (e.g., social-navigation comfort, mined from SCAND teleoperation), depending on what the demonstrations encode. Because the demonstrations are a one-class signal, we generate the missing negatives with counterfactual perturbations and an LLM violation generator and then fit the constraint by evolutionary search. We use the mined constraint to shield a vision-language driving policy on Bench2Drive and two discrete-action navigation policies on HA-VLN-CE. Second, an evolutionary PDDL planner: an LLM proposes and repairs plans, programmatic validators decide which ones survive, and the validated portion of the plan grows over iterations. We test the planner on the open-world ALFWorld Text benchmark, where it beats strong baselines and stays robust when the goal vocabulary does not match the action-model vocabulary. Third, a constrained execution loop: the planner's plan is compiled into waypoints, the waypoints are checked against the mined constraint, and the planner re-plans on a violation. We illustrate the full pipeline via demonstrations using the Gazebo simulator.

</details>


### [250] [A Continual Learning Framework for Adaptive Control of Modular Soft Robots](https://arxiv.org/abs/2607.06740)
*Nilay Kushawaha,Muhammad Sunny Nazeer,Baljinder Singh Bal,Cecilia Laschi,Egidio Falotico*

Main category: cs.RO

TL;DR: This paper proposes a continual learning-based control framework for modular soft robots (MSRs) that adapts to morphological changes without forgetting previous knowledge, validated on both simulated and real-world soft robotic arms.


<details>
  <summary>Details</summary>
Motivation: Designing controllers for MSRs is challenging due to nonlinear dynamics and hyper-redundant nature; existing methods require retraining from scratch when robot morphology changes.

Method: A continual learning inspired control framework that incrementally adapts to MSR configuration changes while preserving previously acquired knowledge; can also be used distributively for module-specific dynamics learning.

Result: Validated via closed-loop trajectory tracking on simulated tendon-driven soft robot and real-world three-module pneumatic soft robotic arm; demonstrated adaptive reaching with selective module activation.

Conclusion: The continual learning approach effectively handles morphological changes in MSRs without retraining, reducing computational overhead while maintaining precision.

Abstract: Soft robots have attracted significant attention in applications such as medical intervention, rehabilitation, and robotic manipulation due to their inherent compliance, flexibility, and high degrees of freedom. Modular soft robots (MSRs), composed of multiple interconnected segments, represent an emerging class of robotic systems with highly deformable and reconfigurable structures capable of performing complex tasks. However, designing controllers for MSRs remains challenging due to their nonlinear dynamics, modeling complexity, and hyper-redundant nature. Existing approaches typically require controllers to be retrained from scratch whenever the robot morphology changes. In this work, we address these challenges through a continual learning inspired control framework capable of incrementally adapting to changes in robot morphology while preserving previously acquired knowledge. Specifically, the proposed framework enables the controller to sequentially learn new MSR configurations without forgetting previously learned ones. In addition, for MSRs with fixed configurations, the same framework can be employed in a distributed manner to learn module-specific dynamics, enabling localized control and improved precision. The proposed approach is validated through closed-loop trajectory tracking experiments in simulation using a tendon-driven soft robot, as well as on a real-world three-module pneumatic soft robotic arm. Furthermore, we demonstrate the adaptive capabilities of the framework through a reaching experiment in which the controller selectively activates only the necessary modules to reach a virtual target position, thereby reducing computational overhead.

</details>


### [251] [G-PROBE: Cross-FOV Place Recognition and Certainty-Coupled Localization for 3D Point Clouds](https://arxiv.org/abs/2607.06782)
*Jinseop Lee*

Main category: cs.RO

TL;DR: G-PROBE is a learning-free global localization framework that works across varying fields of view (FOV), from narrow to panoramic. It uses virtual sensor decomposition, cross-FOV branch ensembles, gamma-SGRT for heading disambiguation, and CG-GICP for pose refinement. It achieves state-of-the-art results on multiple LiDAR datasets, especially under asymmetric or limited FOV conditions where other methods fail.


<details>
  <summary>Details</summary>
Motivation: Global localization from 3D point clouds is challenging under limited or asymmetric FOV because existing place recognition methods assume dense, symmetric coverage. The authors aim to remove this assumption and create a system that works robustly across diverse sensor configurations.

Method: G-PROBE consists of: (1) virtual sensor decomposition that adapts to any FOV configuration; (2) cross-FOV branch ensembles for heading-invariant place recognition; (3) gamma-SGRT, a score-scale-invariant, tuning-free method to suppress heading aliasing under partial FOV; (4) CG-GICP backend that refines coarse registration using a bird's-eye-view certainty map derived from front-end scores for 6-DoF pose estimation without external verification.

Result: Evaluated on five LiDAR datasets and three sensor modalities (mechanical, solid-state, FMCW). Achieves highest learning-free multi-session F1 on average. Competitive in panoramic single-session settings. Under cross-sensor pairing (wide-to-narrow): 55.0% vs ≤6.8% success for baselines. Under FOV asymmetry (360→60°): ~54% Recall@1, ~18× the strongest learning-free baseline.

Conclusion: G-PROVE provides a robust, learning-free solution for global localization that removes the dense/symmetric FOV assumption, significantly outperforming existing methods under limited or asymmetric FOV conditions while remaining competitive in standard panoramic settings.

Abstract: Global localization from 3D point clouds remains challenging under limited or asymmetric fields of view (FOV), which fail to provide the dense, symmetric coverage that place recognition methods assume. We present G-PROBE, a learning-free global localization framework that removes this assumption. A virtual sensor decomposition runs the same pipeline, by design, on configurations ranging from a narrow-FOV sensor to a panoramic or multi-sensor rig. The front-end enumerates cross-FOV branch ensembles that encode heading hypotheses for heading-invariant place recognition. A score-scale-invariant, tuning-free gamma-SGRT suppresses heading aliasing under partial FOV and provably becomes inert at symmetric 360 degrees. The back-end, CG-GICP, refines a coarse full-cloud GICP with a pass restricted to high-certainty co-observed points selected by a bird's-eye-view certainty map (a by-product of front-end scoring). This certainty coupling links descriptor evaluation to 6-DoF metric pose estimation without an external verification module. Evaluated on five LiDAR datasets and three modalities (mechanical, solid-state, FMCW), G-PROBE attains the highest learning-free multi-session F1 on average and is competitive in panoramic single-session settings. Where hand-crafted and zero-shot supervised baselines collapse under wide-to-narrow cross-sensor pairing, it remains usable end-to-end (up to 55.0% vs. no more than 6.8% success), and under FOV asymmetry (360 to 60 degrees) it retains about 54% Recall@1, about 18x the strongest learning-free baseline.

</details>


### [252] [CaLiSym: Learning Symplectic Dynamics of Real-World Systems through Structured Canonical Lifts](https://arxiv.org/abs/2607.06824)
*Aristotelis Papatheodorou,Pranav Vaidhyanathan,Natalia Ares,Ioannis Havoutis,Gerard J. Milburn*

Main category: cs.RO

TL;DR: CaLiSym extends exact symplectic learning to non-conservative robotic systems (dissipative, actuated, contact-rich) by embedding states into a structured lifted canonical phase space, achieving geometry-preserving dynamics prediction with parameter-efficient models on real-world systems.


<details>
  <summary>Details</summary>
Motivation: Physics-informed learning offers data-efficient dynamics prediction, but its strongest geometric guarantees (symplecticity) have been limited to closed conservative systems, excluding actuated, dissipative, and contact-rich robotic systems common in practice.

Method: CaLiSym embeds the physical state and its ports into a structured lifted canonical phase space where dynamics evolve through exactly symplectic maps. The lift is explicit and algebraic (no recurrent states, transformers, or implicit optimization). Uses generalized-ridge SympNet predictors and introduces GRB-SympNet (B-spline variant) for local approximation with exact symplectic structure.

Result: Consistent improvements in out-of-distribution autoregressive prediction on a controlled dissipative double pendulum, real-world quadrotor, and contact-rich quadruped, while using parameter-efficient models. Learned lifted dynamics preserve symplectic form to numerical precision.

Conclusion: Symplectic learning can be extended beyond conservative mechanics through structured canonical lifts, enabling geometry-preserving dynamics models for real-world robotic systems with actuation, dissipation, and constraints.

Abstract: Physics-informed learning promises data-efficient and stable dynamics prediction, yet its strongest geometric guarantees have largely remained confined to closed conservative systems. This excludes many robotic systems of practical interest, where actuation, dissipation, and constraints continuously exchange energy and momentum with the environment. We introduce CaLiSym, a lightweight framework that extends exact symplectic learning to such systems by changing where the geometric prior is imposed. Rather than enforcing symplecticity on the measured physical state, CaLiSym embeds the state and its physical ports into a structured lifted canonical phase space, where the learned dynamics evolve through an exactly symplectic map. The lift is explicit and algebraic, requiring neither recurrent latent states, transformer decoders, implicit optimization, nor inference-time ODE integration. We instantiate the framework with generalized-ridge SympNet predictors and introduce GRB-SympNet, a B-spline variant that combines local approximation with exact symplectic structure. Experiments on a controlled dissipative double pendulum, a real-world quadrotor, and a contact-rich quadruped demonstrate consistent improvements in out-of-distribution autoregressive prediction while using parameter-efficient models. At the same time, the learned lifted dynamics preserve the symplectic form to numerical precision. These results show that symplectic learning can be extended beyond conservative mechanics through structured canonical lifts, enabling geometry-preserving dynamics models for real-world robotic systems.

</details>


### [253] [GemNav: Discrete-Token Visual Robot Navigation using a Multimodal Large Language Model](https://arxiv.org/abs/2607.06882)
*Peter Bohm,Saimunur Rahman,Abdelwahed Khamis,Sagun Man Singh Shrestha,Chris McCool,Peyman Moghadam*

Main category: cs.RO

TL;DR: GemNav adapts a frozen Multimodal Large Language Model for robot navigation using only LoRA on the language tower, with no extra encoder or regression head, training on just 8.7 hours of data, and achieving 0.25-0.42m accuracy across four unseen environments.


<details>
  <summary>Details</summary>
Motivation: To question whether the standard recipe for visual navigation policies (dedicated visual encoder + bespoke action head + massive training data) is necessary, and explore a more data-efficient and minimal approach.

Method: GemNav freezes a Multimodal Large Language Model, applies Low-Rank Adaptation (LoRA) only to the language tower, uses a single discrete token vocabulary for both waypoints and navigation signals (no continuous regression head), and employs a soft-decoded auxiliary loss to recover metric structure from cross-entropy training.

Result: Trained on a single 8.7-hour open corpus (three orders of magnitude smaller than competing datasets), GemNav transfers zero-shot to four unseen environments (open carpark, obstacle carpark, outdoor chemical yard, indoor warehouse) and stops within 0.25-0.42m of the goal over 20 real-world trials. Short image histories improve offline metrics but don't benefit real robot performance.

Conclusion: Discrete-token adaptation of frozen MLLMs offers a data-efficient, deployable alternative for foundation model robot navigation, challenging the need for the traditional large-scale recipe.

Abstract: Visual navigation policies built on large pretrained models have so far followed a common recipe: a dedicated visual encoder, a bespoke action head, and training on thousands of hours of cross-embodiment datasets. We ask whether this recipe is necessary. In this paper, we introduce GemNav, a visual robot navigation policy that adapts a frozen Multimodal Large Language Model (MLLM) for short-to-medium horizon waypoint navigation using Low-Rank Adaptation (LoRA) on the language tower alone, with no auxiliary visual encoder and no continuous regression head. Waypoints and categorical navigation signals share a single discrete token vocabulary generated by the language-model head, and a soft-decoded auxiliary loss recovers the metric structure that pure cross-entropy training discards. On a single 8.7-hour open corpus, roughly three orders of magnitude smaller than competing training sets, the policy transfers zero-shot to four physically distinct unseen environments and stops within 0.25-0.42m of the goal across 20 real-world trials covering an open carpark, an obstacle carpark, a long outdoor chemical yard, and an indoor warehouse. Conditioning on short image histories improves offline metrics but yields no robot benefit, pointing to a ceiling on what temporal context adds once pretrained vision features are in place. These results indicate that discrete-token adaptation of frozen MLLMs can provide a data-efficient, deployable alternative for foundation model robot navigation.

</details>


### [254] [Dynamic Object Detection and Tracking in Construction: A Fisheye Camera and LiDAR Sensor Fusion Model](https://arxiv.org/abs/2607.06896)
*Yilong Chen,Huili Huang,Yong K. Cho*

Main category: cs.RO

TL;DR: A framework for quadruped robots using LiDAR and fisheye camera fusion to detect and track dynamic objects in construction sites via point cloud registration, cylindrical panorama projection, and Kalman filtering.


<details>
  <summary>Details</summary>
Motivation: Enabling robots to safely operate alongside humans in complex construction environments requires robust dynamic object detection and tracking, but existing 3D vision methods rely heavily on pre-trained neural networks and post-processing.

Method: The system uses a LiDAR sensor and upward-facing fisheye camera on a quadruped robot. Moving objects in point clouds are identified, then semantic labels are assigned by projecting 3D coordinates onto a 2D cylindrical panorama, aligning with real-time image-based detections for Kalman filter updates.

Result: High precision, simplicity, and robustness, especially in handling objects transitioning between dynamic and static states in real-world construction environments.

Conclusion: The proposed sensor fusion framework is well-suited for deployment in real-world construction environments, offering effective dynamic object tracking without heavy reliance on pre-trained neural networks.

Abstract: Robust dynamic object detection and tracking are essential for enabling robots to operate safely and effectively alongside humans in complex environments such as construction sites. While LiDAR-based SLAM and occupancy grid methods offer viable solutions for detecting and tracking motion, many state-of-the-art 3D vision approaches rely heavily on pre-trained neural networks and require additional post-processing to identify moving objects. Sensor fusion techniques, combining the precision of LiDAR with the semantic richness of RGB imagery, offer a promising alternative. In this work, we present a novel framework that enhances a quadruped robot equipped with a LiDAR sensor and an upward-facing fisheye camera for real-time dynamic object detection and tracking. After identifying moving objects within a registered point cloud, our method assigns semantic labels by projecting 3D coordinates onto a 2D cylindrical panorama, aligning with real-time image-based detections for observation update of the Kalman filter. The proposed system demonstrates high precision, simplicity, and robustness, particularly in handling objects transitioning between dynamic and static states, thus it is well-suited for deployment in real-world construction environments.

</details>


### [255] [Flow-ERD: Agent-type Aware Flow Matching with Entropy-Regularized Distillation for Diverse Traffic Simulation](https://arxiv.org/abs/2607.06957)
*Seulbin Hwang,Kiyoung Om,Daejung Kim,Jinhan Lee*

Main category: cs.RO

TL;DR: Flow-ERD is a multi-agent traffic simulator that jointly optimizes for both realism and diversity using Agent-Type Aware Flow Matching (AFM) and Entropy-Regularized Distillation (ERD), achieving state-of-the-art results on WOSAC benchmark.


<details>
  <summary>Details</summary>
Motivation: Existing traffic simulation benchmarks prioritize realism, and recent methods have optimized accordingly, leaving diversity underexplored. The authors aim to jointly pursue both realism and diversity.

Method: (1) Agent-Type Aware Flow Matching (AFM): couples flow matching's multi-modal expressiveness with type-specific kinematic execution to preserve diversity while maintaining type-consistent motions. (2) Entropy-Regularized Distillation (ERD): fine-tunes closed-loop rollout distribution using entropy-regularized reverse-KL objective to mitigate covariate shift and prevent mode collapse.

Result: Ranks first on WOSAC test benchmark and dominates the realism-diversity Pareto front among reproducible baselines.

Conclusion: Jointly optimizing realism and diversity yields superior simulation quality; the proposed Flow-ERD sets a new state-of-the-art on traffic simulation benchmarks.

Abstract: Realistic and diverse traffic simulation is essential to autonomous driving development. Yet prevailing benchmarks predominantly reward realism, and recent methods have optimized accordingly, leaving diversity underexplored. We introduce \textbf{Flow-ERD}, a multi-agent simulator that pursues realism and diversity jointly. Its backbone, \textbf{Agent-Type Aware Flow Matching} (AFM), couples flow matching's multi-modal expressiveness with type-specific kinematic execution. It preserves fine-grained diversity while keeping motions consistent with each agent type. A second stage, \textbf{Entropy-Regularized Distillation} (ERD), fine-tunes the closed-loop rollout distribution with an entropy-regularized reverse-KL objective. This mitigates covariate shift while explicitly preventing collapse onto high-density modes. We evaluate Flow-ERD with a log-free diversity metric alongside standard realism scores. Flow-ERD ranks first on the WOSAC test benchmark and dominates the realism--diversity Pareto front among reproducible baselines. Our project page is available \href{https://seulbinhwang.github.io/flow-erd-project-page/}{here}.

</details>


### [256] [End-to-End LLM Flight Planning with RAG-based Memory and Multi-modal Coach Agent](https://arxiv.org/abs/2607.06964)
*Amin Tabrizian,Arsyi Aziz,Aarifah Ullah,Mahyar Ghazanfari,Pouria Razzaghi,Peng Wei*

Main category: cs.RO

TL;DR: FRAMe is an LLM-based flight planning tool using RAG memory and a multi-modal coach agent to generate eVTOL flight plans that align with human operator preferences, achieving up to 93.8% validity across tested LLMs.


<details>
  <summary>Details</summary>
Motivation: There is a gap between human pilot intent and autonomous flight operation, as traditional flight planning algorithms struggle to incorporate flexible human preferences.

Method: FRAMe integrates a planner LLM with a multi-modal coach agent and retrieval augmented generation (RAG)-based memory to generate flight plans satisfying mission constraints while aligning with human operator preferences.

Result: The full FRAMe system achieves up to 93.8% aggregate validity across four LLMs, and 99% validity on Easy scenarios for the strongest planner, while shifting preference-relevant metrics toward operator-favored directions.

Conclusion: Advanced LLMs can be effectively deployed for human-centric mission planning, translating natural language instructions into safe, efficient, and flexible flight routes.

Abstract: Bridging the gap between human pilot intent and autonomous flight operation is critical for real-world electric vertical takeoff and landing (eVTOL) aircraft deployment. Flight planning traditionally relies on classic algorithms that struggle to incorporate flexible human preferences. We present FRAMe, an End-to-End Large Language Model (LLM) Flight Planning tool with RAG-based Memory and Multi-modal Coach Agent. Our system integrates a planner LLM with a multi-modal coach agent and retrieval augmented generation (RAG)-based memory to generate flight plans that satisfy mission constraints while aligning with human flight operator preferences. We demonstrate the system in a range of real-world-inspired scenarios of varying difficulty levels. Across four LLMs, the full FRAMe system (RAG and coach) yields the highest validity for every planner (up to 93.8% aggregate, 99% on Easy scenarios for the strongest planner) and shifts preference-relevant metrics in the operator-favored direction where the metric has headroom. FRAMe signifies how advanced LLMs can be deployed for human-centric mission planning, translating natural language instructions into safe, efficient, and flexible flight routes. The code is available at: github.com/amin-tabrizian/FlightPlanningLLMs

</details>


### [257] [SPECTRA: Context-Conditioned Spectral Movement Primitives for Robot Skill Generalization](https://arxiv.org/abs/2607.06978)
*Boxuan Zhang,Sheng Liu,Chenglin Ming,Ahmed Abdelrahman*

Main category: cs.RO

TL;DR: The paper introduces Spectral Movement Primitive (SMP), a frequency-domain imitation learning framework that preserves task geometry by separating low-frequency task content from high-frequency dynamics, using Fourier coefficients with a phase-coupled regulator to enforce joint-space limits without distorting the end-effector path.


<details>
  <summary>Details</summary>
Motivation: Existing imitation learning pipelines learn task-dependent trajectories and then impose execution limits (filtering, clipping, time scaling) afterward, which can distort task-critical end-effector paths. A method is needed that preserves demonstrated task geometry while producing dynamically admissible robot motions.

Method: SMP uses truncated finite-horizon Fourier coefficients to represent demonstrations. A low-frequency task band captures dominant motion geometry while higher harmonics contribute to derivative growth. A frame-aware GMM/GMR prior predicts task-band coefficients in a canonical task frame. Cartesian trajectories are mapped to joint space via sequential inverse kinematics, and a phase-coupled regulator enforces joint velocity/acceleration limits without modifying spectral coefficients or distorting the path.

Result: Experiments show compact geometric reconstruction, consistent transfer across unseen task frames, substantial reductions in dynamic violations and jerk, preservation of intended end-effector path during phase regulation, robustness to composite demonstration corruption, and successful deployment on a Franka Panda robot.

Conclusion: The SMP framework effectively decouples task-space skill generation from joint-space execution regulation, preserving path geometry while enforcing dynamic admissibility, offering a principled frequency-domain alternative to post-hoc filtering approaches in imitation learning.

Abstract: Robot imitation learning for manipulation should preserve demonstrated task geometry while producing dynamically admissible robot motions. Existing pipelines often learn task-dependent trajectories and impose execution limits afterward through filtering, smoothing, clipping, or time scaling, which may distort task-critical end-effector paths.
  We propose the Spectral Movement Primitive (SMP), a frequency-domain imitation learning framework that couples task-space skill generation with joint-space execution regulation. Demonstrations are represented by truncated finite-horizon Fourier coefficients. An empirically selected low-frequency task band captures the dominant motion geometry, while higher harmonics contribute disproportionately to derivative growth. A frame-aware context-conditioned GMM/GMR prior predicts the task-band coefficients in a canonical task frame, and the resulting Cartesian trajectory is mapped to joint space through sequential inverse kinematics. A phase-coupled regulator then limits the requested phase progression without modifying the spectral coefficients, thereby enforcing joint velocity and acceleration limits while preserving the represented path.
  Experiments evaluate task-band reconstruction, robustness to composite demonstration corruption, out-of-distribution cross-board generalization, joint-space dynamic admissibility, end-effector path preservation, and deployment on a Franka Panda robot. Results show compact geometric reconstruction, consistent transfer across unseen task frames, substantial reductions in dynamic violations and jerk, and preservation of the intended end-effector path during phase regulation.

</details>


### [258] [WAM-TTT: Steering World-Action Models by Watching Human Play at Test Time](https://arxiv.org/abs/2607.06988)
*Yusen Feng,Bingchen Han,Jiangran Lyu,Kai Liu,Yixin Zheng,Yuxuan Wan,Weiheng Liu,Sun Han,Ruiqin Li,Yulong Zhang,Fangfu Liu,Xuesong Shi,Libin Liu,Yizhou Wang,Zhizheng Zhang,He Wang*

Main category: cs.RO

TL;DR: WAM-TTT enables steering robot foundation models by adapting them via test-time training on raw human videos, using a self-supervised video prediction objective and meta-training alignment, without needing robot actions, annotations, or fine-tuning.


<details>
  <summary>Details</summary>
Motivation: Steering robot foundation models toward new tasks or user preferences is challenging and typically requires demonstrations, fine-tuning, or long-context conditioning. The goal is to enable efficient steering using only raw, unlabeled human videos.

Method: WAM-TTT uses a lightweight adaptive memory inside a frozen world action model (WAM). It first meta-trains with paired human-robot data and a key-value memory reconstruction objective to align human and robot behaviors. At test time, unlabeled human videos are used for self-supervised video prediction to adapt the memory, while the WAM remains frozen.

Result: WAM-TTT consistently outperforms in-context human-video conditioning baselines across diverse manipulation tasks and generalization settings.

Conclusion: The framework enables efficient, reusable steering without robot actions, human annotations, or task-specific fine-tuning, while preserving the generalization ability of the foundation model.

Abstract: Steering robot foundation models (RFMs) toward new task variants or user-preferred behaviors remains challenging, often requiring additional robot demonstrations, task-specific fine-tuning, or long-context conditioning. We present WAM-TTT, a test-time training framework for steering world action models from raw human videos. Rather than treating human videos as trajectories to imitate, WAM-TTT absorbs them into a lightweight adaptive memory inside a frozen WAM through self-supervised video prediction. To make this memory useful for control, we introduce a meta-training stage that aligns human demonstrations with robot behaviors using paired human-robot data and a key--value memory reconstruction objective. At test time, only unlabeled human videos are required to adapt the memory, while the pretrained WAM remains frozen. This enables efficient and reusable steering without robot actions, human-side annotations, or task-specific fine-tuning, while preserving the generalization ability of the foundation model. Extensive experiments show that WAM-TTT consistently outperforms in-context human-video conditioning baselines across diverse manipulation tasks and generalization settings.

</details>


### [259] [Ace! Motion Planning of Professional-Level Table Tennis Serves with a Robot Arm](https://arxiv.org/abs/2607.06989)
*Guillem Torrente,Guilherme Jorge Maeda,Divij Grover,Megumu Tsukamoto,Hamdi Sahloul,Peter Dürr*

Main category: cs.RO

TL;DR: This paper presents a novel robotic approach for generating official table tennis serves using motion primitives, MPC, and Bayesian Optimization, achieving spins up to 550 rad/s and speeds up to 6.7 m/s that match or surpass elite human players.


<details>
  <summary>Details</summary>
Motivation: While robotic table tennis has been extensively studied for rallying (returning balls), the serve aspect remains largely unexplored. Generating competitive serves requires extreme physics modeling and control, including high-spin generation from a spinless ball, precise aiming, and multi-objective optimization.

Method: The approach combines motion primitives, Model Predictive Control (MPC), and Bayesian Optimization to generate official rule-compliant table tennis serves.

Result: The system generates serves with spins up to 550 rad/s and speeds up to 6.7 m/s, matching and even surpassing the performance of elite table tennis players.

Conclusion: The proposed method successfully addresses the challenging problem of robotic serve generation, achieving elite-level performance with controllable spin and speed variations while complying with official rules.

Abstract: Table tennis, a dynamic, compact, and popular sport, has received significant attention as a robotics benchmark over the last decades. Most of the research has focused on the rally aspect - returning an incoming ball - requiring high-speed vision, agile motion planning, and tight closed-loop control. However, the other component of table tennis gameplay - the serve - is comparatively a quite unexplored research problem, that in fact requires pushing physics modeling and control to the extremes. Achieving competitive serves with a robot presents domain-specific challenges, such as high-spin generation from a spinless ball, precise aiming, or multi-objective optimization. In this work, we present a novel approach for generating official rule-compliant serves by combining motion primitives, Model Predictive Control, and Bayesian Optimization. Serves generated in this way offer a wide and controllable variation of spins of up to 550 rad/s, and speeds of up to 6.7 m/s, matching and even surpassing those of elite table tennis players.

</details>


### [260] [A Closed-Loop Multi-Agent Framework for Robust Multi-Robot Manipulation](https://arxiv.org/abs/2607.06990)
*Yi-Xiang He,Lan Wei,Haoming Cen,Jian-Jian Jiang,Zhuohao Li,Guanxing Lu,Yihan Yang,Dandan Zhang,Wei-Shi Zheng*

Main category: cs.RO

TL;DR: A hierarchical closed-loop agentic LLM framework with three specialized agents (Planning, Manipulation, Verification) for robust multi-robot manipulation, bridging high-level reasoning and physical execution.


<details>
  <summary>Details</summary>
Motivation: Existing LLM-based approaches lack coordination for multi-robot tasks or fail to address real-world execution uncertainties; single-robot methods lack multi-workspace coordination, while multi-robot frameworks treat manipulation as idealized primitives.

Method: Three-agent hierarchical closed-loop framework: Planning Agent decomposes instructions, Manipulation Agent executes adaptive tool use per robot, Verification Agent monitors outcomes and feeds semantic corrections.

Result: Superior success rates in real-world experiments, robust adaptability from single to cross-workspace manipulation, and generalizability across diverse tasks.

Conclusion: The proposed framework effectively grounds high-level LLM reasoning in physical multi-robot execution, offering a generalizable approach for robust multi-robot manipulation.

Abstract: Multi-robot systems provide the parallelism and redundancy necessary for long-horizon tasks, while Large Language Models (LLMs) offer the reasoning capabilities to decompose these objectives into actionable plans. However, effectively grounding this high-level reasoning in physical multi-robot execution remains an open challenge. Existing LLM-based approaches fall mainly into two categories: Single-robot methods achieve robust contact-rich manipulation but lack the coordination mechanisms required for tasks spanning multiple workspaces. Current multi-robot frameworks focus on high-level planning, often treating manipulation as an idealized primitive that fails to account for real-world execution uncertainties. To address this, we propose a hierarchical closed-loop agentic LLM-based framework to ensure robust multi-robot manipulation. Our system consists of three specialized agents: the Planning Agent decomposes instructions into allocated sub-tasks, the Manipulation Agent for each robot executes actions via adaptive tool use, and the Verification Agent closes the loop by monitoring physical outcomes and feeding back semantic corrections. Extensive real-world experiments demonstrate that our framework achieves superior success rates, ensures robust adaptability ranging from single to cross workspace manipulation, and offers a generalizable approach for diverse manipulation tasks.

</details>


### [261] [PriGo: Test-Time Primitive Guidance to Diffusion and Flow Policies for Adaptive Robotic Manipulation](https://arxiv.org/abs/2607.07076)
*Zezeng Li,Enda Xiang,Thuy Tran,Di Huang,Momath Thiam,Liming Chen*

Main category: cs.RO

TL;DR: PriGo is a primitive-guided test-time adaptive framework that improves pretrained diffusion and flow-based policies for robotic manipulation by refining actions toward semantically consistent behaviors using a lightweight primitive prediction module, without retraining.


<details>
  <summary>Details</summary>
Motivation: Existing imitation learning policies fail to generalize because they imitate superficial action correlations rather than underlying intent.

Method: PriGo introduces PANet (primitive prediction module) and a differentiable primitive guidance mechanism that refines generated actions at test time, steering trajectories toward semantically consistent behaviors. It integrates into pretrained diffusion/flow policies without retraining.

Result: Consistent improvements in robustness, long-horizon execution, and generalization on LIBERO, CALVIN, SIMPLER, and real-world robotic tasks across both diffusion and flow-based policies.

Conclusion: Test-time primitive guidance is an effective, retraining-free way to make pretrained robotic manipulation policies more robust and generalizable.

Abstract: Imitation learning has enabled remarkable progress in robotic manipulation, especially with diffusion and flow-based policies that generate complex visuomotor behaviors directly from demonstrations. Yet, despite their strong performance, these policies often fail to generalize across tasks and environments. A key reason is that existing policies tend to imitate superficial action correlations rather than the underlying intent. Inspired by the compositional structure of human behaviors, we propose PriGo, a primitive-guided test-time adaptive framework for robust robotic manipulation. PriGo introduces PANet, a lightweight primitive prediction module that infers primitive distributions directly from observations. We further propose a differentiable primitive guidance mechanism that refines generated actions during inference, steering trajectories toward semantically consistent behaviors. Unlike prior primitive-conditioned approaches, PriGo operates entirely at test time and can be seamlessly integrated into pretrained diffusion and flow policies without retraining. Extensive experiments on LIBERO, CALVIN, SIMPLER, and real-world robotic tasks demonstrate that PriGo consistently improves robustness, long-horizon execution, and generalization ability across both diffusion and flow-based policies.

</details>


### [262] [GeoProp: Grounding Robot State in Vision for Generalist Manipulation](https://arxiv.org/abs/2607.07101)
*Guoyang Zhao,Quanhao Qian,Gongjie Zhang,Wenhao Li,Jiuniu Wang,Xiaowei Lu,Deli Zhao,Ran Xu*

Main category: cs.RO

TL;DR: GeoProp is a lightweight adapter that aligns robot proprioception with vision by projecting 3D kinematics onto 2D image planes for feature sampling, improving manipulation policies by 4-10.6% across 67 tasks with minimal parameter overhead.


<details>
  <summary>Details</summary>
Motivation: Standard fusion methods treat proprioception as an isolated vector lacking explicit alignment with visual tokens, causing manipulation policies to struggle with grounding the robot's state in the scene and underperform even vision-only baselines.

Method: GeoProp projects robot state onto the image plane to sample localized visual features (grounded state token), injects spatial priors into visual features via FiLM modulation, and samples features at short-horizon predicted coordinates for look-ahead visual context.

Result: Improves Diffusion Policy by 8.7% on 63 simulation tasks, pi_0 by 4.0% on RoboTwin subset, yields 10.6% average gain in real-world tasks, with only 2-3% parameter increase.

Conclusion: GeoProp provides a simple yet high-impact inductive bias for generalist embodied policies by establishing explicit geometric correspondence between proprioception and vision.

Abstract: Proprioception is fundamental to robotic manipulation, yet standard fusion methods often treat it as an isolated vector lacking explicit alignment with visual tokens. Without a direct correspondence between 3D kinematics and 2D feature maps, manipulation policies struggle to ground the robot's state within the scene, frequently underperforming even vision-only baselines. To address this, we introduce GeoProp, a lightweight, plug-and-play adapter that aligns proprioception with vision through explicit geometric grounding and spatial feature sampling. GeoProp projects the robot state onto the image plane to sample localized visual features, constructing a grounded state token. It then injects state-derived spatial priors into the corresponding visual features via FiLM modulation. To capture motion intent, GeoProp further samples features at a short-horizon predicted coordinate derived from recent kinematics, providing look-ahead visual context. Across 67 tasks, GeoProp improves Diffusion Policy by 8.7% on 63 simulation tasks and pi_0 by 4.0% on the RoboTwin subset, and yields a 10.6% average gain across both policy families in the real world, while adding only 2-3% to the parameter count. These results demonstrate that GeoProp is a simple yet high-impact inductive bias for generalist embodied policies. Project page: https://alibaba-damo-academy.github.io/GeoProp/.

</details>


### [263] [Compositional Motion Generation from Demonstration with Object-Centric Neural Fields](https://arxiv.org/abs/2607.07129)
*Ahmet Ercan Tekden,Yasemin Bekiroglu*

Main category: cs.RO

TL;DR: A generative LfD framework using object-centric neural representations and temporal mixture-of-experts for compositional robot behavior modeling, achieving data-efficient learning and systematic generalization.


<details>
  <summary>Details</summary>
Motivation: To enable scalable and data-efficient robot learning by leveraging compositionality—organizing complex behavior as combinations of simpler elements.

Method: (1) Object-centric neural representations (canonical neural fields + latent-conditioned deformations) for rendering scenes; (2) Temporal mixture-of-experts (MoE) with gating mechanism combining object-conditioned movement primitives for trajectory generation.

Result: Successfully completes long-horizon manipulation tasks in simulation with significantly less training data than image-based baselines; shows robustness to noise, category-level generalization, and direct operation on 3D scene representations in real-world experiments.

Conclusion: Spatial-temporal compositionality through shared object-level representations enables data-efficient, generalizable robot learning across diverse scene configurations.

Abstract: Compositionality, by organizing complex behavior as combinations of simpler elements, enables robot learning that is scalable and data efficient. Leveraging this principle, we propose a generative learning-from-demonstration framework that enables compositional modeling of robotic behavior by connecting perception and motion through shared object-level representations. We render scenes from object-centric neural representations that integrate canonical neural fields with latent-conditioned deformations, capturing positional and geometric variations in a smooth, consistent, and interpretable way. For motion generation, a temporal mixture-of-experts (MoE) employs a gating mechanism to combine object-conditioned movement primitives over time, producing complete trajectories. This spatial-temporal compositionality maintains the data efficiency of movement primitives while grounding motion in visual structure, enabling systematic generalization across diverse scene configurations. In simulation, long-horizon manipulation tasks are successfully completed using the proposed model, which requires significantly less training data than other image-based baselines. Real-world experiments further demonstrate the method's robustness to noise, its ability to generalize at the category level through language-based segmentation models, and its capacity to operate directly on 3D scene representations.

</details>


### [264] [Learning Spatiotemporal Tubes for Full Class of Signal Temporal Logic Tasks for Control of Unknown Systems under Input Constraints](https://arxiv.org/abs/2607.07136)
*Ahan Basu,Ratnangshu Das,Soumyodipta Nath,Siyuan Liu,Pushpak Jagtap*

Main category: cs.RO

TL;DR: This paper proposes a PINN-based Spatiotemporal Tube (STT) control framework for nonlinear Euler-Lagrange systems under input constraints to satisfy Signal Temporal Logic (STL) specifications, with validated case studies for both single and multi-agent scenarios.


<details>
  <summary>Details</summary>
Motivation: To achieve STL specification satisfaction for general unknown nonlinear Euler-Lagrange systems under input constraints, bridging the gap between formal task specifications and control of complex physical systems.

Method: Models STT as time-varying balls parameterized by PINNs, incorporates STL robustness metrics as loss functions, and develops a closed-form control law that keeps trajectories inside tubes while respecting input constraints.

Result: Validated through multiple case studies demonstrating the framework's effectiveness in satisfying both local and global STL specifications under input constraints.

Conclusion: The proposed STT-based control framework successfully enables unknown nonlinear EL systems to satisfy complex temporal logic specifications while respecting physical input limits.

Abstract: This paper presents a Spatiotemporal Tube (STT)-based control framework for general unknown nonlinear Euler-Lagrange (EL) systems subject to input constraints, with the objective of satisfying Signal Temporal Logic (STL) specifications, where confinement of the system trajectory within the STT guarantees the satisfaction of the corresponding STL task. For both single and multi-agent scenarios, the STT corresponding to each agent is modeled as a time-varying ball, whose center and radius are jointly parameterized using a physics-informed neural network (PINN). The robustness metric associated with the STL specification corresponding to the agents is incorporated into the training process as a loss function, enabling the learned tube to encode task-level temporal requirements. For a multi-agent scenario, we introduce an additional robustness metric corresponding to the global task, which, when satisfied, ensures the tubes do not collide with each other. To ensure that the system trajectory remains within the learned STT and thereby satisfies the local and global STL specifications, we propose a control strategy that explicitly accounts for input constraints. In particular, a closed-form control law is developed to keep the trajectory inside the tube while regulating the motion of the tube by enforcing bounds on its evolution depending on the input constraints of the system. The proposed approach has been validated over several case studies.

</details>


### [265] [Disturbance-aware Motion Planning for Over-actuated Underwater Vehicles Exploiting Actuation Redundancy for High-fidelity 3D Reconstruction](https://arxiv.org/abs/2607.07139)
*Yuer Gao,Tongqing Xu,Qingyang Liu,Yi Cai*

Main category: cs.RO

TL;DR: An over-actuated ROV uses null-space optimization of thruster allocation to minimize wake-induced sediment disturbance, reducing particle velocity by 67% and improving 3D reconstruction RMSE by 55% compared to disturbance-unaware baselines.


<details>
  <summary>Details</summary>
Motivation: High-power thrusters on underwater robots resuspend sediments and induce turbulence, degrading image quality. Conventional controllers optimize vehicle motion without accounting for how actuation negatively impacts perception.

Method: Exploit redundancy in an eight-thruster ROV by searching the null space of thrust allocations to minimize predicted disturbance in a task-relevant target region. Uses a control-oriented thruster-wake proxy (validated by PIV, R²=0.99 near wake axis) and a real-time redundancy-resolving allocator running at 10 Hz (45 ms/solve).

Result: 440 trials: 67% reduction in target-region particle velocity (p<0.001), 55% improvement in 3D reconstruction RMSE (1.9±0.4 mm vs 4.3±1.8 mm), and 98.5% reconstruction success rate.

Conclusion: The framework effectively mitigates actuation-to-perception coupling in over-actuated underwater robots, supporting both autonomous scanning and operator-assisted inspection.

Abstract: Underwater robots often operate near delicate targets where high-power thrusters resuspend sediments and induce turbulence, degrading image quality at the sensor input. Conventional controllers optimize vehicle-centric objectives, such as tracking and stability, without accounting for the impact of actuation on sensing. We address this actuation-to-perception coupling by exploiting redundancy in over-actuated platforms. For an eight-thruster ROV, multiple thrust allocations can yield the same motion; we search this null space to minimize predicted disturbance in a task-relevant target region while enforcing motion constraints. Our method uses a control-oriented thruster-wake proxy derived from actuator-disk theory with directional attenuation and validated by PIV ($R^2 = 0.99$ near the wake axis; $R^2 > 0.82$ in the primary wake region), together with a real-time redundancy-resolving allocator running at 10 Hz (45 ms/solve). Across 440 trials, the approach reduces target-region particle velocity by 67% ($p < 0.001$), improves 3D reconstruction RMSE by 55% versus a disturbance-unaware baseline ($1.9 \pm 0.4$ mm vs. $4.3 \pm 1.8$ mm), and achieves a 98.5% reconstruction success rate. The framework supports autonomous scanning, which is quantitatively evaluated, and operator-assisted inspection, which is demonstrated in the supplementary materials.

</details>


### [266] [Validate the Dream Before You Trust Its Verdict: Admissibility for World-Model Simulators](https://arxiv.org/abs/2607.07196)
*Christian Oefinger,Finn Rasmus Schäfer,Korbinian Moller,Mattia Piccinini,Johannes Betz*

Main category: cs.RO

TL;DR: World Models used as test oracles must be accredited through an admissibility ladder (L0-L4) before their closed-loop verdicts can be trusted as evidence, as visual fidelity alone (L0) does not predict action-robustness (L1-L2), demonstrated through driving WMs.


<details>
  <summary>Details</summary>
Motivation: Current fidelity metrics like FVD reward visual realism but ignore whether the world model correctly responds to a policy's actions, including unseen actions. Since generative WMs are unverified learned artifacts, they cannot be trusted as test oracles without proper accreditation.

Method: The authors define an embodiment-agnostic admissibility ladder (L0-L4) building on VV&A, SOTIF, and scenario-based testing standards from safety-critical simulation. They instantiate this framework in autonomous driving and apply it to two driving WMs.

Result: The model ranking higher on visual generation quality (L0) ranks lower on action-following (L1-L2), showing visual fidelity does not predict the action-robustness needed for closed-loop verdicts.

Conclusion: WMs used as test oracles must be accredited (climb the admissibility ladder) before their verdicts can serve as assurance evidence, and visual fidelity metrics are insufficient for this purpose.

Abstract: Across robotics, World Models (WMs) are increasingly used to evaluate action policies by simulating the consequences of actions in an imagined world, and returning a success or safety verdict. Yet a verdict is only as trustworthy as the WM that produced it, and the WM itself needs to be certified. In video-generation WMs, fidelity metrics such as Fréchet Video Distance (FVD) reward visual realism, but ignore whether the world responds correctly to the policy's actions, including those unseen in training. Classical simulation-based validation assumes a trusted simulator evaluating an untrusted policy, whereas generative WMs are themselves unverified learned artifacts. Hence, we argue that any WM used as a test oracle must first be accredited before its verdicts can serve as evidence. Building on credibility practices from safety-critical simulation, including Verification, Validation & Accreditation (VV&A), Safety of the Intended Functionality (SOTIF), and scenario-based testing standards, we define an admissibility ladder (L0-L4) that a WM must climb before its closed-loop verdicts are accepted as assurance evidence. Our framework is embodiment-agnostic, and is instantiated in autonomous driving (AD), where assurance methods for traditional simulation are most mature. Applied to two driving WMs, the lower rungs reveal a reversal: the model that ranks higher on visual generation quality (L0) ranks lower on action-following (L1-L2), so visual fidelity does not predict the action-robustness a closed-loop verdict depends on.

</details>


### [267] [Manual, Joystick, or Haptic Control? An In Vitro Comparison of Navigation Strategies for Robotic Interventional Neuroradiology Procedures](https://arxiv.org/abs/2607.07253)
*Benjamin Jackson,Nikola Fischer,Harry Robershaw,Xingyu Chen,S. H. Hadi Sadati,Yang Li,Jeremy Lynch,Nasr Abdelsalam,Jonathon Buwanabala,Matthew Benger,Sara Sciacca,Naga Kandasamy,Marco Mancuso-Marcello,Parthiban Balasundaram,Sahan Guruge,Neelan Das,Alejandro Granados,Kawal Rhode,Thomas C Booth*

Main category: cs.RO

TL;DR: This study evaluated robotic controller interfaces for interventional neuroradiology using a force-sensing platform. Manual navigation was fastest, all modalities were safe (below 0.70N force threshold), device-mimicking controllers outperformed joystick, and haptic feedback showed non-significant benefits.


<details>
  <summary>Details</summary>
Motivation: To evaluate the safety and performance of different robotic controller interfaces (device-mimicking with/without haptics, joystick, manual) for endovascular procedures in neuroradiology, using a force-sensing neurovascular phantom.

Method: Ten interventional neuroradiologists (4 novices, 6 experts) performed simulated navigations using four control modalities (device-mimicking with haptics, without haptics, joystick, manual). Metrics included navigation time, peak vessel-wall forces, incorrect catheterisations, prolapse events, and user perception analysis.

Result: Manual navigation was fastest (47.7s vs up to 392.6s, p<0.001). All modalities were safe (<0.70N force). Joystick caused more prolapse events (1.56 vs 0.13, p=0.018). Experts performed better than novices across controllers, with haptic feedback accentuating experience-related differences. Device-mimicking controllers were perceived as more intuitive than joystick.

Conclusion: Device-mimicking robotic controllers are superior to joystick interfaces for interventional neuroradiology. Haptic feedback shows promising but statistically non-significant benefits. Operator experience remains a critical factor affecting performance regardless of controller type.

Abstract: Objective: To evaluate robotic controller interfaces for interventional neuroradiology procedures in-vitro incorporating a force-sensing platform to assess safety. Methods: A custom endovascular robot, device-mimicking controller, and sensorized neurovascular phantom were developed. Ten interventional neuroradiologists (4 novices, 6 experts) performed simulated navigations using four control modalities: device-mimicking controllers with and without haptic feedback, joystick-based input, and manual navigation. Navigation time, peak vessel-wall forces, incorrect catheterisations, and prolapse events were assessed, alongside user analyses. Results: Manual navigation was fastest (mean 47.7 s) compared to haptic-on (248.7 s), haptic-off (314.7 s), and joystick (392.6 s) modalities (p<0.001). Regardless of controller type, vessel-wall forces were below the 0.70 N puncture threshold; therefore all modalities were considered safe. Joystick produced significantly more prolapse events than manual control (1.56 vs 0.13; p=0.018). Operator experience was relevant to performance: experts made fewer incorrect catheterisations than novices (0.25 vs 0.62; p=0.035) and applied less vessel-wall force (p<0.0005); these effects were sustained across controllers but accentuated when haptics were on. Users perceived haptic on and haptic off as similarly intuitive, and more intuitive than joystick (p=0.033). Conclusion: Device-mimicking robotic controllers outperform joystick interfaces on most metrics; haptic feedback shows promising but non-significant performance benefits.

</details>


### [268] [Programmable Synchronization Graphs for Adaptive and Fault-Tolerant Modular Miniature Robots](https://arxiv.org/abs/2607.07281)
*Okan Kulekcioglu,Arqam Bin Ahmad,Ines Garcia,Filipe Serra Alves,Onur Ozcan,M. Selim Hanay*

Main category: cs.RO

TL;DR: This paper introduces a programmable synchronization-graph framework for coordinating modular miniature robots without a leader, enabling gait pattern programming (e.g., gallop, trot) through sparse graph coupling, with built-in fault tolerance and online learning capabilities.


<details>
  <summary>Details</summary>
Motivation: Coordination of many imperfect modular miniature robots is difficult due to limited computation, communication, and reliability. Existing approaches rely on privileged leaders, fixed gait templates, or dense communication, which are impractical in constrained environments.

Method: Each actuator-sensor pair is a network node; fixed intra-subgraph links synchronize heterogeneous groups, while signed inter-subgraph links program phase relationships. Sparse d-regular topologies replace dense coupling. An upper-confidence-bound edge-selection algorithm learns inter-subgraph links for target phase states.

Result: Experiments with up to nine modules demonstrate emergent synchronization, tunable phase differences (in-phase to out-of-phase), gallop/trot gait patterns, fault tolerance via graph degree control (tolerating more deactivations), and threefold reduction in worst-case phase error over leader-follower control.

Conclusion: Programmable network topology serves as a compact control layer for gait phase programming, online adaptation, and robustness to unit loss in modular miniature robots.

Abstract: Modular miniature robots could provide scalable function in constrained environments, but coordinating many imperfect modules remains difficult when computation, communication and reliability are limited. A central robotics challenge is to coordinate many actuator-sensor modules without assigning a privileged leader, prescribing a fixed gait template, or relying on dense communication. Here we introduce a programmable synchronization-graph framework for modular miniature robots in which each actuator-sensor pair is represented as a network node and locomotor coordination is encoded through graph coupling. Fixed intra-subgraph links synchronize heterogeneous actuator groups, whereas a small number of signed inter-subgraph links program phase relationships between groups. In physical robot collectives with up to nine modules, graph coupling drives the emergence of synchronization, signed links tune the phase difference from in-phase to out-of-phase motion, and floor experiments produce gallop-like and trot-like contact patterns in a five-module robot assembly. Replacing dense all-to-all coupling with sparse d-regular topologies preserves synchronization while reducing the coupling burden. The same graph representation also captures fault tolerance: increasing graph degree increases the number of module deactivations tolerated before desynchronization. Finally, an upper-confidence-bound edge-selection algorithm learns inter-subgraph links that drive the system toward target phase states. In a separate deactivation benchmark, the graph-based controller avoids the leader-specific failure mode observed in centralized leader-follower control and reduces worst-case phase error by about threefold. These results establish programmable network topology as a compact control layer for gait phase programming, online adaptation and robustness to unit loss in modular miniature robots.

</details>


### [269] [TouchWorld: A Predictive and Reactive Tactile Foundation Model for Dexterous Manipulation](https://arxiv.org/abs/2607.07287)
*Jianyi Zhou,Feiyang Hong,Yunhao Li,Yicheng Zhao,Yongjue Cen,Zirui Liu,Jiakang Huang,Zirui Chen,Ruiyang Zhang,Weizhuo Zhu,Xuhua Song,Shuo Yang*

Main category: cs.RO

TL;DR: TouchWorld is a tactile foundation model for dexterous manipulation that uses a hierarchical policy separating vision-language subtask planning, tactile world-model prediction, visuo-tactile goal-conditioned action generation, and high-frequency tactile residual refinement, achieving 65.0% success in clean settings and 53.7% under perturbations, outperforming baselines by 15.7-18.5 percentage points.


<details>
  <summary>Details</summary>
Motivation: Existing dexterous manipulation policies treat tactile sensing as a low-frequency observation stream within monolithic action models, coupling slow task reasoning, action generation, and fast contact feedback in a single loop. This fails to separately handle predictive contact evolution and reactive local error correction (slip, misalignment, unstable grasping, force mismatch).

Method: TouchWorld uses a hierarchical policy with three levels: (1) High-Level Planning Layer producing executable subtasks and predicting tactile subgoals; (2) Visuo-Tactile Goal-Conditioned Policy generating nominal action chunks; (3) Tactile-Conditioned Refinement Policy performing online residual correction using recent tactile and proprioceptive feedback. Touch serves as both predictive contact reference and fast feedback signal.

Result: Across six long-horizon and contact-rich dexterous manipulation tasks, TouchWorld achieves 65.0% success in clean settings and 53.7% success under human perturbations, outperforming the strongest baseline by 15.7 and 18.5 percentage points respectively.

Conclusion: By separating planning, prediction, action generation, and tactile refinement into distinct hierarchical levels, TouchWorld preserves semantic generalization of vision-language-action policies while significantly improving local contact adaptation, demonstrating the value of using touch as both predictive reference and fast feedback.

Abstract: Dexterous manipulation in everyday environments requires both anticipation and reaction: a robot must predict how contact should evolve while rapidly correcting local errors caused by slip, misalignment, unstable grasping, or force mismatch. Vision and language provide semantic and geometric guidance, but they cannot reliably reveal hidden contact states such as force, slip, and contact stability. Although tactile sensing exposes these physical cues, most existing policies treat touch as a low-frequency observation stream within a monolithic action model, coupling slow task reasoning, action generation, and fast contact feedback in a single loop. We introduce TouchWorld, a predictive-and-reactive tactile foundation model for dexterous manipulation. TouchWorld uses a hierarchical policy that separates vision-language subtask planning, tactile world-model prediction, visuo-tactile goal-conditioned action generation, and high-frequency tactile residual refinement. A High-Level Planning Layer produces executable subtasks and predicts tactile subgoals; a Visuo-Tactile Goal-Conditioned Policy generates nominal action chunks; and a Tactile-Conditioned Refinement Policy performs online residual correction using recent tactile and proprioceptive feedback. By using touch as both a predictive contact reference and a fast feedback signal, TouchWorld preserves the semantic generalization of vision-language-action policies while improving local contact adaptation. Across six long-horizon and contact-rich dexterous manipulation tasks, TouchWorld achieves 65.0% success in the clean setting and 53.7% success under human perturbations, outperforming the strongest baseline by 15.7 and 18.5 percentage points, respectively.

</details>


### [270] [Multimodal Voice Activity Projection for Turn-Taking in Social Robots with Voice-Activity-Related Pretrained Encoders](https://arxiv.org/abs/2607.07294)
*Antonio Cano,Guillermo Pérez,Luis Merino,Randy Gomez*

Main category: cs.RO

TL;DR: This paper presents MM-VAP, a multimodal framework for turn-taking prediction in human-robot interaction that extends audio-only voice activity projection to synchronized audio-visual inputs using pretrained backbones, Low-Rank Adaptation, inter-speaker attention, and semantic consistency loss, showing improvements on NoXi and Haru EDR datasets.


<details>
  <summary>Details</summary>
Motivation: Social robots in mediator settings need to anticipate conversational dynamics proactively rather than just react to pauses, but existing methods are limited to audio-only inputs and fail to leverage visual cues for turn-taking prediction.

Method: The MM-VAP framework extends audio-only VAP to multimodal audio-visual inputs using: (1) pretrained audio-visual backbones with Low-Rank Adaptation, (2) independent speaker encoding followed by inter-speaker attention for relational dynamics, and (3) a semantic consistency loss to regularize the 256-state output space based on higher-level dialogue patterns.

Result: Experiments on NoXi and NoXi+J datasets showed improvements over current baselines, particularly for certain turn-taking events. Evaluation on Haru EDR corpus further confirmed suitability for mediation-oriented HRI.

Conclusion: The multimodal extension of VAP with visual cues and specialized architectural components improves turn-taking prediction, demonstrating strong potential for mediator robot applications in human-human interaction settings.

Abstract: Turn-taking prediction is a key requirement for social robots involved in human-human interaction, particularly in mediator settings, where the robot must anticipate conversational dynamics rather than merely react to pauses. This work presents a Multimodal Voice Activity Projection (MM-VAP) framework that extends the original audio-only VAP formulation to synchronized audio-visual inputs while preserving its self-supervised future-projection objective. The proposed approach builds on pretrained audio-visual backbones originally optimized for speech-related tasks and adapts them through Low-Rank Adaptation to the multimodal turn-taking problem. After independent speaker encoding, an inter-speaker attention stage models the relational dynamics required to project future voice activity. In addition, a semantic consistency loss is introduced to regularize the 256-state output space according to higher-level dialogue activity patterns. Experiments on NoXi and NoXi+J showed improvements over the current baselines, particularly for some turn-taking events. Additional evaluation on the Haru EDR corpus further supported the suitability of this direction for mediation-oriented human-robot interaction.

</details>


### [271] [Towards Reliable Aerial Ground Vehicle Collaboration: An Integrated Planning and Autonomy Framework for Field Deployment](https://arxiv.org/abs/2607.07350)
*Md Safwan Mondal,Luca Russo,James D. Humann,James M. Dotterweich,Pranav Bhounsule*

Main category: cs.RO

TL;DR: This paper presents a DRL-based cooperative routing framework for UAV-UGV teams in long-duration missions, featuring joint optimization of visitation sequences and rendezvous points, a YAML-based mission API, a full autonomy stack (PX4/MAVSDK + ROS 2/Nav2), and an online Rendezvous Aware Replanner (RARP) that reduces energy margin violations from 83.33% to 20.00%, validated through outdoor field experiments including a VLM-based search and rescue scenario.


<details>
  <summary>Details</summary>
Motivation: Limited flight endurance restricts UAV operational range in long-duration missions requiring visits to multiple spatially distributed AOIs; pairing UAVs with UGVs for mobile recharging is promising but introduces a tightly coupled cooperative routing problem involving UAV route planning, UGV road-constrained movement, energy management, and rendezvous scheduling under uncertainty.

Method: The authors formulate an energy-constrained cooperative routing problem and solve it using a DRL-based planner that jointly optimizes UAV visitation sequence and rendezvous locations with the UGV. They introduce a two-layer YAML-based mission API for bridging planning and execution, implement a full autonomy stack (PX4/MAVSDK for UAV, ROS 2/Nav2 for UGV), and propose a lightweight Rendezvous Aware Replanner (RARP) for online handling of environmental uncertainties.

Result: The DRL-based planner outperforms baseline heuristics in minimizing total mission time. The RARP reduces energy margin violations from 83.33% to 20.00%. The full system is validated through outdoor field experiments demonstrating robust cooperative navigation and adaptability, including a search and rescue scenario with VLM-based hazard detection.

Conclusion: The proposed integrated planning and autonomy framework enables reliable field deployment of UAV-UGV teams for long-duration missions, effectively addressing the coupled cooperative routing problem and uncertainty during execution through DRL-based planning and online replanning.

Abstract: Limited flight endurance significantly restricts the operational range of unmanned aerial vehicles (UAVs) in long duration missions such as surveillance and inspection, where multiple spatially distributed Areas of Interest (AOIs) must be visited. These tasks require efficient routing determining the sequence of visits which directly impacts mission time, energy consumption, and overall feasibility. Pairing UAVs with unmanned ground vehicles (UGVs) for mobile recharging offers a promising solution, but introduces a tightly coupled cooperative routing problem involving UAV route planning, UGV road constrained movement, energy management, and rendezvous scheduling under uncertainty. In this work, we present an integrated planning and autonomy framework for reliable field deployment. We formulate the problem as an energy constrained cooperative routing task and solve it using a Deep Reinforcement Learning (DRL) based planner that jointly optimizes the UAV visitation sequence and rendezvous locations with the UGV, outperforming baseline heuristics in minimizing total mission time. To bridge the gap between planning and execution, we introduce a standardized two layer YAML based mission API that captures environment states and structures lightweight, synchronized action sequences. This framework is supported by a complete autonomy stack using PX4/MAVSDK for UAV control and ROS 2/Nav2 for UGV navigation. Furthermore, we propose a lightweight Rendezvous Aware Replanner (RARP) that operates online to handle environmental uncertainties, reducing energy margin violations from 83.33% to 20.00%. The full system is validated through outdoor field experiments, demonstrating robust cooperative navigation and adaptability in dynamic tasks, including a search and rescue scenario with vision language model (VLM) based hazard detection

</details>


### [272] [HumAIN: Human-Aware Implicit Social Robot Navigation](https://arxiv.org/abs/2607.07357)
*Daeun Song,Nhat Le,Jeffrey Chen,Mohammad Nazeri,Amirreza Payandeh,Rohan Chandra,Reuth Mirsky,Ross Mead,Ling Xiao,Xuesu Xiao*

Main category: cs.RO

TL;DR: HumAIN is a framework that uses knowledge distillation to fuse implicit social cues (skeletal keypoints, gait, orientation) into robot navigation planning, improving trajectory prediction by 29.8% over baselines.


<details>
  <summary>Details</summary>
Motivation: Effective social robot navigation requires sensitivity to subtle human behavioral cues (e.g., gait, orientation) revealed through skeletal signals, but existing methods struggle to incorporate such implicit social information efficiently in real-time.

Method: (1) A transformer-based teacher model fuses multi-modal inputs (historic images, skeletal keypoints, robot state, target goal) to learn human-aware trajectory representations. (2) Knowledge distillation transfers this knowledge to a lightweight student model that uses minimal inputs, optimized for trajectory reconstruction and latent feature alignment.

Result: Improves trajectory prediction metrics by an average of 29.8% across all metrics compared to state-of-the-art baselines, enabling real-time deployment on resource-constrained platforms.

Conclusion: Using implicit whole-body cues (skeletal keypoints) via distillation bridges the prediction-planning gap, achieving socially compliant, adaptive, and robust human-aware navigation.

Abstract: Effective social robot navigation requires sensitivity to human behavior, often revealed through subtle skeletal cues like gait and orientation. We present Human-Aware Implicit Social Robot Navigation (HumAIN), a novel framework that fuses implicit social cues directly into the planning loop via knowledge distillation. We first employ a transformer-based teacher model that fuses rich multi-modal inputs, including historic images, skeletal keypoints, robot state, and a robot's target goal, to learn robust, human-aware representations for the robot's future trajectory planning. To enable real-time deployment, we then distill this knowledge into a lightweight student model. By optimizing for both trajectory reconstruction and latent feature alignment with the teacher, the student learns to infer complex social dynamics from minimal inputs. Bridging the prediction-planning gap with an efficient distilled architecture, our method enables robots to reason about human behavior in a manner that is adaptive, robust, and socially compliant. We validate HumAIN through extensive experiments, where it improves trajectory prediction metrics by an average of 29.8% across all metrics compared to state-of-the-art baselines. These results highlight the benefit of using implicit, whole-body cues to achieve human-like navigation awareness on resource-constrained platforms.

</details>


### [273] [Behavior Foundations for Quadruped Robots: ABot-C0 Technical Report](https://arxiv.org/abs/2607.07370)
*Xufeng Zhao,Fuzhi Yang,Jianhui Chen,Li Gao,Zhang Meng,Jie Gao,Yao Zheng,Wenyu Liu,Menglin Yang,Minqi Gu,Yaru Zhao,Honglin Han,Shihui Su,Zixiao Tang,Liu Liu,Mu Xu,Yang Cai,Wenbin Tang*

Main category: cs.RO

TL;DR: ABot-C0 is a generalist motion-control system for quadruped robots that integrates a scalable multi-source data pipeline (16,074 motion clips), Flow-Matching policy with scaling law for motion tracking, and a three-stage privileged-to-perceptive framework for all-terrain locomotion, enabling product-level behavioral intelligence in real-world deployment.


<details>
  <summary>Details</summary>
Motivation: Quadruped robot control lacks scalable and physically feasible motion corpora due to scarce animal motion data and fragile cross-embodiment retargeting, unlike humanoid control which benefits from large-scale human motion-capture data.

Method: 1) A data pyramid built from conditional video-generation synthesis, annotated motion capture, teleoperation, and human design producing 16,074 motion clips. 2) A Flow-Matching generalist policy for motion tracking with demonstrated scaling law. 3) A three-stage privileged-to-perceptive framework with temporal LiDAR memory and terrain-predictive supervision for all-terrain locomotion. 4) Multi-policy coordination with smooth transitions, energy efficiency, and safety mechanisms.

Result: First demonstration of quadruped motion tracking scaling law (performance improves consistently with training scale); zero-shot capability to track unseen motions; successful real-world deployment in urban-terrain autonomous navigation and companion-style multimodal interaction, moving beyond single-function demos.

Conclusion: ABot-C0 achieves product-level behavioral intelligence for quadruped robots by combining scalable data construction, generalist policy learning with scaling laws, and robust real-world deployment stacks, overcoming key obstacles in quadruped motion control.

Abstract: In embodied intelligence systems, the motion controller serves as the critical bridge between semantic reasoning and physical execution. Humanoid control has progressed rapidly through large-scale human motion-capture data and motion-tracking paradigm. However, producing quadruped robots motion corpora with scalability and physical feasibility faces more fundamental obstacles: animal motion data is scarce, and cross-embodiment retargeting remains fragile. We present ABot-C0, a generalist motion-control system for quadruped robots that establishes three complementary behavior foundations: a scalable multi-source motion-data pipeline, robust policy learning across motion tracking, locomotion, and scene interaction, and a unified deployment stack for reliable real-world operation. Fundamentally, we construct a data pyramid through conditional video-generation synthesis, annotated motion capture, teleoperation and human design, producing 16,074 physically feasible motion clips as the data foundation for various motion learning demands. We then train a Flow-Matching generalist policy that demonstrates for the first time quadruped motion tracking scaling law that its performance improves consistently as training scales up, with zero-shot capability to track unseen motions. Then, we push a step further for robust all-terrain traversal locomotion by adopting a three-stage privileged-to-perceptive framework with temporal LiDAR memory and terrain-predictive supervision. Collectively, these components form a motion generalist that coordinates multi-policy execution, smooth behavior transitions, energy-efficient control, and safety mechanisms for real-world deployment. Extensive experiments on urban-terrain autonomous navigation and companion-style multimodal interaction demonstrate that quadruped robots move beyond single-function demos toward product-level behavioral intelligence.

</details>


### [274] [PLED-VINS: A Point-Line Event-Based Visual Inertial SLAM for Dynamic Environments](https://arxiv.org/abs/2607.07374)
*Seunghun Lee,Jihun Nam,Dong-Uk Seo,Hyun Myung*

Main category: cs.RO

TL;DR: PLED-VINS is a monocular event camera-based visual-inertial SLAM framework that uses an entropy-recency score map and unified point-line robust bundle adjustment to estimate temporal and geometric reliability of features, enabling robust state estimation in dynamic environments.


<details>
  <summary>Details</summary>
Motivation: Dynamic environments pose a fundamental challenge for visual SLAM because moving objects and rapid motion degrade state estimation accuracy. Existing event-based SLAM frameworks assume static scenes and lack methods to estimate feature reliability.

Method: 1) Entropy-recency score map for temporal reliability of point and line features; 2) Geometric reliability via unified point-line robust bundle adjustment; 3) Adaptive weighting strategy fusing temporal and geometric reliability with motion-conditioned reliability modeling for line features.

Result: PLED-VINS improves state estimation on evaluated dynamic sequences with moving objects.

Conclusion: The proposed reliability-aware framework effectively addresses dynamic scene challenges in event-based SLAM by suppressing unreliable observations from moving objects.

Abstract: Dynamic environments remain a fundamental challenge for visual SLAM, where unreliable observations from moving objects and rapid motion degrade state estimation accuracy. Although event cameras preserve fine-grained spatio-temporal information, most existing event-based SLAM frameworks still assume static scenes and lack approaches to estimate the reliability of features. To this end, we propose PLED-VINS, a monocular event camera-based visual-inertial SLAM framework that enables robust state estimation in dynamic environments. We propose an entropy-recency score map to characterize the temporal reliability of both point and line features based on event temporal statistics. Concurrently, geometric reliability is estimated via a unified point-line robust bundle adjustment. Building upon these, we design an adaptive weighting strategy that fuses temporal and geometric reliability, including motion-conditioned reliability modeling for line features, to suppress unreliable observations. Experimental results demonstrate that PLED-VINS improves state estimation on the evaluated dynamic sequences with moving objects.

</details>


### [275] [Initiation Safety: A Missing Dimension in Generalist-Robot Safety](https://arxiv.org/abs/2607.07420)
*Zhijin Meng,Francisco Cruz*

Main category: cs.RO

TL;DR: The paper argues that generalist robots need a separate safety layer for "initiation authorization"—deciding whether to take a first social action (greeting, grasping, stepping into space)—rather than treating high confidence or engagement as implicit permission to act.


<details>
  <summary>Details</summary>
Motivation: Current safety frameworks focus on motion or dialogue but ignore whether the robot should initiate a first hard-to-undo social action at all. Seeing a person is not the same as having their consent to be addressed.

Method: The authors frame initiation authorization within generalist-robot safety, contrast it with post-plan VLA guardrails, implement a PAS (probe-authorize-speak) pipeline on a doorway humanoid, compare it with direct-init on logged traces, and propose a three-condition user study.

Result: The paper presents a comparative analysis of PAS vs. direct-init approaches on logged traces, though specific quantitative results are not included in the abstract.

Conclusion: Initiation authorization should be treated as a distinct safety layer. Open questions remain on metrics, governance, and where initiation ends and foundation-model generation begins.

Abstract: Safety for generalist robots is usually discussed in terms of motion or dialogue. We argue a third question is missing: should the robot take its first hard-to-undo social action at all, such as a greeting, an uninvited grasp, or stepping into someone's space? We call this initiation authorization. Current frameworks rarely treat it as a separate safety layer. Today's stacks often skip this step: a high engagement score or a confident VLA rollout is treated as permission to act. But seeing a person is not the same as having their consent to be addressed. We frame initiation authorization within generalist-robot safety and contrast it with post-plan VLA guardrails, implementing PAS (probe-authorize-speak) on a doorway humanoid, comparing it with direct-init on logged traces, and proposing a three-condition user study, with open questions on metrics, governance, and where initiation ends and foundation-model generation begins.

</details>


### [276] [Immersive Social Interaction with VR and LLM-Assisted Humanoids](https://arxiv.org/abs/2607.07430)
*Niraj Pudasaini,Geeta Chandra Raju Bethala,Pranav Doma,Anthony Tzes,Yi Fang*

Main category: cs.RO

TL;DR: This paper presents an immersive teleoperation framework for humanoid robots that combines voice-controlled locomotion, VR-based manipulation, and bidirectional social interaction using Apple Vision Pro. The system uses LLM-assisted voice commands and motion tracking, achieving 80% success in object manipulation and 70% in social tasks with novice users.


<details>
  <summary>Details</summary>
Motivation: Existing teleoperation interfaces require physically demanding motion tracking or cognitively demanding low-level control, making them inaccessible to novice users. The authors aim to create a more intuitive, accessible interface for remote humanoid control.

Method: The framework integrates Apple Vision Pro for egocentric visual feedback, an LLM-assisted voice-control module for high-level locomotion commands, and wrist/finger tracking retargeted via inverse kinematics and PD control for manipulation. The system also records multimodal data (RGB, voice, joint states, hand motions, eye-gaze) for future learning.

Result: Evaluated on Unitree H1 humanoid: novice users achieved 80% success in object manipulation and 70% success in social cube-passing tasks after brief familiarization.

Conclusion: The immersive, language-assisted teleoperation framework shows promise as an accessible interface for humanoid interaction, remote assistance, and multimodal data collection, lowering the barrier for novice operators.

Abstract: Humanoid robots can extend human presence to remote, constrained, or hazardous environments, but existing teleoperation interfaces often require physically demanding motion tracking or cognitively demanding low-level control. This paper presents an immersive teleoperation framework that integrates voice-controlled locomotion, VR-based manipulation, and bidirectional social interaction for whole-body humanoid control. Using Apple Vision Pro, the operator receives egocentric visual feedback, issues natural-language locomotion commands, and teleoperates the robot's arms and dexterous hands through wrist and finger tracking. An LLM-assisted voice-control module converts spoken instructions into high-level locomotion commands, while the manipulation module retargets human hand motions to the robot through inverse kinematics and PD control. The system also records multimodal data, including egocentric RGB observations, voice/text commands, joint states, hand motions, and eye-gaze signals, supporting future imitation learning and autonomy. We evaluate the framework on a Unitree H1 humanoid equipped with dexterous hands in manipulation and social interaction tasks. Results show that novice users can successfully operate the system after brief familiarization, achieving 80\% success in object manipulation and 70\% success in a social cube-passing task. These results demonstrate the potential of immersive, language-assisted teleoperation as an accessible interface for humanoid interaction, remote assistance, and multimodal data collection.

</details>


### [277] [GeoGS-SLAM: Geometry-Only Gaussian Splatting for Dense Monocular SLAM](https://arxiv.org/abs/2607.07452)
*Lipu Zhou,Yaoyun Kang,Junxiang Pang,Shengkai Sun,Tingting Bao,Kehan Wang*

Main category: cs.RO

TL;DR: GeoGS-SLAM is a dense visual SLAM system that uses Geometry-only Gaussian Splatting to directly reconstruct 3D scene geometry without appearance modeling, reducing per-primitive parameters by over 80% and achieving state-of-the-art geometric reconstruction quality and mapping efficiency.


<details>
  <summary>Details</summary>
Motivation: Existing 3DGS-based dense SLAM methods model both appearance and geometry, but downstream robotic tasks like navigation and obstacle avoidance primarily need accurate spatial geometry, not photorealistic rendering. This raises the question: can 3DGS do 3D reconstruction without appearance modeling?

Method: GeoGS retains only spatial Gaussian parameters (eliminating color/appearance), reducing per-primitive parameters by >80%. A training framework uses single-view & multi-view geometric/photometric supervision. A local-plane driven initialization aligns primitives with local structures. A map update strategy for loop closure globally transforms the Gaussian map to prevent tearing from inconsistent per-viewpoint pose corrections.

Result: Outperforms SOTA methods on synthetic and real-world benchmarks in online mapping efficiency and geometric reconstruction quality, while reducing primitive count and accelerating convergence.

Conclusion: Geometry-only 3DGS is feasible and beneficial for dense SLAM, offering efficient, robust geometric reconstruction without needing appearance modeling, making it suitable for robotics applications.

Abstract: Dense visual SLAM is a fundamental problem in robotics. Recent advances in 3DGS have demonstrated its potential for dense SLAM. Existing 3DGS frameworks focus on both appearance and geometry modeling. However, scene geometry is typically more critical for SLAM than novel view synthesis because downstream robotic tasks, such as navigation and obstacle avoidance, rely primarily on accurate spatial geometry rather than photorealistic rendering. This observation raises a natural question: Is it feasible for 3DGS to perform 3D reconstruction without scene appearance modeling? Motivated by this, we propose Geometry-only Gaussian Splatting (GeoGS), which directly reconstructs scene geometry, and further present GeoGS-SLAM, a dense visual SLAM system built upon this representation. Specifically, GeoGS retains only spatial parameters to reduce the number of per-primitive parameters by over 80%. In contrast to existing 3DGS methods, GeoGS focuses solely on geometric reconstruction, which significantly reduces the number of Gaussian primitives, accelerates geometric convergence, and enhances robustness to illumination variations. In addition, we present an effective training framework that optimizes the Gaussian primitives via single-view and multi-view geometric and photometric supervision, and speeds up geometry convergence with a local-plane driven initialization that better aligns primitives with local structures. Furthermore, we introduce a map update strategy for loop closure that globally transforms the Gaussian map to align it with the corrected pose estimates, thereby preventing map tearing caused by inconsistent per-viewpoint pose corrections in existing methods. Extensive experiments on synthetic and real-world benchmarks demonstrate that our method outperforms SOTA methods in terms of online mapping efficiency and geometric reconstruction quality.

</details>


### [278] [EmbodiedGen V2: An Agentic, Simulation-Ready 3D World Engine for Embodied AI](https://arxiv.org/abs/2607.07459)
*Xinjie Wang,Liu Liu,Taojun Ding,Andrew Choi,Chaodong Huang,Mengao Zhao,Ziang Li,Jackson Jiang,Chunlei Yu,Shengxiang Liu,Wei Xu,Zhizhong Su*

Main category: cs.RO

TL;DR: EmbodiedGen V2 is a generative 3D world engine that automatically builds sim-ready environments for embodied AI, achieving high human acceptance (96.5%) and significantly improving policy training success rates (from 9.7% to 79.8% in simulation, and from 21.7% to 75.0% on real robots).


<details>
  <summary>Details</summary>
Motivation: Assembling 3D assets into policy-ready task environments remains largely manual, limiting scalable closed-loop learning for embodied intelligence.

Method: A unified sim-ready representation connecting cross-simulator assets, interaction affordances, task-driven worlds, multi-room scenes, and stateful Vibe Coding into a generative, editable, reusable simulation pipeline.

Result: 96.5% human acceptance, 98.6% collision success; 83.3% task worlds directly usable; simulation success improved from 9.7% to 79.8%; real robot task success from 21.7% to 75.0%.

Conclusion: EmbodiedGen V2 serves as scalable simulation infrastructure for training, evaluating, and deploying embodied policies across manipulation, navigation, and mobile manipulation tasks.

Abstract: We present EmbodiedGen V2, a generative 3D world engine for building executable sim-ready environments for embodied intelligence. Sim-ready 3D asset generation has advanced rapidly, yet assembling such assets into policy-ready task environments remains largely manual, limiting scalable closed-loop learning. EmbodiedGen V2 addresses this gap through a unified sim-ready representation that connects cross-simulator assets, interaction affordances, task-driven worlds, large-scale multi-room scenes, and stateful Vibe Coding into a generative, editable, and reusable simulation pipeline. The generated environments support manipulation, navigation, mobile manipulation, cross-simulator deployment, and embodied policy training. In evaluation, the asset pipeline achieves 96.5% human acceptance and 98.6% collision success, and 83.3% of task-driven worlds are directly usable for downstream simulation without manual modification. Online reinforcement learning with generated environments further improves simulation success from 9.7% to 79.8%, and transfers to real robots with task success increasing from 21.7% to 75.0%. These results establish EmbodiedGen V2 as scalable simulation infrastructure for training, evaluating, and deploying embodied policies.

</details>


### [279] [Agent-Exploitation Affordances: From Basic to Complex Representation Patterns](https://arxiv.org/abs/2607.07475)
*Bastien Dussard,Aurélie Clodic,Guillaume Sarthou*

Main category: cs.RO

TL;DR: This paper introduces a tractable ontological representation of cooperative affordances—action possibilities that arise when multiple agents interact—to enable artificial agents in social robotics to understand and exploit multi-agent action opportunities beyond individual capabilities.


<details>
  <summary>Details</summary>
Motivation: While functional affordances (tool/object use) are well-studied in knowledge representation, the social context and presence of other agents remain unexplored. This gap limits social robots' ability to recognize cooperative action possibilities.

Method: The authors propose a tractable ontological representation of cooperative affordances, defining them as situations where agents interact to extend their action possibilities. They combine elementary representation patterns to depict diverse multi-agent scenarios.

Result: The paper demonstrates that the proposed ontological representations can be effectively combined to illustrate a range of cooperative scenarios, validating the tractability and expressiveness of the framework.

Conclusion: Cooperative affordances are a crucial concept for social robotics, enabling agents to understand complementary actions with others. The tractable ontology provides a foundation for artificial agents to reason about and exploit multi-agent action possibilities.

Abstract: In robotics, the capability of an artificial agent to represent the range of its action possibilities, i.e. affordances, is crucial to understand how it can act on its environment. While functional affordances, which refer to the use of tools and objects, have been broadly studied in knowledge representation, the implications of a social context and the presence of other agents have remained unexplored in this field. Consequently, in the field of social robotics, a multi-agent context enables the agents to engage in new actions that are potentially complementary to their individual capabilities, leading to the perspective of agentexploitation. This work focuses on the concept of cooperative affordance within the realm of social affordances. Cooperative affordances refer to situations where agents interact with each other to extend their action possibilities range. From this definition, this paper proposes a tractable ontological representation of this concept with the aim of making it usable by an artificial agent. Expanding on those elementary patterns, we illustrate the effectiveness of these representations by combining them to depict a diverse range of scenarios.

</details>


### [280] [Smooth Operator: A Real-Time Sampling-Based Algorithm for Kinematic Hand Retargeting](https://arxiv.org/abs/2607.07491)
*Robert Jomar Malate,Erik Bauer,Norica Bacuieti,Stefanos Charalambous,Elvis Nava,Robert K. Katzschmann,Benedek Forrai*

Main category: cs.RO

TL;DR: Introduces Sampling-Based Retargeter (SBR), a gradient-free retargeting method that reduces jitter and cognitive fatigue in teleoperation, achieving 54.1% task success rate and lowest NASA-TLX score (36.4) in a user study with 18 participants.


<details>
  <summary>Details</summary>
Motivation: Current gradient-based retargeting algorithms converge to different local minima, causing jitter that degrades teleoperation data quality and user experience, limiting the upper bound of downstream VLA/VAM models.

Method: Proposes Sampling-Based Retargeter (SBR), a gradient-free retargeting method inspired by sampling-based control, designed for low-jitter real-time kinematic retargeting.

Result: SBR achieved 54.1% task success rate (highest) and lowest NASA-TLX workload score of 36.4/100, significantly reducing operator cognitive fatigue.

Conclusion: SBR is an effective, intuitive retargeter for dexterous manipulation, with a rigorous benchmarking methodology to guide future retargeting research.

Abstract: Advances in learning-based robotic manipulation, such as Vision-Language-Action (VLA) models and Video Action Models (VAMs), heavily rely on high-quality teleoperation data. Their capabilities are strictly upper-bounded by the quality of the underlying human demonstrations. Current gradient-based retargeting algorithms often converge to different local minima, resulting in jitter that affects data quality and teleoperation experience. To address this, we introduce the Sampling-Based Retargeter (SBR), a novel gradient-free retargeting method drawn from the rich literature of sampling-based control and explicitly designed for low-jitter, real-time kinematic retargeting. We evaluate SBR both in simulation and through a rigorous real-world user study involving 18 participants performing 3 complex manipulation tasks. Compared to gradient-based baselines, SBR achieved the highest overall task success rate (54.1%) while significantly reducing operator cognitive fatigue, recording the lowest NASA-TLX workload score (36.4 out of 100). Ultimately, we establish SBR as a highly effective, intuitive retargeter for dexterous manipulation, providing the community with a rigorous benchmarking methodology to guide future retargeting research.

</details>


### [281] [Generating Personalized Lower-Limb Kinematics Across Walking Speeds Using Subject-Conditioned Diffusion](https://arxiv.org/abs/2607.07533)
*Diya Dinesh,Adrian Krieger,Changseob Song,Dongho Park,Aaron J. Young,Inseung Kang*

Main category: cs.RO

TL;DR: A subject-conditioned residual diffusion framework generates personalized lower-limb kinematics at unseen walking speeds from just a single seen-speed gait sequence, achieving high accuracy on both able-bodied (3.4° MAE) and stroke subjects (6.0° MAE) without fine-tuning, reducing data collection burden for exoskeleton personalization.


<details>
  <summary>Details</summary>
Motivation: Personalizing exoskeleton assistance requires extensive user-specific gait data across many tasks/speeds, but collecting this data via motion capture is costly, time-intensive, and burdensome, especially for clinical populations. This challenge is most acute across walking speeds where gait changes substantially.

Method: A subject-conditioned residual diffusion framework that: (1) takes sagittal-plane hip, knee, ankle trajectories at a seen speed and a desired unseen speed; (2) generates a residual transforming seen trajectory into unseen one; (3) uses a transformer denoiser conditioned on subject's gait and both speeds via feature-wise linear modulation (FiLM).

Result: 3.4° MAE on held-out able-bodied subjects; 6.0° MAE on out-of-distribution stroke subjects without fine-tuning; over 70% MAE reduction vs supervised feed-forward baselines; single seen speed matched accuracy of four speeds within 0.4°.

Conclusion: Subject-conditioned residual diffusion can synthesize personalized gait across speeds from minimal data (single seen speed), significantly reducing the data collection burden for downstream exoskeleton personalization while retaining subject identity even for clinical populations.

Abstract: Personalizing exoskeleton assistance requires user-specific gait data across many locomotor tasks, yet collecting this data demands repeated motion capture sessions that are costly, time-intensive, and especially burdensome for clinical populations. This challenge is most acute across walking speeds, where gait changes substantially and deviates further in clinical gait. This work introduces a subject-conditioned residual diffusion framework that generates personalized lower-limb kinematics at unseen walking speeds from a subject's gait sequence at a single seen speed. Given sagittal-plane hip, knee, and ankle trajectories at a seen speed and a desired unseen speed, the model generates a residual that transforms the seen trajectory into the unseen one, using a transformer denoiser conditioned on the subject's gait and the two speeds through feature-wise linear modulation. Trained only on able-bodied data, the model achieved a mean absolute error (MAE) of 3.4° on held-out able-bodied subjects. Without any stroke-specific fine-tuning, it achieved a 6.0° MAE on out-of-training-distribution stroke subjects, retaining subject identity for clinical gait. The framework reduced the MAE by over 70% relative to supervised feed-forward baselines, and a single seen speed matched the accuracy of four speeds within 0.4°. These results demonstrate that subject-conditioned residual diffusion can synthesize personalized gait across speeds from minimal data, reducing the collection burden for downstream exoskeleton personalization.

</details>


### [282] [SonoRank: Towards Calibration-Free Real-Time Finger Flexion Detection from Forearm Ultrasound Sequences](https://arxiv.org/abs/2607.07542)
*Dean Zadok,Alon Wolf,Alex M. Bronstein,Oren Salzman*

Main category: cs.RO

TL;DR: SonoRank uses pairwise ranking pretraining on ultrasound video to detect finger flexion without per-user calibration, achieving 28% improvement in F1 score over direct classification baselines.


<details>
  <summary>Details</summary>
Motivation: Powered prosthetic hands are frequently abandoned due to limited functionality of sEMG-based devices. Ultrasound (sonomyography) offers better real-time muscle observation and control but requires per-user fine-tuning, limiting commercialization.

Method: SonoRank first learns to rank pairs of ultrasound sequences by relative motion magnitude for each finger, then fine-tunes to classify finger flexion using a rest reference captured at operation start.

Result: Under 12-fold leave-one-subject-out cross-validation on 12 subjects, SonoRank achieves a 28% improvement in F1 score over direct classification baselines that skip the ranking stage.

Conclusion: Pairwise ranking is an effective pretraining signal for subject-independent control, bringing ultrasound-based prosthetics closer to calibration-free deployment.

Abstract: Powered prosthetic hands are frequently abandoned, largely due to the limited functionality of current devices that rely on surface electromyography (sEMG). Sonomyography (ultrasound) has emerged as a promising alternative, owing to its ability to observe muscle activity in real time and control a greater number of degrees of freedom. Yet, existing ultrasound-based methods require per-user fine-tuning, limiting their commercialization. We propose SonoRank, an important step towards calibration-free finger flexion detection from forearm ultrasound video. SonoRank first learns to rank pairs of ultrasound sequences by their relative motion magnitude for each of the five fingers. The learned representations are then fine-tuned to classify whether each finger is actively flexing, using a rest reference that is captured at the beginning of the operation. Under 12-fold leave-one-subject-out cross-validation on a dataset of twelve subjects with synchronized kinematics, SonoRank achieves a 28% improvement in F1 score over direct classification baselines that skip the ranking stage. These results establish pairwise ranking as an effective pretraining signal for subject-independent control, bringing ultrasound-based prosthetics closer to practical, calibration-free deployment.

</details>


### [283] [Context-Aware Force Estimation for Deformable Tool Manipulation in Robotic Environmental Swabbing via Few-Shot Continual Adaptation](https://arxiv.org/abs/2607.07574)
*Siavash Mahmoudi,Chaitainya Kuppar Reddy,Yang Tian,Dongyi Wang*

Main category: cs.RO

TL;DR: This paper presents a data-driven framework using LSTM with few-shot adaptation (FiLM) to estimate tip-level contact forces in robotic surface swabbing with deformable tools, achieving up to 63% error reduction under domain shift without requiring tool-integrated sensors.


<details>
  <summary>Details</summary>
Motivation: Robotic surface swabbing with deformable tools suffers from nonlinear viscoelastic hysteresis that decouples wrist-mounted force measurements from true contact forces, and tool-integrated sensors are impractical due to sterility and disposability constraints.

Method: A compact LSTM recurrent architecture is selected via comparative evaluation, then augmented with a parameter-isolated few-shot adaptation strategy using feature-wise linear modulation (FiLM) to add low-dimensional context embeddings to a frozen backbone.

Result: On a UR5e platform across nine tool-surface regimes, the approach reduces zero-shot estimation error by up to 63% without catastrophic forgetting, improving robustness under domain shift.

Conclusion: Separating shared deformation-history dynamics from domain-specific conditioning via few-shot adaptation enables reliable force estimation for deformable tool manipulation in non-stationary environments.

Abstract: Robotic surface swabbing requires sustained interaction between a compliant tool and heterogeneous environments, where accurate estimation of tip-level contact force is critical for consistent sampling performance. However, deformable tool dynamics introduce nonlinear viscoelastic hysteresis that decouples wrist-mounted force measurements from true contact forces, while tool-integrated sensors are impractical for deployment due to sterility and disposability constraints. This paper presents a data-driven framework for contact force estimation in Deformable Tool Manipulation (DTM) that leverages proprioceptive sensing without requiring explicit physical models or permanent embedded sensing hardware at the tool tip. A recurrent architecture is first identified through a comparative evaluation of temporal models, where a compact LSTM achieves the lowest estimation error and sub-millisecond inference latency. To address generalization across unseen surfaces and tool compliance conditions, we introduce a parameter-isolated few-shot adaptation strategy that augments a frozen recurrent backbone with low-dimensional context embeddings using feature-wise linear modulation (FiLM). Experiments on a UR5e platform across nine tool-surface interaction regimes demonstrate that the proposed approach significantly improves robustness under domain shift, reducing zero-shot estimation error by up to 63\% while preserving baseline performance without catastrophic forgetting. These results show that separating shared deformation-history dynamics from domain-specific conditioning enables reliable force estimation for DTM in non-stationary environments.

</details>


### [284] [CARLA-GS: Decoupling Representation, Reasoning, and Physics Simulation for Autonomous Driving Corner-Case Synthesis](https://arxiv.org/abs/2607.07601)
*Kaicong Huang,Meng Ma,Ruimin Ke*

Main category: cs.RO

TL;DR: CARLA-GS is a modular pipeline that combines gaussian scene reconstruction, multi-agent LLM reasoning, and physics-based control to generate controllable, photorealistic, and spatiotemporally consistent corner cases for autonomous driving safety evaluation.


<details>
  <summary>Details</summary>
Motivation: Safety evaluation for autonomous driving requires rare, safety-critical corner cases, but existing methods handle visual, scene, and trajectory components separately or struggle with consistency and realism in end-to-end generation.

Method: Decouples the pipeline into: (1) editable gaussian scene reconstruction with geometry-consistent constraints, (2) multi-agent LLM for scene-level reasoning and intent-level waypoint generation, (3) CARLA with PID controller for low-level kinematic/dynamic control, and (4) re-projection of vehicle states into the gaussian scene for egocentric rendering.

Result: Experiments on the Waymo Open Dataset demonstrate controllable corner-case generation with photorealistic, spatiotemporally consistent videos that align with semantic intent and physically feasible motion.

Conclusion: CARLA-GS effectively unifies visual representation, semantic reasoning, and physics-based execution into a single modular framework, addressing the multi-source nature of corner-case generation for autonomous driving.

Abstract: Safety evaluation for autonomous driving is dominated by rare, safety-critical interactions, motivating simulators that can deliberately synthesize corner cases with photorealistic observations. Corner-case generation is inherently a multi-source problem spanning visual representation, scene reasoning, and vehicle trajectory generation and control. Prior knowledge- and model-based approaches typically focus on scene or trajectory components in isolation, while diffusion-based methods attempt end-to-end generation but still struggle to ensure spatiotemporal consistency and physical realism. To unify these aspects within a single framework, we propose CARLA-GS, a modular corner-case synthesis pipeline that decouples visual representation, semantic reasoning, and physics-based execution while maintaining tight cross-module coupling. Starting from real driving data, we reconstruct an editable gaussian scene with additional geometry-consistent constraints. A multi-agent LLM then performs scene-level reasoning to identify risky interactions and generate intent-level waypoint trajectories, while the low-level motion control is delegated to CARLA, where a PID controller ensures kinematic and dynamic feasibility. The simulated vehicle states are finally re-projected into the gaussian scene for ego-centric rendering. This design enables high-level semantic reasoning, low-level physically executable motion, and photorealistic corner-case generation within a unified pipeline. Experiments on the Waymo Open Dataset show, both quantitatively and qualitatively, that our framework enables controllable corner-case generation and produces photorealistic, spatiotemporally consistent videos aligned with semantic intent and physically feasible motion.

</details>


### [285] [Dual Latent Memory in Vision-Language-Action Models for Robotic Manipulation](https://arxiv.org/abs/2607.07608)
*Hongyu Qu,Jianzhe Gao,Xiaobin Hu,Shaohuan Yang,Xinlei Yu,Rui Yan,Wenguan Wang,Xiangbo Shu,Shuicheng Yan*

Main category: cs.RO

TL;DR: LaMem-VLA introduces a latent-memory-native framework that reconstructs historical experience into latent memory tokens and interweaves them directly with VLA reasoning, overcoming the Markovian limitation of mainstream VLAs for long-horizon tasks.


<details>
  <summary>Details</summary>
Motivation: Mainstream VLA models assume Markovian property (current observation only), failing on long-horizon temporally dependent tasks. Existing memory-augmented VLAs put memory outside the native latent embedding space, preventing fluid interleaving with reasoning.

Method: Four components: (1) Curator organizes experience into short-term & long-term vaults; (2) Seeker queries both vaults using multimodal cognition; (3) Condenser reconstructs retrieved evidence into compact latent memory tokens; (4) Weaver injects memory tokens with current observation/instruction into one continuous embedding sequence.

Result: Extensive experiments on SimplerEnv and LIBERO demonstrate superiority over existing methods.

Conclusion: Representing, retrieving, and consuming historical experience entirely in continuous latent space enables memory to directly participate in VLA reasoning and guide action generation under bounded context.

Abstract: Mainstream Vision-Language-Action (VLA) models predict actions primarily from the current observation under a Markovian assumption, thus struggling with long-horizon, temporally dependent tasks. Existing memory-augmented VLAs either expand the observation window or retrieve history from the memory bank as auxiliary policy-side context. However, they leave memory outside the native latent embedding space of VLA reasoning, preventing historical experience from being fluidly interleaved with multimodal reasoning and action formation. To this end, we introduce LaMem-VLA, a latent-memory-native framework that reconstructs historical experience into latent memory tokens and directly interweaves them with VLA reasoning. At its core, LaMem-VLA introduces four coordinated components: (i) a curator that organizes historical experience into two complementary short-term and long-term memory vaults; (ii) a seeker that queries both vaults using the multimodal cognition to retrieve context-relevant evidence; (iii) a condenser that reconstructs the retrieved evidence into compact short-term and long-term latent memory tokens; and (iv) a weaver that injects these memory tokens with the current observation and instruction into one continuous embedding sequence. By representing, retrieving, and consuming historical experience entirely in the same continuous latent space, LaMem-VLA enables memory to directly participate in VLA reasoning and guide action generation under a bounded context. Extensive experiments on SimplerEnv and LIBERO demonstrate the superiority of our LaMem-VLA.

</details>


### [286] [Continuous and large-scale: ELEANOR, the soft architected arm inspired by the elephant trunk](https://arxiv.org/abs/2607.07622)
*Giovanna A. Naselli,Anderson B. Nardin,Seonggun Joe,Ryan Drinkwater,Enrico Donato,Diego Bianchi,Egidio Falotico,Michel C. Milinkovitch,Lucia Beccai*

Main category: cs.RO

TL;DR: The paper presents a 3D-printed, 85cm long biomimetic elephant trunk robot that uses structural continuity and tendon-driven actuation to achieve whole-body grasping, emulating the natural trunk's macroscopic properties.


<details>
  <summary>Details</summary>
Motivation: Prior continuum robots prioritized modularity and small scale; the authors aim to create a more lifelike, large-scale robot that mimics the elephant trunk's dexterity and adaptability through structural continuity rather than prescribed behaviors.

Method: A biomimetic design approach using 3D printing to build an 85 cm compliant, tapered, volumetrically tessellated continuum arm, combined with tendon-driven actuation that mimics longitudinal and oblique muscles of the Loxodonta africana trunk.

Result: The robot demonstrates whole-body grasping of objects with different shapes and dimensions, successfully emulating elephant-like movements and grasping behaviors.

Conclusion: A biomimetic design based on macroscopic properties of the natural trunk enables elephant-like movements and grasping without targeting prescribed behaviors, and the comparison to the biological trunk highlights insights for both biology and robotics.

Abstract: The elephant trunk is a dexterous and versatile manipulator whose performance is still unmatched in robotics. In previous works, modularity was prioritized and relatively small-scale continuum robots were built. We take the natural proboscis of the *Loxodonta africana* species as a model and propose a different design approach which favors structural continuity and dynamic properties that plausibly emulate those of the natural trunk, while conferring high adaptability to the environment and humans. Instead of targeting prescribed behaviors, we show that a biomimetic design based on the macroscopic properties of the natural system enables elephant-like movements and grasping. We build by 3D printing an 85 cm long, compliant, tapered, volumetrically tessellated continuum arm, which is combined with tendon-driven actuation mimicking the longitudinal and oblique muscles of the natural model. We demonstrate whole-body grasping of objects having different shapes and dimensions and discuss a comparison to the biological trunk highlighting aspects of both biology and robotics.

</details>
